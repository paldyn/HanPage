# PLAN ATTACK — PR 2219 HML equation export

Read-only adversarial review of frozen `PLAN.md` against `9083e9d1` and the finalized external `SPEC.md`.

1. **BLOCKER — The frozen PR head does not contain the embed transport being extended.** At `9083e9d1`, `rhwp-studio/src/embed/{protocol,rpc-router,runtime}.ts` and the embed tests do not exist; `main.ts` still has the legacy wildcard-`postMessage` switch and converts exports with `Array.from`. The assumed transport exists only on later `devel` commits (`023041f5`, `9713a84f`). **Probe:** `git cat-file -e HEAD:rhwp-studio/src/embed/runtime.ts` must succeed before Slice C. **Fix:** explicitly merge/rebase the required embed commits (and test that baseline) or expand Slice C to introduce the whole transport; do not implement only additive handlers against files absent from the PR head.

2. **BLOCKER — The verification recipe cannot build or browser-test the changed Rust/WASM code from a clean checkout.** `rhwp-studio/vite.config.ts` aliases `@wasm` to `../pkg`, but this worktree has no `pkg/rhwp.js` or `pkg/rhwp_bg.wasm`; `npm run build` only runs `tsc && vite build` and never invokes `wasm-pack`. A stale/public binding could also make browser proof exercise old code. **Probe:** `test -f pkg/rhwp.js -a -f pkg/rhwp_bg.wasm` and confirm their generation follows the tested Rust SHA. **Fix:** add `wasm-pack build --target web --release` before frontend build/E2E, then use that exact `pkg` artifact (and copy it where the chosen harness requires).

3. **BLOCKER — The specified zero-size adapter mapping does not reserve intrinsic equation space.** The plan/spec proposes `common.width = 0` and `height = 0`, while `compose_paragraph()` and `inline_control_size_hwp()` consume `eq.common.width/height` directly; zero-sized equations therefore reserve no inline width/height even though later paragraph rendering can draw an intrinsic `EqLayout`, causing overlap and incorrect hit/selection geometry. **Probe:** parse `CHAR + EQUATION + CHAR` and assert the rendered equation bbox, following character x-position, line height, and hit target are all non-zero/non-overlapping. **Fix:** resolve VG-04 before freezing the adapter: either materialize width/height from the existing equation layout at import or add one shared intrinsic-size fallback to every layout/measurement path.

4. **HIGH — CDATA is currently silently dropped by the streaming reader.** `read_hml()` handles `Text` and `GeneralRef` but sends `Event::CData` to `_ => {}`; `append_decoded_text()` accepts `BytesText`, so merely adding an `in_script` flag will not satisfy REQ-HML-EQ-003/T-EQ-12. **Probe:** `<SCRIPT>a<![CDATA[<b&c>]]>d</SCRIPT>` must import as `a<b&c>d` and reparse identically. **Fix:** add an explicit, size-limited CDATA decode path into equation script state.

5. **HIGH — Duplicate/nested SCRIPT handling is required but has no explicit RED test or safe parser-state rule.** The spec says a second `SCRIPT` must create a durable warning, yet the test matrix only names unknown attributes/children; a simple boolean/string accumulator will merge duplicate scripts or allow nested unknown content into the accepted script. **Probe:** add cases for two sibling `SCRIPT`s and `FUTURE/SCRIPT`, asserting one modeled script plus one exact-path `HML_UNSUPPORTED_EQUATION_SEMANTICS` blocker. **Fix:** track equation depth and script count, accept text only for the first direct child, and skip-but-warn the unsupported subtree.

6. **HIGH — The required save-state wire shape is not the shape currently available.** Existing WASM `getHmlOpenMetadata()` returns `format`, `hmlSavable`, and `saveBlockers`, and both Rust JSON and TypeScript `HmlSaveBlocker` omit required `preserved: false`; the spec requires a distinct `getHmlSaveState()` result with `sourceFormat`, `blockers`, and `preserved`. Re-exporting existing metadata would violate the parent contract while appearing typed. **Probe:** an exact deep-equality RPC test for HML, non-HML, and unknown-equation inputs, including every field and `preserved === false`. **Fix:** define one canonical Rust/WASM save-state DTO and adapt both studio UI metadata and embed RPC from it without parsing export errors.

7. **BLOCKER — The frozen completion path stops at PR push, but the finalized spec requires hosted deployment proof and a 24-hour observation.** PLAN step 10 only pushes/verifies the PR head; SPEC Definition of Done and Stage 3 additionally require hosted SHA equality, deploy-operator ownership, a reproducible log source/query, and a 24-hour window. These cannot be completed in the stated immediate `max_iterations 8` flow without deployment authority and elapsed observation. **Probe:** identify the deployment workflow/environment, operator, hosted commit marker, and log query before claiming the rollout criterion. **Fix:** either add a post-merge deployment/monitoring phase with authority and wait state, or explicitly redefine this run as PR-readiness and move hosted canary/24-hour observation out of this run's Definition of Done.
