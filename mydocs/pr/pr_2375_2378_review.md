# PR #2375·#2378 검토 — 양식 값·누름틀 undo (lpaiu-cs, #2369 Track 2 완결)

- #2375 (Fixes #2374): 양식 컨트롤 값 변경(체크박스·라디오·콤보·편집 필드)을
  `kind:'record'` + 경량 역연산(`SetFormValueCommand`)으로 기록 — 양식 모드에서
  snapshot 이 게이트에서 드롭되는 조건에서 유일한 기록 경로. 라디오 다중
  쓰기(그룹 해제+선택) 1 엔트리 원자화, before==after 유령 엔트리 필터,
  Enter→blur 이중 커밋 1회 가드.
- #2378 (Fixes #2377): 누름틀 넣기/고치기(일반 모드 전용)를 snapshot 라우팅
  (실패 시 throw 로 no-op 엔트리 방지), 지우기는 **일반 모드만** snapshot —
  양식 모드는 snapshot 드롭 = 무언 폐기 회귀가 되므로 기존 직접 경로 유지
  (비기록, 명시적 범위 외). 원장 25→26 사유 명시.
- 독립 브랜치 2건, 상호 결합 충돌 0 (input-handler.ts 서로 다른 구간)

## 로컬 재실증 (결합 트리)

| 게이트 | 결과 |
|--------|------|
| npx tsc --noEmit / npm test | 362/362 통과 (신규 소스 가드 6 포함) |
| red→green | devel src 원복 시 신규 테스트 6건 전부 FAIL → 복원 시 통과 |
| CI | 양쪽 전 항목 green, devel merge 충돌 0 |

브라우저 게이트(체크박스/라디오/편집필드/콤보 왕복, 손실 게이트, 다이얼로그
경유 넣기/고치기/지우기, 양식 모드 무회귀)는 기여자 실측 기록 — 스냅샷 무언
파괴 차단(게이트 5)이 #2337 계급 해소의 핵심 증거.

## 판단

**둘 다 merge 권고 (#2375 → #2378 순).** #2369 Track 2 완결. 이관 연작에서
확립된 규율(계급 분류, no-op 방지, 원장 갱신, 범위 외 명시)이 그대로 유지됨.

## 처리 결과 (2026-07-18)

merge 완료(#2375 → #2378, admin) + 감사 코멘트 게시. #2374/#2377 은
close-issues 워크플로 자동 close. **#2369 Track 2 완결.**

## draft #2370 메인테이너 답변 (2026-07-18)

설계 판단 6건 답변 게시(승인 후): ①원자성=진짜 원자성 방향·별도 이슈 승격
②커서 착지=한컴 실측 게이트·별도 이슈 ③wasm 인자=현행 유지 ④Escape=취소
의도(한컴 실측 후 확정) ⑤form 분기=도달불가 재확인·방어 존치+주석 갱신
⑥fieldMarker stale=경미 수용. 부수: 클러스터 A 공용 no-op 장치 방향 승인.
Escape 한컴 실측 판정은 작업지시자 후속.
