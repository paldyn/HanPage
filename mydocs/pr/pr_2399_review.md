# PR #2399 검토 — TAC-모순 host 줄박스 가산의 문단 마지막 표 이연 (planet6897)

- #2373(#2352 revert 권고)의 재설계 대안 — revert 대신 지배 형상 수정
- 스택: #2391 위 (선행 merge 됨 → 실 diff = typeset.rs 1파일 +6/−1)

## 변경 본질

#2352 의 host 빈 줄박스 가산이 **표 배치마다 발화**하던 것을 기존 확립 신호
`is_last_placed`(문단 마지막 배치 표) 게이트로 한정. host 줄박스는 문단당
1개이므로 다중 표 문단의 중복 가산(중간 표 fit 과대 → 후속 표 조기 개행)이
지배 형상 — 10k 이분탐색 원장으로 #2352 +29 특정 후 8건 회수. 단일 표
문단(92셋 36392557)은 last==해당 표라 코드상 완전 동일 경로.

## 게이트

| 게이트 | 결과 |
|--------|------|
| 로컬: 전체 스위트(release-test)/fmt/clippy -D warnings | 전건 green |
| CI (결제 잠금 해제 후 재실행) | 전 항목 green |
| 기여자 실측 | 10k 53→45(회귀 0), 92셋 92/92 유지, 358 recount 동일, 민감 핀 유지 |

10k·92셋 코퍼스는 저장소 외 — 기여자 게이트 수치 + 저장소 내 핀 무회귀로 갈음.

## #2373 처리 (작업지시자 승인, 코멘트 게시됨)

revert 기각(92셋 트레이드) — 이연 재설계 채택 + 잔여 21건(두 문서군 판별자
필요)은 #2373 존치 추적, +1축 회귀 게이트 제안 포함.

## 판단

**merge 권고.** 자기 merge 건의 회귀를 스스로 이분·특정하고 revert 아닌
최소 게이트로 회수한 모범 사례.

## 처리 결과

(merge 후 기재)
