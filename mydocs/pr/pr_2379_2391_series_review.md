# planet6897 연작 검토 — #2379 + 스택(#2376→#2382→#2383→#2391)

- 구조: 독립 #2379(task2291-s5) + 4단 스택(#2376 ⊂ #2382 ⊂ #2383 ⊂ #2391)
- 스택 tip #2391 merge 시 안쪽 3건은 커밋 포함으로 GitHub 이 자동 merged 처리
- CI: 5건 전부 green. devel merge 충돌 0, #2379+#2391 상호 충돌 0

## 각 건 요지

| PR | 본질 | 게이트(기여자 실측) |
|----|------|--------------------|
| #2379 | landscape RowBreak short-row bleed 를 rowspan 걸침 행에서 제외 (`!rowspan_touched[r]` 게이트 1줄) — 세로 병합 라벨이 확정한 행 그리드에서 한글은 예산 초과 적재를 하지 않음. RHWP_DIAG_BLEED 판별(연결맵 rs_touched 전부 true vs 편람 전부 false)로 #1672 완화 대상과 완전 분리 | 연결맵 385→414(한글 415), 행 앵커 평균 \|Δ쪽\| 0.84, 편람 무회귀, 92셋 +1 회귀 0 |
| #2376 | lazy-base(page_base 미확립) 사다리에서 sb-형 spacing 트림 차단 — 트림 복원 전제(vpos-snap) 불성립 실측. 판별 대안 3건 실측 기각 기록 | 92셋 −1축 2→1 (36398700 정합) |
| #2382 | 셀 저장 vpos 흐름이 물리모순(extent < 줄높이합 절반)이면 신뢰 철회 → 누적 적층 폴백 — 다문단 vpos=0 겹침 렌더 정정. 렌더 한정, 쪽수 불변 | 하니스 잔차 −14.4pt×7 → +2.5pt. 샘플·회귀 테스트 동봉 |
| #2383 | 저장 ladder 의 TAC host spacing(sb/sa) 누락을 서명 일치(±2px)로만 판별해 성장 + back-snap 차단 — 무서명 1차 시도의 +1쪽 회귀 2건 실측 후 서명 게이트 확정 | 36399374 3~6쪽 경계 한글 정합, 92셋 91 유지 |
| #2391 | 분할 진입 첫 줄 full advance(lh+ls) 요구 — 한글 COM 하단여백 18단 사다리로 정책 실측 확정(orphan 가설 기각). 적용은 OMIT 서명 검출 문서군 한정(전역/HWPX-전체 적용의 회귀 실측 기각 기록) | **92셋 92/92 (100%), 캠페인 완전 소거(86→92)**, 358 recount 기준선 동일 |

## 로컬 재실증 (결합 트리: devel + #2379 + #2391 스택)

| 게이트 | 결과 |
|--------|------|
| 결합 merge | 충돌 0 (#2379 ↔ 스택 typeset.rs 상호 간섭 없음) |
| `cargo test --tests` 전체(release-test) | 실패 0 (issue_1733=242, byeolpyo, 핀·골든 포함) |
| fmt / clippy --all-targets -D warnings | 통과 |
| 연결맵 쪽수 | devel-원복 빌드 **385** → 결합 **414** (한글 2022 오라클 415쪽 — pdfinfo 재확인) |
| #2382 회귀 테스트 red→green | devel src 원복 시 FAIL → 복원 시 통과 |

92셋·358 recount 원본 문서군(기관 결재문서)은 저장소 외 기여자 코퍼스로 로컬
재실측 불가 — 기여자 게이트 수치와 CI green, 저장소 내 핀 전체 무회귀로 갈음.

## 시각 판정 자료

`assets/pr2379_rowbreak_bleed_review_p130_3way.png` — 콘텐츠 정렬 3-way:
한글 2022 p129 | rhwp after p130 | rhwp before p128. before 는 쪽당 ~2행
과다 적재(bleed 누적), after 는 오라클과 행 밀도 일치·잔차 ~1행(문서 전체 −1쪽,
앵커 평균 0.84쪽). 작업지시자 판정 대기.

## 판단

**전건 merge 권고** — 순서: #2379 → #2391(tip). 다섯 건 모두 서명·판별
게이트가 좁고, 대안 기각의 실측 반증이 본문에 기록된 모범 연작. #2279
캠페인 92셋 100% 완결이자 #2291 잔여 −34→−1 해소.
