# PR #2412 검토 — OMIT fresh-재계산 문서군 페이지말 정합 (planet6897, #2279)

- 본질: 계획된 "CS-근사 제거"의 전제를 한글 PDF 밴드/절대좌표 실측으로
  **자기 기각** — +272px 의 실체가 재저장 오라클의 stale 좌표(마스킹 전
  원본 레이아웃 보존)였음을 규명. **재저장 오라클 기법의 새 함정 기록.**
  devel 의 36392557 "6쪽 정합"이 상쇄 허상(2+4 vs 1+5)이었다는 쪽수 게이트
  사각 실증 포함.
- 수정 4건 (typeset.rs +236): ①OMIT-eager 사전 판별(3요건 동시, 임계 실측
  분리 0.39~0.80 vs 0~0.10, lazy 판별과 별도 필드 — 공유 시 +1 실측)
  ②페이지말 빈 문단 다음 쪽 귀속(텍스트 문단 반증 2건으로 범위 한정)
  ③합성 경계 sa 후방 스냅 금지(절대좌표 실측 3건) ④TAC host 줄박스 이중
  계상 억제 — #2373 잔여 트랙과 접점.
- 판별 반증 실측 5건(36386907/156652332/36391159/156577742/sample16)을
  기록하고 전부 최종 판별에서 제외 — 연작 특유의 반증 규율 유지.

## 게이트

| 게이트 | 결과 |
|--------|------|
| 로컬: 전체 스위트(release-test)/fmt/clippy --all-targets | 0 실패 / OK / 0 |
| CI | 전 항목 green |
| 기여자: 92셋 | **92/92 유지** |
| 기여자: 10k 발산 13건 COM 오라클 판정 | **FIXED 10 / REGRESSED 0** / BOTH_DIFF 3 |

발동 문서군(결재·보도자료 계열)은 외부 코퍼스 — 저장소 내 반증 핀(sample16
#2158 등) 무회귀 + 기여자 정량 게이트로 갈음 (연작 관례).

## 판단

**merge 권고.** 오라클 자체의 함정을 실측으로 규명하고 자기 계획을 기각한
뒤 원인을 4개로 분해한, 이 연작에서도 손꼽을 정밀도.
