# PR #2223 검토 — 비-Percent 줄간격 2×스케일 /2 재제출 (PR #2210 병합 누락 복구)

- 작성자: planet6897 (다수 기여자 — #229/#2184/#2198/#2205/#2210, 직전 사이클 연속)
- base: devel / head: `fix/2070-linespacing-resubmit` / MERGEABLE, CI 12항목 전부 SUCCESS
- 성격: **머지+회귀정정 패턴** — PR #2210이 `/2` 커밋(b1bdea5d) 직전 시점(65cdd3ce)에서
  머지되어 검증된 수정 하나가 devel에 누락된 것의 재제출. 검토일: 2026-07-12

## 변경 요약

| 파일 | 내용 |
|---|---|
| `src/renderer/style_resolver.rs` | 비-Percent 줄간격(Fixed/SpaceOnly/Minimum) `hwpunit_to_px(...) / 2.0` — 여백·문단간격·탭과 동일한 "저장값=유효×2" 규약 정렬 |
| `tests/issue_2070_rowbreak_density.rs` | 핀 시장구조조사 357→**307**, issue2063 159 유지 |
| `mydocs/report/task_m100_2070_v2_report.md` | 검증표·잔여 기록 갱신 (+42 → −8 과소 반전 명기) |

근거 체계: 통제 사다리(#2197) 계약 + 실문서 이중 실증(본문 ps Fixed 3320HU ↔ 한글
PDF 줄 pitch 22.1px = 3320/2) + 편람 한컴 HWPX case/default 이중 저장(1560/3120).

## 작업지시자 지정 검증 항목 — #2220과의 중복 여부: **중복 아님 (양면 실증)**

1. **레이어 상이 (코드)**: #2220은 layout.rs `tac_seg_applied` 음수 ls 분기의
   **흐름 전진 기점** 정정 — 저장 LINE_SEG(파일 지오메트리)의 lh/ls 를 사용하며
   tac 표 호스트 + om 포함 lh 증거 케이스 한정. #2223은 style_resolver 의
   **ResolvedParaStyle.line_spacing 값 스케일** — LINE_SEG 와 무관한 문단 스타일
   해석 층. 접점 없음.
2. **핀 실증 (동작)**: #2220 반영 상태의 devel에서 구 핀(357) 테스트가 **통과**
   = 시장구조조사가 여전히 357쪽(2× 줄간격 회귀 현존). #2220이 이 회귀를
   해결했다면 쪽수가 이동해 구 핀이 깨졌어야 한다. PR 적용 결합 상태에서
   신 핀(307/159) 통과 확인(아래 게이트).

역방향(이 PR이 #2220 효과를 되돌리는지)도 게이트의 `issue_2220` 표적 테스트로
확인. 기여자도 #2211 `relaxed_pad` 가드와의 무간섭을 사전 실측해 본문에 명기.

## 결합 검증 (devel 9aad129b + PR, 로컬)

| 항목 | 결과 |
|------|------|
| fmt / clippy (release-test, all-targets) | 통과 / 0 |
| `cargo test --tests --no-fail-fast` | **3,048 / 실패 0** — 신 핀(시장구조조사 **307**·issue2063 **159**), golden 8/8, 주보 아크 표적 4종(#2189/#2207/#2211/#2220) 포함 |
| OVR baseline 5샘플 (분리 폴더) | 개체 회귀 0건 |
| 역방향 간섭 (#2220 표적 테스트) | 통과 — 이 PR이 #2220 효과를 되돌리지 않음 |

## 판단

**merge 권고.** 검증된 커밋의 병합 누락 복구(머지+회귀정정 패턴)로, 근거 체계
(통제 사다리 계약 + 한글 PDF 실측 + 편람 이중 저장)가 견고하고 결합 게이트
전부 통과. #2220과의 중복 아님이 코드 레이어·핀 동작 양면으로 실증됐다.
잔여 −8(과소 반전)은 기여자가 별도 서브축으로 명기 — #2070 open 유지 정합.
