# PR #2184 검토 — NO_LS 셀 회계 정합 (planet6897, Task #2169)

- base devel / MERGEABLE / CI 전 항목 pass / 작성일: 2026-07-11
- 사이클: 최다 재기여자(이번 주기 74건). #2148 구성 매트릭스의 "부분 참값 전부 실패"
  상쇄 구조를 12 스테이지 통제 프로브로 해체한 후속 — 방법론 계보 정합.

## 검토

1. **정식화 3건의 근거가 전부 통제 실측**: 중첩 표 가산식(anchor_ladder 실측 + 80168
   r6 검산 160.1≈한글 160.2), 빈 문단 em 승격(empty_ladder 3위치 역산 2.001/3.000/4.001),
   왕복 판별(synthetic lineseg tag) — **하드코딩 보정 금지 규칙의 모범 사례**
   (복원 규칙 첫 적용: 샘플 번호는 주석 근거만, 유일 계수 0.85는 기존 baseline 관례).
2. **픽스처 재생성(#1891) 타당**: 구 픽스처가 과거 export 의 셀 lineseg 소실본
   (= 결함 자산)이라 stored-lineseg 표가 NO_LS 로 오판 — #1380 대칭 serializer 산출물로
   갱신. 원본 .hwp 직파싱은 재생성 전에도 통과(157)라 판정 기준 완화가 아님.
3. **보류 축의 정직성**: per-row 참이지만 상쇄 미해명인 축(#2150 em 공식·글자 채움)을
   실험 브랜치 보존으로 분리 — 게이트 통과 부분집합만 정식화.
4. 자체 게이트: issue_1891 8샘플 전부 + **359문서 recount IMP3/WOR0** (개선 3건 전부
   pdf 정합 방향) + 80168 per-row 오라클 일치.

## 메인테이너 재현 검증 (전수 통과)

무충돌 병합(R11/R17 구조 정합) / fmt ✓ / clippy 0 / 전수 **3,039/0** /
표적: issue_1891 3/3 · 1842 2/2 · 1116 13/13 · 2015 2/2 · 1937 1/1 / golden 8/8 /
OVR 5샘플 회귀 0.

## 판단

**approve + merge.** 시각 판정은 359문서 recount(IMP3/WOR0, 전건 pdf 정합 방향) +
per-row 한글 오라클 일치 + OVR/golden 무변동으로 갈음 (선택 적용 거버넌스).
