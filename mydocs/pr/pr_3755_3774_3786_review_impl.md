---
kind: review_plan
status: local-validation-passed
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-03
---

# planet6897 누적 검토·반영 기록 - PR #3755, #3774, #3786

기준은 `upstream/devel`의 `6ab503fe97b7abfd1839800c5c018da9f9abf4c5`다. 작업지시자
지시에 따라 planet6897의 열린 PR 중 [#3740](https://github.com/edwardkim/rhwp/pull/3740)만
제외하고, 나머지 #3755·#3774·#3786을 기본 작업트리의 devel 기준 가시성 branch에 누적했다.

| 순서 | 원 PR | 원 적용 SHA | 검토 브랜치 commit | 충돌 |
| --- | --- | --- | --- | --- |
| 1 | #3755 | `801a16d42` | `f00143987` | 없음 |
| 2 | #3774 | `0834a6a3b` | `928b5f639` | 없음 |
| 3 | #3786 | `2a16b2a52` | `a547ee0d2` | 없음 |

#3774 원 head의 devel 병합 commit `229d5b21c`는 기능 변경이 아니므로 누적 체리픽에서
제외했다. 원 PR branch에는 push하지 않았고, 검토 branch `review/planet6897-20260803`는
기본 `~/rhwp` 작업트리에 checkout해 VS Code와 Git Graph에서 `devel` 위 3커밋으로 보인다.

## 검증 순서와 현재 결과

1. #3755 focused regression: 2 / 2 통과.
2. #3774 focused regression: 2 / 2 통과.
3. 신규 HWPX fixture IR field sweep: 589행 TSV 기준선 일치.
4. 신규 HWPX fixture overflow-cell: 22행 TSV 기준선 일치.
5. `cargo test --profile release-test --tests`: 실패 표식 없이 완료, 마지막
   `visual_roundtrip_baseline` 3 / 3 통과.
6. Native Skia 라이브러리: 58 / 58 통과.
7. Native Skia fixture 2종은 각각 2 / 2, 4 / 4 통과했다. `wasm-pack build --target web
   --out-dir pkg`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets -- -D warnings`도
   현재 후보에서 모두 통과했다.
8. #3774 fixture는 MCP 기준 PDF(2쪽, validation `ok`)와 SVG sweep 2쪽을 비교해 구조 후보 `0 / 2`를
   확인했다. #3755 fixture는 MCP가 빈 PDF를 반환해 기준 PDF가 무효였으므로, rhwp SVG/PNG 2쪽과
   focused out-of-page 계약으로 확인하고 한컴 호환성 한계로 기록했다.

## #3786 측정 범위 보정

r29 측정은 #3740·#3755·#3774 세 commit 조합이다. 이번 누적 후보에는 #3740이 없으므로,
r29 결과를 이 후보의 성능 기준선으로 주장하지 않는다. 측정 기록에는 세 commit 포함 사실과
현재 누적 후보와의 차이를 명시한다.

## 후속 단계

1. 통합 PR의 최신 head·mergeability·required check를 재확인한다.
2. 최신 GitHub Actions 통과 뒤 작업지시자 승인에 따라 merge와 후속 정리를 수행한다.
