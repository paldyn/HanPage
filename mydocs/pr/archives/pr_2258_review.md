# PR #2258 검토 — HWP5 취소선 모양 화이트리스트 (yeonic, 첫 PR)

- 작성자: yeonic (**rhwp 첫 PR**) / 검토일: 2026-07-14
- base: devel / 2파일 +128/−7 / MERGEABLE / 단일 커밋 `0a967e0d`
- 요지: HWP5 `parse_char_shape` 취소선 판정을 `bits > 1` → **비트≠0 &&
  모양 화이트리스트(표 25 선 종류 0..=12)** 로 교체. HWPX #154
  (`is_real_strike_shape`)와 동일 원리의 자기정합.

## 검증

### 스펙 교차검증 (렌더링 의미 추정 금지 원칙)

- 스펙 표 25(테두리선 종류): **0..=12 = 실선~물결 2중선 13종** — HWPX
  이름 목록(SOLID..DOUBLE_WAVE→0..12 매핑)과 **정확히 1:1**. **13~16 =
  3D 계열**(placeholder 실증 15 = "3D 단선") — HWPX #154 가 배제한 "3D"
  이름군과 대칭. fail-closed 방향 일치.
- 유의: PR·기존 코드 주석 모두 "표 27"로 표기하나 저장소 스펙 문서
  기준으로는 **표 25** (표 27은 대각선 종류). 기존 관행 답습이므로 PR
  결격 아님 — 저장소 차원 정정 후보로 기록.

### 실측

- **수정 전 FAILED 실증**: patch-revert 로 파서 수정만 되돌려
  `hwp5_strikeout_shape_parity` 실패 재현 → 복원 후 2/2 통과.
- **자기정합 완전 성립**: 발동 쪽(111쪽) HWP vs HWPX export-svg
  **바이트 동일**. 취소선 획 실제 렌더 확인 — DOUBLE_SLIM(shape 7)이
  두 평행선(y=698.7/700.7)으로 그려짐 (수정 전 HWP5 는 취소선 0개 인식).
- 페이지 수 핀: issue_1949 테스트 115쪽 통과 (분할 무영향 주장 정합).

### 게이트 (PR head, 로컬)

| 게이트 | 결과 |
|--------|------|
| `cargo test --profile release-test --tests --no-fail-fast` | **3,160 / 0** |
| fmt --check / clippy(all-targets) | 통과 / 0 |
| OVR 5샘플 (±2px, 분리 폴더 `output/poc/pr2258/`) | 회귀 0건 |

### 구조/범위

- HWP5 파서 단일 지점 + 테스트만. 렌더러·공통 모듈 비접촉. 유닛 테스트
  경계값(0/1/3 비트 × 0/1/12/13/15 모양) 충실.
- 사내 문서 189건 집계는 비공개 참고 자료로만 취급 (판정 근거는 저장소
  샘플 쌍 + 스펙 + HWPX 대칭으로 충분).

## 판단

**approve → merge 수용 권고.** 스펙·자기정합·실측 3중 근거 성립, 첫 PR
수준을 넘는 완성도(수정 전 실패 실증까지 자체 수행).
