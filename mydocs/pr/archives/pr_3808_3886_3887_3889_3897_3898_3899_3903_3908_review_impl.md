---
kind: pr_review_implementation
status: local-validation-passed
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-04
---

# Kevin PR #3808·#3886·#3887·#3889·#3897·#3898·#3899·#3903·#3908 누적 구현·검토 기록

## 범위와 provenance

| PR | 누적 source commit |
| --- | --- |
| [#3808](https://github.com/edwardkim/rhwp/pull/3808) | `b0876fef9` → `fd04add7b` |
| [#3886](https://github.com/edwardkim/rhwp/pull/3886) | `c0db48565`, `69ab1c140` |
| [#3887](https://github.com/edwardkim/rhwp/pull/3887) | `8b4461124` |
| [#3889](https://github.com/edwardkim/rhwp/pull/3889) | `477f7afaa` |
| [#3897](https://github.com/edwardkim/rhwp/pull/3897) | `919bec6f0` |
| [#3898](https://github.com/edwardkim/rhwp/pull/3898) | `dc13fb675` |
| [#3899](https://github.com/edwardkim/rhwp/pull/3899) | `01cf461cc` |
| [#3903](https://github.com/edwardkim/rhwp/pull/3903) | `ef67bc1af`, `9cb92a9f8` |
| [#3908](https://github.com/edwardkim/rhwp/pull/3908) | `4ab58e306` |

모든 source commit은 최신 `upstream/devel` `ad67e5a63` 위
`review/kevin9327-20260804`에 `cherry-pick -x`로 누적했다. 원 contributor branch와 원 PR은
수정하지 않아 author·원 SHA provenance가 유지된다.

## 누적 검토에서 발견한 메인터너 보정

1. `fb33dfd72 fix(review): 계획과 진단 CLI 계약 정합 보정`
   - export-plan-schema가 비어 있는 `fieldExists`/`textFound`/`fieldEquals.name`을 허용하던
     schema↔runner 불일치를 수정하고 contract test를 추가했다.
   - diagnostics `bench`·dump의 option value 검증과 failure stdout 계약을 명시했다.
2. `45e53d42e fix(review): 내부 진단 CLI 플래그 계약 보정`
   - 확장된 preflight가 찾아낸 `gen-pua`, `gen-table`, `measure-width`, `test-caption`,
     `test-field`, `test-shape`의 미지 flag 침묵 수용을 제거했다.
   - flag가 output/input positional이나 numeric option value로 오인되는 경우도 실패하게 하고,
     `--` 뒤 dash-leading text는 보존했다.

두 보정은 원 기여 commit과 분리돼 있으며, 원 PR의 provenance를 rewrite하지 않는다.

## 검증

| 검증 | 결과 |
| --- | --- |
| diagnostics focused (`diagnostics_flag_contract`, `cli_exit_codes_diagnostic_commands`) | 28 passed |
| plan schema contract | 26 passed |
| `python3 tools/agent_preflight.py --repo . --bin target/review-kevin9327-20260804/release-test/rhwp` | 30개 command 계약 성공 |
| `cargo test --profile release-test --tests` | 종료 코드 0 |
| `cargo test --profile release-test --doc` | 4 passed, 2 ignored |
| `cargo fmt --check` | 성공 |
| `cargo clippy --all-targets -- -D warnings` | 성공 |
| `cargo check --target wasm32-unknown-unknown --lib` | 성공 |
| 변경 Markdown 상대 링크 검사 | 25개 문서 성공 |

`cargo check --target wasm32-unknown-unknown` 기본 명령은 native 전용 CLI binary까지 선택해 기존
`src/main.rs`의 native API 세 곳에서 실패한다. 이번 후보의 변경 위치와 무관하며, 실제 WASM
라이브러리 산출 경로인 `--lib` 검사는 성공했다. 렌더링·layout 변경은 없으므로 별도 PDF visual sweep은
수행 대상이 아니다. 전체 release-test 안의 기존 visual baseline 계약은 성공했다.

## 결론

로컬 누적 후보는 수용 가능하다. 원격 integration PR을 올린 뒤에는 그 head의 required CI와
mergeability를 다시 확인하고, 사용자 merge 승인 뒤에만 실제 merge·원 PR close·branch/격리 target
정리를 진행한다.
