# PR #2315 검토 — tac 대형 그림 문단 통밀림 수정 (#2311)

- PR: https://github.com/edwardkim/rhwp/pull/2315 (planet6897)
- 이슈: #2311 (10k r15 서베이 확증 결함) — Closes 선언
- base=devel, 1커밋 + 샘플/테스트 동봉, MERGEABLE/BEHIND
- 스택: #2321/#2323 의 선행 건

## 변경 본질

전면 tac 그림(ls[0] vpos=5435) + 후행 tac 헤더 표(ls[1] vpos=0 저장 리셋)
문단에서 저장 intra-para 분할을 무시하고 문단 전체를 다음 쪽으로 밀던
2결함 정정:

1. **pre-flush 판정**: 후행 단일 TAC 표가 저장 리셋으로 새 쪽 시작을 명시
   하고 선행 줄이 전부 tac 그림/도형이면 **리셋 이전 줄 높이만** fit 기준.
2. **유령 높이 계상 제거**: 리셋으로 새 쪽에 온 표는 자기 줄부터만 계상 —
   이전 쪽에 남은 선행 줄 높이의 새 쪽 유령 계상 차단.

가드: 단일 TAC + 빈 텍스트 + lineseg:controls 1:1 + 저장 vpos==0 + 선행
전부 treat_as_char 그림/도형 — 전부 저장 필드 기반 케이스 구조 가드.
저장 지오메트리 신뢰 계보(#2112/#2299/#2320)와 방향 정합 — #2320(텍스트
줄 경로)과 상보적인 TAC 표 경로의 같은 의미론.

## 로컬 재실증 (merged tree = devel bd97b614 + head)

| 게이트 | 결과 |
|--------|------|
| PR 회귀 테스트 | 2/2 |
| 156744475 페이지 수 | devel **5 → 3** (한글 PageCount 정합, 주장 일치) |
| byeolpyo1 / byeolpyo4 핀 | 4쪽 / 26쪽 유지 |
| `cargo test --tests` release-test | 실패 0 |
| fmt --all / CI(head) | 통과 / 전 항목 pass |

픽셀 일치율(58.9→95.4%) 주장은 한글 PDF 대조 산출이며 페이지 수·시각
구조(백지 유령 쪽 소멸)로 방향 확인.

## 판단

**merge 권고.** 저장 신호 존중 계보의 정석적 정정 — 최소 가드 + 자기 샘플
+ 회귀 테스트 동봉.
