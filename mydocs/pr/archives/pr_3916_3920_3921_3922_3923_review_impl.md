---
kind: pr_review_implementation
status: local-validation-passed
canonical: mydocs/manual/pr_review_workflow.md
last_verified: 2026-08-04
---

# Kevin PR #3916·#3920·#3921·#3922·#3923 누적 구현·검토 기록

## 범위와 provenance

최신 `upstream/devel` `874dae394` 위 기본 작업트리 branch
`review/kevin9327-20260804`에 source head를 번호순으로 누적했다. 원 contributor branch를 수정하지 않아
source author와 SHA provenance를 보존한다.

| PR | source head | 누적 결과 |
| --- | --- | --- |
| [#3916](https://github.com/edwardkim/rhwp/pull/3916) | `197391d9b` | `ae166a73c` — `d0a1dc360`은 이미 반영된 #3884 중복이라 제외 |
| [#3920](https://github.com/edwardkim/rhwp/pull/3920) | `06209a37d` | `53aba93f3` |
| [#3921](https://github.com/edwardkim/rhwp/pull/3921) | `96161b03e` | `85b06f958` |
| [#3922](https://github.com/edwardkim/rhwp/pull/3922) | `a3c0351d6` | `db94bde05`, `31fac9e4c`, `90fc51355` |
| [#3923](https://github.com/edwardkim/rhwp/pull/3923) | `7fdfc2a76` | `19aaf9398` |

## 충돌·메인터너 보정

- #3916의 `d0a1dc360`은 current devel의 #3884 CLI flag contract와 중복돼 적용하지 않았다. #3915의
  실제 두 축 보고 보정만 적용했다.
- #3920의 `mydocs/tech/README.md` 충돌에서는 현행 agent architecture와 incoming autonomous
  maintenance 색인을 함께 남겼다.
- `38d3b3b75`: CLI integration test의 nextest binary 경로와 HWP3 malformed-input error oracle을
  보정했다.
- `554eebaa6`: `rhwp-agent chunk-plan` 실행 힌트를 구조화 argv로 바꾸고 빈 field expectation을
  사용법 오류로 고정했다.
- `c6550e426`: 새 agent CLI 매뉴얼의 필수 문서 metadata를 추가했다.

## 시각 검증 판정

이번 누적 변경은 parser safety, CLI, agent CLI, 장기 문서와 테스트다. renderer, layout, typeset,
visual fixture, SVG/PDF golden을 변경하지 않으므로 별도 PDF visual sweep은 대상이 아니다. 전체
release-test에 포함된 기존 visual baseline은 실행 결과로 함께 기록한다.

## 검증

| 검증 | 결과 |
| --- | --- |
| #3916 focused (`issue_3915_verify_axes_both_reported`) | 3 passed |
| #3921 focused (`wmf_text_negative_string_length_no_panic`) | 3 passed |
| #3923 focused (`hwp3_slice_index_oob_no_panic`) | 2 passed |
| #3922 focused (`agent_toolkit_contract`) | 13 passed |
| 실제 #3916 CLI JSON | `verifyPages`·`verify` 동시 보고, exit 4 확인 |
| `cargo test --profile release-test --tests` | 종료 코드 0 |
| `cargo fmt --check` | 성공 |
| `cargo clippy --all-targets -- -D warnings` | 성공 |
| `cargo check --target wasm32-unknown-unknown --lib` | 성공 |
| 변경 Markdown 상대 링크 (`--changed-from upstream/devel`) | 517개 문서, 변경 38개, 이상 없음 |
| 문서 metadata | 이번 후보 신규 오류 없음; 전역 기존 오류 3건 별도 확인 |

`git diff --check upstream/devel`도 성공했다. metadata 전역 검사에 남은 3건은 이번 후보와 무관한 기존
문서 `mydocs/tech/envelope_provenance.md` 및
`mydocs/tech/task_m100_3604_password_encryption_cpp_review.md`의 허용되지 않은 kind/canonical 누락이다.

전체 release-test는 최초 검증 기준 `2971a1d9a`에서 실행했다. PR 생성 직전 `874dae394`로 rebase한
8개 devel commit은 CI workflow·운영 문서만 바꾸며 이 후보의 Rust source·test와 겹치지 않는다. 새 PR
head의 GitHub CI가 이 현재 기준을 최종 검증한다.

## 결론

로컬 누적 후보는 수용 가능하다. 모든 변경 문서 검사를 마친 뒤, 사용자 승인 후에만 원격 integration
PR 생성·push·CI 확인·merge·원 PR close·branch/전용 target 정리로 넘어간다.
