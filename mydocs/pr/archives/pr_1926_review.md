# PR #1926 검토 — Task #1842: 셀 내부 tac 묶음 전용 문단 라인높이 퇴화 수정

- 작성일: 2026-07-05 / planet6897 → devel / 시간순 1/7 (01:31)
- 연결 이슈: #1842 (Refs — 잔여 축 있어 close 판단 요청)

## 요지
`has_tac_shape` 라인높이 축소 분기가 max_fs=0(텍스트 없는 tac-전용 줄)에서 font_lh=0으로
퇴화 — 셀 내부 저장 lh(3401HU) 소실로 후속 블록 −33pt 시프트(3114781 p2). 수정 = 가드 1줄
`(cell_ctx.is_none() || max_fs > 0.0)`.

## 검토
- **반례 기반 정제 신뢰**: 1차 전역 가드가 issue_1116 핀을 깨뜨린 반증을 기록하고 셀 한정
  으로 좁힘 — 본문은 보상 기계가 축소값 전제로 한컴 정합이라 종전 유지. #1919와 같은
  판별 정제 패턴.
- 오라클 정량: Δbaseline median **−33.93 → +0.06pt** (한글 2022, n=54). big 코퍼스
  5,000 A/B 회귀 0/개선 1.
- 라운드 2 재구성(paragraph_layout.rs)과 충돌 없음(프리앰블 존, 추출 경계 밖).
- 신규 핀 `issue_1842.rs` + 픽스처. 렌더 변경 → OVR 게이트 대상 (결합 게이트에 포함).

## 판단
머지 권고. #1842는 잔여 국소 축(−11pt/+15pt)이 기록되어 있어 close 여부 작업지시자 판단.

## 결합 게이트 결과 (devel `d982067b` + 7건 시간순 통합, 2026-07-05)

fmt 통과 / clippy 경고 0 / `cargo test --profile release-test --tests` **2,889 통과·실패 0**
(신규 핀 11건 포함) / OVR baseline 5샘플 **개체 회귀 0건** (기준 00014ecf)
