# PR #3487 검토 — 채운 값이 안내문과 같아도 보존

Issue: #3380 / author: planet6897 / reviewer: edwardkim / milestone: v1.0.0
연작: planet6897 7건 누적 검토(2026-07-28), 체리픽 4순위

## metadata (작성 시점 참고값)

| 항목 | 값 |
|---|---|
| 기능 커밋 | `2851576f7` → 누적 `45510c468` |
| 규모 | +105 -0 (`queries/field_query.rs` +14, 테스트 +91) |
| CI | 원 PR head SUCCESS, mergeable CLEAN |

## 변경

누름틀 적재 정규화가 "채운 값 == 안내문 텍스트" 인 경우를 미기입 잔재로 오인해 실값을
버렸다. 사용자가 안내문과 동일한 문자열을 실제 값으로 넣는 경우(예: 안내문이 곧 기본값인
서식)가 실재한다. 수정은 기입 여부 플래그를 우선하고 문자열 동등성만으로 잔재 판정하지
않는다.

## 검증

- focused 2건 통과 (동일 문자열 실값 보존, 진짜 미기입 잔재는 종전대로 제외)
- 누적 branch 전체 게이트: release-test 4253 passed / 0 failed, fmt·clippy 클린

## 시각 판정

불필요 — 필드 값 질의 표면 변경, 렌더 무관.

## 권고

**merge (통합 PR 경유).** kevin9327 #3478(fill-fields 반복 항목)과 같은 누름틀 축이지만
층위가 다르다(적재 정규화 vs 기입 CLI) — 중복 아님을 확인했다.
