# PR #1931 검토 — Issue #1916: raw 미보존 HWP5 표 CommonObjAttr 왕복 소실 (정제 재제출)

- 작성일: 2026-07-05 / planet6897 → devel / 시간순 5/7 (05:22)

## 요지 및 관계
소스 수정부는 **기머지된 #1924 커밋 1과 동일**(변수명/경로만 상이) — supersede 체인의
중복 재제출 사례. 잔여 가치 = **실파일 픽스처**(`samples/hwp5-tbl-attr-1916.hwp`, 관세청
별지 서식 20KB 최소본) + **전용 핀**(`tests/issue_1916_tbl_common_attr.rs`, attr
0x082a2311 왕복). #1924의 인메모리 핀보다 실증력이 높다.

## 처리
충돌(serializer/control.rs)은 기머지판 유지로 해소, 픽스처+핀만 수용해 머지. 컨트리뷰터
코멘트로 중복 경위(배치 #1924 선머지)와 수용 범위를 공유한다.

## 판단
머지 권고(픽스처·핀 수용 목적). #1916은 이미 클로즈 상태 유지.

## 결합 게이트 결과 (devel `d982067b` + 7건 시간순 통합, 2026-07-05)

fmt 통과 / clippy 경고 0 / `cargo test --profile release-test --tests` **2,889 통과·실패 0**
(신규 핀 11건 포함) / OVR baseline 5샘플 **개체 회귀 0건** (기준 00014ecf)
