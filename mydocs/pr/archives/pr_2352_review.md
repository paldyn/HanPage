# PR #2352 검토 — TAC-자리차지 모순 표의 host 줄박스 가산 (#2279 반복분)

- PR: https://github.com/edwardkim/rhwp/pull/2352 (planet6897) — #2340/#2351
  스택 최종 건 (merge 후 잔여 1커밋 확인)

## 변경 본질

기계생성 결재문서(HWPX)의 모순 조합(treat_as_char + wrap=자리차지) 표에서
한글 fresh 가 배치하는 **host 빈 줄박스**(호스트 글자모양 크기)를 가산 —
한글 warm PDF **괘선 벡터 좌표** 실측(진입 22px ≈ om_top 1.9 + 15pt 줄박스
20px)으로 확정. 스코프 가드: HWPX 한정(`is_hwpx_source` — HWP5 재저장본
6건 실측 반증으로 확정) + 빈 host + 모순 조합만. 잠정 핀 갱신
(prep_1790387 141→144, 정답 146 방향 +3)은 핀 주석 지침 그대로 이행.

## 로컬 재실증 (merged tree)

| 게이트 | 결과 |
|--------|------|
| prep_1790387 핀 | 1/1 (144, 정답 146 방향) — devel 141 대비 실측 확인 |
| `cargo test --tests` | 실패 0 / svg_snapshot 8/8 / 시장 312 |
| 핀 | byeolpyo 4/26 · 연결맵 385 유지 / fmt/clippy 0 |

## 시각 검증 (거버넌스 — 발동 샘플이 저장소에 있음)

- 발동 지점 실측: p3 의 14×13 모순 조합 표(pi=27) 위 host 줄박스 +13.3px
  (used 897.6→910.9). p3 자체는 픽셀 동일(표 위치 불변) — 가시 차이는
  누적 시프트가 페이지 경계를 넘는 p101 부터 (항목 구성 diff 44페이지)
- **before/after 판정 자료**: `pr/assets/pr2352_tac_host_review_p101_{before,after}.png`
  — before 는 마지막 절("바. 타연구/차기연구에 활용")까지 조밀하게 밀어
  넣고, after 는 host 줄박스 가산으로 다음 페이지 이월 (한글 fresh 방향,
  141→144, 정답 146)

## 한컴 정답지 대조 (pdf-large 로컬 실콘텐츠, 146쪽)

- 전역: 한컴 146 > head 144 > devel 141 — PR 이 정답 방향 +3 (잔여 −2)
- 국소: 한컴 p106(제6장 시작)은 "바" 절 포함 = before 구성과 일치, after 는
  이월 — 앞 페이지들의 누적 차(+2) 흡수로 장 시작 구성이 어긋난 잔여
  (컨트리뷰터 "남긴 것"의 cap 재흡수 계열). 3-way/OVL 패널: pr/assets
- 작업지시자 판정 (2026-07-18): **연작 대기 고려, merge 승인** — 국소 어긋남은
  캠페인 잔여 축으로 추적

## 판단

**merge 권고 + 작업지시자 시각 판정 대기.** 괘선 벡터 실측·반증 기반 스코프
가드·핀 지침 이행 — 캠페인 규율 유지.
