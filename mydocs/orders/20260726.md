# 오늘 할일 - 2026년 7월 26일

## #3326 — v0.8.0 릴리즈 main 통합 (완료)

- 릴리즈 준비 PR [#3327](https://github.com/edwardkim/rhwp/pull/3327) CI 전 항목 green으로
  devel(`8bb8f277d`) merge 완료. merge된 devel 로컬 재검증(publish_guide 4단계): cargo build,
  release-test 전수(exit 0), Docker wasm 빌드 모두 통과.
- **main↔devel 이력 분기 발견**: v0.7.19 직후 main 직접 반영 문서 커밋(#2331~#2335,
  `Merge local/devel` 계열)으로 devel→main PR이 문서 20파일 충돌. `git log --find-object`
  전수 대조로 main측 변경 전부가 devel 이력의 과거본임을 확인(devel이 내용상 superset).
- 해소: `release/v0.8.0-main-sync` = devel + merge origin/main(충돌 20건 devel측 해소,
  `44c3aa17a`). 해소 트리가 origin/devel 트리와 tree hash 동일(`361a0b30`) — 내용 무변경 증명.
- 릴리즈 PR [#3328](https://github.com/edwardkim/rhwp/pull/3328) CI 전 항목 green → 작업지시자
  승인 후 **merge commit 방식**으로 main 통합(`60911e822`). main 트리 == devel 트리 재확인.
  squash 금지 사유: ancestry 소실 시 다음 릴리즈에서 동일 충돌 재발.
- 교훈: 릴리즈 사이 main 직접 문서 merge는 다음 devel→main merge를 충돌시킨다. main 반영분은
  devel에도 동일하게 들어가야 한다.
- GitHub Pages 배포 success, 사이트 200 확인. (배포 로그에 Actions 캐시 예산 소진 경고 —
  당분간 캐시 읽기 전용, 빌드 시간 증가 가능.)
- **후속 완료(작업지시자 승인, 1→2→3 순)**: ① 태그 v0.8.0(`60911e822`) + GitHub Release 생성
  → Publish All Packages success — npm @rhwp/core·@rhwp/editor 0.8.0, VS Code Marketplace·
  Open VSX 0.8.0 확인. ② 확장 zip 3종 + AMO source zip 빌드(main=v0.8.0 태그 트리, npm ci
  선행): rhwp-chrome-0.8.0.zip / rhwp-edge-0.8.0.zip (31.5MB), rhwp-firefox-0.8.0.zip
  (web-ext lint errors 0) + rhwp-firefox/rhwp-source-0.8.0-amo.zip (34.8MB, 제외 규칙 준수).
  ③ #3326 close (배포 결과 코멘트 포함).
- 잔여: 스토어 수동 업로드 — Chrome은 메인테이너 로컬 테스트 후 제출, Edge 동일 zip,
  Firefox AMO는 확장 zip + source zip 동시 업로드. 제출 문서는 mydocs/feedback/
  {chrome-0.8.0_kor/eng, edge-0.8.0_reviewer_notes, firefox-0.8.0_amo_notes}.md.

## v0.8.0 배포 점검 → SO-SUEOP 계보 3건 완결 (2026-07-26 오후)

작업지시자 "0.8.0 배포판에서 수정들이 미적용" 보고로 시작. 실측 결과 **머지 누락이
아니라** ① 미착수 버그(#3303)와 ② 오분류 위 증축(#3313 계보)이 원인이었다.
배포 산출물 자체는 전부 정상임을 실측(Pages 42쪽 픽셀 diff 0.011%, 확장 zip wasm
md5 동일, PWA SW 캐시 주의)으로 확정.

| Issue | 결과 | PR |
|---|---|---|
| #3303 문단 테두리 '없음' 오렌더 | **완결** — has_border Solid 합성을 한컴 실측(선 없음 매핑)대로 정정. 근거: 원시 바이트+한컴 UI+한컴 변환 HWPX 3중 + 코퍼스 271 스윕(border=1은 SO-SUEOP 유일). 음영 좌우 세로선(sample4)도 동일 뿌리로 함께 정정. 스펙 보완 주석 2건(표 13 offset 181·182 극성 반대) | [#3361](https://github.com/edwardkim/rhwp/pull/3361) merged |
| #3348 dev 전용 fetch 가드 | **완결** — populateExternalImagesFromDevServer 를 `import.meta.env.DEV` 가드. 확장 Errors 소음 제거 | [#3365](https://github.com/edwardkim/rhwp/pull/3365) merged |
| #3363 내장 OLE payload 추출 | **완결** — 1쪽 "이미지"는 외부 연결이 아니라 **내장 글맵시 OLE**(pic_type=1, 추가 정보 블록 id=2). 추출·재포장(mini_cfb, 한컴 동형 md5 실증) + Picture→Ole 변환 + OlePres000 WMF preview 렌더 + **WMF POLYPOLYGON 구멍 소실 정정**(단일 path 병합). issue_1692의 오분류 고정 테스트 교체. studio·확장 시각 판정 통과 | [#3378](https://github.com/edwardkim/rhwp/pull/3378) merged |

- **#3313 잔여 재정의**: 사이드카 공급 UX는 진짜 pic_type=0 문서 한정 과제로 축소
  (#3313 코멘트 기록). `samples/00000000.OOO`는 수동 추출 워크어라운드였으며
  **제거 완료**(`1135f0594`) — #3363 payload 추출 배선으로 존재 이유가 사라졌고,
  `issue_1692_so_sueop_hwp3_title_renders_from_embedded_ole`이 외부 파일 없는 렌더를
  검증한다. 관련 테스트 45건 통과로 무회귀 확인.
- **신규 이슈**: [#3377](https://github.com/edwardkim/rhwp/issues/3377) 확장 첫 로딩
  1·2쪽 세로축 어긋남(42쪽 이동 후 복귀 시 소멸 — 첫 페인트 과도 상태, dev headless
  비재현).
- **교훈 기록(작업지시자 지적)**: 글맵시 오분류가 워크어라운드+스냅샷 테스트로
  제도화됐던 사슬 —
  [트러블슈팅 문서](../troubleshootings/hwp3_ole_reference_name_misclassification.md)
  (환각 대응 요약: ① 스펙 비정확성 인지 ② 한컴 HWPX 변환본 비교) + 메모리 기록.
- **잔여 결정 대기**: 0.8.1 릴리즈 범위(#3303·#3348·#3363 포함 여부), #3377 착수,
  ir-diff PS 비교에 border_fill/선 종류 추가(후속 이슈 후보), WMF 병합 광역 영향은
  다음 10k 서베이에서 시각 확인 권장.

## #3319 — SO-SUEOP HWPX OLE 개체 선택 (완료)

[#3321](https://github.com/edwardkim/rhwp/pull/3321)은 HMapsi OLE preview가 화면에 보이지만
선택되지 않던 문제를 보정하는 `devel` 대상 PR이다. HWP 3.0 글맵시 계보의 문서를 HWPX로
변환해 rhwp-studio에서 열었을 때 개체가 선택되지 않던 버그다.

- 구현: HMapsi preview `RawSvg`가 원본 OLE `(section, paragraph, control)` ref를 보존해
  Studio `getPageControlLayout()`의 선택 대상으로 노출한다.
- 검증: Rust OLE 선택 10개, SO-SUEOP 렌더 11개, Studio headless 실제 클릭 E2E, CI·CodeQL·Render
  Diff 모두 성공했다.
- 증적: `mydocs/report/assets/task_m100_3319/so_sueop_hwpx_ole_selected.png`의 실제 선택 테두리·
  회전 핸들.
- **완료(2026-07-25)**: merge commit `61b13fad4`로 병합, #3319 close. 작업 branch와 검토 전용
  target은 2026-07-26 로컬 브랜치 정리에서 제거했다.

## planet6897 #3310·#3318 — 폰트 fallback 통합 검토 (완료)

- `review/planet6897-font-20260726`은 최신 `upstream/devel` `61b13fad4` 위에서
  #3310 `dc2f505` → #3318 `2e60b06`을 누적 cherry-pick한 검토 branch다.
- 메인터너 보정 `678494aa0`은 Web Canvas의 빠진 base-family fallback 네 경로와,
  headless form caption의 bundled Noto 최후 폴백을 보완한다. 원 contributor head에는 push하지 않는다.
- 검증: focused 폰트 tests, Native Skia 공식 3종(57·2·4 tests), release-test 전수 실행,
  fmt·clippy·diff check 통과. WASM build는 작업지시자가 수동 실행한다.
- #3300의 `156467716`, #3314의 `1.hwpx` 및 기준 PDF는 PR·issue·저장소에 없어 independent visual
  sweep은 보류로 기록했다. 자료가 제공되면 실제 PDF/PNG 대조를 추가한다.
- **완료(2026-07-25)**: 통합 PR [#3322](https://github.com/edwardkim/rhwp/pull/3322)의 merge
  commit `0324cd9`로 `devel` 반영. 원 PR #3310·#3318은 supersede 처리로 close하고 반영 위치를
  안내하는 감사 코멘트를 남겼다(머지+supersede 패턴).

## kevin9327 #3305 — WMF/CFB parser 안전 검토 (완료)

- 최신 `upstream/devel` `0324cd9` 위 가시성 검토 branch
  `review/kevin9327-parser-safety-20260726`에서 원 head `1b02247`을 `d307bac`으로 cherry-pick했다.
- 메인터너 보정 `e7a34e0`은 이미 parser가 거부하는 음수 BitmapInfoHeader width 변경을 제거하고,
  PolyPolygon 총점 65,536 및 추가 DIFAT의 중복 FAT SID 회귀 테스트를 추가한다.
- 검증: release-test lib **2921 passed, 0 failed, 7 ignored**, release-test integration 전체,
  fmt·diff check·clippy 통과. renderer/fixture 변경이 없어 visual sweep·IR baseline·WASM build는 대상이 아니다.
- contributor가 최신 `devel`을 merge해 source head `3009a46`과 기존 full CI를 갱신했다.
- **완료(2026-07-25)**: PR #3305 merged (`fix(parser): WMF/CFB 안전성 결함 2건 보정`, 이슈 #3301).

## kevin9327 #3345–#3394 — CLI·서식·정합 도구 15 PR 통합 검토 (완료)

- 최신 `upstream/devel` `19ed96d5ed79621cf81af109708423adc87130b5` 위
  `review/kevin9327-20260726`에서 #3345, #3347, #3352, #3354, #3356, #3360, #3362,
  #3364, #3369, #3371, #3374, #3376, #3384, #3390, #3394를 최신 contributor head까지
  누적 cherry-pick했다. #3391은 #3384에 포함된 이슈이며 별도 PR이 아니다.
- 수용 판단: **15건 모두 메인터너 보정 포함 수용 가능**. #3347/#3354의 search 절단 JSON
  계약을 단일·batch 양쪽에서 일치시켰고, #3384 `set-cell`은 대상 셀의 글꼴·크기·장평·자간을
  보존하면서 검정 일반 글씨만 적용하게 보정했다. `--row`/`--col`의 `u16` wrap도 입력 오류로
  막았다. #3394 갤러리 front matter를 manifest 허용 `reference`로 정정하고, 12쪽 전수
  몽타주의 글꼴 누락 상단 제목 strip을 제거했다(실제 페이지 p1–p12는 보존).
- 검증: 전용 `target/review-kevin9327-20260726`, `CARGO_INCREMENTAL=0`에서
  `cargo test --profile release-test --tests`, `cargo clippy --all-targets -- -D warnings`,
  `cargo fmt --check`, `git diff --check` 통과. `edit_set_cell_contract` 집중 5개도 통과.
  일반기안문 HWPX는 `fill-fields` 후 재독해 23개 필드 처리(`filledCount=23`; 의도된 빈 값
  4개 제외 19개 non-empty)를 확인했다.
- Windows 검증: `win10-ted` **cmd.exe**에서 독립 review target으로 `rhwp.exe`를 build하고,
  #3390 한컴 공식 PDF 대조를 실행했다(p1 2.55%, p2 10.50%, p3 15.09%). 표·문단·페이지
  흐름은 실제 PNG로 확인했고, PNG는 [#3390 검토 기록](../pr/archives/pr_3390_review.md)에
  인라인으로 남겼다. #3376 법정 서식·#3384 K-Startup 작성본·#3394 12쪽 몽타주도 같은 방식으로
  실제 이미지를 검토 기록에 포함했다.
- PR 준비물: 개별 수용 판단은 `mydocs/pr/archives/pr_3345_review.md`부터
  `pr_3394_review.md`까지, 통합 충돌·보정·검증 이유는
  `mydocs/pr/archives/pr_3345_review_impl.md`에 기록했다. 새 fixture·renderer/layout 변경이
  아니므로 IR field sweep baseline·별도 visual sweep·WASM build는 이번 통합 PR의 필수 게이트가
  아니다.
- **완료(2026-07-26)**: 통합 PR [#3397](https://github.com/edwardkim/rhwp/pull/3397)이 merge
  commit `5a4aed0bf`로 `devel` 반영. 원 PR 15건은 supersede 처리로 전부 close했다.

## lpaiu-cs #3323 — 머리말/꼬리말 필드 표시·모델 좌표 보정

- `review/lpaiu-cs-hf-field-20260726`은 최신 `upstream/devel` `99732b2a1` 위에서 contributor의
  `d9c5b325` → `ca8219d232`를 누적 적용한 통합 검토 branch다.
- 메인터너 보정 `a99db52f6`은 AutoNumber(Page)가 명시 쪽번호 marker까지 blanket 치환하지 않도록
  위치별 `display_text` run으로 분리하고, Studio undo가 native의 실제 `insertedAt`·`insertedLength`
  범위를 사용하도록 한다.
- contributor의 최신 P2 `5ce61c9`도 `cf2e52cc7`으로 적용했다. PUA 표시 문자열이 field marker 기준으로
  split될 때 조각이 원 run 전체의 stale display text를 상속하지 않게 하고, page text·Markdown 추출에
  display text를 배선한다.
- 최초 #3325 CI의 #1692 실패는 render-tree JSON이 `displayText`를 내보내지 않아 실제 footer `5`를 raw
  marker로만 검사한 문제였다. raw 모델 `text`를 보존한 채 `displayText`를 직렬화하고, #1692 계약을 표시 문자열
  우선으로 보정했다. 이어 전체 Rust가 #1100의 AutoNumber 뒤 `fwSpace` SVG 앵커 회귀를 포착해 raw run의
  `display_text` 재구성 및 명시 field의 bbox/캐럿 좌표 보정을 추가했다.
- 검증: 최종 `release-test` 전수는 lib **2,923 passed, 0 failed, 7 ignored** 및 모든 integration binary exit 0,
  #3216 5·#1100 3·#1692 1 tests, Studio build·637 tests, Native Skia 공식 3종(57·2·4 tests), fmt·diff check
  통과. `samples/SO-SUEOP.hwpx` 5쪽의 머리말·꼬리말과 쪽번호 `5`를 최종 Native Skia PNG로 재생성했고 기존
  asset과 SHA-256 `c4aa6cd11853ccc2a59db16631e5d04d58a2f19980b6c8d0860e579ff75211da`가 바이트 단위로 동일했다.
- **완료(2026-07-26)**: 메인테이너 독립 재검증(집중 9건·전체 4086/4086·Skia 3종·Studio 637·5쪽 시각
  무회귀 픽셀 diff 0) 후 merge commit \`9c26be219\`로 병합 — lpaiu-cs 커밋 2개 authorship 보존.
  #3323 close+크레딧 안내, #3216 완결. #3324는 별개 이슈로 open 유지(충돌 없음 확인).
  참고: asset SHA 바이트 동일성은 렌더 환경 민감 — 내용 동등성으로 검증.

## v0.8.1 릴리즈 준비 (#3401) — dependabot 선행 처리 + lpaiu-cs #3400 통합

v0.8.0 이후 `devel` 누적분을 0.8.1 PATCH 로 릴리즈한다. 신규 기능 6건이 포함되나 전부 CLI
도구 계층이고 라이브러리 API 변경이 없어 PATCH 로 확정(작업지시자).

- 이슈 [#3401](https://github.com/edwardkim/rhwp/issues/3401) 등록, 브랜치
  `task/3401-release-v0.8.1`, 수행계획서 `mydocs/plans/task_m100_3401.md`.
- **dependabot 연작 선행 처리**: postmelee 가 개별 PR 9건을 통합 PR 2건으로 묶어 둔 상태였다.
  둘 다 merge commit 방식으로 병합해 `cherry-pick -x` provenance 와 dependabot authorship 을
  보존했다.

  | 통합 PR | 범위 | merge commit |
  |---|---|---|
  | [#3392](https://github.com/edwardkim/rhwp/pull/3392) | GitHub Actions 7건 | `53ed38a40` |
  | [#3393](https://github.com/edwardkim/rhwp/pull/3393) | base64 0.23.0, snafu 0.9.2 | `ce2156dad` |

  개별 PR 9건 정리: #3330·#3331 기머지, #3332·#3333·#3335·#3336·#3338 은 #3392 머지 후
  dependabot 자동 close, #3340·#3343 은 Cargo lockfile 해석 차이로 자동 감지가 안 돼 반영
  사실 확인 후 수동 close.
- **lpaiu-cs [#3400](https://github.com/edwardkim/rhwp/pull/3400) 통합**(작업지시자 지시로
  0.8.1 범위 포함): 스타일 생성·수정·삭제가 편집 라우터를 우회해 undo 되지 않던 문제 해소.
  merge commit `91bd61758`, 이슈 [#3387](https://github.com/edwardkim/rhwp/issues/3387)
  auto-close 성공. 검토 기록은 `mydocs/pr/archives/pr_3400_review.md`.
  - 검증: merge simulation 충돌 0, `tsc --noEmit` 오류 0, studio `npm test` 641 pass,
    PR 신규 테스트 9 pass. studio TypeScript 한정 변경이라 시각 검증은 불필요 판정
    (intake_and_review 2.6 네 조건 모두 미해당).
  - 삭제를 snapshot 으로 기록한 선택이 타당하다 — 전문서 style_id 재배정은 역연산이 어렵고
    스냅샷 복원이 스타일 해석 캐시까지 재구성한다.
- 릴리즈 기준선은 #3400 merge 로 `91bd61758` 로 재확정. 기준선이 바뀌었으므로 앞서 통과한
  Rust 전체 테스트(lib 2932 pass)는 재사용하지 않고 재실행한다.
- 남은 열린 PR 4건(#3399 #3379 #2529 #2370)은 이번 릴리즈에서 제외한다.

## 로컬 저장소 위생 — 메모리 덤프 현행화 + 브랜치 정리 (2026-07-26 저녁)

이슈 없이 로컬에서 처리한 정비 작업이다.

- **메모리 덤프 현행화**: `mydocs/manual/memory/` 77 → 91 파일. 2026-07-17 덤프 이후 축적된
  피드백·프로젝트 메모리를 반영했다(커밋 `cd27bf6ff`, `5e7ec1046`). 신규 14건 중 주요 항목은
  환각의 테스트 제도화 사슬(#3363), `git stash pop || true` 금지, self-hosted 러너 실험 종료,
  wasm Docker 빌드 표준, r23 서베이 기준선이다.
  - 단순 복사는 저장소 front matter(kind/status/canonical/last_verified)를 지워 `48029fe9b`의
    메타데이터 정정을 되돌린다. 헤더를 보존하고 본문만 교체하는 방식으로 수행했고, 런타임 전용
    필드(originSessionId·modified)는 저장소 문서에서 제외했다. MEMORY.md는 historical 출처
    안내문을 보존하고 색인 항목만 교체했다.
  - `feedback_stash_pop_no_fallback`이 파일만 있고 색인에 없어 항목을 추가했다.
- **문서 전용 변경은 CI 상태 체크 예외**(작업지시자 확정): `devel` push 시 나오는
  `Required status check "Build & Test" is expected`는 문서만 바뀐 경우 무시한다. 소스·설정·
  워크플로가 섞이면 예외가 아니다. 메모리 `feedback_docs_only_ci_exempt`로 기록했다.
- **로컬 브랜치 정리**: 219 → 3개(`devel`, `local/devel`, `main`). PR 처리 과정에서 누적된
  작업용 임시 브랜치 283개를 삭제했다.

  | 라운드 | 수 | 기준 |
  |---|---|---|
  | `local/*` | 156 | `local/devel` 제외 전부. 미포함 76개도 최종 커밋 2026-03~06월의 종결 사본 |
  | 2주 경과 | 68 | 최종 커밋 2026-07-12 이전(`main`·`devel` 제외) |
  | 머지 확인 | 59 | PR MERGED/CLOSED 또는 `devel` 통합을 개별 확인 |

  - squash 머지 때문에 `git branch --merged`만으로는 42개가 미포함으로 잡혔다. PR·이슈 상태와
    `devel`의 실제 커밋을 대조해 전부 종결 건임을 확인했다. 예: `pr2372/2388/2390/2394`는
    MERGED, `pr/task-*-json` 8개는 기능이 `f68aa8be5`로 통합, `docs/survey-r23-*`는
    `survey_10k_r23_20260725.md`가 이미 report에 존재.
  - 복원 목록 3건을 홈에 남겼다(`~/rhwp-*-backup-20260726*.txt`, 283개 브랜치명+해시).
  - 판정 중 `git cherry`와 `diff.renameLimit 0`이 각각 타임아웃을 유발했다. 대규모 저장소에서는
    `git branch --merged/--no-merged` + PR 상태 조회가 빠르고 정확하다. renameLimit 설정은
    되돌렸다.
- 미추적 파일 정리: `rhwp-firefox-0.8.0.zip`, `rhwp-studio/e2e/tmp-*.mjs` 6종 제거(재생성 가능).
- **#3319 상태 정정**: 문서에 merge 대기로 남아 있었으나 실제로는 2026-07-25에 `61b13fad4`로
  병합·close 완료였다. 위 섹션을 실제 상태로 갱신했다.

## #3406 — Update branch 뒤 stale CI run 자동 force-cancel

- 이슈 [#3406](https://github.com/edwardkim/rhwp/issues/3406), 브랜치
  `task/3406-ci-force-cancel`.
- `devel` 대상이며 head가 `edwardkim/rhwp` 안에 있는 PR의 `synchronize`에서, 같은 PR의 이전 SHA
  active run만 force-cancel하는 `Cancel stale PR runs` workflow를 추가했다. live head를 취소 직전에
  다시 확인하고 PR source checkout·shell·PR 제공 script 실행은 하지 않는다.
- external fork PR은 GitHub가 `pull_request` token write 권한을 읽기 전용으로 낮추므로 자동 처리를
  우회하지 않고, `multi_pr_update_branch.md`의 수동 force-cancel 절차를 fallback으로 유지한다.
- 격리 fork PR [jangster77/rhwp#9](https://github.com/jangster77/rhwp/pull/9)에서 이전 SHA
  `db82b24`의 CI·CodeQL·Render Diff 3개가 `cancelled`되고, Update branch 새 SHA `4a049c8`의
  세 run이 다시 시작됨을 확인했다. reaper 증적: [run 30198335771](https://github.com/jangster77/rhwp/actions/runs/30198335771).
- 정적 검증: `actionlint`, Ruby YAML parse, embedded Script `node --check`, Markdown 링크 검사,
  `git diff --check` 통과. Rust·WASM·frontend·fixture 변경은 없어 해당 빌드·시각 검증은 대상이 아니다.
