# 오늘 할일 - 2026년 7월 26일

## 이월 — SO-SUEOP 판정 계보 잔여 (2026-07-25)

| Issue | 상태 | 비고 |
|---|---|---|
| #3303 | 착수 대기 | [hwp3/skia] 문단 테두리 '없음' 오렌더 — skia 축 유효(우측 세로선 실측), canvas2d 미렌더(백엔드 분기). 용의 커밋 bde926e4b, 수정 시 4-backend 대조 필수 |
| #3313 | 잔여 범위 | [studio] 프로덕션 사이드카 공급 UX(폴더 열기/다중 드롭 → injectExternalImage). 1차 정정(뷰 갱신 훅)은 PR #3317 merge 완료 |

## #3319 — SO-SUEOP HWPX OLE 개체 선택

[#3321](https://github.com/edwardkim/rhwp/pull/3321)은 HMapsi OLE preview가 화면에 보이지만
선택되지 않던 문제를 보정하는 `devel` 대상 PR이다.

- 구현: HMapsi preview `RawSvg`가 원본 OLE `(section, paragraph, control)` ref를 보존해
  Studio `getPageControlLayout()`의 선택 대상으로 노출한다.
- 검증: Rust OLE 선택 10개, SO-SUEOP 렌더 11개, Studio headless 실제 클릭 E2E, CI·CodeQL·Render
  Diff 모두 성공했다.
- 증적: `mydocs/report/assets/task_m100_3319/so_sueop_hwpx_ole_selected.png`의 실제 선택 테두리·
  회전 핸들.
- 다음: merge 승인 뒤 #3319 자동 close, `devel` 동기화, 작업 branch와 검토 전용 target 정리.

## planet6897 #3310·#3318 — 폰트 fallback 통합 검토

- `review/planet6897-font-20260726`은 최신 `upstream/devel` `61b13fad4` 위에서
  #3310 `dc2f505` → #3318 `2e60b06`을 누적 cherry-pick한 검토 branch다.
- 메인터너 보정 `678494aa0`은 Web Canvas의 빠진 base-family fallback 네 경로와,
  headless form caption의 bundled Noto 최후 폴백을 보완한다. 원 contributor head에는 push하지 않는다.
- 검증: focused 폰트 tests, Native Skia 공식 3종(57·2·4 tests), release-test 전수 실행,
  fmt·clippy·diff check 통과. WASM build는 작업지시자가 수동 실행한다.
- #3300의 `156467716`, #3314의 `1.hwpx` 및 기준 PDF는 PR·issue·저장소에 없어 independent visual
  sweep은 보류로 기록했다. 자료가 제공되면 실제 PDF/PNG 대조를 추가한다.
- 다음: 작업지시자 승인 뒤 통합 branch를 upstream 임시 head로 push하고 `devel` 대상 PR을 생성한다.

## kevin9327 #3305 — WMF/CFB parser 안전 검토

- 최신 `upstream/devel` `0324cd9` 위 가시성 검토 branch
  `review/kevin9327-parser-safety-20260726`에서 원 head `1b02247`을 `d307bac`으로 cherry-pick했다.
- 메인터너 보정 `e7a34e0`은 이미 parser가 거부하는 음수 BitmapInfoHeader width 변경을 제거하고,
  PolyPolygon 총점 65,536 및 추가 DIFAT의 중복 FAT SID 회귀 테스트를 추가한다.
- 검증: release-test lib **2921 passed, 0 failed, 7 ignored**, release-test integration 전체,
  fmt·diff check·clippy 통과. renderer/fixture 변경이 없어 visual sweep·IR baseline·WASM build는 대상이 아니다.
- contributor가 최신 `devel`을 merge해 source head `3009a46`과 기존 full CI를 갱신했다. 다음: 작업지시자
  push 승인 뒤 이 SHA를 재확인하고 contributor fork head에 보정·review 기록을 별도 commit으로 적용한다.
  보정 뒤 새 head의 full CI가 merge 전 조건이다.
