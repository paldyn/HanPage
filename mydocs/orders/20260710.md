# 오늘 할일 - 2026년 7월 10일

## planet6897 열린 PR 체리픽 검토 3차

| 항목 | 내용 | 상태 |
|------|------|------|
| #2107, #2109, #2119, #2133 / PR #2135 | `codex/planet6897-cherrypick-20260710` 브랜치에서 `upstream/devel` 기준 open PR 4건을 누적 체리픽. #2107 `layout.rs` 충돌은 `hide_fill`과 첫 쪽 배경 이미지 제한을 병합했고, #2109 `typeset.rs` 충돌은 RowBreak 포함 선언-fit 게이트로 통합. 원 PR별 review 문서 4건을 옵션 1 기준으로 `mydocs/pr/archives/`에 포함. | PR #2135 생성 및 #2134 merge 이후 branch update 완료. `git diff --check`, `cargo fmt --check`, focused cargo tests, `CARGO_INCREMENTAL=0 cargo test --profile release-test --tests`, `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings` 통과. merge 전 최신 CI 확인 예정. |
| #2129 / PR #2140 | `johndoekim`의 C1d 라인 누적(stacked/percentStacked) + 표식 렌더 PR 검토. 기준 PDF `pdf/chart/라인/*-2022.pdf` 대비 라인 5종 x hwp/hwpx 10개 visual sweep, `mydocs/pr/assets/pr_2140_*_review.png` 대표 증적 3장 보존. | PR #2140 approve 후 merge 완료(`38d1b5a2d7a7b6f068f6d436b94066dcf48497dd`). 옵션 2로 review 문서/asset/오늘할일을 docs-only 후속 PR 로 분리 진행 중. |
| #2141, #2142, #2143, #2144, #2147, #2149, #2155, #2157 / PR #2161 | `planet6897` 열린 PR 8건을 `codex/planet6897-cherrypick-review-20260710` 브랜치에서 `upstream/devel` 기준 누적 체리픽. 각 PR reviewer assign 완료, README 포함 PR은 README를 리뷰 근거에 반영. MCP 기준 PDF는 `pdf/task2136`, `pdf/task2137`, `pdf/task2098`, `pdf/task2146`, `pdf/hwp3`에 저장. 원 PR별 review/review_impl 문서 16건과 PR #2161 자체 review 문서를 옵션 1로 포함. #2087 `DocumentCore: Send` 회귀 방지 integration test도 현재 PR에 포함. | PR #2161 생성 완료. 체리픽 충돌 없음. #2149 테스트 주석 clippy 들여쓰기만 maintainer 보정. `git diff --check`, `cargo fmt --check`, Python tool compile, focused cargo tests, 관련 회귀 테스트, `CARGO_INCREMENTAL=0 cargo test --profile release-test --tests`, `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings` 통과. CI 대기 중. |
| #2152 / PR #2160 | `planet6897`의 per-pi oracle 미주 PageItem 정렬 PR 검토. `dump-pages` 미주 라벨이 `PartialParagraph`, `Table`, `PartialTable`, `Shape`까지 확장됐고, oracle 도구가 `[미주]` line을 본문 PI mapping에서 제외하는 것을 확인. | PR #2160 approve 후 merge 완료(`c03fff7b3c551835d205da0c8e0ca4a95f9cba8c`). 옵션 2로 review 기록 docs-only 후속 PR 진행 중. #2152는 잔여 PARA_COUNT 축이 있어 open 유지. |

## planet6897 PR #2163, #2165, #2168 통합 처리

| 항목 | 내용 | 상태 |
|------|------|------|
| #2156 / PR #2163 | 함초롬 계열 라틴 폭 대체를 최신 `devel` 위 통합 브랜치에서 재검토. 메인터너가 Haansoft Batang 적용 범위를 검증된 `함초롬바탕`/`HCR Batang` 정확한 별칭으로 제한하고, Dotum/확장 별칭 negative 회귀와 HCR Batang alias 회귀를 추가했다. 기준 PDF와 width-ladder visual sweep을 review asset으로 보존한다. | 옵션 1 archive 문서·PDF·대표 PNG 포함. 깨끗한 `target` 전체 Rust/Studio/WASM 사전 검증 통과, Open PR 생성 예정. |
| #2158 / PR #2165 | HWPX 저장 vpos 리셋 보존 변경을 #2163/#2168과 누적 체리픽했다. sample16 HWPX/HWP5 64쪽 pin은 통과했으며, HWP2020 MCP PDF 64쪽과 대표 visual sweep PNG를 보존한다. page 3/62/63/64의 기존 font/layout fidelity 차이는 serializer 보정과 분리해 잔여로 기록한다. | 옵션 1 archive 문서·PDF·대표 PNG 포함. 깨끗한 `target` 전체 Rust/Studio/WASM 사전 검증 통과, Open PR 생성 예정. |
| #2148 / PR #2168 | 80168의 em 줄높이·NO_LS 중첩 표·래핑 폭 상쇄 분석과 Windows Hancom COM 진단 도구를 누적 체리픽했다. production renderer는 변경하지 않으며, Python 문법/CLI 검증을 완료했다. | 옵션 1 archive 문서 포함. 깨끗한 `target` 전체 Rust/Studio/WASM 사전 검증 통과, Open PR 생성 예정. |

## Task #2164 Chrome 확장 표 셀 Enter 문단 겹침

| 항목 | 내용 | 상태 |
|------|------|------|
| Issue #2164 / PR #2166 | Windows 11 / Chrome / rhwp 확장 v0.2.8에서 문단 부호 표시 후 표 셀에 입력하고 Enter를 누르면 새 문단과 기존 내용이 겹치는 문제를 재현·정정한다. 원본 HWP는 `samples/issue2164/의견제출서(양식).hwp`로, HWP 2020 기준 PDF는 `pdf/issue2164/의견제출서(양식)-2020.pdf`로 보존한다. | PR #2166은 CI/CodeQL/Render Diff 성공 후 merge commit `f3712542e980b1eab625624f54eb49e789ddfc5d`로 반영됐고 #2164는 자동 close됐다. 반복 Enter의 셀 path/flat 커서 인덱스 불일치와 신규 문단 vpos reset 오인을 정정했으며, 실제 원본에서 세 번째 문단 캐럿 유지와 전체 Rust/프론트/WASM 사전 검증을 확인했다. 기준 PDF와 visual sweep은 1/1쪽 자동 후보 0건, 글꼴 fidelity는 별도 축이다. 옵션 2 docs-only fast-pass PR로 검토 기록과 증적 보존 진행. |
