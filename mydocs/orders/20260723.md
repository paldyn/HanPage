# 오늘 할일 - 2026년 7월 23일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #2431 | Actions cache quota 재발 방지와 Native Skia writer 정합성 | 진행중 | Draft PR #3123 전체 CI 통과, cold·restore-only 확인, devel save와 후속 warm restore 대기 |
## kevin9327 범위연작 — #2762~#3030 축별 배치 5건

작업지시자 지시로 오늘 범위를 "열린 PR 중 kevin9327 님의 #2762~#3030 연작"으로 한정하고,
축별 배치 5개로 나눠 cherry-pick 통합으로 처리했다. 23건 전부 재실증 후 채택됐다.

| 배치 | 축 | 원 PR | 통합 PR | merge |
|---|---|---|---|---|
| A | hwpx/hml 왕복 | #2822 #2881 #2888 #3003 #3015 #3026 #3030 #2762 | [#3197](https://github.com/edwardkim/rhwp/pull/3197) | `9c387b8346` |
| B | 문서 코어 편집 | #2821 #2829 #2849 #2859 #2910 #2923 | [#3198](https://github.com/edwardkim/rhwp/pull/3198) | `eb76c82773` |
| C | rhwp-studio UI | #2883 #2889 #2944 #2950 | [#3199](https://github.com/edwardkim/rhwp/pull/3199) | `0e7c013181` |
| D | 렌더러 | #2848 #2900 | [#3200](https://github.com/edwardkim/rhwp/pull/3200) | `4a1f3f2662` |
| E | hwp3 파서 + wmf | #2836 #2997 #3002 | [#3201](https://github.com/edwardkim/rhwp/pull/3201) | `5dac608fb3` |

- 전 배치 공통 절차: devel 에서 브랜치 → 오래된 순 `cherry-pick -x`(provenance 보존) →
  건별 BEFORE/AFTER 커밋 해시로 반영 검증 → 게이트(fmt / clippy / 전체 `--tests`
  release-test) → 승인 → merge → 원 PR 크레딧 코멘트+close → 연결 이슈 close 확인 →
  devel 동기화.
- **merge 방식 정정(07-24)**: A·B 는 메인테이너가 squash merge(`Co-authored-by:
  kevin9327`) 했으나, C·D·E 는 CI green 직후 jangster77 님이 merge commit 방식으로
  처리해 kevin9327 원 커밋이 authorship 그대로 devel 에 들어갔다. 내용은 검증본과
  동일하며 provenance 는 오히려 커밋 단위로 보존됐다. 당초 이 문서가 "squash" 로
  일괄 서술했던 것을 바로잡는다.
- 연결 이슈 21건 종결: #2751 #2755 #2784 #2825 #2831 #2835 #2843 #2853 #2875
  #2878 #2880 #2887 #2895 #2912 #2941 #2946 #2995 #3000 #3011 #3022 #3028.

### 충돌 해소 — 대부분이 "형제 병치" 유형

충돌 9건 중 7건이 같은 자리를 두고 겹친 형제 PR/기merge 커밋과의 병치였다(테스트 5,
필드/가드 2). 각 항목을 완전한 형태로 복원해 양쪽 모두 유지하면 되고, 소스 로직이
실제로 부딪힌 것은 2건뿐이다.

- **#3030 (배치 A)** — devel 의 bullet raw splice(#2790) 구조와 실질 병합. 동적
  attrs(+checkedChar)와 checkable 판정은 기여 PR 을, 원본 paraHead 무손실 splice 와
  필드 기반 폴백 값은 devel 을 유지했다.
- **#2762 (배치 A)** — 낡은 base 가 되살리려던 text_wrap 저장 차단 조건은 devel 에서
  이미 정정·제거된 것이라 거부하고, 신규 inflated_rows 조건만 수용했다.

### #2762 — 이틀 보류가 원칙대로 풀린 사례

회귀 테스트 부재로 07-21 부터 보류하고 테스트 골격을 코드로 제시했던 건. 기여자가
**제시된 골격 그대로** 테스트 2건을 작성하고, 브랜치에 얹혀 있던 #2760 커밋도 스스로
정리해 재제출했다. red→green 재현(가드 원복 시 정확히 FAIL)으로 확인 후 즉시 편입했다.
보류 기준을 일관되게 적용하면 기여자가 따라온다는 실증이다.

### 배치 D 시각검증 판단

전체 스위트 무실패에 기존 golden SVG 변화 없음 — 두 수정이 건드는 경로(테두리 문단
TAC, plane 다른 동일 이미지 겹침)가 현행 golden 샘플에 나타나지 않아 기존 렌더링
회귀는 없다. #2848 은 본문 텍스트 경로에서 이미 제거된(a30dca73) 이중 inset 을 TAC
경로에 일치시키는 선례 기반 정정이라, 시각 검증 거버넌스의 선택 적용 기준에 따라
한컴 대조 없이 채택했다(작업지시자 승인). 이슈 샘플(#2835 #2895) 한컴 대조는 필요
시 후속으로 잡는다.

## hwp3 각주 축 — 통합 리팩터링 시도와 보류

- 어제 남긴 hwp3 충돌 9건을 검토하니 각주 관련 4건(#3036 #3049 #3059 등)이 같은 함수
  (`hwp3_default_endnote_shape`)를 서로 다른 시그니처로 리팩터링하고 있었다
  (bracket:bool / +between_margin / &Hwp3DocInfo). 작업지시자 A 안 결정에 따라
  `&Hwp3DocInfo` 로 통일하는 메인테이너 통합 리팩터링을 만들어 [#3183] 으로 올렸다.
- **CI 에서 issue_1692 계약 테스트 2건이 실패**했다 — HWP3 endnote_shape 이 HWPX 와
  일치해야 한다는 기존 계약과 각주 margin 실배선이 충돌한다. 로컬에서 `--lib` 만
  돌리고 `--tests` 를 생략한 것이 원인으로, 이후 게이트는 처음부터 `--tests` 전체로
  바꿨다. #3183 은 close 하고 통합 커밋은 `task/kevin-hwp3-endnote-refactor` 브랜치에
  보존했다. **#1692 계약과 각주 margin 의 관계는 한컴 기준 시각 판정이 필요한
  사안**이라 각주 3건과 함께 별도 배치로 미뤘다.

## 절차 사고와 교정 3건

- **cherry-pick abort 를 성공으로 오판** — 순차 루프의 내부 플래그만 믿고 "9건 전부
  흡수"로 보고했으나 실제 2건이었다. 이후 모든 cherry-pick 에 BEFORE/AFTER 커밋 해시
  비교를 도입했고, 오늘 배치 전부에 적용했다.
- **낡은 stash pop 으로 트리 오염** — devel 대조를 위해 `git stash -u` 후 복귀했는데,
  깨끗한 트리라 stash 가 만들어지지 않았고 `git stash pop || true` 가 1년 전
  stash(pr1021_pending)를 꺼내며 충돌 상태로 풀렸다. `|| true` 가 실패를 숨겨 그 위에
  커밋까지 했다. 커밋 stat 의 무관 파일(+448줄)로 즉시 발견, hard reset 후 의도한
  2파일만 재커밋했다. 낡은 stash 는 보존돼 있다. 교훈: pop 은 `stash push` 가 실제로
  만들어졌을 때만, 그리고 커밋 전 `--stat` 확인.
- **턴 마비** — 반복 텍스트 무한 출력으로 작업지시자가 강제 중지했다. 상태 브리핑으로
  복구한 뒤 범위를 재확정받고 재개했다.

## 기타

- 기여자 force push 질문에 `--force-with-lease` 를 안내했다.
- 배치 B 에 부수 정리 1커밋 — #3083 으로 devel 에 유입된 clippy 경고 2건(johab
  테스트의 no-op `| 0`, byte_writer 테스트의 manual str::repeat) 제거.
- #2883(누름틀 안내문/메모 가드)은 devel 의 필드 이름 가드(#2851 계열)와 형제 필드
  병치로 해소 — 두 다이얼로그에서 이름/안내문/메모 세 가드를 모두 유지.

## 오늘 처리 집계

| 구분 | 건수 |
|---|---:|
| kevin9327 PR 채택(merge) | 23 |
| 통합 PR | 5 (#3197 #3198 #3199 #3200 #3201) |
| 연결 이슈 close | 21 |
| 보류(각주/시각 판정 대기) | 4 (#3183 close 보존 + #3036 #3049 #3059) |

- 잔여: 리베이스 대기 #2739 #2746(범위 밖), 존치 #2550(C안 대기)·#2659(로드맵 정독 대기).
- 채택률·중복률 추이: 기여자 중복 제출이 1차 23% → 2차 6.7% 로 감소. close 안내문과
  기여자 가이드(Claude 활용 6단계)가 작동하고 있다.
## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #2308 | `render_normalized`를 revision 기반 derived state/overlay로 전환 | 진행중 | draft PR #3130, 한컴 oracle gap #3128 분리, CI·review/결과 코멘트 대기 |
| #3126 | 브라우저 인쇄/PDF 저장 경로 안정화 후속 이슈 등록 | 완료 | #2657 Stage 1 이관, 구현은 후속 진행. 완료: 12:44 |
| #3126 | 발견 가능한 PDF 저장 진입점과 same-origin 인쇄 surface 구현 | 진행중 | 구현·Chrome/CDP 검증 완료, Windows Edge native dialog 게이트 잔여 |
| #2657 | v1 직접 PDF 저장 보류 결정 기록 및 RFC 종결 | 완료 | 원문 보존, 상세 재검토와 최종 결정 댓글 분리, #3126 이관. 완료: 12:56 |
