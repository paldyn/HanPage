# 오늘 할일 - 2026년 7월 25일

## #3319 — SO-SUEOP HWPX OLE 개체 선택 회귀

[`SO-SUEOP.hwpx`](../../samples/SO-SUEOP.hwpx) 1쪽의 그림형 OLE가 rhwp-studio에서 렌더되지만
클릭 선택되지 않는 문제를 해결한다. 작업 브랜치는 최신 `upstream/devel` 기준
`task/3319-hwpx-ole-selection`이다.

- 범위: 기존 `한셀OLE` 전용 Studio OLE 선택 E2E를 실제 문제 fixture까지 확장하고, page control
  layout·hit-test·객체 선택 경로 중 누락 지점을 고친다.
- 비범위: HWP3와 HWPX의 OLE/Picture IR 유형 통일, OLE 본문 편집 기능 확대, #3302/#3313의 외부
  연결 그림 적재 경로 변경.
- 검증: focused browser E2E에서 `SO-SUEOP.hwpx` 1쪽 OLE 클릭 후 `ole` 객체 선택 상태와
  원본 control 좌표를 확인한다. Rust/WASM 경계가 바뀌면 해당 Rust 회귀 테스트와 WASM build를
  함께 수행한다.
- 결과: HMapsi preview가 `RawSvgNode::new()`로 원본 OLE ref를 잃던 누락을
  `RawSvgNode::ole()`로 보정했다. `issue_2069_ole_object_selection` 10개, #1692의 SO-SUEOP
  렌더 11개, Studio headless E2E, TypeScript·fmt·diff 검사를 통과했다. 실제 선택 화면은
  `mydocs/report/assets/task_m100_3319/so_sueop_hwpx_ole_selected.png`에 보관했다. PR 생성은
  작업지시자 승인 대기다.

## lpaiu-cs #2370·#3272 — 최신 devel 통합 PR

외부 기여자 `lpaiu-cs`의 수용 가능 변경을 `upstream/devel`
`efc4b6c4c58b696c2fd4d28bbb82cbfeb6e0499d` 위
`review/lpaiu-cs-20260725`에 누적해 단일 integration PR로 올린다. 원 contributor branch에는
push하지 않는다.

- 수용: [#2370](https://github.com/edwardkim/rhwp/pull/2370)의 회전·대칭 뒤 중복
  `document-changed` emit 제거, [#3272](https://github.com/edwardkim/rhwp/pull/3272)의 그림 변환
  결과 메모와 BMP 표본-key P1 보정.
- 메인터너 보정: #2370 코멘트의 #2375 Edit-field Escape 취소/blur 무commit과 #2378 form-mode
  `removeCurrentField()` 방어 경로 설명을 코드·browser E2E로 분리 반영했다. #2370은 여전히
  누적 P3 tracker이므로 통합 뒤에도 close하지 않는다.
- 검증: review 전용 `target/lpaiu-cs-20260725-review`, `CARGO_INCREMENTAL=0`에서 release build·lib,
  release-test 전체, Native Skia 공식 3종, fmt, Clippy, doctest, WASM build, Studio typecheck·637개
  test와 form Escape browser E2E를 순차 완료했다. 최신 devel rebase 뒤 renderer 비교 PNG도 다시
  생성해 review asset에 반영한다.
- 증적: [#2370 review](../pr/pr_2370_review.md), [#3272 review](../pr/pr_3272_review.md),
  `mydocs/pr/assets/pr_3272_lpaiu-cs_issue2520_p001_review.png`.
- merge 전 조건: integration PR의 최신 head full CI와 mergeability 성공. PR 본문의 `Closes #2520`은
  merge 후 실제 issue 상태를 재확인한다. 이후 #3272는 기여자 credit·증적 comment 뒤 close하고,
  `devel` 동기화·review branch·전용 target 정리를 진행한다.

## #3309 — 문서-only 후속 기록의 green PR head 재사용

[#3309](https://github.com/edwardkim/rhwp/issues/3309)의 #3304 재현을 보정한다. 같은 PR에서 구현 commit의
full CI가 성공한 뒤 `mydocs/**` review·오늘할일 기록만 후속 push했을 때, CI·CodeQL·Render Diff preflight가
현재 base를 포함하는 직전 green head를 재사용해야 한다.

- 구현 PR: [#3312](https://github.com/edwardkim/rhwp/pull/3312), branch `task_m100_3309`.
- 구현 범위: 세 workflow의 candidate 최신순 탐색·current-base ancestor 확인·missing/pending 후보의 이전
  candidate 재탐색을 통일하고, 최신 완료 후보 실패·base 불일치·비허용 merge는 full CI fallback으로 유지한다.
  Update branch 뒤 stale run은 일반 cancel 없이 force-cancel API를 바로 사용하도록 절차를 맞춘다.
- 증적 정책: `mydocs/**`의 PDF, HWP/HWPX, PNG 등은 파일 상태와 확장자에 관계없이 문서-only 허용 범위다.
- 검증: 구현 SHA `7fed99fd2`의 full CI·CodeQL·Render Diff가 성공했다. 이 후속 문서 commit push 뒤 최신 head의
  preflight, `Build & Test` aggregate, CodeQL, Render Diff가 직전 green SHA를 재사용해 통과하는지 확인한다.
- merge 전 조건: 최신 head CI 성공, mergeable 재확인, 작업지시자 merge 승인. merge 후 #3309 close와
  `upstream/devel` 동기화·작업 branch 정리를 수행한다.

## kevin9327 CLI JSON 8건 — v2 통합 PR

`kevin9327`의 CLI JSON 기능 PR 8건을 최신 `upstream/devel` 위
`review/kevin9327-cli-json-20260725-v2`에 누적 체리픽하고, 개별 contributor PR은 변경하지 않는 통합 PR로
준비한다. 메인터너 보류 상태인 #3036은 대상에서 제외한다.

- 원 feature: [#3258](https://github.com/edwardkim/rhwp/pull/3258),
  [#3262](https://github.com/edwardkim/rhwp/pull/3262),
  [#3264](https://github.com/edwardkim/rhwp/pull/3264),
  [#3276](https://github.com/edwardkim/rhwp/pull/3276),
  [#3280](https://github.com/edwardkim/rhwp/pull/3280),
  [#3282](https://github.com/edwardkim/rhwp/pull/3282),
  [#3285](https://github.com/edwardkim/rhwp/pull/3285),
  [#3288](https://github.com/edwardkim/rhwp/pull/3288).
- v2 메인터너 보정: JSON 단일 입력 계약(#3258/#3262/#3280), 표 `containerPath`·control 역참조(#3280),
  글상자 주소·Unicode offset(#3285), `export-svg --json` 실패 stdout 0-byte(#3288), JSON command/MCP
  inventory 정합(#3264). `rhwp_bin()` 런타임 경로 호환도 6개 CLI integration test에 함께 반영했다.
- 검증: `cargo test --profile release-test --tests --quiet` 전체, focused contract tests,
  `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `git diff --check` 성공. renderer/layout/fixture/golden
  변경이 없어 visual sweep과 IR field-sweep baseline은 대상이 아니다.
- merge 전 조건: 통합 PR 최신 head의 full CI·mergeability 성공과 작업지시자 merge 승인. merge 뒤 원 PR/관련
  issue의 처리 방식, `upstream/devel` 동기화, review branch와 전용 target 정리는 별도 확인한다.

## #3296 — PR 검토 워크플로 조건별 가이드 분리

[#3296](https://github.com/edwardkim/rhwp/issues/3296)의 방대한 단일 PR review 매뉴얼을 canonical
라우터와 `mydocs/manual/pr_review/` 조건별 자식 가이드로 분리하고 PR로 통합한다.

- 공통 계약·권한·승인·라우팅·CI 병렬 구조는 모 문서에 유지하고, 역할별 처리·로컬 검증·시각 증적·
  review-only fast-pass·merge 후속 처리는 필요한 자식 문서만 읽도록 분리했다.
- 실제 CI의 Lint/Frontend 병렬 gate, archive/Native Skia 병렬 실행, 8개 test shard와 fast-pass A/B 조건을
  workflow 구현과 대조했다. 로컬 Cargo·wasm-pack의 순차 실행과 Cargo lock 방지 규칙은 유지했다.
- maintainer review 제안 A/B/C를 반영해 좀비 queued run과 `synchronize` 복구, devel 규약 변경의 열린 PR
  일괄 파급·보정, 신규 CLI 통합 테스트의 런타임 우선 `rhwp_bin()` 규약을 추가했다.
- 검증: `git diff --check`, 문서 메타데이터 398개, 내부 Markdown 링크 405개, 자식 문서 라우팅 완전성 검사를
  통과했다. 문서-only 변경이므로 Cargo 검증은 생략했다.
- merge 전 조건: 최신 PR head의 CI preflight와 `Build & Test` aggregate 성공, mergeable 상태 재확인,
  작업지시자 승인.

## lpaiu-cs 머리말·undo 5건 통합 PR 준비

외부 기여자 `lpaiu-cs`의 열린 PR 중 수용 후보 다섯 건을 최신 `upstream/devel` 위
`review/lpaiu-cs-20260724`에 누적 체리픽해 통합 PR로 준비한다.

- 수용 후보: [#3213](https://github.com/edwardkim/rhwp/pull/3213),
  [#3223](https://github.com/edwardkim/rhwp/pull/3223),
  [#3228](https://github.com/edwardkim/rhwp/pull/3228),
  [#3231](https://github.com/edwardkim/rhwp/pull/3231),
  [#3240](https://github.com/edwardkim/rhwp/pull/3240)
- 보류: [#2370](https://github.com/edwardkim/rhwp/pull/2370)은 범위가 확정되지 않은 Draft이므로
  이번 통합에서 제외한다.
- collaborator 보정: #3213의 field 표시/model offset 불일치와 #3223의 square-OLE 문단 병합 undo
  메타 복원 누락을 별도 커밋과 회귀 테스트로 보정한다.
- 검증: 전체 Rust integration test(`ir_field_sweep_baseline` 포함), fmt, Clippy, doctest,
  Native Skia 3종, WASM build, Studio typecheck·636개 테스트를 완료했다. #3240은 Windows
  Native Skia PNG를 실제 시각 검증 자산으로 포함한다.
- merge 전 조건: 통합 PR 최신 head의 GitHub Actions 성공과 작업지시자 승인. merge 뒤에는 원 PR별
  이슈 상태 확인·검증 근거를 담은 후속 코멘트·기여자 감사·`devel` 동기화·브랜치/검토 전용 target 정리를
  수행한다.

## 공통 — 운영 작업 (CI self-hosted)

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3284 | 검증 워크플로 self-hosted 전환 | 완료 | PR #3286 squash merge(4db23dec2)로 close |
| #3289 | 러너 20 인스턴스화 + 병렬성 회복 | 완료 | 8-shard 동시 배정 실측, CI 22m38s→**9m55s**(호스티드 이하). 공유 홈 결함 4종 수정(.path 형식, nextest·rustup 설치 레이스, CARGO_BIN_EXE 경로 고착 sweep 12파일) + 아카이브 호스트 캐시 공유·critical path CARGO_BUILD_JOBS 12. 거버넌스: `mydocs/report/task_m100_3289_report.md` |
| #3290 | CodeQL·Render Diff 호스티드 재배치 검토 | 신규 등록 | CI만 self-hosted 유지안 — 작업지시자 결정으로 후속 분리 |

## planet6897 #3270 — #2813 para-float stack 메인터너 보정

외부 기여자 `planet6897`의 [#3270](https://github.com/edwardkim/rhwp/pull/3270)을 최신
`upstream/devel` 위 검토 브랜치에서 실제 적용해 검토했다. 초기 P2였던 blank anchor의 overflow와
존재 검증 누락은 메인터너 보정으로 처리한다.

- 보정: 같은 host의 para-relative `TopAndBottom` 표가 둘 이상 바로 선행한 공백-only anchor는
  `PageItem` 문서순을 보존하면서 draw/flow advance만 생략한다. 회귀 테스트는 anchor 존재와
  **표 → 표 → anchor** 순서를 필수 assertion한다.
- 검증: full Rust integration test(`ir_field_sweep_baseline` 포함), Native Skia 3종, fmt, Clippy,
  doctest, WASM build, HWP 2020 기준 2쪽 visual sweep(자동 후보 0/2)을 보정 후 다시 통과했다.
- merge 전 조건: 사용자 push 승인 뒤 원 PR head에 메인터너 보정·검토 기록을 추가하고, 새 head의
  GitHub required CI가 모두 성공한 뒤 merge한다.
| #3290 | CodeQL·Render Diff 호스티드 재배치 | 완료 | PR #3292 merge(73be61921). CodeQL·RD는 호스티드, CI 13 job·sweep만 self-hosted. CodeQL ram 캡 제거(호스티드 자동 감지) |
| — | devel 첫 push 사고 2종 hotfix | 완료 | PR #3291 merge(125411f06): rust-cache save가 공유 ~/.cargo/bin의 nextest 삭제 → hosted 게이트 / CodeQL 무캡 JVM 2개(40~55GB)로 호스트 100G 포화·스톨 → 캡(이후 #3290으로 대체). 호스트 RAM 100→160GiB 증설(작업지시자). devel green 복구(CI 9m52s) |
| — | 동시 쓰기 레이스 전수 점검 hotfix | 완료 | PR #3293 merge(5242c1fad): rustup settings.toml 파손(dtolnay 7곳 게이트) + 전수 점검으로 wasm-pack·actions/cache 3곳·apt 2곳 정정. 점검표는 task_m100_3289_report.md. fix/2813 PR CI green 복구 확인 |
| — | kevin9327 PR 8건 rhwp_bin() 일괄 적용 | 완료 | maintainer edit로 각 브랜치 push + 안내 코멘트. 재검증 7 green / 3288만 시간상한 flake(호스티드 복귀 후 재검증 예정) |
| — | **self-hosted 실험 종료 — 호스티드 원상복원** | 완료 | 작업지시자 결정(공유 홈 레이스 연쇄 + CPU 간섭 flake + Pro 동시 40 상향). PR #3297 merge(d803c4343): ci.yml·sweep을 #3284 이전으로 복원(timeout·rhwp_bin·wasm-pack 원자설치는 유지), PR #3295 close. 러너 20개 등록 해제·서비스 제거·디렉터리 삭제(LXC 163GB 회수). 보고서는 재실험 함정 카탈로그로 존치. #3288은 devel merge push로 호스티드 재검증 |

## PR #3293 — 공유 Rustup settings 레이스 보정

[#3293](https://github.com/edwardkim/rhwp/pull/3293)은 `edwardkim`이 올린 self-hosted CI
hotfix이며, `5242c1fad39585e37177a853b56e78deb1871c5f`로 squash merge됐다.

- `dtolnay/rust-toolchain`의 병렬 job별 `~/.rustup/settings.toml` 재작성 경로를 hosted 전용으로
  게이트하고, self-hosted는 호스트 toolchain과 `rust-toolchain.toml` override를 사용한다.
- CI run `30148354306`의 lint, frontend, archive, Native Skia, 기본 테스트 8 shard, Build & Test와
  CodeQL·Render Diff가 모두 성공했다.
- 관련 [#3289](https://github.com/edwardkim/rhwp/issues/3289)은 PR #3286에서 이미 CLOSED였으므로
  재-close하지 않는다. 추가 후속 과제는 없다.

## planet6897 #3298 — 10k 서베이 r23 보고서

| PR | 처리 | 비고 |
|---|---|---|
| #3298 | merge (be65c568c) | docs 단일 +78. 쪽수 회귀 0, 픽셀 +0.007pp, 측정 갭 65(역대 최소). r22 폰트 오염 양방향 규명 → **r23 폰트-클린 신규 기준선 채택**, 이후 하니스 RHWP_FONT_PATH 필수. 메인테이너 감사 코멘트 완료. CI 큐 고착(러너 전환 과도기)은 force-cancel + devel merge push로 해소 |

## kevin9327 #3036 → #3306 인수·보완 (#3032)

| 항목 | 처리 |
|---|---|
| PR #3306 | rebase merge(018eb12a0, kevin9327 authorship 보존) — footnote_between_margin 각주 전용 ×4 배선 |
| 판정 근거 | 한컴 자체 HWP3→HWPX 변환 실측(footNotePr 284=71×4 / endNotePr 0) — 원 PR의 endnote 배선·raw 스케일 정정 |
| 검증 | issue_1692 골든 11/11, 전체 4005/4005, wasm 빌드, 시각 판정 통과(42·43쪽 diff 0px) |
| 후속 | PR #3036 close+크레딧 코멘트, #3032 완결. 파생 이슈 #3302(skia 1bpp BMP)·#3303(문단 테두리 '없음' 오렌더) 등록, #3302는 진단 완료 |

## #3302·#3313 — 외부 연결 그림 미표시 2축 정정

| Issue | 처리 | 비고 |
|---|---|---|
| #3302 | 완결 (PR #3316, de49a0c1c) | 오진 2회(1bpp 디코더·도형 오인식) 끝에 export-png/pdf의 populate 누락 확정. 1쪽 실 콘텐츠(상관 0.932)·42/43쪽 diff 0·4005/4005·시각 판정 통과 |
| #3313 | 1차 정정 merge (PR #3317, e29d4349a), open 유지 | 주입 후 뷰 갱신 훅 배선. headless e2e 0.0%→18.0% 자동 표시. 잔여: 프로덕션 사이드카 공급 UX |
| — | 교훈 | "SVG 되고 skia 안 됨" 대칭 증상 → 디코더 비교 전에 **입력 데이터 동일성(적재 단계)** 먼저 검증. gh 2.45에 update-branch 부재 → git merge+push로 대체 |
