# 오늘 할일 - 2026년 7월 22일

## planet6897 PR 3건 체리픽 통합 검토

- 대상: [#2807](https://github.com/edwardkim/rhwp/pull/2807), [#2810](https://github.com/edwardkim/rhwp/pull/2810), [#2811](https://github.com/edwardkim/rhwp/pull/2811).
- 최신 `upstream/devel` `4775e8c2`에서 실제 기여 커밋 4개만 오래된 순서로 체리픽했다. 각 원 PR의 `Merge branch 'devel'` 커밋은 제외했고 충돌은 0건이다.
- #2810은 한글 2020 MCP Print 기준 PDF 2개(HWP 2쪽, HWPX 1쪽)를 새로 만들고 visual sweep 3쪽에서 자동 구조 후보 0건을 확인했다. #2807은 10k 조사 보고서, #2811은 #2668 가설 반증·진단 도구로 기본 제품 동작은 바꾸지 않는다.
- 로컬 focused test, IR field-sweep baseline, WASM build와 세 원 PR의 최신 CI·CodeQL·Render Diff가 모두 성공했다. 검토 기록은 `mydocs/pr/archives/pr_2807_review.md`, `pr_2810_review.md`, `pr_2811_review.md`에 남긴다.
- 원격 push, 통합 PR 생성·merge, 원 PR/issue 후속 코멘트는 작업지시자 승인을 받은 뒤 진행한다.

## kevin9327 PR 11건 처리 — 낡은 base 대응

- 착수 시점에 [@kevin9327](https://github.com/kevin9327)의 열린 PR 11건이 남아 있었다. 밤사이 신규
  제출은 없었고, 어제 처리분(31건)의 잔여다.
- **11건 전부 base 가 낡았다.** `devel` 보다 12~32 커밋 뒤처져 있어 GitHub diff 에 4,400~6,900줄
  삭제로 표시됐는데, 실제 삭제가 아니라 base 이후 devel 에 들어간 커밋이 반대 부호로 잡힌 것이다.
  merge 는 3-way 라 파일이 지워지지는 않지만, cherry-pick 충돌과 커밋 얽힘을 유발한다.
- 실측해 보니 **충돌은 2건뿐**이었다. 나머지는 base 가 32커밋 뒤처져도 깨끗이 적용돼, 작업지시자
  지시에 따라 C 안(선별 처리)으로 진행했다.

### 처리 결과

| 그룹 | PR | 통합 | merge |
|---|---|---|---|
| A | #2738 글상자 귀속 · #2761 README DTO | [#2796](https://github.com/edwardkim/rhwp/pull/2796) | `f6d42001ef` |
| B | #2741 소스 가드 · #2763 스윕 하네스 | [#2797](https://github.com/edwardkim/rhwp/pull/2797) | CI 대기 |
| C | #2766 축 오용 · #2776 히스토리 라우팅 | [#2799](https://github.com/edwardkim/rhwp/pull/2799) | CI 대기 |

- **리베이스 요청 2건** — [#2739](https://github.com/edwardkim/rhwp/pull/2739),
  [#2746](https://github.com/edwardkim/rhwp/pull/2746). cherry-pick 충돌로 진행 불가.
- **중복 close 2건** — [#2773](https://github.com/edwardkim/rhwp/pull/2773)은 어제 #2754 로 merge 완료.
  [#2785](https://github.com/edwardkim/rhwp/pull/2785)는 본문이 `Closes #2723` 을 표방하지만 실제
  커밋 3개 중 #2723 수정이 없고, 어제 merge 한 #2717(Backspace)과 #2753(파일명 rename)을
  **되돌리는** 내용이었다. #2723 의 실체는 #2738 에 있어 그쪽을 채택했다.
- **보류 유지 1건** — [#2762](https://github.com/edwardkim/rhwp/pull/2762)는 어제 요청한 회귀
  테스트가 아직 없다(커밋 시각 07-21 22:29 그대로). 같은 배치의 #2731·#2758 은 red→green 을
  확인하고 merge 했으므로 기준을 일관되게 적용해 제외했다. 작성할 테스트 골격을 코드로 제시했다.
  이 브랜치에 #2760 커밋이 얹혀 있는 문제도 여전해 함께 안내했다.

## 기여자 가이드 — Claude Code 활용 방식으로 작성

- 작업지시자 지시로, 기여자도 Claude 를 쓰는 점을 고려해 **그대로 복사해 실행할 수 있는 형태**로
  작성했다. [#2739 코멘트](https://github.com/edwardkim/rhwp/pull/2739)에 전문, #2746 에 참조 링크를 달았다.
- 구성: ①낡은 base 가 만드는 문제(충돌·커밋 얽힘, #2762 의 #2760 얹힘을 실제 사례로) ②지금 할
  리베이스 절차 ③앞으로는 로컬 `devel` 이 아니라 **`upstream/devel` 에서 직접 브랜치를 딸 것**
  ④Claude Code 세션 시작 시 지시할 6단계 블록.
- 6단계에는 `git fetch upstream devel` → 이슈 `state`·`assignees` 확인 → `git grep` 으로 이슈 주석
  확인 → `upstream/devel` 에서 브랜치 → 게이트 3종 → red→green 재현 후 PR 본문 기재를 담았다.
  rhwp 는 수정마다 근거 이슈를 `[#1234]` 주석으로 남기므로 `git grep` 한 줄이 중복을 가장 많이 막는다.

## IR 필드 전수 스윕 하네스 — 기존 게이트의 맹점 실증

- [#2763](https://github.com/edwardkim/rhwp/pull/2763)이 드러낸 사실: `serializer/body_text.rs:28` 이
  `raw_stream` 이 있으면 원본 바이트를 그대로 되돌려주므로, **편집하지 않은 문서의 HWP5 왕복은
  바이트 재생**이라 발산 0건이 나온다. 제품에서 한 글자만 고쳐도 `raw_stream = None` 으로
  무효화되고 레코드 재생성 경로를 타는데, 그 경로를 재현하니 **481행 발산**이 나왔다.
- 즉 **왕복 게이트가 초록인 이유는 무손실이라서가 아니라 그 필드를 보지 않아서**였다. 최근 30일
  왕복 1속성 소실 37건이 정확히 그 사각지대에 있었다.
- 하네스가 찾아낸 결함의 수정은 한 글자다(`false` → `is_last_text_char`). placeholder 가 문단의
  **마지막 문자**면 `next_offset` 이 없어 판정이 항상 실패했고, 그러면 공백을 리터럴로 쓰고 남은
  컨트롤을 뒤에 다시 방출하므로 재파싱 때 placeholder 가 하나 더 생겨 **저장할 때마다 공백이
  1개씩 무한히 늘었다**(수렴하지 않음). 쪽번호 자동번호가 든 머리말/꼬리말이 대표 사례다.

### baseline 재생성 (메인테이너 보정)

- #2763 이 동결한 baseline 은 base `1658d0bb`(07-21 21:31) 기준이라 그 이후 merge 된 직렬화 변경이
  빠져 있었고, 그대로 두면 하네스가 **첫 실행부터 6건 회귀로 실패**한다.
- 증가 6건의 귀속을 확인했다. `char_count`·`control_mask`·`list_header_width_ref` 증가는
  **#2747(캡션 `LIST_HEADER` 방출 추가, `472d4b17`)** 이 원인이다. 캡션을 방출하면서 문단의 컨트롤
  개수가 실제로 늘어난 것이므로 의도된 변경이며 직렬화기가 필드를 잃는 것이 아니다.
- 같은 실행에서 개선 34건도 함께 보고됐다(래칫이 회귀만 실패시키므로 개선분은 baseline 에 반영되지
  않은 채 남아 있었다). 현재 devel 기준으로 재생성해 순감소 28행이 됐다.
- **이 사건 자체가 하네스의 가치를 보여준다.** 실패 메시지가 어느 샘플의 어느 IR 경로가 몇 건에서
  몇 건으로 변했는지까지 제시해 귀속이 즉시 가능했다.

## 패스스루 무효화 소스 가드

- [#2741](https://github.com/edwardkim/rhwp/pull/2741)은 #2698 을 개별 버그가 아니라 **재발 계급**으로
  규정하고, studio 의 `mutation-routing-guard.test.ts`(#2327) 설계를 Rust 절반으로 포팅했다.
  신규 파일 1개만 추가하고 기존 소스·테스트는 건드리지 않았다.
- 검사 5개 중 `guard_scanner_self_check` 가 핵심이다. 스캐너가 찾은 범위 내 `pub fn (&mut self)`
  수에 하한을 두어, 경로·파싱이 깨져 대상이 0건이 되면 검사 1~4 가 **"공허하게 통과" 하는 대신
  실패**한다. 가드가 아무것도 검사하지 않으면서 초록불을 켜는 상태를 스스로 막는 구조다.
- 면제 8건에는 각각 근거가 달려 있고, 판정 보류 항목은 "지속성 계약이 미확정이라 추정으로 고치지
  않는다" 고 명시했다.

## 추가 처리 — HWP3 차례 표시 (#2798)

- 오늘 배치 처리 중 신규 제출된 [#2798](https://github.com/edwardkim/rhwp/pull/2798)
  (`Closes #2765`)을 이어서 처리했다. 통합 PR은 [#2802](https://github.com/edwardkim/rhwp/pull/2802).
- HWP3 파서의 `parse_simple_control_char()` 가 코드 24(하이픈)와 코드 25(제목/표/그림차례 표시)를
  **같은 match arm** 으로 처리해, 비가시 TOC 표식 자리에 하이픈 글리프 `'-'` 가 본문 텍스트로
  삽입되고 있었다.
- 스펙 조항을 각각 명시해 분리했다 — §10.18 표 59(하이픈)와 §10.19 표 60(차례 표시, `hunit` 이
  0=제목/1=표/2=그림차례). **바이트·hchar 소비량은 6 bytes = 3 hchar 로 동일하게 유지**해 후속
  문자 정렬과 `char_shape` 위치를 보존하고, `char_offsets.len() == text.chars().count()` 불변식도
  지킨다. 글리프만 방출하지 않는다.
- 근거를 추정이 아니라 한컴 HWP5 변환본 정답지(이 표식에 가시 글리프가 없음)로 제시한 점,
  `parser/hwp3/` 밖 `.rs` 변경이 0건이라 CLAUDE.md 의 HWP3 아키텍처 경계를 지킨 점이 좋았다.
- red→green: `24 | 25` 를 다시 합치니 `task2765_hwp3_toc_mark_does_not_emit_hyphen` FAIL →
  복원 시 통과. hwp3 테스트 21건 green.

## PR #2658 — postmelee 확장 설정 방어 (merge 완료)

- [#2658](https://github.com/edwardkim/rhwp/pull/2658)은 Chrome/Edge 확장에서 사용자가 끈
  `autoOpen=false`가 업데이트 후 되돌아온다는 제보 대응이다. **원 제보가 재현되지 않았음을 먼저
  밝히고 "Chrome 업데이트가 설정을 초기화한다고 단정하지 않는다"고 범위를 그은 접근**이 좋았다.
  추정으로 고치는 대신 정적 조사와 오류 주입으로 확인된 방어 공백만 제거했다.
- [@jangster77](https://github.com/jangster77) 님이 남긴 `CHANGES_REQUESTED`에서,
  지적하신 공백을 **코드로 재현했다**. `loadSettingsForAutomaticActions()`의 `syncReadable`은
  `syncResult.ok`, 즉 **storage 호출의 성공**만 판정한다. 개별 키 누락은 조용히
  `DEFAULT_SETTINGS`로 대체되므로, local snapshot이 아직 없는 업데이트 직후에 `autoOpen` 키만
  사라지면 기본값 `true`가 되고 그 값이 snapshot으로 굳는다.
- 즉 **"읽기는 됐지만 값이 없다"와 "읽기가 안 됐다"가 자동 동작 관점에서는 같은 상태**인데
  전자만 기본값으로 흘러가던 공백이 있었다. 기여자는 update lifecycle에서 sync 설정을 보존하고
  `SYNC_META_KEY`/legacy key를 근거로 partial sync를 clean-install과 구분하도록 보완했다.
- 최신 head에서 requested-changes 재현 회귀, options 저장 실패, 중복 download event, 확장 배포 계약을
  재검증했고 GitHub Actions도 통과했다. [#2658](https://github.com/edwardkim/rhwp/pull/2658)은
  `bb99b8903bb3cec077ff808a6573a87b96739ee9`로 merge됐으며,
  [#2656](https://github.com/edwardkim/rhwp/issues/2656)은 auto-close 됐다.
- merge 후 확정된 review 기록과 이 오늘할일은 옵션 2 후속 문서 PR로 보존한다.
- **검토 절차상 반성** — 코멘트를 확인할 때 `head -10`으로 잘라 postmelee 님 것만 보고
  jangster77 님의 리뷰를 놓친 채 merge 준비를 진행했다. 작업지시자 지적으로 막혔다. 앞으로 PR
  검토는 `gh pr view --json reviews`로 리뷰 상태를 확인하는 것을 첫 단계로 둔다.

## PR #2667 — johndoekim 호스트 저장 통지 API

- [#2667](https://github.com/edwardkim/rhwp/pull/2667)(`Closes #2660`)을 통합 PR
  [#2803](https://github.com/edwardkim/rhwp/pull/2803)(`f85e689752`)으로 merge 했다.
- 호스트가 `exportHwp()`로 받은 바이트를 서버에 저장해도 스튜디오는 그 사실을 알 수 없어,
  미저장(dirty) 상태가 유지되고 자동복구 draft가 남는다. 다음 접속 때 "복구할 문서가 있습니다"가
  잘못 뜨는 원인이었다.
- 계약을 **"언제 resolve 하는가"** 수준까지 정의한 점이 좋았다. draft 삭제가 끝난 뒤 resolve
  하므로 호스트가 저장 직후 창을 닫아도 안전하다. capability 게이팅으로 구버전 스튜디오에서는
  조용히 성공한 척하지 않고 예외를 던진다.
- **핵심은 autosave 경합 방어다.** `saveDraft` 진행 중 `discard`가 끼어들면 저장이 완료되면서
  draft가 부활하는데, 이를 세대 카운터(`discardGeneration`)로 감지해 재삭제한다. 정상 flush에서는
  삭제하지 않는 반대 방향까지 테스트로 고정했다.
- red→green: 경합 감지를 무력화하니 해당 테스트가 정확히 FAIL → 복원 시 studio 503건 통과.
- 원 PR base가 `devel`보다 **115커밋 뒤처져** 두 곳에서 충돌했다. 둘 다 문서였고 코드 충돌은
  없었다. `npm/editor/README.md`는 오늘 merge한 #2761의 `getHmlSaveState` 문서와 같은 구역이라
  **양쪽을 모두 보존**했다.
- 작업지시자 지시로 크레딧 코멘트에 **devel 동기화 안내**를 담았다. 최근 이틀 사이 외부 기여
  PR을 30건 넘게 merge 했고 상당수가 `npm/editor/`·`rhwp-studio/`를 건드려, 브랜치를 딴 지 하루만
  지나도 base가 100커밋 이상 뒤처질 수 있다는 맥락과 함께 `upstream/devel`에서 직접 브랜치를 딸
  것을 안내했다.

## #2804 — CI github-script retries 미설정

- `devel` push 후 `Close linked issues` job이 실패했다
  ([run 29879631612](https://github.com/edwardkim/rhwp/actions/runs/29879631612)). GitHub API
  `/commits/{sha}/pulls`가 HTTP 500과 잘린 JSON을 반환한 **API 측 일시 오류**이며 저장소 코드
  결함이 아니다. 대상 이슈 #2660은 이미 수동 close 해 둔 뒤라 실질 피해는 없었다.
- 실패한 job 하나만 고치려다 전수 조사하니 **4개 워크플로의 github-script step 5곳 모두**
  재시도 설정이 없었다. 특히 `Detect review-only fast pass`는 `ci`·`render-diff`·`codeql`
  3곳에 중복돼 있어, 이 판정이 실패하면 후속 job이 전부 막힌다. **이번에 걸린 것이 영향이 가장
  작은 job이었을 뿐**이다.
- 5곳 모두 API를 읽기만 하므로 재시도가 안전하다(멱등). `retries: 3`만 추가했고,
  `retry-exempt-status-codes` 기본값(400,401,403,404,422)이 4xx를 제외하므로 5xx 계열만
  재시도된다. 순수 추가 15줄, 로직 무변경. [PR #2805](https://github.com/edwardkim/rhwp/pull/2805).

## 잔여와 다음 단계

- 통합 PR 은 전부 merge 했다 — [#2796](https://github.com/edwardkim/rhwp/pull/2796) `f6d42001ef`,
  [#2797](https://github.com/edwardkim/rhwp/pull/2797) `f2c02b6669`,
  [#2799](https://github.com/edwardkim/rhwp/pull/2799) `e1296cd66b`.
  크레딧 게시, 연결 이슈 6건 자동 close, 원 PR close, devel 동기화까지 마쳤다.
  [#2802](https://github.com/edwardkim/rhwp/pull/2802)(#2798)는 CI 대기 중이다.
- **기여자 조치 대기 3건으로 오늘 처리 가능분은 소진됐다.**
  - 리베이스 재제출 — #2739(공용 도형 `hp:sz`, ellipse/arc/polygon/curve/chart 5태그 경로)·
    #2746(`hp:pic` numberingType·groupLevel). 둘 다 실재하는 결함이고 어제 merge 한 #2719 의
    잔여라 정리 가치가 있다.
  - 테스트 보강 — #2762. 작성할 골격을 코드로 제시했다.
- [PR #2805](https://github.com/edwardkim/rhwp/pull/2805)(#2804 CI retries)는 CI 대기 중이다.
- 어제분 오늘할일은 [PR #2795](https://github.com/edwardkim/rhwp/pull/2795)(`d668053d6e`)로 반영했다.
- 존치: [#2550](https://github.com/edwardkim/rhwp/issues/2550) C 안 권고 후 기여자 구현 대기,
  [#2659](https://github.com/edwardkim/rhwp/issues/2659) 로드맵 정독 대기.

## 오늘 처리 집계

| 기여자 | merge | 중복 close | 대기 |
|---|---:|---:|---|
| kevin9327 | 7 | 2 | 3 (리베이스 2 · 테스트 1) |
| johndoekim | 1 | — | — |
| postmelee | 1 | — | — |
| 메인테이너 | — | — | #2804 CI 대기 |

merge 된 통합 PR: [#2796](https://github.com/edwardkim/rhwp/pull/2796) `f6d42001ef` ·
[#2797](https://github.com/edwardkim/rhwp/pull/2797) `f2c02b6669` ·
[#2799](https://github.com/edwardkim/rhwp/pull/2799) `e1296cd66b` ·
[#2802](https://github.com/edwardkim/rhwp/pull/2802) `ad4cf05d3f` ·
[#2803](https://github.com/edwardkim/rhwp/pull/2803) `f85e689752`

연결 이슈 close 9건: #2656 #2660 #2723 #2724 #2740 #2752 #2756 #2759 #2765

## #2809 — 나눔정렬 마지막 줄과 Canvas 음수 자간 정정

- 최신 `upstream/devel` `58991a768`에서 `task/2809-distribute-align` 브랜치를 만들어
  이슈 원본 HWP와 HWP 2020 기준 PDF를 대조했다.
- `Alignment::Split`을 일반 `Justify`와 같은 마지막 줄 규칙으로 처리해 한 줄
  `다 같 이`의 공백 분배가 빠지던 문제를 분리했다.
- 실제 rhwp 기본 백엔드는 `canvas2d`였다. 음수 자간으로 줄어든 cluster advance에
  glyph 폭을 맞추면서 위쪽 한글만 가로로 눌렸고, 폭만 정상화하면 마지막 `이`가
  셀 우측에서 잘렸다. Canvas glyph 폭 맞춤 제한과 마지막 glyph 잉크 여유 예약을
  결합해 두 문제를 함께 정정했다.
- 최종 픽셀 계약: 위·아래 첫 glyph `28/28px`, 위쪽 마지막 `이` `22px` 무잘림,
  분배 span `77.973/76.693px`. E2E assertion `7/7`을 통과했다.
- 전체 lib `2512 passed; 7 ignored`, SVG snapshot 8건, clippy, fmt, WASM build,
  Studio production build와 144dpi visual sweep(`flagged=0/1`)을 통과했다.
- PR 증적은 원본 ZIP/HWP, 기준 PDF, 최종 review PNG 한 장과 E2E HTML만 남긴다.
  중복 PNG와 tar.gz 압축본은 제외한다.
- PR 검토는 `mydocs/pr/archives/pr_2834_review.md`, 상세 결과는
  `mydocs/report/task_m100_2809_report.md`에 기록한다. push/PR 생성은 작업지시자
  승인 후 진행한다.
