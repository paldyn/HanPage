# 오늘 할일 - 2026년 7월 4일

## PR 처리

| PR | 내용 | 처리 |
|----|------|------|
| #1873 | #1695 HWP3 `LINE_SEG` rewind 페이지 경계 반영 | merge 완료. merge commit `e5ff8ab9311ff0a22c127f5a5081c4f1cacc421d`. `upstream/devel` update branch 후 option 1 review 문서/visual asset/오늘할일을 PR head 에 포함. 최종 head `3e64bbbfdc1fb3d378ae398906880ca76dc91941` 기준 GitHub Actions 통과. 로컬 검증(`issue_1695`, 영향권 회귀, full release-test, clippy, `git diff --check`) 통과. `SO-SUEOP.hwp` vs `SO-SUEOP-2024.pdf` visual sweep 46/46쪽, 자동 후보 0/46 확인. 대표 review PNG 4건을 `mydocs/pr/assets/`에 반영. #1695 auto-close 완료 |
| #1887 | #1811 HWPX saved bounds RowBreak 조판 보정 | merge 완료. merge commit `4676e2db7949a649e1eee3edc71c404854704042`. 잘못 생성한 #1885는 draft #1875 계열 커밋 혼입으로 close/force-cancel/브랜치 삭제 처리. #1887은 `upstream/devel` 기준 clean 브랜치에서 #1811 RowBreak/saved bounds 커밋만 재구성. 옵션 1로 review 문서/visual asset/오늘할일을 같은 PR head 에 포함. PR 생성 전 `release-test --tests`, `clippy --all-targets`, focused regression 3건, `git diff --check` 통과. `saved_bounds_cumulative_page_break.hwpx` p4/p5 visual sweep 자동 후보 0/1 확인. GitHub Actions 최신 head 통과 후 admin merge. #1811 auto-close 확인 및 PR/issue 후속 코멘트 완료 |
| #1886 | #1770 HWPX-origin 마커 기반 변환 HWP pagination 자기정합 | 원 코드 PR merge 완료. merge commit `86c3addd92cfb43fa1aaa487fdde6eb9aaaefa7c`. PR comment 첨부 기준 PDF 를 `pdf/issue1770_rowsplit_tolerance-2024.pdf` 로 복사해 검증. 옵션 2로 review 문서/visual asset/PDF/오늘할일을 별도 docs-only PR 에 포함. 로컬 검증(`issue_1770_hwpx_origin_marker`, `hwpx_to_hwp_adapter`, `issue_rowbreak_chart_overlap`, `hwp5_roundtrip_baseline`, clippy, `git diff --check`) 통과. HWPX 원본 및 변환 HWP 모두 Hancom PDF 기준 4/4쪽, visual sweep 자동 후보 0/4 확인. #1770 은 docs-only PR merge 후 asset 링크와 함께 후속 코멘트 및 close 처리 예정 |
| #1875 | 미주 배치·구분선·번호 렌더링 한컴 정합 | 원 코드 PR merge 완료. merge commit `2b21caef86ac1d3aa8918a30e937bcb7be5f6c18`. 로컬 검증(`issue_1139_inline_picture_duplicate`, `issue_1082_endnote_multicolumn_drift`, 관련 parser focused test, `release-test --tests`, clippy, fmt, `git diff --check`) 통과. 한컴 2024 기준 PDF 2건으로 visual sweep 수행: 2구역 fixture p1-p2 자동 후보 0/2, 3구역 다중 미주 fixture p3 자동 후보 0/1. review 문서, 기준 PDF, 대표 visual asset은 옵션 2 후속 문서/asset PR 로 분리 반영 예정. #1875 자체에는 auto-close 대상 이슈 없음 |
| #1890 | #1882 C1c 차트 스타일 4갭 보정 (#1431 Track C) | 원 코드 PR merge 완료. merge commit `4708f08beecfacfe590af55a53418102a904a757`. 로컬 검증(`ooxml_chart`, `issue_1882_chart_style_gaps`, `issue_1431_scatter`, `issue_1453_chart_3d_ofpie_routing`, full `release-test --tests`, clippy, fmt, `git diff --check`) 통과. chart 대표 7종 visual sweep 수행, 자동 후보 0건 확인. 팔레트/범례/축/공통 제목 개선은 merge 로 수용하되, 원형 기준 PDF 제목 `판매` vs rhwp `차트 제목` 잔여 차이는 후속 요청으로 분리. review 문서와 대표 visual asset 7건은 옵션 2 후속 문서/asset PR 로 반영 예정. #1882 는 auto-close 대상 아님(`Refs`)이며 후속 코멘트 후 open 유지 예정 |
| #1894 | #1858 발현 2 하단앵커 표 실측 높이 배치 | 원 코드 PR merge 완료. merge commit `f0dfb58b95f7f8446e99499767925196b15832a3`. `upstream/devel` update branch 후 최종 head `d4790d381cf9edceba715e4dcc0374ea199335d5` 기준 GitHub Actions 통과. 로컬 검증(`issue_1858_bottom_anchor_flush`, `issue_1858`, #1611/#1624/#1658/#1459 focused regression, full `release-test --tests`, clippy, fmt, `git diff --check`) 통과. `36389312` HWPX vs 한컴 2024 기준 PDF visual sweep 1/1쪽, 자동 후보 0/1, frame overflow 0, content bottom delta `-3.0px` 확인. review 문서와 대표 asset `mydocs/pr/assets/pr_1894_36389312_p177_review.png` 는 옵션 2 docs-only PR 로 반영 후 #1858 상태 확인 및 PR/issue 후속 코멘트 예정 |
| #1902 | #1638 convert/export-hwpx 변환 검증 게이트 | 옵션 1 self PR 생성. `convert`/`export-hwpx`에 `--verify`, `--verify-pages` 추가. Focused 검증, self PR full CI equivalent, Linux `ubuntu-ted`, Windows `win10-ted` SSH/cmd/PowerShell 검증 통과. review 문서 `mydocs/pr/archives/pr_1902_review*.md`를 PR head에 포함. visual sweep 대상 아님. GitHub Actions 대기 후 merge 및 #1638 후속 코멘트 예정 |
| #1906 | #666 시험지 ingest roundtrip 검증 복구 | 옵션 1 self PR 생성. `build-from-ingest` HWPX 직렬화 실패를 기본 DocInfo pool 초기화로 복구. 4종 시험지 PDF 전체 텍스트 레이어 기준 ingest→HWPX→export-text 보존 검증 통과(국어 1993/1993, 영어 680/680, 수학 408/408, 교육 통합형 1995/1995). `cli_commands.md`에 `build-from-ingest` 사용법과 검증 흐름 보강. 원본 PDF 시각 정합 검증 PR 이 아니므로 visual asset 없음. review 문서 `mydocs/pr/archives/pr_1906_review*.md`를 PR head에 포함 후 GitHub Actions 대기 |
| #1901 | #1893 빈 누름틀 HWPX 라운드트립 렌더 분기 | 원 코드 PR merge 완료. merge commit `ced8f37751be73191157982ca07a695a70c6b586`. 최종 head `433fb908455d670808333a91c468f4331ff0bb58` 기준 GitHub Actions 통과. 로컬 검증(`issue_1893`, `issue_258_clickhere_form_mode`, `hwpx_form_roundtrip`, `serializer::hwpx::section::tests`, full `release-test --tests`, clippy, fmt, `git diff --check`) 통과. `render-diff --via hwpx` 최대 변위 `0.00px`, `hwpx-roundtrip` diff=0 확인. 기준 PDF는 누름틀 표시를 보존하지 않는 출력물이므로 빨간 누름틀 안내문 차이는 실패로 보지 않고, 표/필드 배치와 1쪽 페이지 구성 기준 visual sweep 통과로 판정. review 문서 `mydocs/pr/archives/pr_1901_review*.md`와 대표 asset 2건은 docs-only PR 로 반영 후 #1893 auto-close 확인 및 PR/issue 후속 코멘트 예정 |
| #1905 | #1892 대법원 서식 HWP3 → HWP5 라운드트립 렌더 대변위 수정 | 원 코드 PR merge 완료. merge commit `23aea574acca7d1f20cfeb27e4ab4d06df8ad317`. maintainer 권한으로 보고서 placeholder 보정 커밋 `f4dee14aa854e514232ea480161c52963305020a` 를 PR head 에 반영. 로컬 검증(`issue_1892`, `issue_1244_tab_extended_fallback`, 대표 샘플 2건 `render-diff --via hwp`, full `release-test --tests`, fmt, clippy, `git diff --check`) 통과. 최종 head 기준 preflight 성공, heavy job skip, `Build & Test` 성공 후 admin merge. #1892 auto-close 확인. review 문서/오늘할일은 옵션 2 후속 문서-only PR #1907 로 반영 진행 |

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1667 | Build & Test `Dirty rhwp` / target cache 실효성 분석 코드 PR 보강 | 완료 | #1876 merge 및 issue close 완료: 03:24 |
| #1665 | Build & Test job 병렬 분리 설계 착수 | 진행중 | #1877 merge 후 PR/push 표본 P50/P90 최종 측정 문서 PR #1908 생성 완료 |

## 외부 PR / 내부 타스크 (오후)

| 항목 | 내용 | 처리 |
|------|------|------|
| PR #1878 | 분할 co-anchored 빈-host float 예산 이중차감 교정 (#1860) | 시각 판정(한글 2024 정답지 p44/45 3자 대조) 통과 → admin merge, #1860 close |
| PR #1879 | HWPX→HWP roundtrip fidelity 하니스 (도구 전용) | Linux 실전 스모크(SAME=3) 후 admin merge |
| #1868 | CLI `export-hwpx` 명령 추가 (HWP→HWPX) | 3단계 완료 — 구현 + 테스트 3(전체 2812 pass) + #1879 하니스 왕복 SAME + cli_commands.md 등재. merge 승인 대기 |
| — | WASM 재빌드 (devel `3d9945e5` 기준) | pkg 갱신 완료 |
| #1883 | 리팩토링 계획 수립 (#1582 umbrella) | 3단계 완료 | 재진단(대시보드 실측: 파일70·CC>25 80) + 마스터 플랜 초안 + 컬래버레이터 리뷰 게시. 리뷰 회신 대기 |
| — | 대시보드 스냅샷 보관소 (mydocs/metrics/) + metrics_history 통합 | 완료 | #1883 영점 스냅샷 커밋, --snapshot 옵션, history 무효 JSON 버그 수정, 추이 빈 상태 안내 |
| #1895 | #1747/#1884 `export-pdf` fallback/equation font 옵션 | PR 생성 및 옵션 1 보강 진행 | macOS/Linux/Windows 3경로 검증 PDF를 `mydocs/report/assets/task_m100_1747_1884/`에 첨부. PR 전 full CI(`build --release`, `test --release --lib`, `release-test --tests`, fmt, diff-check, clippy, doctest, Studio tsc/test, wasm-pack) 통과. review 문서 `mydocs/pr/archives/pr_1895_review*.md`와 대표 시각 검증 asset `mydocs/pr/assets/pr_1895_pdf_font_options_cross_platform_review.png`를 PR head에 포함 |
