# 오늘 할일 - 2026년 7월 28일

## Issue #3481 — HWP3 비밀번호 암호 문서 복호화

[#3481](https://github.com/edwardkim/rhwp/issues/3481)을 [PR #3483](https://github.com/edwardkim/rhwp/pull/3483)에서
처리한다.

- 압축 HWP3 비밀번호 문서의 DES 복호화, raw DEFLATE CRC32/ISIZE 검증, 512 MiB 압축 해제 상한을
  `src/parser/hwp3/`에 구현했다. 잘못된 비밀번호와 손상 입력은 같은 일반 오류로 처리하며, 비압축
  HWP3 암호 payload는 명시적으로 거부한다.
- 공용 parser·CLI·WASM·Studio의 기존 암호 입력 경로로 HWP3를 열 수 있게 했고, 실제 fixture로
  미입력·오입력·성공·CLI stdin·평문 저장 재열기를 고정했다. Studio는 HWP3/HWP5 모두에 취소·오입력·Enter
  성공·storage 비보존 브라우저 E2E를 실행한다.
- 새 fixture의 IR field sweep은 803개 샘플에서 baseline TSV와 차이가 없었다. Rust 전체 release-test,
  fmt·clippy, WASM build, Studio 674개 node test·production build, Chrome·Firefox 확장 build를 통과했다.
- 실제 Studio에서 HWP3 문서를 열어 24쪽·1쪽 canvas 화면을 확인한 증적은
  `mydocs/pr/assets/pr_3483_hwp3_password_open_review.png`에 보존했다. renderer fidelity 변경을
  주장하지 않으므로 Hancom 기준 PDF 대조·visual sweep은 수행하지 않는다.
- 이전 형식 표기는 `HWP3`로 통일했다. 원본 문서 본문의 "한글 97" 표기는 포맷 용어가 아니므로
  변경하지 않는다.

최종 merge 조건은 review·오늘할일·증적을 포함한 최신 PR head의 CI 통과, 메인터너 검토 및 작업지시자
승인이다.

## Issue #3486 — HWP3 암호 문서 한컴 기준 PDF 렌더링 정합 (진행 중)

[#3486](https://github.com/edwardkim/rhwp/issues/3486)은 #3483/#3516의 암호 해제·열기 범위와 분리해,
같은 HWP3 원본을 한컴 기준 PDF와 대조하는 renderer 정합 작업으로 진행한다.

- Stage 1은 한컴 오라클·24쪽 기준선과 아래아 누락 회귀 계약을 기록했다.
- Stage 2는 HWP3 추가 정보 블록 #6의 쪽 배경 이미지, 확인된 한컴 회사명 PUA, 문단 간격과
  인라인 개체 stream 좌표의 구조 손실을 복원했다. focused Rust 테스트와 HWP3/HWPX 1쪽 visual
  sweep 결과는 `mydocs/working/task_m100_3486_stage2.md`에 기록했다.
- 현재 남은 중앙 그림의 색조·effect와 본문/목차 기하 차이는 HFT·공개 폰트 차이와 분리해 Stage 3에서
  원인별로 조사한다. 자동 일치율은 후보 탐지값이며 최종 한컴 시각 판정은 작업지시자에게 있다.

## kevin9327 #3471 — ir-diff 표 셀 재귀 비교 (완료)

작업지시자 지시로 #3470 부터 착수했으나, 접수 단계에서 **같은 이슈 #3469 를 같은 파일에서
고치는 중복 PR**(planet6897 [#3470](https://github.com/edwardkim/rhwp/pull/3470) / kevin9327
[#3471](https://github.com/edwardkim/rhwp/pull/3471))임을 확인하고 판단을 요청했다.

- **작업지시자 결정: #3471 머지 + #3470 supersede close.** 채택 기준은 ① 글상자용 기존
  비교기(`diff_textbox_paragraph_lists`) 재사용으로 표/글상자 비교 로직이 한 곳에 남는 것,
  ② 결함을 발견한 실사례(보도자료 서식 CLI·MCP) 보고서가 증적으로 담긴 것.
- 결함: ir-diff 가 표 속성만 비교하고 셀 안 문단을 보지 않아 **변환이 표의 모든 텍스트를
  손상시켜도 `--verify` 가 통과**했다. 글상자는 #1807 이 닫은 구멍인데 표만 열려 있었다.
- 검증: focused 3 pass, release-test 전체 **4233 passed / 0 failed**, clippy 0, fmt·diff
  check 통과. **결함 검출 실증** — `src/main.rs` 만 수정 전으로 되돌리면 2 FAILED. 테스트가
  통과를 장식하는 게 아니라 결함을 실제로 잡음을 확인했다.
- 시각 검증 불필요 판정 — CLI diff 로직만 변경, 렌더 경로 무변경. PNG 2장은 렌더 증적이
  아니라 실사례 보고서 자산.
- **완료**: reviewer edwardkim 지정, merge commit `6c18949bb`(`--admin`, authorship 보존),
  이슈 #3469 auto-close 확인, #3470 supersede close + 교차 확인 감사 코멘트.
  검토 기록은 `mydocs/pr/archives/pr_3471_review.md`.
- 검증 부산물 참고: 결함 검출 실증 중 실패한 실행이 정리 코드에 도달하지 못해 저장소 루트에
  임시 hwp 가 남았다. PR 결함이 아니라 검증 방식의 부산물로 확인하고 제거했다.

## planet6897 연작 7건 — 통합 완료 (#3502)

작업지시자 지시로 planet6897 열린 PR 전체를 연작 처리했다. reviewer=edwardkim,
assignee=planet6897, milestone=v1.0.0 전 건 지정(`gh pr edit` 가 GraphQL deprecation 에
걸려 조용히 실패 → REST API 우회).

- **범위 경과**: 착수 8건 → #3491·#3497 을 컨트리뷰터가 kevin9327 선행 PR(#3478·#3482)과의
  중복을 스스로 확인하고 자진 close(#3470/#3471 선례가 규범으로 작동) → 신규 #3499 추가 →
  최종 7건. `pr_triage.sh` 와 대조 일치.
- **통합**: `review/planet6897-20260728` 에 오래된 번호순 `cherry-pick -x` 누적, 충돌 0.
  #3472(model 어순) #3475(query 수식) #3479(query 분할 표 쪽) #3487(core 실값 보존)
  #3488(render 인쇄 프로필) #3493(hwp3 마커) #3499(text PUA).
- **검증**: focused 20 pass, #3472 red-check 실증(수정 대상만 FAILED), release-test
  **4253 passed / 0 failed**, svg golden 무변화(#3488 screen·#3499 렌더 불변 교차 확인),
  IR sweep 통과, clippy 0.
- **#3488 시각 판정**: 신규 sweep 생략 — 근거 4가지(확립된 한컴 placeholder 인쇄 억제
  정책과 동형·screen 무변화 golden 실증·흐름 불변식 테스트·print 계약 고정)를
  `mydocs/pr/archives/pr_3488_review.md` 에 기록.
- **완료**: 통합 PR [#3502](https://github.com/edwardkim/rhwp/pull/3502) merge commit
  `ad16eb457`(CI SUCCESS 20). 이슈 7건(#3466 #3413 #3403 #3380 #3375 #3492 #3385) auto-close
  확인 — #3413 은 부분 해결로 열려 있었으나 #3475 가 마지막 누락 축을 닫아 함께 close.
  원 PR 7건 supersede close + PR별 맞춤 감사 코멘트. review 문서 7건 + 통합 구현 기록은
  통합 PR 에 포함돼 devel 반영.

## kevin9327 #3399 — bug-hunter capability 최종 검증 + 계약 실전 시행

27일 통합 검토에서 작업지시자가 제외했던 PR. 그 사이 원저자 kevin9327 과 collaborator
jangster77 의 3자 협업이 진행됐다 — 원저자 검토·찬성 2회, collaborator 보완 커밋(등록부
자동 검사, #3402 를 playbook 예시 6 으로 편입, 메인테이너 보정), 마지막 코멘트가
"@edwardkim 최종 메인테이너 검증·승인 필요" 로 공을 넘긴 상태였다.

### 최종 메인테이너 검증 — 4개 축 전부 적합

| 축 | 판정 근거 |
|---|---|
| CAP-<Issue> 식별번호 규칙 | 원자 발급 키, "문서 번호는 이슈 번호"(#2753) 규약과 동근거 |
| 시각 판정 권한 문구 | 픽셀 diff=후보·무회귀 한정, 최종 판정=작업지시자/maintainer — 거버넌스 일치 |
| 등록부 검사기 | unittest 4 pass, 링크·메타데이터 검사 실측 통과 |
| 문서 체계 | canonical manifest 등록, 예시 6 샘플 사실 정정(법학적성시험 A3) 확인 |

### 계약 실전 시행 (작업지시자 요청 — "메인테이너쪽에서도 동작하는가")

PR 정의 파일을 트리에 임시 반영하고, 서브에이전트가 **문서만 읽고** 6단 계약을 밟는 1회
시행을 실행했다. 여정: playbook 예시 2, `2022년 국립국어원 업무계획.hwp` 1~10쪽 ↔ 한글
2022 기준 PDF(양쪽 SHA-256 기록). GitHub 상태 변경 금지 dry-run, 산출물 스크래치패드 격리,
시행 후 트리 원상 복구(devel 일치 확인).

**결과: 방법이 문서만으로 실행 가능했고 10쪽에서 실질 격차 후보 4건을 원인 경로까지 특정.**

| # | 후보 | 성격 |
|---|---|---|
| 1 | 쪽번호 감추기(PageHide) 누수 — 두 쪽 걸친 문단에서 번호 시작 쪽(3쪽 `- 1 -`) 소실. `pagination/engine.rs:2844~` 컨트롤 무구분 para_index 비교 | 기계 판정 가능 실효 결함 |
| 2 | 목차 탭 점선 리더 극히 흐림 — 한컴 가운뎃점 589자 vs rhwp 0.1px dash (`svg.rs:3049`) | 시각 판정 필요 |
| 3 | PUA 책괄호 U+F0854/F0855 — 이 정답지 『』 vs 현행 매핑 《》(Task #528 exam_kor 근거) | **환각 제도화 사슬 패턴 후보 — exam_kor 정답지와 3중 교차검증 필요** |
| 4 | 글자 그림자를 rhwp 는 그리는데 한컴 2022 PDF 엔 없음 — placeholder 인쇄 억제 계열 가능성 | **한컴 편집기 실측 필요(작업지시자)** |

무격차 확인도 유효 결과로 기록(p4·p5·p8·p9·p10 문자 멀티셋 완전 일치).

**방법론 수확**: 실질 후보 4건 전부가 픽셀 diff 가 아니라 **기준 PDF 텍스트층 ↔ SVG 텍스트
쪽별 문자 멀티셋 대조**에서 나왔다(픽셀 랭킹은 폰트 잡음 11~18% 에 묻힘). 이 대조법의
playbook 편입 등 개선 5건을 PR 코멘트로 게시했다
([게시 코멘트](https://github.com/edwardkim/rhwp/pull/3399#issuecomment-5103259389) —
텍스트층 대조·fidelity_compare 크로스 플랫폼·--out-dir·REG 글롭 등급 명시·캡처 재시도).

### 처리 방침 (작업지시자)

- **merge 는 collaborator(jangster77)가 수행한다.** 메인테이너 검증·코멘트까지가 이 세션의
  범위. CI 는 head `61d1d1b79` 기준 CLEAN(SUCCESS 20).
- 격차 후보 4건의 이슈 등록은 작업지시자 판단 대기 — 후보 3 은 교차검증, 후보 4 는 한컴
  편집기 실측이 선행돼야 한다. 시행 산출물은 세션 스크래치패드에 있어 이슈 등록이 결정되면
  증적을 안정 경로로 옮긴다.

## #3508 — stale run 자동 cancel 을 fork PR 로 확장 (핫픽스 진행)

collaborator 실불만("내 PR 은 자동 cancel 되는데 fork PR 처리 땐 main 머지 제약으로 못
쓴다, 하나씩 처리할 때 너무 귀찮다")에서 출발. 진단: #3409 출하본이 원안(#3406 의
`pull_request_target`)보다 보수적인 `pull_request` 이벤트라 fork PR 토큰 강등 → skip.
"main 제약"은 `workflow_run` 대안에만 해당하고 `pull_request_target` 은 base 브랜치(devel)
파일로 동작해 main 불필요.

- 방침(작업지시자): 우리가 이슈 등록해 처리. A(reaper `pull_request_target` 전환, fork run
  의 `pull_requests` 배열 공백 quirk 대응) + B(수동 2.5 절차 원커맨드 스크립트) 병행.
- 이슈 #3508 등록, assignee edwardkim, 브랜치 `task/3508-fork-pr-stale-cancel`.
- 구현·검증·PR 완료: reaper `pull_request_target` 전환(head_repository+head_branch 대조,
  안전 경계 불변 주석), `scripts/cancel_stale_pr_runs.sh` 원커맨드 폴백, multi_pr 2.5 갱신.
  스크립트 실검증(fork 2건·닫힘 1건)과 jq 필터 필드 실데이터 확인까지 통과.
- **PR [#3509](https://github.com/edwardkim/rhwp/pull/3509) squash merge (`8ce70526d`)**,
  CI SUCCESS 20. PR 본문에 jangster77 격리 저장소 사전 검증 요청을 선택지로 남김.
- **실증 실패(12:02 UTC, #3503 수동 update branch)** — reaper run 0건. 실측 확정:
  `pull_request_target` 트리거는 **default 브랜치(main) 파일 기준으로 등록**된다.
  "base 브랜치로 충분" 은 내 오판이었고 collaborator 의 "main 에 머지되어야" 가 옳았다.
  2차 피해로 #3509 는 same-repo 자동 cancel 까지 끊었다(merge-ref 가 새 파일을 담아
  `pull_request` run 미발동).
- **핫픽스(작업지시자 승인)**: 이중 트리거 — `pull_request`(same-repo, 즉시 복원) +
  `pull_request_target`(fork, 릴리즈로 main 반영 후 개시). concurrency group 에 event_name
  포함(상호 cancel 방지), 2.5 문서 정정, stage2 보고서에 교훈 3건 기록.
- **핫픽스 PR [#3517](https://github.com/edwardkim/rhwp/pull/3517) squash merge (`94cf3cf25`)**.
  rebase push 의 synchronize 에서 same-repo reaper 발동·정상 판정 실증(run 30360295018,
  "force-cancelled 0" — 이전 run 완료 상태라 취소 대상 없음은 정직 기록). 교훈은 세션
  메모리 `feedback_workflow_trigger_default_branch` 로 저장.
- 잔여: 실취소 동작 실증(발동 순간 active stale 필요) → 다음 릴리즈 후 fork 실증 → #3508 close.

## PR #3516 — 암호 HWPX 기본 복호화·열기와 HWP3 아래아 보존

[PR #3516](https://github.com/edwardkim/rhwp/pull/3516)은 암호 HWPX의 ODF AES-256-CBC/PBKDF2
복호화와 HWP3 Johab 아래아 보존을 추가한다. [#3486](https://github.com/edwardkim/rhwp/issues/3486)와
관련 있지만 이 PR이 이슈를 닫지 않는다.

- 실제 암호 HWPX·평문 대조 fixture로 미입력·오입력·정상 입력, parser·CLI stdin·Rust/WASM 공개 API를
  검증했다. release-test 전체, fmt·clippy, wasm-pack build와 생성 npm tarball의 별도 설치·실행도 통과했다.
- **수용 범위는 기본 복호화·파싱·공개 API 열기뿐이다. HWP3/HWPX의 한컴 PDF 대비 시각적 정합은 검증·수용
  범위에서 제외한다.** #3486의 본문 흐름·폰트 메트릭·그림 배치 보정은 열린 후속 작업으로 남긴다.
- 암호 HWPX Studio 비밀번호 UI, 암호화 저장, 다른 ODF 암호화 계약은 이번 변경에 포함하지 않는다.
- 최종 merge 조건은 review·오늘할일을 포함한 최신 PR head의 CI, 메인터너 검토와 작업지시자 승인이다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #3507 | SectionDef CTRL_DATA 중복 직렬화 수정 | 완료 | 로컬·macOS·Windows 검증 완료, PR 생성 승인 완료·devel 통합 대기 · 완료: 20:50 |
| #3137 | stable 셀 입력 focused geometry 재사용 구현 | 완료 | Stage 3: exact cursor query full rebuild 제거 · 완료: 16:42 |
