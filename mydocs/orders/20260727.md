# 오늘 할일 - 2026년 7월 27일

> v0.8.2 핫픽스는 [7월 26일](20260726.md) 저녁에 시작해 날짜를 넘겨 이어진 연속 흐름이다.
> v0.8.1 배포 점검 → 확장 인쇄 실패 발견(#3433) → 수정 → v0.8.2 핫픽스 → **27일 새벽 1시
> 스토어 심사 요청**까지가 한 사이클이다.

## chrisryugj PR 2건 — 체리픽 통합 검토·HWPX XML 보정

`@chrisryugj`의 열린 [#3421](https://github.com/edwardkim/rhwp/pull/3421)과
[#3425](https://github.com/edwardkim/rhwp/pull/3425)를 최신 `upstream/devel`
`2d7303c5bea13eaf072e782cd7f7b4a6db59b35e` 위 가시 branch
`review/chrisryugj-20260727`에 기능 commit만 체리픽했다. source branch에는 push하지 않았다.

- #3421은 XML 1.0 비허용 scalar가 SVG·PDF·HWPX 방출 경로에 남지 않게 하는 변경이고, #3425는 HWPX의
  정상적인 `controls`/`ctrl_data_records` 길이 불일치에서 인라인 control 삽입이 패닉하지 않게 한다.
- #3421 정적 검토에서 공용 HWPX event writer의 text·attribute 경로가 빠진 것을 찾아
  `ad7ce8ca6`으로 보정했다. 해당 helper와 field 수동 attribute가 같은 XML 1.0 filter를 사용하고,
  실제 writer 출력 회귀 시험이 `U+0003` 제거와 markup escape 보존을 고정한다.
- #3382에는 재현 HWPX·한글 기준 PDF가 없어 시각 sweep을 꾸미지 않았다. 정상 문서 배치를 바꾸지 않는
  XML 유효성 변경이므로 실제 writer 출력·serializer 회귀를 증적으로 기록했다. 새 fixture가 없어
  IR field-sweep baseline TSV 변경도 없다.
- 전용 Cargo target에서 release-test 전체 **2,962/0**, Native Skia 공식 3종 **57/0·2/0·4/0**,
  fmt·diff check·clippy·WASM lib check를 통과했다. source CI도 두 PR 모두 전체 성공·`CLEAN`이다.
- 개별 review 2개와 통합 구현 기록을 같은 통합 PR에 넣는다. remote push·PR 생성은 승인 뒤에만 하고,
  통합 head CI·mergeable 성공 뒤 실제 merge 및 원 PR/issue 후속 처리를 진행한다.
## v0.8.2 핫픽스 릴리즈 (#3445) — 완료

v0.8.1 배포 직후 발견된 **브라우저 확장 인쇄 전면 실패**를 복구하는 핫픽스다.
태그 `v0.8.2` = `9b16aa9e2`, `v0.8.1..v0.8.2` 16커밋.
최종 보고서는 `mydocs/report/task_m100_3445_report.md`.

26일에서 이어진 항목: #3410/#3396(TAC 인라인 표 outMargin)과 #3379(r24 서베이)는 26일 문서에
"0.8.1 릴리즈 대기" 로 적혀 있었으나, 실제로는 0.8.1 이 아니라 이 v0.8.2 에 담겨 배포됐다.

| 대상 | 결과 |
|---|---|
| GitHub Pages | success, 사이트 200 |
| npm @rhwp/core · @rhwp/editor | 0.8.2 |
| VS Code Marketplace · Open VSX | 0.8.2 |
| 확장 zip 4종 | 빌드 완료 → **3개 스토어 심사 요청 완료**(메인테이너, 2026-07-27 새벽 1시) |

### 범위

| 항목 | 내용 |
|---|---|
| 확장 인쇄 복구 | [#3433](https://github.com/edwardkim/rhwp/issues/3433) — `print.html` 이 확장 빌드에 복사되지 않아 Chrome·Edge·Firefox 인쇄가 "파일을 찾을 수 없음" 으로 실패. v0.8.0 부터 영향 (PR [#3446](https://github.com/edwardkim/rhwp/pull/3446)) |
| 렌더 정정 | #3396 — TAC 인라인 표 x-원점 outMargin 좌/우 배선 (PR #3410) |

### 근인과 구조적 처방

`print-surface.ts` 가 `new URL('print.html', baseUrl)` 로 확장 루트 기준 파일을 열지만
`build.mjs` 가 복사하지 않았다. 확장은 `vite publicDir:false` 라 `public/` 자산을 개별
복사해야 하는데 `theme-init.js`·`favicon.ico`·아이콘 SVG 만 대상이었다.

더 근본적인 문제는 **`copy()` 가 원본 부재 시 경고만 내고 넘어가 누락이 빌드 성공으로
위장된 것**이다. 그래서 v0.8.0·v0.8.1 두 릴리즈를 그대로 통과했다. 런타임 필수 6종의 존재를
확인하고 없으면 `exit 1` 로 실패시키는 게이트를 추가했고, 원본을 임시 제거해 실제로
잡히는지(`MISSING: print.html` → exit 1) 실증했다.

기존 print 테스트 15개는 URL 계산·명령 계약만 검증해 이 결함을 잡지 못했다. **로직 테스트와
산출물 검증은 다른 층위**라는 점이 이번 사건의 교훈이다.

### 검증

- cargo test **4160 passed / 0 failed**, clippy 경고 0, fmt·diff check 통과
- Docker WASM 빌드 성공, studio `npm test` 641 pass
- `web-ext lint` errors 0, 확장 zip 양쪽에 `print.html` 1436 bytes 포함
  (0.8.1 zip 에서는 0건이었다)
- **작업지시자 Chrome 확장 실기 인쇄 테스트 통과** — 이번 핫픽스의 본체 검증

### v0.8.1 교훈의 적용 결과

계획서에 명시한 다섯 가지가 실제로 효과를 냈다. v0.8.1 과 달리 **계획대로 진행됐다.**

| 항목 | v0.8.1 | v0.8.2 |
|---|---|---|
| 기준선 이동 | 3회 | **0회** (`732147a30` 한 번에 확정) |
| CHANGELOG 보완 PR | 필요(#3424) | 불필요 — main 통합 직전 재점검으로 누락 0 확인 |
| 릴리즈 이슈 자동 close | 발생(재개방) | **미발생** — PR 본문에 `Closes` 미사용 |
| 버전 갱신 파일 수 | 계획 10 → 실제 9 | 계획 9 = 실제 9 |

특히 **merge commit 정책의 복리 효과**가 확인됐다. v0.8.1 을 merge commit 으로 통합한 결과
이번 merge-base 가 `ace187d52` 로 이어졌고 main 독자 변경이 0파일이라 충돌 없이 통합됐다.
v0.8.0 에서 20파일 충돌을 겪은 것과 대조된다.

### 미해결

- [#3450](https://github.com/edwardkim/rhwp/issues/3450) — studio E2E
  `print-pdf-issue3126` PDF 안내 모달 실패. **인쇄 surface 자체는 정상**이며 테스트·소스가
  v0.8.1 태그 이후 무변경이라 이번 범위 밖에서 비롯했다. 근인 미진단.
- [#3412](https://github.com/edwardkim/rhwp/issues/3412) — studio E2E `issue-2214` 페이지
  로컬 리페인트 계약 실패. 회귀 여부 미확정.
- ~~스토어 수동 업로드~~ → **완료(2026-07-27 새벽 1시)**: 3개 스토어 심사 요청 완료.
  v0.8.1 심사는 취소했으므로 0.8.2 는 신규 제출이다. 제출 문서는 `mydocs/feedback/` 의
  `chrome-0.8.2_{kor,eng}.md`, `edge-0.8.2_reviewer_notes.md`, `firefox-0.8.2_amo_notes.md`.
  이후 각 스토어 심사 결과 확인이 남는다.

## planet6897 PR 6건 — 체리픽 통합 검토

`@planet6897`의 열린 [#3458](https://github.com/edwardkim/rhwp/pull/3458),
[#3459](https://github.com/edwardkim/rhwp/pull/3459),
[#3461](https://github.com/edwardkim/rhwp/pull/3461),
[#3462](https://github.com/edwardkim/rhwp/pull/3462),
[#3463](https://github.com/edwardkim/rhwp/pull/3463),
[#3464](https://github.com/edwardkim/rhwp/pull/3464)를 최신 `upstream/devel`
`6e9d0821889c3a5bd64da37ff17bcde49e684633` 위 가시 branch
`review/planet6897-20260727`에 누적했다. source branch에는 push하지 않았다.

- #3458 표 수직 정합과 #3459 r25 10k 모집단 보고는 하나의 수용 단위다. #3458의 제출 조건이 r25
  동승 판정이고 #3459가 그 결과를 기록한다. #3461 PDF 안내 E2E, #3462 대형 문서 idle pagination,
  #3463 Windows tsc, #3464 HWPX SVG picture도 개별 review와 기준 PDF·실제 PNG 증적까지 확인해
  기술적으로 수용 가능하다.
- #3459의 `20260728` run label은 source commit/CI 날짜(2026-07-27)와 달라, 향후에는 실행 시각 또는
  label 의미와 `BINARY_FINGERPRINT.txt` 보관 위치를 함께 남기도록 권고한다. 현재 코드·기준 PDF·전체
  회귀 검증과 독립된 기록상 개선점이며 통합 수용의 blocker로 분류하지 않는다.
- 전용 Cargo target에서 release-test 전체와 IR sweep 2/2, Native Skia 3종
  **57/0·2/0·4/0**, fmt·clippy·WASM lib check를 통과했다. Studio는 focused 32/0, 전체
  **670/0**, TypeScript·production build를 통과했고, #3463은 Windows PowerShell 17/0도 확인했다.
- review record 6개와 통합 구현 기록, #3464 한글 2020 기준 PDF 및 시각 PNG는 같은 통합 PR에 넣는다.
  6개 PR 모두가 후보이며, PR 생성·source PR comment/close/merge는 사용자 승인 뒤에만 수행하고,
  최신 통합 head CI·mergeable이 성공한 뒤 merge 후속처리를 한다.

## kevin9327 PR 10건 — 통합 검토·메인터너 보정

`@kevin9327`의 open PR 가운데 작업지시자가 제외한 #3399를 빼고
[#3404](https://github.com/edwardkim/rhwp/pull/3404),
[#3408](https://github.com/edwardkim/rhwp/pull/3408),
[#3419](https://github.com/edwardkim/rhwp/pull/3419),
[#3420](https://github.com/edwardkim/rhwp/pull/3420),
[#3426](https://github.com/edwardkim/rhwp/pull/3426),
[#3428](https://github.com/edwardkim/rhwp/pull/3428),
[#3430](https://github.com/edwardkim/rhwp/pull/3430),
[#3447](https://github.com/edwardkim/rhwp/pull/3447),
[#3448](https://github.com/edwardkim/rhwp/pull/3448),
[#3449](https://github.com/edwardkim/rhwp/pull/3449)를 최신 `upstream/devel`
`7f8fcfef08610df7bf9f5cc2f4b32a9a711f5e2d` 위 `review/kevin9327-20260726-v2`에 누적한다.
다른 contributor의 #3452는 이 범위에 넣지 않는다.

- contributor 원 commit과 authorship을 보존하고, #3419의 conflict에서는 기능과 무관한 줄끝 churn을
  제외했다. 원 PR branch에는 push하지 않는다.
- 메인터너 보정은 수식 평문·Markdown·검색, 중첩 머리말, chart 설명문, TotalPage, CLI 오류 계약,
  실제 복학원서 완성본, 자리차지 표 무회귀를 별도 commit으로 보강했다. #3404의 `123건·83%` 주장은
  실제 검색 2건과 맞지 않아 README·독립 증적으로 정정했고 잘못된 수치가 박힌 합성 PNG는 제거했다.
- 검증은 전용 Cargo target에서 release build, release lib **2943/0/7 ignored**, release-test 전체와
  IR field sweep 2/2, Native Skia 3종 **57/0·2/0·4/0**, fmt, diff check, Clippy, doc test,
  wasm-pack web build를 통과했다. #3419·#3420·#3430·#3449는 기준 PDF 또는 독립 asset으로
  시각 판정까지 남겼다. 최신 devel의 버전·lockfile 동기화 뒤에는 locked metadata와 release-test
  all-targets compile을 다시 통과했고, Rust source·test 변경은 없었다.
- 개별 review 문서 10개와 통합 구현 기록은 `mydocs/pr/archives/`에, 안정 증적은
  `mydocs/pr/assets/`에 함께 반영한다. #3413은 부분 해결이므로 close하지 않는다.
- v0.8.2 릴리즈는 완료됐으므로 #3445의 당시 핫픽스 기준선 고정은 현재 merge 보류 사유가 아니다.
  최종 조건은 통합 PR 최신 head의 CI·mergeable 성공이다.
- 통합 PR이 실제 merge된 뒤에만 원 PR 코멘트·close/merge와 관련 issue 상태 확인을 별도 후속 단계에서
  수행한다. 이 후속 GitHub 변경은 작업지시자가 자동승인했다.

## lpaiu-cs PR 9건 — 통합 검토·메인터너 보정

`@lpaiu-cs`의 open PR 가운데 누적 정리용 draft #3456을 제외하고
[#3411](https://github.com/edwardkim/rhwp/pull/3411),
[#3423](https://github.com/edwardkim/rhwp/pull/3423),
[#3440](https://github.com/edwardkim/rhwp/pull/3440),
[#3441](https://github.com/edwardkim/rhwp/pull/3441),
[#3442](https://github.com/edwardkim/rhwp/pull/3442),
[#3443](https://github.com/edwardkim/rhwp/pull/3443),
[#3444](https://github.com/edwardkim/rhwp/pull/3444),
[#3452](https://github.com/edwardkim/rhwp/pull/3452),
[#3455](https://github.com/edwardkim/rhwp/pull/3455)를 최신 `upstream/devel`
`7779e737ac5c5df3428d1a06f1099be16375be49` 위 `review/lpaiu-cs-20260727`에 누적했다.

- contributor commit과 authorship은 보존하고, source branch에 직접 push하지 않았다. #3440·#3442에
  잘못 들어온 로컬 절대경로 `node_modules` symlink는 통합 후보에서 제거했다.
- 메인터너 보정은 공개 Rust `Vec<u8>` API 호환, source guard의 중첩 괄호·구간 계산, 다이얼로그 실패
  원자성, wasm-pack 다운로드의 재시도·압축 해제 분리, base64 길이 나머지 0 회귀, 문서 세대별 그림
  신원·prefetch 서명 계약을 보강했다. 실제 브라우저에서 image op의 중첩 `bbox` 때문에 정규식이
  조기 종료하는 추가 결함을 찾아 JSON 구조 순회와 회귀 테스트로 고쳤다.
- #3452 체리픽은 선행 #3411의 바이트 타입 누적에 맞춰 `queries/rendering.rs`와 `paint/json.rs`의
  `data.as_slice()`를 `&data[..]`로 의미 동등하게 적응했다. 이 때문에 source와 통합 commit의
  patch-id가 다른 사실도 개별·통합 review 문서에 기록했다.
- 전용 Cargo target에서 release build, release lib **2949 passed / 0 failed / 7 ignored**,
  release-test 전체와 IR field sweep, Native Skia 3종 **57/0·2/0·4/0**, fmt, Clippy, doc test,
  wasm-pack web build를 통과했다. Studio는 TypeScript, production build와 **670 passed / 0 failed**를
  확인했다.
- OVR 5종 142쪽은 개체 회귀 **0건**이다. `issue_241.hwpx`의 후보·기준 SVG는 SHA-256이 같고,
  한컴 PDF 대조 sweep은 1쪽 중 자동 flag 0건이었다. 브라우저에서는 #3440 no-op/redo·삭제 undo,
  #3441 이벤트·숫자 서식 undo/redo, #3443 여섯 다이얼로그·실패 원자성, #3455 그림 서명 기록·
  동일 파일 재열기 세대 갱신·동일 그림 LayerTree 재조회 0회를 확인했다.
- #3423 source head에는 required CI가 없어 `BLOCKED`지만 통합 후보에서 full CI를 실행한다.
  코드·test·action·Studio가 함께 바뀌므로 review-only fast-pass 대상이 아니다.
- 통합 PR 본문은 #3422·#3431·#3434·#3435·#3436·#3437만 `Closes`로 연결한다. #3315는 여러 Track을
  포함한 umbrella issue이므로 이 통합으로 닫지 않는다.
- 개별 review 문서 9개와 통합 구현 기록, 기준 PDF·브라우저 PNG를 같은 통합 PR에 포함한다. 최신 head
  CI와 mergeable이 모두 성공한 뒤에만 merge하고, 그 뒤 원 PR과 관련 issue 코멘트·상태 확인을 한다.

## scari PR #3405 — HWP5 비밀번호 문서 복호화 검토 (보류)

외부 contributor `scari`의 [#3405](https://github.com/edwardkim/rhwp/pull/3405)를 source head
`5a41caff10` 기준으로 검토했다. collaborator 보정은 source branch의 별도 local commit
`ecdfd9ca4`에만 준비했으며 remote push는 하지 않았다.

- 작업지시자 지시에 따라 최상위 `CHANGELOG.md`·`README.md`·`README_EN.md`의 PR 추가 내용을
  제거했다. 실제 암호 HWP5 fixture는 `samples/`에 유지하고, 공개 Rust API·CLI stdin·오답·평문
  저장 재열기를 고정하는 전용 회귀를 추가했다.
- 전용 target에서 fmt·diff check, Clippy, WASM lib check, HWP5/IR baseline, 최종
  `cargo test --profile release-test --tests`를 통과했다. renderer/layout 변경이 없어 SVG 64페이지
  export는 parser smoke로만 기록했고 visual fidelity 판정은 하지 않았다.
- CodeQL의 새 alert 57건은 annotation·source·`--password`/`--password-stdin` sentinel 실행으로
  실제 유출 경로가 아닌 과탐지임을 확인했다. cleartext logging 41건은 `false positive`, `#[cfg(test)]`
  고정 벡터 16건은 `used in tests`로 dismiss했다. source push 뒤 새 CodeQL run에 새 alert가 없는지
  확인한다. `--password-stdin` 권장과 UI 비보존 요구는 유지한다.
- **merge 보류:** PR이 주장한 압축 해제 상한이 lazy BinData 외 DocInfo·BodyText에는 적용되지 않는다.
  전 스트림 상한 구현(또는 작업지시자의 명시적인 보안 범위 결정) 뒤에만 최신 full CI·수용을 재판정한다.
- UI에서는 아직 암호 입력을 받을 수 없어 [#3474](https://github.com/edwardkim/rhwp/issues/3474)로
  별도 등록했다. 이 이슈는 #3405를 닫지 않는다.
