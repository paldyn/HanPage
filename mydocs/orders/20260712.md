# 오늘 할일 - 2026년 7월 12일

## 이슈 #2228 — Canvas2D 정적 그림 표 셀 clip 보존

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2229 / [#2228](https://github.com/edwardkim/rhwp/issues/2228) | 정적 flow 그림 DOM 분리 시 table-cell clipRect 계보가 사라져 같은 binary 그림의 TopAndBottom control 두 개가 셀 밖에 반복 표시되던 회귀를 정정. clip 교집합 보존, clip wrapper, 단위 회귀 테스트 추가. 기준 PDF와 Canvas2D 2쪽 증적을 PR asset으로 보존. | **CI 대기**. 검토 기록 `pr/archives/pr_2229_review.md`. |

## 이슈 #2211 — 중첩 표 행높이 팽창 (주보 p1 하단 클리핑)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2211 / #2189·#2207 연장선 | 행 성장 판정 pad 이중 계상(행당 +282HU) 제외 + 저장 셀 흐름 신뢰(악보 가사 절단 해소). 구조 가드(중첩/TAC 한정, #1748/#1858 상호 배타 실측). 전수 3,045/0, golden 재생성 1건(0.027px), rowbreak baseline 현행화. | **시각 판정 통과**. 최종 보고 `report/task_m100_2211_report.md`. **close 완료**. |

## 이슈 #2220 — BehindText+tac 표 호스트 줄 om 이중 계상 (p1 우측 단)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2220 / #2211 분리 | 저장 lh(=표+om)를 om 선·후가산과 겹쳐 +22.7px 이중 계상 → 우측 단 +29px 절단. stored_lh_covers_om 증거 가드로 정정. 표적 테스트 신설, 전수 3,048/0, OVR 0건. | **시각 판정 통과**. 최종 보고 `report/task_m100_2220_report.md`. **close 완료**. |

## M100 — v1.0.0 조판 엔진 체계화

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #2211 | 중첩 표 행높이 팽창 → p1 하단 클리핑 | **close 완료** | 커밋 `6b2d23cc`. close 완료. 후속 이슈 등록: #2220(우측 단 BehindText 호스트 줄) / #2221(상위 float 표 pad) / #2222(렌더 캐시). |
| #2212 | 중첩 표 셀 경로 bbox 해석 실패 (편집) | 등록 완료, 처리 대기 | #2211과 분리. |
| #2206 | 08서울한강체 M/L 메트릭 2계층 등록 | 등록 완료, 처리 대기 | #2189 보완축. |
| #2220 | BehindText+tac 호스트 줄 om 이중 계상 | **close 완료** | 커밋 `06c2ace8`. 주보 아크 4건 완결. |

## 외부 PR #2223 — 비-Percent 줄간격 /2 재제출 (planet6897)

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2223 / Refs #2070 | PR #2210 병합 누락 커밋 복구. #2220 중복 아님을 코드 레이어·핀 동작 양면 실증(작업지시자 지정 검증 항목). 결합 게이트 3,048/0, OVR 0건. | merge 완료(d2149914). #2070 open 유지. 검토 기록 `pr/pr_2223_review.md`. |

## 이슈 #2221 — 측정·렌더 행높이 회계 내부 일관성 (B안 재정의)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2221 / B안 | 원 전제(비-tac 상위 표 pad 팽창) 실측 기각 → 내부 일관성으로 재정의. 중첩 표 relaxed_pad 측정 미러(depth>0) — 결재란 하단앵커 3.73px 들뜸 → 밀착. 1858 허용오차 6→2px. 전수 3,048/0. | **시각 판정 통과**. 최종 보고 `report/task_m100_2221_report.md`. **close 완료**. |

## 이슈 등록 — #2225 그림 미지정 placeholder export 억제

| 항목 | 내용 | 상태 |
|---|---|---|
| #2225 | 한컴 규칙(작업지시자 확인): 편집기 표시 / 인쇄 미출력. rhwp export 계열만 억제, studio 편집 뷰 유지. 재현: 36389312 "심볼" bin_id=0. | 등록 완료, 처리 대기 |

## 이슈 #2226 — 셀 내 겹침 배치 다중 그림 flow 회계 (주보 p2 로고 표)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2226 / 편집기 스샷 1차 정답지 | 행높이 1.9× 팽창 3축 정정(wrap 기점/저장 흐름 신뢰/그림 앵커). 반례 캘리브 2건(KTX TOC·#1282) 게이트 실측으로 증거 가드 수렴. 전수 3,049/0. | **close 완료**. 최종 보고 `report/task_m100_2226_report.md`. |

## 이슈 #2217 — 로컬 글꼴 다국어 별칭 및 CanvasKit face 등록

| 항목 | 내용 | 상태 |
|---|---|---|
| #2217 | macOS SFNT 이름 레코드의 한글/영문 family alias를 정규화하고 legacy Macintosh mojibake를 제외. CanvasKit에 local Typeface를 등록하고, 글꼴 확인·재감지 중 입력 비활성화와 화면 재로딩 대기를 줄였다. 한컴형 글꼴 메뉴의 문서/시스템 분류와 스크롤을 보강했다. 재현 샘플 `samples/issue2217/20200830.hwp`, 기준 PDF `pdf/issue2217/20200830-2020.pdf`. | **PR 생성 예정**. 콜드 CI급 로컬 검증 완료: Rust release-test·WASM check·Clippy·native-skia 48/0·Studio 204/0·WASM build. |

## 외부 PR #2224 — CanvasKit readiness gate + opt-in 강화 (seo-rii, P34)

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2224 | 진단 exact allowlist·요청/실효 분리·canvas pool 소유권 이관·readiness gate(최대 매칭). 프론트 전용, 기본 렌더러 비변경. npm 189/0 + CI 12항목 green. | merge 완료(ca69182b). 검토 기록 `pr/pr_2224_review.md`. |

## 외부 PR #2216 리뷰 — 프론트 CI gate (#2183, postmelee)

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2216 | fresh dev WASM 포함 frontend worker + aggregate 집계 + fork fast-pass fallback. ci.yml 전문 검토 + 실측 검증. | **APPROVE 게시** (비차단 제안 2건 + orders 충돌 rebase 요청). merge는 postmelee 진행. 검토 기록 `pr/pr_2216_review.md`. |

## 외부 PR #2219 리뷰 — HML 열기/저장 (cskwork, #1157)

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2219 | HWPML 2.91 파서+손실 거부 직렬화기+Studio 저장 흐름. 결합 게이트 3,121/0·studio 225/0. 실측 렌더 차이 2건(표 폭/인라인 병합)은 후속 이슈 분리. | **B안 — 문서 수정 2건 요청(CHANGES_REQUESTED)**, 반영 후 approve·merge 예정. 검토 기록 `pr/pr_2219_review.md`. |

## 이슈 #2212 — 중첩 표 셀 경로 bbox 해석 실패 (편집)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2212 | run_tacs 인라인 TAC 표의 cell_context 미전파 정정 — 2단 경로 11→101종, 조회 Err→Ok, 렌더 바이트 동일. 전수 3,050/0. | **close 완료**. 최종 보고 `report/task_m100_2212_report.md`. |

## 이슈 #2206 — PR #2227로 해소 close

| 항목 | 내용 | 상태 |
|---|---|---|
| #2206 | WASM 측정은 브라우저 measureText 기반 → #2227의 로컬 글꼴 이름 해석+CanvasKit face 등록으로 측정·드로잉 실폰트화. 네이티브 잔여는 #2189 스필오버가 방어. | **close 완료** (작업지시자 판단 + 메커니즘 검증) |

## 이슈 #2222 — 렌더 성능 (A안 부분 완결)

| 항목 | 내용 | 상태 |
|---|---|---|
| #2222 | 네이티브 무회귀 확정(0.7.18 벤치) + renderPage당 1MB JSON 재직렬화를 변형 캐시로 233배 절감. 첫 렌더 파이프라인 축은 #2228(jangster77) 이관. | **close 완료** (부분 완결 + #2228 이관 + 조건부 재개). 보고 `report/task_m100_2222_report.md`. |

## 이슈 #2225 — placeholder 컨텍스트 분기 (완료) + #2230 등록

| 항목 | 내용 | 상태 |
|---|---|---|
| #2225 | MissingPicture 의미 노드 — 편집 뷰 점선+아이콘 / export 무방출. 전수 3,052/0. | **close 완료**. |
| #2230 | placeholder 선택+그림 삽입 편집 기능 (후속, 작업지시자 지시) | 등록 완료, 처리 대기 |

## 이슈 #2093 — HWPX 비정상 저장 줄 metrics 재조판

| 항목 | 내용 | 상태 |
|---|---|---|
| PR #2231 / [#2093](https://github.com/edwardkim/rhwp/issues/2093) | `SectionDef`+`ColumnDef` 구역 첫 줄의 `line_height`/`text_height`가 예상 advance의 40배를 넘을 때만 글꼴 metrics로 재조판하고 stale baseline·vpos를 함께 복원. HWP 2020 PDF와 rhwp가 모두 1쪽, visual sweep 후보 0건. #2098 큰 줄 footer 경계와 #1692 HWPX 미주 범위를 회귀 보호. | **CI 대기**. 검토 기록 `pr/archives/pr_2231_review.md`, 증적 `pr/assets/pr_2231/`. |
