# 오늘 할일 - 2026년 7월 8일

## 리팩토링 (라운드 6, #2029)

| 항목 | 내용 | 상태 |
|------|------|------|
| #2029 | layout_partial_table 해체 (표 계열 첫 진입) | 4단계 완료 — **CC 169→55 (+cells 115 분리)**, 1위 6라운드 연속 해소, 최대 CC 153, 행동 회귀 0. 승인 대기 |

| PR | 내용 | 처리 |
|----|------|------|
| #2052 / #2007 | 1x1 중첩셀 콘텐츠 페이지 분할 | 옵션 1 진행. PR에 기준 PDF가 없어 HWP 2020 MCP로 `samples/basic/issue2007_nested_cell_pagination_42065.hwp` 를 `pdf/basic/issue2007_nested_cell_pagination_42065-2020.pdf` 로 변환해 17쪽 기준 PDF를 확보. rhwp는 15쪽으로 계산되어 PR의 6p→15p 개선 주장을 확인했고, visual sweep 대표 asset `mydocs/pr/assets/pr_2052_issue2007_*` 와 review 문서 `mydocs/pr/archives/pr_2052_*` 를 PR head 에 포함. 남은 한컴 17쪽 대비 rhwp 15쪽 pagination fidelity 는 후속 잔여 축으로 유지 |
| #2062 / #2057 #2058 #2059 | #2057/#2058/#2059 표 속성 편집·저장 회귀 수정 체리픽 통합 | 옵션 1 진행. 원 PR 세 건에 reviewer 지정 후 `upstream/devel` 기준 #2057 → #2058 → #2059 순서로 기능 커밋만 충돌 없이 체리픽해 통합 PR 생성. `rhwp-studio` `npm ci`, `npm test` 181건, `npm run build`, `git diff --check`, `cargo fmt --check`, `cargo test --profile release-test --lib`, `cargo test --profile release-test --tests`, `cargo clippy --all-targets -- -D warnings`, `wasm-pack build --target web --out-dir pkg`, WASM build 후 `rhwp-studio npm run build` 통과. 기존 `localhost:7700` 앱에 실제 접속해 표/셀 속성 undo/redo, 그림 `Through` 보존, `samples/calc-cell.hwp` HWP 저장 왕복 속성 보존을 확인. review 문서는 원 PR 번호별 `mydocs/pr/archives/pr_2057_*`, `pr_2058_*`, `pr_2059_*` 로 작성하고 앱 검증 asset 은 `mydocs/pr/assets/pr_2057_*`, `pr_2059_*` 로 PR head 에 포함 |
| #2056 / #2020 | 첨부 문서 잔여 렌더링 차이 해결 및 #2020 close PR | 옵션 1 진행. 여권 낫표 advance, visual sweep footer/TAC filler false-positive, FSC HWP/HWPX page count 및 2쪽 표 유지, 복학원서 접수증 날인선/도장 원/표식 위치를 보정. `cargo build --release`, `cargo test --release --lib`, `cargo test --profile release-test --tests`, fmt, diff check, clippy, doc test, WASM build, rhwp-studio 타입체크/테스트 통과. 대표 visual sweep 6개 대상 flagged 0이며 증적은 `mydocs/pr/assets/pr_2056_issue2020_*` 파일명으로 보존. 한컴 전용 폰트 부재의 pixel-level font fidelity 는 #2020 close blocker 가 아닌 별도 축으로 기록하고 PR body 에 `Closes #2020` 포함 |
| #2036 / #2020 | 렌더링 차이 재현 자산과 회귀 게이트 보강 | 옵션 1 진행. 코드 커밋 `ecf2c8247` 로 FSC RowBreak 조기 이월 보정, SVG 첨자 baseline/크기 보정, #2020 회귀 테스트 추가. 이슈 본문 스크린샷 PNG 13개와 비교 PDF/HWP/HWPX/MCP 기준 PDF 를 `samples/issue2020/`, `pdf/issue2020/` 에 보존. review 문서와 대표 visual asset 을 PR head 에 포함. #2020 은 여권/복학원서/폰트 잔여 차이로 open 유지 |
| #2033 / #2032 | floating 그림 restrictInPage 하단 클램프 | first-time contributor PR 로 검토. reviewer 지정, 코드 diff 확인, #2032/#1079/#2027 영향권 테스트와 WASM 빌드 통과. ta-pic on/off 기준 PDF visual sweep 은 자동 후보 0/1. approve review 게시 후 원 PR 을 merge commit `2b213ab706f7824f32bbb64b06831d9b794bce5d` 로 병합. 옵션 2에 따라 review 문서와 대표 visual asset 은 docs-only 후속 PR 로 분리 |
| #2034 / #1994 | 용지-앵커 글뒤로 표 RowBreak 겹침 소거 | contributor PR 검토 후 approve review 게시, 원 PR 을 merge commit `e5f9e00864f2b0423c9292fc170a2477b2882504` 로 병합. #1994 표적 테스트와 #1858/#2019/#2015/#703 영향권 테스트, `cargo build --bin rhwp`, WASM 빌드 통과. issue 댓글 첨부 `issue_1994.pdf` 는 `samples/issue1994/`, `pdf/issue1994/` 에 같은 SHA 로 보존하고 4쪽 visual sweep 기준 PDF 로 사용. 대표 visual asset 은 `mydocs/pr/assets/pr_2034_issue1994_review_p003.png` 로 보존. 옵션 2에 따라 review 문서와 자산은 docs-only 후속 PR 로 분리 |
| #2039 / #2037 | 찾아 바꾸기/모두 바꾸기 undo stack 기록 | first-time contributor PR 로 검토. `doReplace()`/`doReplaceAll()` 을 `executeOperation({ kind: 'snapshot' })` 경로로 보내 #1320 편집 라우터 계약에 맞춘 변경으로 판단. `rhwp-studio` `npm test` 177건, `npx tsc --noEmit`, `npm run build`, `git diff --check` 통과. GitHub Actions 전체 통과 후 원 PR 을 merge commit `7649e2180c6f9b823327081e48d897ad50806301` 로 병합. 옵션 2에 따라 review 문서는 docs-only 후속 PR 로 분리 |
| #2040 / #2038 | BinData storage id 충돌 방지 | first-time contributor PR 로 검토. `ImageAttr.bin_data_id` 의 위치 id 의미와 `BinDataContent.id`/`BinData.storage_id` 의 storage id 의미를 분리해야 한다는 주장을 확인했고, storage id 구멍이 있는 문서에서 기존 `len()+1` 채번이 `BIN%04X` stream 충돌을 만들 수 있음을 검증. 표적 회귀 테스트, `cargo test --profile release-test --tests`, `cargo fmt --check`, `git diff --check`, `cargo clippy --release --lib -- -D warnings` 통과. GitHub Actions 전체 통과 후 원 PR 을 merge commit `b12f99f7b4a47fe2be36e15f9a5803d2d4ef73a3` 로 병합. 옵션 2에 따라 review 문서는 docs-only 후속 PR 로 분리 |
- 원격 전진분(#2039/#2040 — lpaiu-cs 재기여 2건 등) 동기화·결합 재검증(2,938/0)
- 스캐너 사각지대 체크리스트 5종 확정 (r4~r6)

## 리팩토링 (라운드 7, #2064)

| 항목 | 내용 | 상태 |
|------|------|------|
| #2064 | typeset_endnote_paragraphs 내부 분해 1차 회전 (P3+P8, 보수 목표) | 4단계 완료 — **CC 153→138**, 신규 2함수(compute_endnote_metrics/emit_endnote_split) 모두 CC<25, 예외 목록 무증가(87), 최대 CC 146, 행동 회귀 0. P6/P7은 국면 지도와 함께 2차 회전 인계. 스캐너 사각지대 6호(클로저 캡처 변이) 확정. 승인 대기 |
