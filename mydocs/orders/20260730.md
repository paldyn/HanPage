# 오늘 할일 - 2026년 7월 30일

## Issue #3486 — HWP3 암호 문서 기준 PDF 렌더링 정합 (Stage 12-A/B PR 준비)

[#3486](https://github.com/edwardkim/rhwp/issues/3486)의 현재 범위는 렌더 결과를 성급히 완료로
판정하지 않기 위한 visual sweep 증적 계약 보강과, 한컴 PDF로 확인된 닫힌 legacy 제품명 display
projection이다. HWP3 제품명 옛한글 glyph의 전역 보정은 Stage 11 결론대로 제외하며, p3 표·목록의
원인 보정은 이 PR 범위에 넣지 않는다.

- `task1274_visual_sweep.py`가 입력 HWP/PDF·Git HEAD·실행 binary·실행 스크립트의 provenance를
  확인하고, 완성 artifact를 갖춘 페이지에만 원자적 checkpoint를 남기도록 한다.
- `--resume`은 provenance가 같은 shard만 합치며, target/root summary는 요청·완료·누락 페이지와
  `run_state`를 명시한다. 불완전 checkpoint는 재생성한다.
- focused Python 회귀와 독립 1쪽 smoke에서 첫 실행, resume 재사용, DPI provenance 거부를 확인했다.
  실제 암호 HWP3 p3은 current `devel` 전용 binary로 재확인했으며, 24쪽 전체 sweep 완료나 renderer
  fidelity 합격을 주장하지 않는다.
- p3·p19 기준 PDF가 현대 글리프로 인쇄하는 `ᄒᆞᆫ글`·`ᄒᆞᆫ메일`·`ᄒᆞᆫ팩스`·`ᄒᆞᆫ소프트`만 `displayText`에
  투영한다. 표 셀의 composer 우회 경로까지 render tree 최종화에서 덮고, raw IR/검색/caret offset과
  일반 옛한글은 바꾸지 않는다.
- current release-test binary의 실제 p3·p19 sweep은 legacy glyph 후보 0건이다. p3의 표·줄높이·본문
  배치 차이(`content_bottom_drift`, pixel 93.68602%, ink 6.82500%)는 남아 있으므로 전체 fidelity 합격으로
  가장하지 않는다.

## PR #3580 — delete_text_at char_shape ref 중복 제거 (merge 완료)

[PR #3580](https://github.com/edwardkim/rhwp/pull/3580) (yuyu04, **rhwp 첫 PR**)은
[#3576](https://github.com/edwardkim/rhwp/issues/3576)의 전체삭제→재삽입 시 보조 글자모양이 새
텍스트를 덮는 결함을 `delete_text_at` 클램핑 뒤 `dedup_by_key` 1줄로 정정하고 3계약 회귀
테스트를 추가한다.

- merge: `b33502137` (admin merge — base 115커밋 behind, 통합 트리 로컬 검증으로 대체. 작업지시자
  승인, 2026-07-30). 최신 head CI 전체 통과, 로컬 focused 3/3·red-check 실증·release-test 369
  바이너리·fmt·clippy 통과. 검토 기록은 `mydocs/pr/archives/pr_3580_review.md`.
- 후속: #3576 close 완료(작업지시자 승인 — `close-issues-on-devel-push` 워크플로가 merge
  직후 자동 close, maintainer 증적 코멘트 별도 게시). 부모 #3552는 post_merge 7.3.1
  체크리스트(가드 테스트 PR merge 대기) 적용, yuyu04 테스트-only PR 접수 대기(#3552
  코멘트로 요청됨).

## PR #3564 — 10k 서베이 r26 보고서 (merge 완료, fast-pass)

[PR #3564](https://github.com/edwardkim/rhwp/pull/3564) (planet6897)는
`mydocs/report/survey_10k_r26_20260729.md` 신규 1파일의 review-only fast-pass PR이다.

- merge: `3e2bcae37` (admin merge, 작업지시자 승인 2026-07-30). CI는 문서 전용 fast-pass
  (preflight만 실행, aggregate success). 수치 내부 정합 재계산 일치, 기준선 규율(r23
  폰트-클린 계보) 확인. 검토 기록은 `mydocs/pr/archives/pr_3564_review.md`.
- 핵심 결과: r25 동등(PI 93.75%), 제출 PR 6건+보류 패치 2건 쪽수·배치 무영향. 회귀 1건은
  upstream `f8e0c37fd` 귀속(#3561, 순수 devel 재현).
## PR #3583 — 표 CommonObjAttr 저장 가드 5계약 (merge 완료) + #3552 close

[PR #3583](https://github.com/edwardkim/rhwp/pull/3583) (yuyu04 2번째 PR, 테스트 전용
+233)은 #2055가 닫은 FLAGS 동기화 축을 5계약으로 고정한다. merge `94022bb7b`
(admin merge, 작업지시자 승인 2026-07-30). 통합 트리 release-test 370 바이너리 전부
ok, red-check 실증(동기화 비활성 시 ①만 실패). 검토 기록은
`mydocs/pr/archives/pr_3583_review.md`.

- [#3552](https://github.com/edwardkim/rhwp/issues/3552) close 완료 — **post_merge 7.3.1
  첫 적용**: ①#3576 close ②가드 PR merge ③판정 경위 comment(오등록 판정, #2055 귀속,
  not planned) ④작업지시자 승인. yuyu04 첫 기여 사이클(Discussion #3498 → #3552/#3576 →
  PR #3580/#3583) 완결.
- 잔여: Discussion #3498의 document_core 연산 9종 upstream 수용 절차.

## Issue #3591 — 가로 팬 여백 과대 + 줌 후 중앙 미수렴 (착수)

[#3591](https://github.com/edwardkim/rhwp/issues/3591) 등록(2026-07-30). `applyHorizontalPanSpace`
가 콘텐츠 좌우에 창 폭만큼씩 팬 여백을 붙여(`totalWidth = base + viewportWidth×2`)
스크롤 영역의 75%가 빈 공간이고, 창 폭 비례라 4K에서 극대화된다(실측: 창 1229 →
totalW 3291.7). 콘텐츠가 항상 창보다 넓어 수평 배치를 scrollLeft 가 전담하는데 줌
완료 후 중앙 수렴 규칙이 없어 앵커 잔류가 줌 비례로 증폭(실 Chrome CDP 실측:
복귀 구간 편차 47.5→403.5px, 그리드 진입 −546px). #3377 과 동근(같은 팬 설계).

- **완결**: 정책 `pan = 그리드?0 : (콘텐츠<=창)?0 : clamp(창×0.25, 80, 240)`. 첫 로드
  스크롤 영역 3291.7→833.7(내용 25%→100%), 줌 전 구간 slErr ≤1.3px(종전 −546),
  studio 682/682. 그리드 진입(zoom 0.5) 팬 오적용과 앵커 오버슈트도 함께 정정.
  **작업지시자 4K 실기동 판정 통과**. 최종 보고 `mydocs/report/task_m100_3591_report.md`.
  [PR #3594](https://github.com/edwardkim/rhwp/pull/3594) merge `bb0deb995`(작업지시자 승인,
  CI 22개 green), #3591 자동 close(completed) 확인.
- 계측 하니스: 실 Windows Chrome CDP(포트 19222) 확보. 디버그 Chrome 에 확장
  압축해제 로드됨(ID nifgnncjmpdokdlbkichnigdiljignln).

## Issue #3377 — 확장 첫 로딩 1·2쪽 세로축 어긋남 (완료)

[#3377](https://github.com/edwardkim/rhwp/issues/3377) 확장 첫 로딩 1·2쪽 어긋남 —
당일 완결. 근인: 스크롤바 등장(clientWidth −15px) 후 recalcLayout 좌표 갱신을
기렌더 캔버스가 따라가지 않는 stale 배치(작업지시자 실측 스니펫으로 확정, 수식
역산 전항 일치). 수정: `positionPageElement` 단일 관문 + `recalcLayout` 끝
활성 페이지 재배치(canvas-view.ts). 검증: 결정 재현 하니스 red-check(true→false),
studio 678/678, **작업지시자 실기동 시각 판정 통과**. 최종 보고
`mydocs/report/task_m100_3377_report.md`. [PR #3590](https://github.com/edwardkim/rhwp/pull/3590)
merge `d92657fb9`(작업지시자 승인, CI 22개 전부 green), #3377 자동 close(completed) 확인.

## kevin9327 PR 4건 — #3571 mcp-serve + docs 3건 (merge 완료) + #3140 close

작업지시자 일괄 승인(2026-07-30)으로 4건 순차 admin merge, #3140 close(completed).
검토 기록은 `mydocs/pr/archives/pr_{3571,3577,3578,3579}_review.md`.

- [#3571](https://github.com/edwardkim/rhwp/pull/3571) `f66c7694b` — `rhwp mcp-serve`
  (stdio JSON-RPC MCP 서버, 무상태 13종 CLI 재사용 + 세션 3종, 의존성 무추가). 계약
  테스트 6종 + 실 프로토콜 스모크(도구 16종, 세션 왕복, stdout 순수성) + 373 바이너리.
- [#3577](https://github.com/edwardkim/rhwp/pull/3577) `eae564edf` — MCP 통합 가이드
  (#3571 적층). [#3578](https://github.com/edwardkim/rhwp/pull/3578) `6710a560d` — 서식
  자동화 심화 가이드. [#3579](https://github.com/edwardkim/rhwp/pull/3579) `8d504a305` —
  에이전트 실패 사전(본 세션 실측 "-o" 함정과 일치 — 실효성 입증).
- README 3행 삽입 지점 상이로 순차 merge auto-resolve — 충돌 0.
- [#3140](https://github.com/edwardkim/rhwp/issues/3140) close: 0단계(#3263) + 실행
  층(#3571)으로 범위 완결, 판정 경위 코멘트 게시. 후속 확장(세션 편집·rmcp)은 수요
  발생 시 별도 이슈.

## PR #3586 — 셀프 호스팅 CDN 웹폰트 차단 스위치 (merge 완료)

[PR #3586](https://github.com/edwardkim/rhwp/pull/3586) (JamesPsh 3번째 PR)은
`RHWP_DISABLE_EXTERNAL_WEBFONTS=1` 빌드 스위치로 함초롬 CDN(비상업 조건) 요청을
셀프 호스팅에서 차단한다. merge `7cb1ba73b` (admin merge, 작업지시자 승인 2026-07-30).

- 검증: npm ci + 678/678 테스트(신규 3계약), 스위치 ON 빌드 end-to-end 성공. 기본
  동작 불변, storage 명시값 우선 유지. #3585 자동 close 확인(completed).
- 검토 기록: `mydocs/pr/archives/pr_3586_review.md`.

## MCP 한컴 개방 스모크 검증 — #3565 3중 확인 + 서버 점검 (완료)

hwp2020 MCP(canonical: `mydocs/manual/mcp_hwp2020Convert_usage.md`)로 #3565 축을 이
세션에서 재검증했다.

- rhwp convert 저장본(384쪽 편람 HWP5)을 `--target hwp`로 요청 → 한컴 2020 정상
  개방·재저장(`run_status 0`, `validation ok`). 작업지시자 한컴 편집기에서도 rhwp
  저장본·재저장본 모두 **개방+렌더링 양호** 판정 — 기여자 COM / 2020 MCP / 편집기
  3중 독립 검증 완성. 산출물 2건은 `/mnt/e/hwp/`에 복사.
- 서버 점검 캘리브레이션: 6쪽 수십 초 / 27쪽 KTX 7.6초 / 384쪽 편람 PDF는 1500초
  지정에도 ~13분 `-32001` — **병목은 쪽수가 아니라 콘텐츠 복잡도**. 매뉴얼 문제 해결
  절에 실측 보강, 세션 메모리에도 기록.
- 함의: 직렬화기 변경 PR 처리 시 "한컴 개방 스모크"를 Linux 세션 게이트로 사용 가능.

## planet6897 PR 3건 — #3566·#3581·#3584 (merge 완료)

작업지시자 일괄 승인(2026-07-30)으로 3건 admin merge. 검토 기록은
`mydocs/pr/archives/pr_{3566,3581,3584}_review.md`.

- [#3566](https://github.com/edwardkim/rhwp/pull/3566) `0555b7ca5` — 그룹 자식 목록 종류
  선언 정정(`$lin`→`$col`, `gso `→`$con`), 한컴 개방 실패 해소. red-check 2/2 실증,
  한컴 실물 검증(387쪽 정상 개방) 완비. #3565 자동 close 확인(completed).
- [#3581](https://github.com/edwardkim/rhwp/pull/3581) `7f493a2b1` — 추적 도구 2종
  (extract-pages CLI + hwp_open_bisect 하니스). CLI 실동작 스모크(35→5쪽) 확인.
- [#3584](https://github.com/edwardkim/rhwp/pull/3584) `0f85270de` — 쪽 번호 폭 실측
  메트릭 전환(어림 계수 제거). Native Skia 3종·wasm 빌드·golden 산술 정합 확인.
- 잔여: ①#3566 조사 별건 5종(PARA_RANGE_TAG 소실 등) 이슈 등록 대기 — 접수 시 추적
  ②#3048 close 여부 판단(#3584는 Related — 크기 상수 축은 275건 재측정으로 해소,
  잔여 범위 확인 필요).

## 10k 서베이 시리즈 종합 인사이트 문서 (작성·승인 완료)

r5~r26 17개 회차 보고서를 전수 검토해 시리즈 종합 인사이트를
`mydocs/feedback/survey_10k_series_insights_20260730.md`로 작성했다 (작업지시자 승인,
2026-07-30). 두괄식 요약·개요(목적/이유)·5단계 진화·인사이트 6개·함의·맺음말(지식의
증류 코퍼스 철학)·devel 실링크 색인·일반 사용자용 용어 설명 36개로 구성. 원저자
planet6897 크레딧 명시. 교정 5건(수치 분해 정정, 최상급 제거, 회귀 수열 정정, 추정
서술 제거)을 거쳤다.

- 잔여 결정 완료: ①보류 skia 패치 2건(`717bf7426`·`510b5dd75`)은 **반려**(작업지시자 결정,
  2026-07-30) — r26 전수 실측이 기대 효과 기각(−0.04pp 노이즈), 무효과 보정은 원칙(보정
  근거=문서 속성/스펙)과 불합치. 한컴 실측 규약 증명 또는 표적 문서 시각 증적 제시 시
  정확성 차원 재검토 여지를 #3564 코멘트로 안내. ②#3561은 작업지시자 승인으로
  close(not planned, 2026-07-30) — 10k 중 유일 1건 ±1쪽 잔차 수용, `f8e0c37fd` 유지,
  원인 규명 기록은 이슈·r26 보고서 보존, 재오픈 조건 명시.
