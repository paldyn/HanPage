# 2026-08-04 — 오늘 할일

## Task #50 — 문서 열기 멈춤 수정

- ✅ **완료** — 폰트 로드 타임아웃/예산(FontFace.load 무한 대기 차단), macOS 펜딩 큐 전역화(파일 연결 문서 유실), 브리지 재드레인. 실측 A/B 및 로그로 원인 확정. 보고서는 #51 릴리스에 포함.

## Task #51 — 엔진 0.7.18 → 0.8.2 재동기화 + 릴리스

- ✅ **완료** — 재베이스(커밋 2,552) + paldyn 레이어 재적용 + pdf-large strip + 전 검증(엔진 3241, studio 759) + devel/main 반영 + **v0.8.2 서명·공증 릴리스** + 구 v0.7.18 삭제. 보고서 `report/task_m100_hp51_report.md`.
- 신규 판별 규칙: "업스트림 삭제분"을 paldyn 전용으로 오분류하지 않도록 옛 고정점 존재 여부로 판별.
- 이슈 #50·#51 클로즈 승인 대기.

## Task #59 — 자동 업데이트 UX 개선

- ✅ **완료** — "묻고 받는다" → "받아두고 묻는다"(Claude 앱 방식). 조용한 선다운로드 + 비침습 토스트 + 즉시 적용. 실측으로 전 구간 통과(40MB 수신 → ready → 번들 교체·재시작). **v0.8.3 서명·공증 릴리스**, 구 v0.8.2 삭제. 보고서 `report/task_m100_hp59_report.md`.
- 새 UX 체감은 0.8.3 설치 이후 업데이트부터(현재 배포본은 옛 업데이터).
