# 오늘 할일 — 2026-07-15

## 메인테이너 처리 (edwardkim)

| 항목 | 내용 | 상태 |
|------|------|------|
| Docker Desktop WSL 오류 | 프록시 바이너리 0바이트(프로비저닝 실패) — DD 종료→docker-desktop distro 재기동→DD 재시작으로 복구, compose 스모크 정상 | **해결** |
| PR #2272 (planet6897, #2269) | PDF 채번 비결정 원인 규명(업스트림 실증) + 정규화 도구. 3중 재현 검증 | **merge 완료**. #2269 는 근본 정정 축으로 **open 유지(B안)**. 비차단 개선(exit 2) 요청. 기록 `pr/archives/pr_2272_{review,report}.md` |
| PR #2280 (planet6897) | #2272 비차단 개선(입력 부재 exit 2) 반영 — 전 오류 경로 재검증 | **merge 완료** |
| #2269 근본 정정 | 업스트림 svg2pdf 기여로 결정(1안) — 컨트리뷰터에 전달, 업스트림 PR 링크 대기 | 진행 중 (planet6897) |
| #2268 | export-pdf 간헐 행 — 검증 중 2회 재발(sungeo, 문서 무관) 기록 보강 (누적 4회) | 조사 대기 |

## PR #2254 (postmelee) — merge 완료

assets/fonts canonical 이전 — 바이트 보존 36/36 독립 검증 + 계약 테스트
4/4 + 5개 소비자 빌드 성공. 기록 `pr/archives/pr_2254_{review,report}.md`.

## PR 큐 (열린 10건)

- planet6897 신규 5: #2271(#2097 float 컬럼 예약) / #2273(#2234 setup-node v6) / #2274(#2233 wasm-pack pin) / #2275(#2261 PROTECTED_SKIP) / #2276(#2148 마커 도구)
- postmelee: #2241(#2214 — CI 2회 연속 실패, 컨트리뷰터 정리 대기) / #2254(폰트 root)
- seo-rii: #2248(CanvasKit replay) / humdrum00001010: #2249·#2250(draft)

## PR #2248 (seo-rii) — Render Diff 실패 원인 진단 완료

자체 신설 readiness 게이트 2건이 CI 환경에서 성립 불가: ①table-core
renderCountDelta 2(이중 렌더 — #1456 재시도 계열 추정) ②font-native-bitmap
이미지 캐시 완전 비활동(사이드카는 방출 정상 — software surface 환경 편차
추정) + base DIRTY(#2254/#2265 rebase 필요). 진단 코멘트 게시, 3건 반영 후
재리뷰 대기.

## 대기 결정

- #2230 close 승인
