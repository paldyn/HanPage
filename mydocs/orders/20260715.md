# 오늘 할일 — 2026-07-15

## 메인테이너 처리 (edwardkim)

| 항목 | 내용 | 상태 |
|------|------|------|
| Docker Desktop WSL 오류 | 프록시 바이너리 0바이트(프로비저닝 실패) — DD 종료→docker-desktop distro 재기동→DD 재시작으로 복구, compose 스모크 정상 | **해결** |
| PR #2272 (planet6897, #2269) | PDF 채번 비결정 원인 규명(업스트림 실증) + 정규화 도구. 3중 재현 검증 | **merge 완료**. #2269 는 근본 정정 축으로 **open 유지(B안)**. 비차단 개선(exit 2) 요청. 기록 `pr/archives/pr_2272_{review,report}.md` |
| PR #2280 (planet6897) | #2272 비차단 개선(입력 부재 exit 2) 반영 — 전 오류 경로 재검증 | **merge 완료** |
| #2269 근본 정정 | 업스트림 svg2pdf 기여로 결정(1안) — 컨트리뷰터에 전달, 업스트림 PR 링크 대기 | 진행 중 (planet6897) |
| #2268 | export-pdf 간헐 행 — 검증 중 2회 재발(sungeo, 문서 무관) 기록 보강 (누적 4회) | 조사 대기 |

## PR #2254 (postmelee) — merge 완료

assets/fonts canonical 이전 — 바이트 보존 36/36 독립 검증 + 계약 테스트
4/4 + 5개 소비자 빌드 성공. 기록 `pr/archives/pr_2254_{review,report}.md`.

## PR 큐 (열린 10건)

- planet6897 신규 5: #2271(#2097 float 컬럼 예약) / #2273(#2234 setup-node v6) / #2274(#2233 wasm-pack pin) / #2275(#2261 PROTECTED_SKIP) / #2276(#2148 마커 도구)
- postmelee: #2241(#2214 — CI 2회 연속 실패, 컨트리뷰터 정리 대기) / #2254(폰트 root)
- seo-rii: #2248(CanvasKit replay) / humdrum00001010: #2249·#2250(draft)

## PR #2271 (planet6897, #2097) — merge 완료, 옵션 2 기록 PR 준비

나란히 배치한 `TopAndBottom` float의 가로-컬럼 예약으로 여분 페이지를 줄이는
후속 PR. 작업지시 기준 PR 본문만 검토하여 **merge 수용 권고**로 기록했고,
2026-07-15 squash merge (`7ff94666`) 완료.
본문의 파급 6건 중 `pdf 근접`으로만 적힌 3건은 전후 페이지 수 또는 대조 기준을
보완 권고. review 문서·오늘할일을 포함한 옵션 2 후속 기록 PR로 보존.

## PR #2248 (seo-rii) — Render Diff 실패 원인 진단 완료

자체 신설 readiness 게이트 2건이 CI 환경에서 성립 불가: ①table-core
renderCountDelta 2(이중 렌더 — #1456 재시도 계열 추정) ②font-native-bitmap
이미지 캐시 완전 비활동(사이드카는 방출 정상 — software surface 환경 편차
추정) + base DIRTY(#2254/#2265 rebase 필요). 진단 코멘트 게시, 3건 반영 후
재리뷰 대기.

## PR #2281 (planet6897) — merge 완료 + #2269 close

svg2pdf 벤더 패치(업스트림 아카이브 대체) — fork 전량 감사 + 결정성 재실증
(2회 바이트 동일). #2269 close(승인), krilla-svg 축은 **#2283 신설 분리**.
기록 `pr/archives/pr_2281_{review,report}.md`.

## 대기 결정

- #2230 close 승인
| PR #2288 (johndoekim) | C2a stock 렌더 + 2D fidelity 4갭 — 코퍼스 28종 placeholder 0건 (#1431 Track C 커버리지 완료) | **merge 완료** (격리·실측 규칙·3,191/0 재실증). #2277 close 승인 대기. 기록 `pr/archives/pr_2288_{review,report}.md` |
| **#2292 처리 (메인테이너 직접)** | skia RawSvg viewBox 페이지 좌표 창 정정 (`9d1805a7`) — FAILED 실증 + default 3,191/0 + native-skia 3,241/0 + OVR 0건 | **시각 판정 통과** — 최종 보고 `report/task_m100_2292_report.md`, close 승인 대기 |
| **#2293 처리 착수 (메인테이너 직접)** | RawSvg 조각 래스터 텍스트 소실 — fontdb ttfs/ 미로딩 + 폴백 하드 고정 (실험 증명 잉크 0→451). 브랜치 `local/task2293` | 수행계획서 작성 중 |
| #2292 신규 등록 | PNG(skia) 차트 전 유형 잘림 — RawSvg 좌표계 계약 불일치(viewBox 로컬 가정 vs 페이지 절대 좌표, image_conv.rs:353) 근인 확정. 재현 PNG `output/png/chart_check/` 5종. #2288 검증 중 발견(무관 기존 결함) | 등록 완료 (edwardkim assignee) |
