# 오늘 할일 - 2026년 7월 3일

## PR 처리

| PR | 내용 | 처리 |
|----|------|------|
| #1758 | #1757 `verify_pi_page_vs_hangul.py` 알려진 한계 문서화 (@planet6897) | PR review 완료. 변경은 Python 도구 docstring + 매뉴얼/계획서 추가이며 실행 로직 변경 없음. 로컬 최소 검증(`py_compile`, `--help`, `git diff --check`) 통과. 작업지시자 지시에 따라 full CI 대기 없이 review 기록을 PR head에 remote push |
| #1761 | #1759 razor-thin 메트릭 드리프트 측정 하니스 + 1차 서베이 (@planet6897) | PR review 완료. reviewer assign 완료. Python 도구 문법 검증, `cargo fmt --check`, `git diff --check` 통과. 최신 GitHub Actions success 확인. merge 후보이나 PDF 캐시 파일명 일반화는 후속 보완 후보. 옵션 1 방식으로 review 문서 archive 포함 |
| #1762 | #1760 표 렌더 높이 계통 과대 조사 재분류 (@planet6897) | #1761 다음 순서로 PR review 완료. reviewer assign 완료. 문서 전용, `cargo fmt --check`, `git diff --check` 통과. 최신 GitHub Actions success 확인. merge 후보. 옵션 1 방식으로 review 문서 archive 포함 |
| #1764 | #1763 셀 선언높이 권위 trailing line-spacing 보정 (@planet6897) | #1756 스택 충돌은 clean head로 해소 완료. reviewer assign 완료. #1761/#1762 다음 순서로 PR review 완료. targeted test + visual sweep(HWP/HWPX p1, 1/1쪽, 자동 후보 0/1) + 최신 GitHub Actions success 확인. 옵션 1 방식으로 review 문서 archive, visual asset, 메인터너/콜라보레이터 추가 기준 PDF/HWPX 샘플 포함 |
| #1766 | #1765 병합 셀 경로 가설 기각 조사 (@planet6897) | PR review 진행. reviewer assign 완료. 실제 조사 커밋만 `upstream/devel` 기준 cherry-pick. `git diff --check`, `cargo fmt --check` 통과. 기준 PDF/HWPX 샘플 추가 확인 후 HWP/HWPX p2 visual sweep 완료(4/4쪽, 자동 후보 0/1). README/계획서의 초기 가설 서술은 메인터너 보정으로 최종 보고서의 가설 기각 결론과 일치시킴. 옵션 1 방식으로 review 문서 archive, visual asset, 기준 PDF/HWPX 샘플 포함 후 최신 CI 통과 시 merge 후보 |
