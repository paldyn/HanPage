# 오늘 할일 - 2026년 8월 1일

## lpaiu-cs PR #3665·planet6897 PR #3666 통합 검토

[#3665](https://github.com/edwardkim/rhwp/pull/3665)와
[#3666](https://github.com/edwardkim/rhwp/pull/3666)은 개별 merge하지 않고, 최신 `devel`
`3b28ab597` 위 [#3671](https://github.com/edwardkim/rhwp/pull/3671)
`integrate/lpaiu-planet-20260731` 하나에 author 기능 commit만 순서대로 누적했다.

- #3665는 narrow flow-image query의 ancestor clip 동등성과 document-bound object URL lifecycle을
  보정한다. 후속 P1 commit은 새 문서가 그림을 조회하지 않아도 기존 blob URL을 문서 로드 경계에서
  revoke하고, 동일 문서 reload에서는 캐시를 보존한다. #3315는 umbrella issue라 open으로 유지한다.
- #3666은 split-cell nested table 뒤 문단의 절대 vpos 스냅이 쪽 밖 glyph를 만드는 기전을 조각 원점과
  physical content bottom으로 제한한다. #3637은 이미 closed다. p10 focused 계약은 통과했지만 HWP 2020
  PDF 12쪽↔rhwp 13쪽과 p2·p4·p12의 기존 visual candidate는 전체 PDF 정합 성공으로 확대하지 않는다.
- `cargo test --profile release-test --tests` 최종 exit 0, focused 회귀, fmt·clippy, Native Skia 3종,
  doc test, WASM build, TypeScript, Studio 705 tests/build와 HWPX visual sweep을 완료했다. code candidate
  `3a1db3234`의 CI preflight, lint, frontend gate, Native Skia, test archive, default-feature 8 shards,
  `Build & Test`, CodeQL, Canvas visual diff도 모두 success다.
- 현재 tail에는 archive review, 공유 반영 계획, HWP 2020 기준 PDF, p10/p12 review PNG, 오늘할일만 둔다.
  LFS 사전 판독 후 remote push하고 review-only fast-pass의 최신 preflight·aggregate를 확인한다.
- fast-pass 성공과 `CLEAN`·`MERGEABLE` 재확인 뒤 #3671만 merge한다. 이후 원 PR 두 건에 저자별 감사와
  구체적 검토 근거를 실제 줄바꿈 comment로 남기고 supersede close하며, `devel` sync·issue 상태·정확한
  review branch/ref/target 정리까지 후속 처리한다.

상세 근거는 [#3665 review](../pr/archives/pr_3665_review.md),
[#3666 review](../pr/archives/pr_3666_review.md),
[공유 계획](../pr/archives/pr_3665_review_impl.md)에 기록한다.

## Issue #3236 — 쪽 넘는 1×1 표 셀 분할 복원 (완결 단계)

전날(7/31) 착수분을 자정 넘겨 완결. 근인은 #1891 단일행 선언-신뢰 특례의 상한 부재
(선언 322.6px vs 측정 910.8px 문서까지 통짜 배치 → 셀 내용 쪽 밖 clip 소실).

- 추적 2차: 실조판 경로는 `typeset.rs`(engine.rs 는 죽은 경로). 특례 차단 실험으로
  분할점 한컴 글자 단위 일치 확정. #1891 fixture 4건 특례 미발동 실측.
- Stage 2 (옵션 A 승인): `SINGLE_ROW_DECLARED_TRUST_MAX_RATIO = 1.5` 상한 추가
  (+11줄). red-check 통과. 회귀 테스트 신설. `87d1c667f`.
- Stage 3 (승인): release-test 전체·clippy·Skia 3종·wasm 재빌드·664건 쪽수 A/B(차이 0)·
  IR sweep(발산 0 + #3507 래칫 2행 조임)·CDP 잉크(p2 하단 0.02→14.01%)·정답지 스왑
  세트(`/mnt/e/hwp/swap3236/`). `df60b773c`.
- **Stage 4: 작업지시자 시각 판정 통과.** 최종 보고 `mydocs/report/task_m100_3236_report.md`.
- PR [#3673](https://github.com/edwardkim/rhwp/pull/3673) 생성(승인),
  [#1936 회귀 기록 코멘트](https://github.com/edwardkim/rhwp/pull/1936#issuecomment-5147626476) 게시.
- #3671 merge 로 conflict 발생(오늘할일 add/add) → devel 병합·양쪽 기록 보존으로 해소.
  #3666 이 분할 셀 인접 수정이라 병합 후 focused 재검증 수행.
- **완결**: CI 21 pass(스킵 2는 preflight 정상 생략) → merge 승인 →
  [#3673 MERGED](https://github.com/edwardkim/rhwp/pull/3673) (2026-07-31 22:27Z,
  원격 브랜치 삭제) → 이슈 자동 close(github-actions) →
  [리포터 회신 게시](https://github.com/edwardkim/rhwp/issues/3236#issuecomment-5147991563)
  (재현 확인·원인·적용 예정 버전·확인 방법, 릴리스 시 버전 재코멘트 약속).
  devel·local/devel 동기화 완료.

## Issue #3307 — 개요 자동번호 소실 (착수)

외부 리포터 Neurlect(7/25, 7일 무응답). 별지 서식 표에서 1.~4. 개요 번호 소실
(5.~6.은 리터럴이라 생존). 현행 devel 재현 — 미해결 잔존 결함.

- 근인: 문서에 개요 모양 정의 없음(`hh:numbering` 0개, `outlineShapeIDRef=0`) →
  `resolve_numbering_id`→0→`return None`. 한컴은 내장 기본 개요 모양 적용, rhwp 는 부재.
- fixture `samples/task3307/`, 정답지 `pdf/task3307/`(MCP run_status 0, p7 에 1.~6. 전부).
  기관 발행 PDF 쌍도 확보. 계획서 승인 → Stage 1(한컴 수준 스윕 실측으로 기본
  모양 전 수준 ^N 확정, HWP5 동일 대상 확증, 선회신) → Stage 2(fallback ~30줄,
  red-check·과발동 가드) → Stage 3(전 게이트 + 666건 A/B 0 + 이중 baseline 신판
  첫 적용 + 이미지 스왑 코멘트) → **Stage 4 시각 판정 통과** → 최종 보고 →
  **완결**: [#3677 MERGED](https://github.com/edwardkim/rhwp/pull/3677)
  (2026-08-01 02:00Z, CI 20 pass) → 이슈 자동 close →
  [리포터 완결 회신](https://github.com/edwardkim/rhwp/issues/3307#issuecomment-5148981100)
  (적용 예정 버전·확인 방법·#3308 안내·벤치마크 재감사). devel·local/devel 동기화.

## Issue #3308 — 직인 텍스트박스 위치·용지규격 문구 (착수 예정)

#3307 과 동일 리포터(Neurlect)·동일 문서·동일 p7 의 별개 증상 2건. **작업지시자
결정: #3307 완결 후 별도 착수** (fixture·정답지 재사용).

- 착수 실측(좌표 정량): 직인 x **+97.5px 이탈**(y 정합), 용지규격은 위치까지
  **정합 — 해소됨**(#3127 귀속 확정 예정). 작업지시자 구조 판독: **표 안 표 +
  왼쪽 셀 테두리 빨간색** 조판 — 중첩 표 열 폭/원점 해석 축으로 좁힘.
- 계획 승인 → Stage 1(스트레치 근인·비율 분리 0.679↔0.956·가운데 모델 0.6px,
  작업지시자 "표 속성 값 미사용" 판정) → Stage 2(하한 0.9 + 셀 내 가운데, red-check
  두 축 독립 증명, 직인 x 696→600 vs 한컴 598.7) → Stage 3 전 게이트 통과 →
  **시각 판정 통과** → **완결**: [#3678 MERGED](https://github.com/edwardkim/rhwp/pull/3678)
  (2026-08-01 03:30Z, CI 20 pass) → 이슈 자동 close →
  [완결 회신](https://github.com/edwardkim/rhwp/issues/3308#issuecomment-5149595354)
  (증상 1 다음 릴리스·증상 2 는 v0.8.0 #3127 로 기해소 안내). devel·local/devel
  동기화. 최종 보고 `mydocs/report/task_m100_3308_report.md`.

## Issue #3311 — malformed CFB 패닉 (착수)

외부 리포터 Neurlect(7/25, cargo-fuzz 발견). `LenientCfbReader::open` 이 손상 CFB 에서
OOB 슬라이스 패닉. chrisryugj 가 "현행 devel 에 경계 검사 존재" 정보 공유(7/26).

- **실측 확정**: worktree 로 리포터 커밋(`8d3bfa4b`, 7/17) 재현 성공 —
  `cfb_reader.rs:407`, 리포터 보고와 동일 지점·형태. `6a761a793`(#3220 kevin9327
  축배치 J, 7/24)에서 해소, 현행 177 케이스 무패닉. **해소 커밋 귀속 완료.**
- 남은 작업: **회귀 가드 부재** — 이 계열을 고정하는 테스트가 없어 재유입 시 미검출.
  본 타스크 산출물은 가드 신설(#3668 과 같은 성격).
- 계측 신뢰도: 합성 하니스가 구 커밋에서 실제 패닉을 재현해 red-check 완료.
  worktree 사용(stash 함정 회피 — 오늘 교훈 적용, 상주 stash 6개 무손상).
- 브랜치 `local/task3311`, 수행계획서 `mydocs/plans/task_m100_3311.md` — 승인 대기.

## 추적 항목 (이월)

- ~~MCP 응답 전달 조사~~ — **해결 확인 (2026-08-01)**. 새 클라이언트
  (`tools/hwp-convert-mcp-client-20260731-230819.tar.gz`, task #3604)의 비동기
  `start`/`status`/`download` 흐름으로 384쪽 편람 재시도: 서버 385초 succeeded →
  **19.3MB 를 download 5.3초에 수신, SHA-256 일치**. 지난 실패(동기 인라인 27분
  타임아웃)의 우회 확립. `status` 의 delivery 블록으로 콜레보레이터 모니터링 없이
  메인테이너 단독 판별 가능해짐. `.vscode/mcp.json` 새 tarball 로 교체(로컬).
  산출 PDF `/mnt/e/hwp/pyeonram_3589-2020.pdf` (383쪽 — 원본 HWP 는 rhwp 기준
  384쪽, 쪽수 차이는 별도 관찰 항목). **작업지시자 판정: 산출 PDF 정상 (2026-08-01)**
  — MCP 대용량 전달 건 완전 종결
- 편람 쪽수 편차 [#3674](https://github.com/edwardkim/rhwp/issues/3674) 등록 —
  rhwp 386 vs 한컴 383 (+ 저장 전후 387→386). 세션 통용 "384"는 #3565 등록 시점
  수치로 판명(실측 재검증에서 발견). #3236 수정과 무관함 실측 확인
- #3236 릴리스 포함 시 리포터에게 버전 재코멘트 (회신에서 약속)
- #3668 (`LAYOUT_OVERFLOW_CELL` 집계, #3236 sub-issue) — **Stage 1·2 완료 (2026-08-01)**.
  계획서 승인 → 코어 카운터+봉투(red-check 23 정확)+계약 33 passed+렌더 불변 →
  전수 원장 662건·0 아닌 22종·4,896줄 동결(결정성 2회). issue2007 지목 샘플 2,980줄
  발현 즉답. 게이트 2분07초 — **(a) 기본 스위트 편입 결정(작업지시자)**.
  편람 96/53줄은 #3674 단서. Stage 3 전 게이트 통과. **완결**: CI 20 pass →
  [#3675 MERGED](https://github.com/edwardkim/rhwp/pull/3675) (2026-08-01 00:19Z) →
  이슈 자동 close → [#3674 단서 코멘트](https://github.com/edwardkim/rhwp/issues/3674#issuecomment-5148547699)
  (편람 96/53줄 + 쪽수 편차와의 관계 가설 + overflowCellLines 조사법) →
  devel·local/devel 동기화. 최종 보고 `mydocs/report/task_m100_3668_report.md`
- #3664 (Native FFI 빌드 붕괴)·#3648 (hwpctl b-2) — 기여자 착수 여부 추적
- planet6897 별건 5종 이슈 등록 대기 (#3566 조사 파생)
- #3202 — bindings/python PR 접수 시 3조건 검토
- lpaiu-cs draft #3662 — draft 해제 시 처리
- kevin9327 #3608 로드맵 5-PR 스택 — 컨트리뷰터 단계별 진행
