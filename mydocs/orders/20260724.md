# 오늘 할일 - 2026년 7월 24일

## 첫 외부 기여 PR #3132 — DocLang v0.6 내보내기 메인터너 보정·통합

- 대상: [#3132](https://github.com/edwardkim/rhwp/pull/3132), contributor `myeolinmalchi`, 관련 이슈
  [#3131](https://github.com/edwardkim/rhwp/issues/3131). 첫 기여 PR로 확인된 대형 DocLang 기능 PR이다.
- contributor source head `587e786d` 위에 메인터너 보정 commit `0afa775b3`을 별도로 준비했다. 기존
  contributor commit은 rewrite·amend·force-push·체리픽하지 않는다.
- 보정 범위는 `export-doclang` 출력 symlink/hard link가 원본을 덮어쓰는 결함의 동일 파일 identity guard,
  실제 CLI 회귀 테스트, EqEdit doctest의 현재 crate import 정정이다.
- `cargo fmt --check`, DocLang CLI 5건, 전체 `release-test`, doctest, Clippy, WASM library check와
  diff-check를 전용 target에서 통과했다. renderer/layout/Studio 출력·sample/golden 변경이 없어 visual
  sweep 대상은 아니다.
- archive review·implementation 기록과 contributor PR head 직접 보정·remote push 절을 별도 운영 문서
  commit `315aa5acc`으로 준비했다. 이 파일도 같은 PR head에 포함한다.
- 승인 뒤 contributor branch에 두 collaborator 추가 commit을 직접 push하고, code/test 변경이 있으므로
  문서-only fast-pass 없이 최신 head 기준 GitHub Actions를 기다린다.
- CI 성공 후 최신 mergeability를 확인해 merge하고, 원 PR과 [#3131](https://github.com/edwardkim/rhwp/issues/3131)에
  로컬·CI 검증 요약을 남긴다. 첫 기여의 완성도와 메인터너가 안전성·문서 실행성만 보정한 이유를 구체적으로
  설명하고, 다음 기여도 기대한다는 감사 코멘트를 남긴다.
