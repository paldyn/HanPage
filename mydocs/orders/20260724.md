# 오늘 할일 - 2026년 7월 24일

## kevin9327 축배치 연작 — 열린 PR 62건 중 45건 merge

어제 확정한 "충돌 형제를 같은 축에 흡수" 방침으로 축별 배치 7개 + 문서 1건을 처리했다.
전 배치 공통 절차는 어제와 동일하며(cherry-pick -x → 해시 검증 → 게이트 → 승인 →
squash merge → 크레딧+close → 이슈 확인 → devel 동기화), 오늘부터 처리 대상 PR 에
Assignee(kevin9327)/Reviewer(edwardkim)/Milestone(v1.0.0) 셋팅을 병행했다.

| 배치 | 축 | 건수 | 통합 PR | merge |
|---|---|---:|---|---|
| F | hwpx-section 왕복/CDATA | 11 | [#3205](https://github.com/edwardkim/rhwp/pull/3205) | `1d6d0073b4` |
| G | hwpx-header 왕복/리터럴 | 8 | [#3210](https://github.com/edwardkim/rhwp/pull/3210) | `d2dfa35cf6` |
| H | hwpx lock 연작·fieldEnd | 5 | [#3215](https://github.com/edwardkim/rhwp/pull/3215) | `a5e4c72858` |
| I | hwp3 파서 | 6 | [#3217](https://github.com/edwardkim/rhwp/pull/3217) | `02562b2979` |
| K | 문서 코어 편집 | 7 | [#3218](https://github.com/edwardkim/rhwp/pull/3218) | `3bc17ee955` |
| M | rhwp-studio UI | 6 | [#3219](https://github.com/edwardkim/rhwp/pull/3219) | `74cba524c1` |
| J | 파서 견고성(DoS 방어) | 6 | [#3220](https://github.com/edwardkim/rhwp/pull/3220) | `6a761a7930` |
| L | 소형 종합 | 10 | [#3221](https://github.com/edwardkim/rhwp/pull/3221) | `cda0a708c7` |
| — | #3089 문서 정정 직merge | 1 | — | `e1c901d732` |

- 연결 이슈 44건 종결. 통합 배치의 원 PR 은 전건 크레딧 코멘트 후 close.
- 충돌 흡수 실적: 배치 흡수 대상 충돌 PR 14건 중 13건 편입, 1건(#3185)은 회귀로 반려.
  충돌의 대부분은 이번에도 형제 병치(테스트/속성/match arm)였고, 실질 판단이 필요했던
  것은 #3107(파서-방출 간극 동시 동기화), #3118(instance_id 근사 대신 end_field_id
  정밀 보존), #3171+#3180(CLI 종료 코드 상호보완 결합), #3193(#3119 와 중복 정리),
  lock 삼중 중복(#3111 기축 통일) 정도다.

## RFC #3141 수용 — cargo-fuzz 인프라 (#3158, `e78883617e`)

작업지시자 결정으로 RFC 를 수용하고 1단계 구현을 merge 했다. fuzz/ 독립 크레이트
(workspace 분리로 본 빌드·테스트 무영향), 하네스 4개(parse_hwp/hwp3/hwpx/hml),
시드 corpus(저장소 기존 샘플 10개), 운영 문서. **조건**: cargo-fuzz 빌드 검증(nightly
필요)은 로컬 재실증 범위 밖이라 CI/후속 단계에서 확인한다. #3141 은 2단계 이후를 위해
열어 둔다.

## #3185 반려와 #3172 오판 정정 — 이분 탐색의 교훈

배치 G 게이트에서 IR 필드 스윕 회귀 5건(`cells[].paragraphs[].raw_header_extra` +2,
전수 799샘플 중 aift.hwpx 등)이 잡혔다.

- 처음에 #3172(charPr/paraPr id 매핑)를 원인으로 지목해 제외하고 반려 코멘트까지
  게시했는데, **오판이었다**. 검증이 "해당 커밋 이전 시점과의 비교"였지 "해당 커밋만
  제외"가 아니었다. 이분 탐색을 끝까지 진행하니 진범은 **#3185(memoProperties)** 단독.
- 그 사이 회귀 5건을 그대로 구워 넣은 baseline 갱신 커밋을 만들었다가, diff 의
  "5 insertions" 를 보고 즉시 폐기했다. 최종 baseline 은 순개선 3건만 반영했다(배치 G).
- #3172 에는 정정·사과 코멘트를 게시하고 배치에 도로 편입했다(스윕 전수에서 무해 실증).
  #3185 에는 회귀 실증·추정 지점·재현 명령을 담아 재작업을 요청했다.
- **교훈**: 회귀 귀속은 "용의 커밋만 제외한 상태"로 검증해야 한다. 전/후 비교는
  누적 변경을 통째로 지목할 뿐이다.

## #3036 CI 실패 분석과 렌더 비교 게시

작업지시자 요청으로 #3036(각주 사이 간격 배선)의 CI 실패를 분석했다 — #3183 을
보류시킨 것과 동일한 **#1692 계약 충돌**(HWP3 미주 = 한컴 2020 PDF 쪽 범위 일치).
배선이 미주 간격을 실제로 벌려 43쪽 마지막 미주 번호가 58→53 으로 밀린다.

devel 판과 PR 적용판을 각각 native-skia 로 빌드해 SO-SUEOP.hwp 42·43쪽을 렌더,
나란히 합성한 비교 이미지를 assets PR [#3209](https://github.com/edwardkim/rhwp/pull/3209)
(`e2386b2212`)로 devel 에 올리고 #3036 에 판독 포인트와 함께 게시했다 — 42쪽 픽셀
동일 / 43쪽 마지막 번호 58→52 / PR 판 좌측 컬럼 텍스트 겹침(LAYOUT_OVERFLOW 부합).
**갈림길(배선 해석 오류 vs 정답지 fixture 갱신)은 한컴 실물 대조로 확정해야 하며**,
판정 전까지 각주 3건(#3036 #3049 #3059)은 보류 유지.

## 절차 사고와 교정

- **stash pop 낡은 stash 오염**(어제 기록 보강): 재발 방지를 메모리에 등록 —
  pop 은 자기 stash 확인 후, 커밋 전 `--stat` 확인.
- **#3172 회귀 귀속 오판**: 위 절 참조. 반려 코멘트를 냈다가 정정·사과했다.
- worktree 격리로 게이트 실행 중에도 렌더 빌드를 병행하는 절차를 확립했다
  (`git worktree add` + 별도 target — 본 체크아웃 무간섭).

## 부수

- 어제 오늘할일의 merge 방식 서술 정정([#3204](https://github.com/edwardkim/rhwp/pull/3204)
  `4f6b1acf1f`) — C·D·E 는 jangster77 님이 merge commit 방식으로 처리했음을 명기.
- FieldRange.end_field_id 확장(#3118)에 못 따라온 doclang(전일 유입)·serializer 테스트
  리터럴 3곳 보강. doclang 모듈과의 첫 접점이었다.

## seo-rii #2939 (P39) merge — CanvasKit 위치 확정 특수 텍스트 시각

[#2939](https://github.com/edwardkim/rhwp/pull/2939) (by seo-rii) 를 collaborator-mediated 경로로
검토·merge 했다. merge commit `1ddfc7da12`.

- 범위: 글자 겹침·탭 리더·밑줄/취소선/강조점·공백/탭/문단끝/줄바꿈 부호를 CanvasKit direct replay로 승격,
  run당 글리프별 폰트 fallback(문서→기본→옛한글)·한컴 박스 숫자 PUA. schema `1.19`. 공개 기본은 Canvas2D 유지.
- 검증: BEHIND 해소 후 최신 head `aa017fee` GitHub Actions 21건 pass(WASM Build 조건부 skip). 로컬은
  fmt·clippy(`-D warnings`) clean, `cargo test --profile release-test --tests` 실패 0, `svg_snapshot` golden
  결정성 pass, rhwp-studio `tsc`/`npm test`(539)/`e2e:renderer-contract` pass.
- 시각: CanvasKit↔Canvas2D ink-mask `paragraph-text-marks` 0.00645915<0.02, `pua-special-glyphs`
  0.00373927<0.01 (render-diff readiness / Canvas visual diff 하드 게이트). 기준 샘플 lseg-05-tab.hwp·pua-test.hwp 저장소 존재.
- 설계 강점: producer·preflight·runtime 3계층 동일 4,096 상한 + completeness fail-closed, 미해결 글리프는
  `textRun:glyphMapping` 진단, 겹침 run legacy 우회 + `legacyVisuals:"mirror"` 이중 paint 차단.
- 이슈 #536은 트래킹이라 open 유지, P39 완료 + 남은 follow-up(실제 HWP focused fixture, 세로/회전/장평/효과,
  showControlCodes 구조 조판부호)을 코멘트로 기록.
- review 문서: `mydocs/pr/archives/pr_2939_review.md`.

## 잔여와 다음 단계

- **각주 3건**(#3036 #3049 #3059) — #1692 한컴 시각 판정 대기(비교 이미지 게시 완료).
- **재작업 대기 2건** — #3185(memoProperties 스윕 회귀), 리베이스 대기 #2739 #2746.
- planet6897 render 4건(#3129 #3086 #3084 #3019)은 최신 devel 위 누적 통합과 검토 문서·KTX visual sweep을 마쳤으며, 통합 PR의 최신 head CI 확인 대기다.
  seo-rii #2939(P39)는 merge 완료. postmelee draft 4건·기타 draft 2건은 draft 해제 대기.
- 존치: #2550(C안 대기), #2659(로드맵 정독 대기).

## planet6897 렌더 4건 통합 PR 준비

- 수용 대상: [#3019](https://github.com/edwardkim/rhwp/pull/3019), [#3084](https://github.com/edwardkim/rhwp/pull/3084), [#3086](https://github.com/edwardkim/rhwp/pull/3086), [#3129](https://github.com/edwardkim/rhwp/pull/3129). 관련 이슈는 [#2999](https://github.com/edwardkim/rhwp/issues/2999), [#3048](https://github.com/edwardkim/rhwp/issues/3048), [#3085](https://github.com/edwardkim/rhwp/issues/3085), [#3127](https://github.com/edwardkim/rhwp/issues/3127)다.
- 최신 upstream/devel 위로 원 작성자 commit 4개를 순서와 저자 정보 그대로 재배치했다. #3019와 #3086 golden 충돌은 작은 가운뎃점 반지름과 투명 추출 텍스트를 함께 보존해 해결했다.
- 로컬 검증: fmt, SVG snapshot 8/8, partial TAC table 6/6, KTX 쪽 번호 1/1, layout #634 8/8, native-skia cargo check, diff check 통과. aift PDF 추출 가운뎃점은 7개로 golden의 투명 텍스트 7개와 일치한다.
- 시각 검증: samples/KTX.hwp와 한컴 기준 pdf/KTX-2022.pdf p002 대조의 자동 후보 0건. 안정 PNG와 개별 review 문서는 mydocs/pr/assets/pr_3129_planet_ktx_p002_review.png 및 mydocs/pr/archives/pr_3019_review.md, pr_3084_review.md, pr_3086_review.md, pr_3129_review.md에 보관한다.
- 다음 단계: 원본 저장소 통합 PR을 열고 최신 head CI 성공 후 merge한다. merge 뒤 원 PR 및 관련 이슈 close 상태를 확인하고, 감사·통합 안내 코멘트는 별도 승인 후 처리한다.

## 오늘 처리 집계

| 구분 | 건수 |
|---|---:|
| kevin9327 PR merge (배치 7 + 소형 + 문서) | 60 |
| 인프라 채택 (#3158, RFC #3141 수용) | 1 |
| 통합 PR | 8 (#3205 #3210 #3215 #3217 #3218 #3219 #3220 #3221) |
| 연결 이슈 close | 44 |
| 반려(재작업 요청) | 1 (#3185) |
| 메타데이터 셋팅 | kevin PR 21건 + 통합 PR |

이틀 누적으로 kevin9327 연작 83건이 merge 됐고, 남은 열린 PR 은 각주 3건과 재작업
1건 수준으로 줄었다. 유입 속도를 처리 속도가 따라잡았다.

## 오후 추가분 — 각주 축 2건 채택 (#3049 #3059)

- **#3049**([#3225](https://github.com/edwardkim/rhwp/pull/3225) `988aee4f84`) —
  footnote_line_margin(오프셋 104) ×4 → separator_margin_top 배선.
  `hwp3_default_endnote_shape` 시그니처를 `&Hwp3DocInfo` 로 통일(A안)해 각주 배선의
  공통 기반을 만들었다.
- **#3059**([#3229](https://github.com/edwardkim/rhwp/pull/3229) `9931fb8a4a`) —
  footnote_text_margin(오프셋 106) ×4 → note_spacing 배선.
- **판정 요지**: 두 건 모두 정답지 쪽 범위 계약(`answer_endnote_pages_match_pdf_ranges`)을
  엄격 통과 — #3036 과 달리 조판을 흔들지 않는다. 교차 포맷 동일성만 샘플 쌍
  (SO-SUEOP.hwp/.hwpx)의 저장값 차이(12·8 HWPUNIT ≈ 0.04·0.03mm)로 어긋나, 작업지시자
  승인 하에 해당 비교를 ±16(1 hunit 반올림) 근접 검증으로 완화했다(사유 주석 명기).
- 이슈 #3046 #3054 close. **각주 축 잔여는 #3036 하나** — 실제 조판 변화라 한컴 실물
  대조가 필요하며, 판정용 비교 이미지는 게시돼 있다.
- 잔여 집계 갱신: kevin9327 열린 PR 2건(#3036 시각 판정 대기, #3185 재작업 대기).
  오늘 kevin9327 merge 누적 62건.
