# 오늘 할일 - 2026년 7월 31일

## #3536 CanvasKit P40 사전검증·이미지 재생 guard 검토

[#3536](https://github.com/edwardkim/rhwp/pull/3536)은 `@seo-rii`의 CanvasKit P40 guard 변경이다. 최신
code head `b14d245`가 `devel` `a435f41`을 포함하고 full CI, CodeQL, Canvas visual diff, `Build & Test`를
통과했다. 로컬 focused Rust, Studio 685 tests, 전체 release-test integration(3,030 passed), Native Skia,
fmt·clippy·TypeScript·renderer contract도 확인했다.

- 원 PR에는 archive review·실행계획·오늘할일을 추가했고, LFS 사전 판독과 source/local/remote SHA 대조 뒤
  review-only fast-pass A 경로의 preflight·최종 aggregate를 통과해
  [`b432958`](https://github.com/edwardkim/rhwp/commit/b432958cc2028b1f063e36dd34036a801ea967b8)로 merge했다.
- P40 `image-crop` CanvasKit 4조합은 2% tolerant budget 이내(최대 1.4700637%, image replay failure 0)였다.
  다만 broad 23-sample sweep의 `glyphMapping`·`invalidLayout` hard gate 16건은 성공으로 바꾸지 않고 별도
  renderer 후속 과제로 기록했다.
- [#536](https://github.com/edwardkim/rhwp/issues/536)은 P40의 남은 단계를 위해 open으로 유지했고, contributor
  감사·검토 결과 comment와 `devel` sync도 완료했다. visual sweep을 검토 근거로 쓴 만큼 대표 backend-parity panel을
  post-merge fast-pass 기록 PR에 보완한다. 상세 근거는 `mydocs/pr/archives/pr_3536_review.md` 및
  `mydocs/pr/archives/pr_3536_review_impl.md`에 남긴다.

## #3638 CanvasKit P41 text·stroke replay 검토

[#3638](https://github.com/edwardkim/rhwp/pull/3638)은 `@seo-rii`의 CanvasKit P41 text·stroke replay 확장이다.
현재 code head `bdee8b6`은 `devel` `3bd523f`을 포함하며, CI preflight, Lint, frontend package gates,
Native Skia, default-feature 8 shards, Build test archive, `Build & Test`, CodeQL, Canvas visual diff를 모두
통과했다. 로컬에서는 isolated target으로 CanvasKit policy 42건, Studio renderer contract, `wasm-pack build`를
확인했다. 전체 `cargo test --profile release-test --tests`는 작업지시에 따라 다시 실행하지 않고, 위 current-head
CI aggregate로 대체한다.

- CanvasKit은 복합 shaping·mark/format control·옛한글/boxed PUA와 paint effect 조합을 fail-closed로 남기고,
  안전한 producer-positioned nominal text와 dash·vertical text·paint replay만 넓힌다. public 기본 Canvas2D도
  유지된다.
- [Canvas visual diff](https://github.com/edwardkim/rhwp/actions/runs/30621047246/job/91125438952)의
  `table-border-style` readiness는 고정 CI surface에서 8/8이다. p0 selected diff `0.0045106778 ≤ 0.005`,
  dash/punctuation/vertical run count `12/2/14`, unsupported/error 0을 증적으로 남겼다. local Chrome
  146/software에서는 같은 p0이 `0.0054706828`로 7/8이어서 환경 차이를 숨기지 않고 review에 기록하며,
  merge 판정은 CI canonical run으로 한다.
- archive review·3-way backend-parity panel·오늘할일을 원 PR source head에 추가한다. LFS 대상 사전 판독과
  source/local/remote SHA 대조 뒤 review-only fast-pass를 통과하면 승인·squash merge한다. #536은 P41 뒤도
  후속 단계가 있어 open으로 유지하고, merge 뒤 contributor 결과 comment, `devel` sync 및 검토 자원 정리를
  수행한다. 상세 근거는 `mydocs/pr/archives/pr_3638_review.md`에 남긴다.

## #3640 #3592 중첩 표 행 필터 세로정렬 검토

[#3640](https://github.com/edwardkim/rhwp/pull/3640)은 첫 기여자 `@NacreousCloud`의 2-file layout 수정이다.
행 범위 filter가 존재한다는 이유만으로 중첩 표 모든 cell을 Top으로 덮던 동작을, 실제로 visible range
`[sr, er)` 밖으로 걸치는 cell에만 적용하도록 좁힌다. 따라서 온전히 들어온 Center cell은 HWP model의
세로정렬을 유지하고, 상·하단이 잘린 rowspan cell은 Task #44의 기존 Top 정책을 유지한다.

- 최신 code head `45b05be`의 CI preflight, Lint, archive, default-feature 8 shards, Native Skia,
  `Build & Test`, CodeQL, Canvas visual diff는 모두 success다. 로컬 focused integration
  `issue_3592_row_filter_valign`도 2/2 통과했다. 중복 전체 release-test는 작업지시에 따라 중단했고,
  결과를 성공 근거로 쓰지 않는다.
- `kps-ai` candidate/devel SVG 78쪽 중 76쪽은 byte-identical이며, 변화는 의도한 rhwp 66·67쪽뿐이다.
  한컴 2022 PDF 65 ↔ rhwp 66의 직접 3-way/OVL에서 devel의 상단 라벨이 candidate에서 정답지 중앙으로
  복구된 것을 증적으로 보존한다. 기존 77/78쪽 pagination offset과 기존 page 23·36 overflow diagnostic은
  이번 수정의 성공으로 확대하지 않는다.
- archive review·3-way/OVL panel·오늘할일을 원 PR source head에 추가한다. LFS 사전 판독과
  source/local/remote SHA 대조 뒤 review-only fast-pass를 통과하면 승인·squash merge한다. merge 뒤
  #3592 close, 첫 기여자 감사 comment, devel sync와 검토 자원 정리를 수행한다. 상세 근거는
  `mydocs/pr/archives/pr_3640_review.md`에 남긴다.

## kevin9327 PR 5건 — 산출물·MCP 세션 축 통합 PR 준비

[#3597](https://github.com/edwardkim/rhwp/pull/3597),
[#3599](https://github.com/edwardkim/rhwp/pull/3599),
[#3602](https://github.com/edwardkim/rhwp/pull/3602),
[#3607](https://github.com/edwardkim/rhwp/pull/3607),
[#3610](https://github.com/edwardkim/rhwp/pull/3610)은 kevin9327의 산출물 JSON·MCP 세션 편집/검색·생성/미리보기·운영
playbook 적층이다. 최신 `devel` 위 누적 검토는 충돌 없이 완료했고, release-test 통합 트리와 focused 계약을
통과했다. #3597의 계약 테스트 rustfmt 세 곳만 collaborator test-only commit으로 정렬했다. 원 #3597 source
branch의 후속 `src/main.rs` 서식 보정은 통합 base에서 이미 적용돼 별도 patch가 필요하지 않았다.

- 원 contributor commit만 `integrate/kevin9327-20260731`에 누적해 **통합 PR 하나**로 검증·merge한다.
  원 PR source branch는 추가 push·개별 review·개별 merge 대상이 아니다. 이미 시작된 원 PR CI는 취소하지
  않되 통합 merge의 근거로 쓰지 않는다.
- code·test가 포함되므로 통합 PR 최신 head의 **full CI**가 merge 전 조건이다. #3607의 +1,000 lines 이상
  범위 검토도 통합 review 기록에서 유지한다.
- merge 뒤 #3596, #3598, #3601, #3600 close 상태를 확인하고, 다섯 원 PR에 통합 PR 링크와 감사 comment를
  남긴 뒤 supersede close한다. merge SHA·issue close·comment 완료는 사전에 단정하지 않는다.
- 통합 branch push 전에는 source/local/remote SHA 대조와 LFS 대상 사전 판독을 수행한다. 상세 근거와
  rollback 경계는 `mydocs/pr/archives/pr_3597_review.md` 및
  `mydocs/pr/archives/pr_3597_review_impl.md`에 남긴다.

## kevin9327 PR 12건 — MCP 작업 단위·에이전트 계약 통합 PR 준비

[#3612](https://github.com/edwardkim/rhwp/pull/3612)부터
[#3634](https://github.com/edwardkim/rhwp/pull/3634)까지의 열린 12건은 세션 조회·표 수정·HWP5/HML JSON
봉투·k번째 치환·문서 분할·profile/digest와 그 지식·증적 문서의 적층이다. 원 PR을 개별 merge하지 않고,
최신 `devel` `187086d` 위 `integrate/kevin9327-20260731`에 contributor 원 commit만 누적했다. source
branch의 merge commit은 중복을 피하기 위해 제외했고 conflict는 없었다.

- 통합 검토에서 profile 목록 필터의 `tools/call` 우회, JSON page/row/column narrowing wraparound,
  0쪽 문서 page 진단 underflow, `hwp_digest.maxChars` 미전달을 발견했다. contributor 이력은 보존하고 별도
  `94cdd74ce`, `208c3f618` 보정으로 dispatch 권한, 명시적 범위 오류, 빈 문서 오류, optional CLI argument와
  회귀를 추가했다.
- 전용 `target/review-kevin9327-20260731`, `CARGO_INCREMENTAL=0`에서 전체
  `cargo test --profile release-test --tests --quiet`를 완료까지 실행해 exit 0을 확인했다. release build,
  fmt, `clippy -D warnings`, 변경분 Markdown 링크(441개)·metadata(428개) 검사도 통과했다.
- 포함 PNG는 각 CLI/MCP operation의 설명 증적이다. renderer·layout·typeset 변경이 없으므로 visual sweep
  성공으로 확대하지 않는다. 통합 PR의 정확한 최신 head full CI·CodeQL·aggregate와 `MERGEABLE`이 merge
  전 조건이다. 이후 LFS 판독, 통합 PR 한 건, issue 재판단, contributor 원 PR supersede close, devel sync와
  정확한 검토 자원 정리를 수행한다. 세부 기록은 `mydocs/pr/archives/pr_3612_review_impl.md` 및 원 PR별
  `pr_3612_review.md`–`pr_3634_review.md`에 남긴다.

## planet6897 PR 4건 — Skia·layout 관측 통합 PR 준비

[#3636](https://github.com/edwardkim/rhwp/pull/3636),
[#3639](https://github.com/edwardkim/rhwp/pull/3639),
[#3643](https://github.com/edwardkim/rhwp/pull/3643),
[#3644](https://github.com/edwardkim/rhwp/pull/3644)를 최신 `devel` 위 하나의 통합 후보로 누적했다.

- IR field sweep(805 samples), focused #3637/#1285/CLI, 전체 release-test integration(387 test 묶음),
  Native-Skia 3종, WASM build가 통과했다. `dump-extents` 0쪽 범위 메시지와 cross-run 말미 공백 폭은
  maintainer 보정 `fac610dbe`로 안전하게 정리했다.
- 한글 2020 기준 PDF는 2쪽, rhwp는 3쪽이다. 공통 1쪽에서 본문 소실 해소는 확인했으나, 2쪽은
  페이지 대응 자체가 달라 pixel 지표를 통과 근거로 쓰지 않는다. 페이지 수 불일치는 #3639의 범위 밖으로
  남긴다.
- 통합 PR 최신 head의 full CI 성공이 merge 전 조건이다. merge 뒤 #3637만 close 여부를 재확인하고,
  source fork branch는 보존한 채 원 PR들을 통합 PR로 supersede close한다. 상세 근거는
  `mydocs/pr/archives/pr_3636_review_impl.md` 및 각 원 PR review 기록에 남긴다.

## #3641·#3642 셀 문단 앵커·중첩 표 행 정체성 통합 검토

[#3641](https://github.com/edwardkim/rhwp/pull/3641)과
[#3642](https://github.com/edwardkim/rhwp/pull/3642)는 `@NacreousCloud`의 연속된 layout 수정이다.
원 PR을 개별 merge하지 않고 `devel` `44d046bf7` 위
[#3657](https://github.com/edwardkim/rhwp/pull/3657)에 기능 commit만 순서대로 체리픽했다. 원 head의
`devel` merge commit은 제외했고, #3642의 pagination 변화에도 #3641 fixture 회귀가 같은 셀을 찾도록
`f45799a46`으로 안정화했다.

- #3657 current code head `f45799a46`의 CI preflight, lint, Native Skia, test archive, default-feature
  8 shards, `Build & Test`, CodeQL, Canvas visual diff가 모두 success다. 로컬 release-test는 원 PR과
  통합 PR CI가 확인한 범위를 중복하지 않도록 작업지시에 따라 실행하지 않았다.
- #3641은 둘째 이후 셀 문단의 `vpos=0`을 앵커 미기록 센티널로 해석해 겹침·높이 붕괴를 막는다. 한컴
  2024 PDF 37쪽 3-way/OVL은 대표 증적으로 보존하되, font·조판 차이가 큰 ink 지표를 PDF 전체 정합 성공으로
  확대하지 않는다.
- #3642는 컷 유닛의 실제 `nested_row`를 사용해 다문단 셀에서도 continuation이 행 0부터 다시 그려지지
  않도록 한다. PDF가 없는 fixture는 render-tree marker와 p3→p4·p59→p60 panel로 구조 계약을 확인한다.
- 마지막 꼬리 문단을 남기는 빈 `end_cut` 종료는 별개 [#3658](https://github.com/edwardkim/rhwp/issues/3658)로
  분리했고, `#[ignore]` 회귀를 삭제하지 않는다. review·asset·오늘할일만 추가한 head의 fast-pass가
  통과하면 #3657을 merge하고 #3593/#3595 close 상태, #3658 open 상태, 원 PR supersede와 contributor
  comment, devel sync·검토 자원 정리를 순서대로 확인한다.

## JamesPsh HWPX serializer PR 2건 통합 검토

[#3646](https://github.com/edwardkim/rhwp/pull/3646)과
[#3651](https://github.com/edwardkim/rhwp/pull/3651)은 `@JamesPsh`의 HWPX serializer 수정이다.
원 PR을 개별 merge하지 않고 `devel` `6d36bafa` 위
[#3659](https://github.com/edwardkim/rhwp/pull/3659)에 기능 commit만 번호순으로 체리픽했다. 원
head의 `devel` 병합 commit은 제외했고 `git range-diff`에서 두 patch의 동등성과 conflict 없음을 확인했다.

- #3646은 `hp:equation`에서 스키마 비허용 `instid` 방출을 제거하고 `id` 유지 계약을 고정한다.
  #3651은 `fieldBegin@dirty`와 `Field.properties` bit 15를 양방향 보존해 안내문과 같은 값의
  ClickHere 필드가 HWPX 재저장 후 사라지지 않도록 한다.
- 원 PR 두 건과 #3659 current code head `00450ccb`의 full CI에서 lint, Native Skia, default-feature
  8 shards, `Build & Test`, CodeQL이 모두 성공했고, #3651 및 통합 PR의 Canvas visual diff도
  success다. 로컬 전체 Cargo는 이미 성공한 원 PR·정확한 통합 head CI와 중복하지 않도록 작업지시에
  따라 실행하지 않았다.
- HWPX 속성 직렬화 변경이므로 새 PDF 정합이나 수동 visual sweep을 성공 근거로 주장하지 않는다.
  대신 #3651은 실제 `form-01.hwpx` fixture에서 dirty 읽기·방출·재적재와 초기 상태 무회귀를 고정한다.
- archive review·통합 계획·오늘할일만 추가한 review-only head의 fast-pass를 확인한 뒤 #3659을
  merge한다. merge 뒤 #3543은 close, 초기 상태 텍스트의 물리 삭제 설계 축이 남은 #3545는 open으로
  유지하고, 원 PR 두 건에는 통합 근거와 감사 comment를 남긴 뒤 source fork를 보존한 채 supersede
  close한다. 상세 근거는 `mydocs/pr/archives/pr_3646_review.md`,
  `mydocs/pr/archives/pr_3646_review_impl.md`, `mydocs/pr/archives/pr_3651_review.md`에 남긴다.

## #3646·#3651 HWPX 수식 스키마·ClickHere dirty 통합 검토

[#3646](https://github.com/edwardkim/rhwp/pull/3646)과
[#3651](https://github.com/edwardkim/rhwp/pull/3651)은 `@JamesPsh`의 HWPX 직렬화 계약 수정이다.
원 PR을 개별 merge하지 않고 `devel` `6d36bafa` 위
[#3659](https://github.com/edwardkim/rhwp/pull/3659)에 기능 commit만 번호순으로 체리픽했다. 원 head의
`devel` 병합 commit은 제외했고, `4b4eed64` → `30d850618`, `1ac0270b` → `00450ccb`은 각각
`git range-diff`에서 patch 동등이다.

- #3646은 `hp:equation`에 스키마상 허용되지 않는 `instid`가 나오지 않도록 하고, `id`는 남는 출력
  계약을 회귀로 고정한다. #3651은 `fieldBegin@dirty`와 `Field.properties` bit 15를 양방향으로
  보존해 안내문과 같은 ClickHere 입력값도 HWPX 재저장 뒤 사라지지 않게 한다.
- #3659 code head `00450ccb`의 CI preflight, lint, Native Skia, test archive, default-feature 8 shards,
  `Build & Test`, CodeQL, Canvas visual diff는 모두 success다. 원 #3646·#3651 current head CI도 모두
  success다. 로컬 전체 Cargo는 이 원 PR 및 통합 head 검증과 중복되므로 작업지시에 따라 실행하지 않았다.
- 두 변경은 renderer·layout·typeset 범위가 아니므로 PDF 정합이나 수동 visual evidence를 성공 근거로
  확대하지 않는다. Canvas visual diff success는 CI guard 결과로만 기록한다. #3543은 통합 merge 때
  close하고, 초기 `dirty="0"` 안내문 물리 삭제라는 별도 설계 축이 남은 #3545는 open으로 유지한다.
- archive review·통합 계획·오늘할일만 추가한 head의 LFS 사전 판독 및 review-only fast-pass가 통과하면
  #3659을 merge한다. 이후 원 PR 두 건에는 통합 근거와 감사 comment를 실제 줄바꿈으로 남기고 supersede
  close하며, source fork branch는 보존한다. `devel` sync와 통합·source refs 정리까지 후속 처리한다.
