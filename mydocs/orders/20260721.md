# 오늘 할일 - 2026년 7월 21일

## PR #2518 embed loadFile 대화상자 교착 수정 처리 완료

- [PR #2518](https://github.com/edwardkim/rhwp/pull/2518)은 `@rhwp/editor.loadFile()`의 생략 기본값을
  비대화형으로 바꿔 iframe 안내창 입력 대기로 인한 timeout을 막았다. 명시적 `suppressDialogs: false`와
  raw protocol·Studio 직접 열기 기본값은 유지된다.
- 원 PR 최신 head의 CI, CodeQL, Render Diff, Native Skia, frontend package gates 및 default-feature shard
  성공을 확인한 뒤 merge commit [`a621ac1`](https://github.com/edwardkim/rhwp/commit/a621ac19c95a5bc2f79086408b17c9a6ae94f442)로
  `devel`에 반영됐다.
- npm package는 unit/contract 19건, public embed/WASM contract 3건, TypeScript declaration, publish tarball
  파일 목록, 기본 renderer 실제 iframe smoke까지 확인했다. 상세 절차는
  [PR 검토 워크플로](../manual/pr_review_workflow.md)에 보강했다.
- 관련 [#2513](https://github.com/edwardkim/rhwp/issues/2513)은 원 PR merge 뒤 GitHub의 자동 close로 처리됐다.

## PR #2510 한양·휴먼 ASCII 메트릭 교정 검토 및 후속 기록

- [PR #2510](https://github.com/edwardkim/rhwp/pull/2510)은 한양·휴먼 face가 HY 계열로 오귀속되던 문제를
  바로잡고 ASCII 실측 메트릭을 추가했다. 대표 `21868765` 문서는 rhwp 7쪽에서 한컴 기준 4쪽으로 회복했다.
- 원 PR 최신 head의 CI, CodeQL, Render Diff, Native Skia, frontend package gates와 default-feature shard
  성공을 확인했다. 로컬에서는 메트릭 재생성 verify, native focused 회귀, fresh WASM build, Chrome HWP/HWPX
  focused E2E를 추가로 통과했다.
- HWP/HWPX의 55→56 입력 경계 E2E 증적은 [PR #2510 검토 기록](../pr/archives/pr_2510_review.md)과
  `mydocs/pr/assets/pr_2510/`에 보존한다.
- 원 PR은 merge commit [`215a174`](https://github.com/edwardkim/rhwp/commit/215a17430a558ebd93f579cf6df3c343a48b1b4e)로
  반영됐다. 관련 [#2430](https://github.com/edwardkim/rhwp/issues/2430)은 나머지 10건의 별도 경로와
  ladder/preflight 증적 표현 정정을 추적하기 위해 open으로 유지한다.
- `pr_2510_review.md`의 '비차단 후속 보완' 2건 중 COM 불필요한 부분을 collaborator self PR로
  정정한다: (1) `EVIDENCE.md` ladder TSV SHA-256을 LF 체크아웃(Git 저장 바이트) 기준으로 재기록,
  (2) `hy_ascii_ladder.py` preflight를 `requested_face` 누적 병합으로 바꿔 per-face 실행에서도 5종
  identity 행이 `preflight_report.tsv`에 보존되게 하고 `EVIDENCE.md` §2 라벨을 실제 파일 내용에 맞게
  정정했다. 한양 4종 5행 실측 재보존은 Windows+한컴 COM 재실행이 필요해 [@planet6897](https://github.com/planet6897)에게
  별도 이슈로 안내하고 [#2430](https://github.com/edwardkim/rhwp/issues/2430)에서 추적한다.

## PR #2680 순수 RawSvg 프리페치 경로 보완 준비

- [PR #2680](https://github.com/edwardkim/rhwp/pull/2680)은 [PR #2654](https://github.com/edwardkim/rhwp/pull/2654)의
  조기 재렌더 위에, [PR #2522](https://github.com/edwardkim/rhwp/pull/2522)에서 제안한 순수 `rawSvg` SVG data URL
  프리페치만 이식한다. 내부 raster data URL이 든 SVG는 중복 처리하지 않는다.
- 실제 Studio `PageLayerTree` bbox 계약인 `x/y/width/height`를 고정하는 회귀 테스트를 추가했다. 구형
  `getPageRenderTree`의 `w/h`와 혼동하지 않으며, #2522의 필드명 보정은 필요하지 않다.
- 최신 `devel` rebase 중 [#2635](https://github.com/edwardkim/rhwp/issues/2635)의 URL 미수집 fallback은 유지했다.
  URL을 만들 수 있을 때는 #2680의 실제 프리페치 완료 경로를 사용하고, 만들 수 없을 때만 기존 조기 재렌더로 복구한다.
- `npm test` 466건, production build, 400ms RawSvg first-paint headless E2E를 통과했다. 시각 증적과 상세 판단은
  [PR #2680 검토 기록](../pr/archives/pr_2680_review.md)에 보존한다.
- 최신 PR head의 CI, CodeQL, Render Diff 성공을 확인한 뒤 merge한다. merge 후 [PR #2522](https://github.com/edwardkim/rhwp/pull/2522)는
  대체 PR 안내 코멘트와 함께 close한다.

## PR #2702 각주 빈 꼬리말 밴드 회수 메인터너 통합 검토

- [PR #2702](https://github.com/edwardkim/rhwp/pull/2702)은 [PR #2627](https://github.com/edwardkim/rhwp/pull/2627)의
  각주 빈 꼬리말 밴드 회수와 [PR #2655](https://github.com/edwardkim/rhwp/pull/2655)의 남은 `dump-pages`
  오류 처리만 최신 `devel`에 보정 통합한다. 이미 반영된 [PR #2552](https://github.com/edwardkim/rhwp/pull/2552)의
  범위 검사는 중복하지 않았다.
- 전체 release-test 회귀, clippy, 리베이스 후 focused 회귀, 포맷/diff 검사를 통과했다. HWP 2020 MCP 기준 PDF는
  92쪽, rhwp는 94쪽이며 잔여 +2쪽과 knife-edge 8건은 [#2559](https://github.com/edwardkim/rhwp/issues/2559)를
  닫지 않고 [#2668](https://github.com/edwardkim/rhwp/issues/2668)에서 이어서 추적한다.
- 대표 1·46·92쪽 visual sweep은 자동 구조 후보 0/3이다. 기준 PDF와 rhwp의 페이지 수·글꼴 차이 때문에
  완전한 한컴 fidelity를 주장하지 않으며, p82/p86 overflow 진단 후보도 review 기록에 남겼다.
- 검증 PDF, 대표 PNG, 상세 판단은 [PR #2702 검토 기록](../pr/archives/pr_2702_review.md)과
  `mydocs/pr/assets/pr_2702/`에 보존한다. 최신 head CI 통과 후 merge하고 원 [PR #2627](https://github.com/edwardkim/rhwp/pull/2627),
  [PR #2655](https://github.com/edwardkim/rhwp/pull/2655)의 후속 처리를 진행한다.
