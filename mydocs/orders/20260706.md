# 오늘 할일 - 2026년 7월 6일

## PR 처리

| PR | 내용 | 처리 |
|----|------|------|
| #1961 | #1943 HWPX `write_line` 직렬화 결손 보정 | **머지**. `LineShape.connector` 기반 `hp:connectLine` 방출, `hp:startPt`/`hp:endPt`, `hp:controlPoints`, component block, `lineShape`, `fillBrush`, `shadow`, `groupLevel`, `instid` 보존으로 `connectLine` 변질과 그룹 자식 line 좌표/스타일 소실을 보정. 최신 head `6c846710e1365c5ba597ef14692676f951cd80f5` 기준 GitHub Actions 통과, 로컬 검증(`serializer::hwpx::shape`, full `cargo test --profile release-test --tests`, `clippy --all-targets`, `fmt`, `diff-check`) 통과. merge commit `bb2df91c63de36606a0bd642cb56fcfe137a031c`. review 문서는 옵션 2 docs-only PR로 archive 반영. #1943은 merge 직후 open 상태라 docs-only PR merge 후 후속 코멘트와 close 처리 필요 |
| #1963 | #1948 교차 문단 고아 `fieldEnd` gap과 말미 slot 순서 보정 | **머지**. `src/serializer/hwpx/section.rs`에서 같은 `char_idx`의 미방출 `orphan_field_end`가 현재 8 UTF-16 unit gap 소유자인 경우 일반 slot보다 먼저 방출하도록 보정. 첨부 기준 PDF `issue1948_cross_para_fieldend-2024.pdf`와 신규 HWPX 샘플 1쪽 visual sweep 수행(`flagged=0/1`), `hwpx-roundtrip` `PASS diff=0 r2=0`, `render-diff --via hwpx` `PASS`, 로컬 검증(`serializer::hwpx::section`, `hwpx_roundtrip_baseline`, full `cargo test --profile release-test --tests`, `clippy --all-targets`, `fmt`, `diff-check`) 통과. GitHub Actions 통과 후 admin merge, merge commit `2e1c6929acc2b0f02bb6a2473597c45900fe70f4`. #1948은 GitHub Actions auto-close로 `2026-07-05T17:03:55Z`에 CLOSED 확인. docs-only PR merge 후 후속 검증 코멘트 필요 |
| #1968 | #1921 tail-before-vpos-reset 표의 각주 안전마진 완화 | **체리픽 통합 PR 준비**. 실제 변경 commit `750477549840af6d75c6f23d970739fe990594c4`를 `fd176b441`로 체리픽. #1969와 순서대로 적용했으며 충돌 없음. 보정은 다음 문단의 저장 `LineSeg.vertical_pos` 새 쪽 시작 신호와 표 각주 높이에 근거. 원 기준 `75828`은 현재 checkout에 없어 직접 visual sweep하지 못했고, #1969 회귀 guard `36389312` 기준 보조 visual sweep(`flagged=0/1`)과 로컬 검증(`build`, targeted tests, full `cargo test --profile release-test --tests`, `clippy --all-targets`, `fmt`, `diff-check`) 통과 |
| #1969 | #1920 하단 고정 틀 저장 flow 이중 계산 보정 | **체리픽 통합 PR 준비**. 실제 변경 commit `b0a0477a58e8bf14a4d2cb42bff25be0daec7cc7`를 `2cfac2f4e`로 체리픽. #1968 뒤에 적용. `available - prospective_excl` 판정과 bottom-fixed 소비 높이의 vpos base 반영으로 후속 문단 스냅/배타 영역 이중 계산을 보정. 원 기준 `36373162`는 현재 checkout에 없어 직접 visual sweep하지 못했고, `36389312` 1쪽 유지 visual sweep 결과 `flagged=0/1`, 대표 asset `mydocs/pr/assets/pr_1968_1969_36389312_review_177.png`. 로컬 검증(`build`, targeted tests, full `cargo test --profile release-test --tests`, `clippy --all-targets`, `fmt`, `diff-check`) 통과 |
| #1973 | #1964 raw IME/iOS 입력 경로 page-local refresh 안전 가드 보강 | **옵션 1 PR 준비**. `afterTextInputEdit()`가 `PageLocalTextEditOptions`를 받아 raw IME/iOS 입력도 command 입력과 같은 `insertedText`, `beforePageIndex`, `afterPageIndex` 힌트를 전달하도록 보강. iOS fallback은 첫 입력 앵커 기준 page index를 보존한다. 정적 PDF/SVG 렌더 변경이 아니라 Studio 입력 invalidation 계약 보강이므로 visual sweep은 제외하고, 로컬 검증(`node --test tests/input-edit-invalidation.test.ts`, `npm run build`, `git diff --check`) 통과. review 문서와 오늘할일을 같은 PR head에 포함. merge 전 최신 GitHub Actions 통과와 작업지시자 승인 필요 |
| #1974/#1976/#1983/#1985/#1989/#1990/#1991/#1996 | planet6897 PR 8건 체리픽 통합 | **통합 PR 준비**. 오래된 PR 번호 순으로 `upstream/devel` 기준 `task_m100_1974_1996_planet_cherrypick` 브랜치에 체리픽. #1990에서 `src/serializer/hwpx/section.rs` 충돌이 있었고 #1987 `replace_secpr_scalars`와 #1984 `replace_footnote_shape`를 모두 유지하도록 해결. #1989는 메인터너 보강 commit `fcee51770`로 `breakLatinWord` 주석 이슈 번호와 `HYPHENATION` 보존 테스트를 보강. 로컬 검증(`target` 삭제 후 `git diff --check`, `cargo fmt --check`, targeted tests, `env CARGO_INCREMENTAL=0 cargo test --profile release-test --tests`, `env CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings`) 통과. #1985는 기준 PDF `pdf/issue1949_giant_cell_nested_tables_perf-2024.pdf` 대비 SVG 112쪽/PDF 115쪽, `flagged=100/112`로 한컴 기준 시각 정합은 후속 후보이며 성능 개선 PR로 수용 판단. #1991은 기준 PDF `pdf/issue1950_hwp3_tab_charoffset-2024.pdf` 대비 SVG/PDF 1/1쪽, `flagged=0/1`, pixel match `95.90899%`로 통과. review 문서는 PR별로 `mydocs/pr/archives/pr_{N}_review*.md`에 생성했고 대표 visual asset은 `mydocs/pr/assets/pr_1985_issue1949_review_103.png`, `mydocs/pr/assets/pr_1991_issue1950_review_003.png`에 보관 |
| #1992 | #1975 README 로드맵 링크 경로 수정 | **옵션 2 문서 통합 PR 준비**. PR head에는 `devel` merge commit이 포함되어 있어 실제 변경 commit `e3cff6a6319977b7648e9d0ee5843e3d2d34dbee`만 `a0c88d5ba`로 체리픽. README 링크를 `mydocs/report/archives/rhwp-milestone.md`로 정정하고 target 파일 존재를 확인. review 문서 `mydocs/pr/archives/pr_1992_review*.md`와 오늘할일을 같은 문서 통합 PR에 포함. merge 후 원 PR #1992는 체리픽 반영 코멘트 후 close 처리 필요 |
| #1997 | #1993 render-diff 노드 인덱스 매칭 허위 변위 차단 | **merge 완료 후 옵션 2 문서 통합 PR 준비**. GitHub Actions 통과 후 merge commit `6e819dc93dbd85538433d70e8318c3b086b277b9`. review 문서가 active 경로 `mydocs/pr/pr_1997_review*.md`로 남아 있어 `mydocs/pr/archives/pr_1997_review*.md`로 이동. PR 본문에 closing keyword가 없어 #1993은 아직 open 상태이며, 문서 통합 PR merge 후 후속 코멘트와 수동 close 처리 필요 |
| #2005 | #1999 issue1949 기준 PDF 115쪽 vs rhwp 쪽수/시각 정합 보정 | **옵션 1 PR 진행**. `RowBreak` 거대 셀의 non-inline flow fragment, `TopAndBottom` flow, hard break 완화 조건을 문서 속성 기반으로 보정. HWPX/HWP 샘플 모두 기준 PDF와 115쪽 정합 확인. 로컬 검증(`cargo fmt --check`, `git diff --check`, `issue_1949_giant_cell_render_perf`, `issue_rowbreak_chart_overlap`, full `cargo test --profile release-test --tests`, `clippy --all-targets`) 통과. visual sweep은 HWP/HWPX 14~16쪽 모두 `flagged=0/3`, 평균 pixel match `92.3305%`. review 문서와 대표 asset `mydocs/pr/assets/pr_2005_issue1999_p16_hwpx_review.png`를 같은 PR head에 포함. merge 전 최신 GitHub Actions 통과와 작업지시자 승인 필요 |

## 리팩토링 (라운드 3)

| 항목 | 내용 | 상태 |
|------|------|------|
| #1925 | 라운드 2 | **클로즈** (작업지시자 승인, 산출물 생존 확인) |
| #2001 | 라운드 3 — parse_paragraph_list 해체 + metrics.sh 소품 | 4단계 완료 — **CC 234→76, 2,270→896줄**, 행동 회귀 0. 승인 대기 |

## 관측/대기

- #1936발 OVR 이탈 3건 (rowbreak-problem-pages p13/p14) — 시각 판정 대기, 3-way 자료 output/poc/pr1936/
- typeset.rs 재성장 공식 확인 (104→117, 112→127) — 재축적 추세 감시
## 리팩토링 (라운드 4, #2003)

| 항목 | 내용 | 상태 |
|------|------|------|
| #2003 | B 블록 emit_line_runs + GSO 디스패치 해체 | 4단계 완료 — **layout_composed 226→146, object_control 104→37**, 최대 CC 179 (첫 200 미만), 행동 회귀 0. 승인 대기 |

- dependabot 4건 처리 완료 (#1977/#1978/#1979 quick-xml 로컬 게이트/#1980)
