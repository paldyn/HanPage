# 오늘 할일 - 2026년 7월 6일

## PR 처리

| PR | 내용 | 처리 |
|----|------|------|
| #1961 | #1943 HWPX `write_line` 직렬화 결손 보정 | **머지**. `LineShape.connector` 기반 `hp:connectLine` 방출, `hp:startPt`/`hp:endPt`, `hp:controlPoints`, component block, `lineShape`, `fillBrush`, `shadow`, `groupLevel`, `instid` 보존으로 `connectLine` 변질과 그룹 자식 line 좌표/스타일 소실을 보정. 최신 head `6c846710e1365c5ba597ef14692676f951cd80f5` 기준 GitHub Actions 통과, 로컬 검증(`serializer::hwpx::shape`, full `cargo test --profile release-test --tests`, `clippy --all-targets`, `fmt`, `diff-check`) 통과. merge commit `bb2df91c63de36606a0bd642cb56fcfe137a031c`. review 문서는 옵션 2 docs-only PR로 archive 반영. #1943은 merge 직후 open 상태라 docs-only PR merge 후 후속 코멘트와 close 처리 필요 |
