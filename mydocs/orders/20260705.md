# 오늘 할일 - 2026년 7월 5일

## PR 처리

| PR | 내용 | 처리 |
|----|------|------|
| #1911 | #1655 HWPX 수식 `flowWithText` roundtrip 보존 | 옵션 1 self PR 생성. `render_equation`의 `flowWithText="1"` 하드코딩을 제거하고 `Equation.common.flow_with_text`를 방출하도록 수정. 수식 `flowWithText=0` serialize→parse roundtrip 테스트와 `diff_documents` `ObjectFlowWithText` 게이트 테스트 추가. 로컬 검증(`task1655`, `serializer::hwpx`, fmt, diff-check, `clippy --lib`) 통과. renderer/layout 변경이 아니라 visual sweep 대상 아님. review 문서 `mydocs/pr/archives/pr_1911_review*.md`를 PR head에 포함 후 GitHub Actions 대기 |


## 타스크

| 이슈 | 내용 | 상태 |
|------|------|------|
| #1917 | HWPX BinData 64MB 상한 — 대형 이미지 거부 + 왕복 pic 소실 | 상한 512MB 상향·문서는 **PR #1924** 소관(중복 정리). 본 세션 기여분은 로드 실패 placeholder(pic 왕복 보존)로 **PR #1927** 에 포함 — 실표본 156731165(103.7MB BMP) roundtrip PASS 실측 확인 |
| #1880 | convert-HWP 자리차지 표 host_before 비대칭 (PI_MOVED 4건) | **완료** — 근본 원인(원시 table.attr vs HWPX 미채움) 해소, 2건(2780073·3075729 oracle p13) 정합. A/B 하니스 2,005건 개선 +2/회귀 0, 전체 스위트 2,863 통과(기존재 svg_snapshot 노이즈 5건 외 실패 0). 잔존 2959953·3171755·3235145 는 별개 클래스로 보고서 기록. 최종 보고서 승인 대기 |

## 리팩토링 (#1904 / #1883)

| 항목 | 내용 | 상태 |
|------|------|------|
| #1904 | 1차 리팩토링 라운드 1 (Phase 0 freeze + object_ops 분할 + typeset 미주 분리 + 재평가) | 라운드 1 완료 — typeset CC 282→104, 행동 회귀 0. devel 반영 |
| #1883 | 리팩토링 계획 v2 | postmelee 리뷰 대기 지속 |

## PR 처리 (오전 — planet6897 4건 시간순)

| PR | 내용 | 처리 |
|----|------|------|
| #1912 | #1898 tac 그림 문단 뒤 +11.7px — 재역산 bridge 생략 | **머지**. 게이트 2,870/0·OVR 5/5. p9 3전이 33.1px 완전 수정 실측 |
| #1913 | #1891 BinData Link 왕복 소실 + 숫자 불변식 | **머지**. 2,871/0·roundtrip PASS diff=0 r2=0. #1891은 축 A 잔존으로 OPEN 유지 |
| #1919 | #1898 동일 결함 상류 수정 (vpos 초기화 면제) | **머지**. 단독은 부분 수정(2/3, 87→88 잔존 실측 — 리셋 원인이 표) — #1912와 상호 보완 확정, 결합 상태 3전이 완전 해소. 실측표 PR 코멘트 공유 |
| #1922 | #1914 roundtrip 게이트 FORMAT_SKIP (매직 바이트) | **머지**. 결합 게이트 2,875/0. 제품 경로 무접촉 |

- 결합 최종 검증: devel 트리 = 결합 게이트 트리 동일, clippy 0, fmt OK, OVR 5/5 무회귀
- 이슈: #1898·#1914 클로즈(머지 자동 + 검증 코멘트), #1891 OPEN 유지
- 검토 문서 4건 `mydocs/pr/archives/pr_19{12,13,19,22}_review.md`

## 리팩토링 라운드 2 (#1925)

| 항목 | 내용 | 상태 |
|------|------|------|
| #1925 | layout_composed_paragraph 해체 — 추출 3건 (마커/est/빈줄) | 라운드 2 완료 — CC 288→226, 3,771→3,071줄, 행동 회귀 0. 승인 대기 |

## 리팩토링 계획 (#1883)

| 항목 | 내용 | 상태 |
|------|------|------|
| #1883 | 마스터 플랜 v2 — 두 컬래버레이터 리뷰 취합 (금지 목록·gate 이원화·산식 고정·PR 규격) | **확정·클로즈** (devel 반영) |

## PR 처리 (오후 — planet6897 2건)

| PR | 내용 | 처리 |
|----|------|------|
| #1923 | #1915 HWP3-origin page_def 소실 (어댑터 계약을 직렬화기에 적용) | **머지**. 결합 게이트 2,878/0. #1915는 잔여 축(쪽수 +1, #1637 계열)으로 재오픈·유지 |
| #1924 | #1916+#1917 배치 (표 CTRL_HEADER 합성 + BinData 512MB) | **머지**. #1916/#1917 클로즈 |

- 특이: #1915를 github-actions 봇이 자동 클로즈 → 방침(잔여 축 추적)대로 재오픈
- 신규 열린 PR 7건 유입 (#1926~#1935, planet6897) — **#1931이 기머지된 #1924 커밋 1과 동일 수정의 정제 재제출**(fixture·핀 추가). 처리 지시 대기

## PR 처리 (오후 2차 — planet6897 7건 시간순)

| PR | 처리 | 특이 |
|----|------|------|
| #1926 셀 tac 라인높이 | 머지 | 오라클 −33.93→+0.06pt, 라운드2 재구성과 충돌 없음 |
| #1927 자리차지 판정 교체 | 머지 | #1924 중복분 컨트리뷰터 자진 정리 |
| #1928 빈-앵커 스택 sb | 머지 | **#1927 동일라인 → 메인테이너 통합** (합성 predicate) |
| #1930 imgDim 왕복 | 머지 | #1929 클로즈 |
| #1931 #1916 재제출 | 픽스처+핀만 수용, close | 소스부는 #1924 기반영 (supersede) |
| #1934 pi-page 도구 | 머지 | 72건 분류, REAL→캘리브레이션 축 재분류 |
| #1935 휴리스틱 마커 게이트 | 머지 | #1880 봇 클로즈 → 잔존 2건으로 재오픈 |

- 결합 게이트: 2,889/0 · OVR 5/5 · clippy 0. devel `6c9714b6`
- 이슈: #1929 클로즈, #1842/#1880(재오픈)/#1920 잔여 축 추적 유지

## PR 처리 (오후 3차 — #1891 잔여 쪽수 보정)

| PR | 내용 | 처리 |
|----|------|------|
| #1936 | #1891 HWP5-origin HWPX 쪽수 보정 | **머지**. #1913 merge 후 남은 HWP5-origin HWPX pagination 잔여분 처리. 공식 PDF/HWP/HWPX/HWP export HWPX 페이지 매트릭스 4종 모두 일치(82/157/17/65), GitHub CI·CodeQL·Render Diff 통과. merge commit `a6469f4f61388abbe5987f2a6088ca0ffc903fa1`. review 문서와 오늘할일은 후속 docs-only PR로 분리 처리 |

- #1891은 #1936 merge 직후에도 GitHub auto-close가 동작하지 않아 후속 docs-only PR merge 후 수동 검증 코멘트와 close 처리 예정
