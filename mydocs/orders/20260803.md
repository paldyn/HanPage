# 오늘 할 일 - 2026년 8월 3일

## PR #3741 - CanvasKit exact GlyphRun strict consumer 메인터너 보정

- [PR #3741](https://github.com/edwardkim/rhwp/pull/3741)의 contributor 원격 head `bc2b3f801` 위에
  synthetic style, bidi direction/level, vertical writing mode를 strict GlyphRun direct replay에서
  fail-closed로 제외하는 메인터너 보정을 추가했다. 정상 exact TTC control은 유지하고 비지원 값은
  TextRun fallback을 선택한다.
- renderer 44건, Text IR 19건, JSON serializer 14건, Studio 759건, CanvasKit E2E·manifest 80건,
  release-test 전체, Native Skia 58+2+4, fmt, clippy, Studio production build를 통과했다. release-test의
  `overflow_cell` baseline은 279.92초 후 통과했다.
- WASM package는 release compile과 wasm-opt까지 정상 진행했으나, 작업지시자가 CI 전 push를 우선하도록
  지시해 최적화 완료 대기는 중지했다. 보정 code PR이므로 push 뒤 최신 full CI로 WASM 포함 최종 결과를
  확인한 뒤 merge한다.

상세 근거: [PR #3741 검토](../pr/archives/pr_3741_review.md),
[메인터너 보정 기록](../pr/archives/pr_3741_review_impl.md).

## Dependabot PR #3844–#3861 통합 검토

- 최신 `upstream/devel` `6fd26c5d` 위 현재 작업트리 branch `review/dependabot-20260803`에 열린 Dependabot PR 18건을 `cherry-pick -x`로 누적했다. source author, `Signed-off-by`, 원 SHA provenance는 유지했고 contributor head는 수정하지 않았다.
- RustCrypto major bump은 현재 password crypto call site의 제거된 trait/API와 SHA-1 digest graph가 맞지 않아 별도 maintainer commit `de2c2c226`으로 최소 호환 보정을 추가했다. 알고리즘/암호문 wire contract 변경 없이 HWP3·HWP5·HWPX 실물 password fixture를 통과했다.
- `cargo check`, fmt, clippy, 전체 `cargo test --profile release-test --tests`, WASM package, Studio/Chrome/Firefox/VS Code clean install·build/typecheck, frontend CI contract, actionlint, `git diff --check`를 모두 명시적 성공으로 확인했다. Studio audit의 low 1/high 2는 기준선과 동일한 기존 transitive dev dependency 경고이며, 나머지 세 package audit은 0건이다.
- local review 기록을 준비했다. 원 저장소 branch push와 `devel` 대상 integration PR 생성은 작업지시자 승인 뒤에만 수행하며, 최신 full CI 통과 뒤 merge를 별도로 승인받는다.

상세 근거: [통합 구현·검토 기록](../pr/archives/pr_3844_review_impl.md), [원 PR 검토](../pr/archives/pr_3844_review.md).
## PR #3771 - F5 셀 크기 조절 한컴 정합 메인터너 보정

- [PR #3771](https://github.com/edwardkim/rhwp/pull/3771)의 contributor 원격 head
  `efd25b455` 위에서 Alt 인접 셀 조절, 병합 셀 범위, F5 단일 셀 이동의 내부 위치를 보정했다.
- F5 셀 선택 중에는 텍스트 캐럿을 숨기고, `Escape`에서는 표 전체 선택으로 전환하지 않고 마지막
  선택 셀의 편집 캐럿을 복원하도록 정정했다. 작업지시자의 Vite 브라우저 검증도 완료됐다.
- focused 6건, TypeScript 검사, Studio 전체 749건, production build와 `git diff --check`를
  통과했다. 코드·테스트 변경이므로 최신 보정 head 전체 GitHub Actions를 확인한 뒤 merge한다.
- merge 뒤 contributor PR에 메인터너 보정과 검증 결과를 comment로 남기고 `devel` 동기화와
  review branch 정리를 수행한다.

상세 근거: [PR #3771 검토](../pr/archives/pr_3771_review.md),
[메인터너 보정 기록](../pr/archives/pr_3771_review_impl.md).

## Kevin 기능 PR 13개 누적 통합

- [PR #3816](https://github.com/edwardkim/rhwp/pull/3816)을 최신 `devel`
  `3b0349a56` 기준 draft로 생성했다. 원 PR #3788, #3791, #3795, #3797, #3799,
  #3800, #3802, #3804, #3805, #3806, #3807, #3809, #3811의 source head를 고정해
  누적했고, 원 contributor branch는 수정하지 않았다.
- 통합에서 드러난 출처 표지, `inspect` 하위 명령 capability, MCP password stdin,
  provenance sweep 누락을 contributor 원 기능과 분리한 보정 commit으로 고쳤다.
- 누적 검토 release-test/Native Skia 결과를 유지하고, 최신 보정 후
  `unicode_deception_contract` 14건, MCP password 4건, MCP server 22건,
  provenance 7건 등 영향 범위 계약을 통과했다. `cargo fmt --check`와
  `git diff --check`도 통과했다.
- GitHub Actions에서 확인된 Node binding 생성 타입/명령 래퍼 누락을 보정했다.
  생성 봉투 31개를 최신화하고 표 CSV 입출력·데이터 추출·보안 검사·출처 지도 래퍼를
  추가했다. `gen:check`, TypeScript 검사, Node 439건, 패키지 build/pack dry-run을
  통과했다.
- `Cancel stale PR runs`의 종료 run force-cancel 409 경합을 재조회 방식으로 보정했고,
  workflow YAML 형식과 스크립트 JavaScript 구문 검사를 통과했다.
- 대형 코드 PR이므로 최신 head의 전체 CI 통과와 mergeability 재확인을 기다린다.
  작업지시자 승인 전에는 draft 해제나 merge를 하지 않는다.

상세 근거: [PR #3816 검토](../pr/archives/pr_3816_review.md),
[통합 구현 기록](../pr/archives/pr_3816_review_impl.md).

## planet6897 PR #3755, #3774, #3786 누적 검토

- 작업지시자 지시에 따라 [#3740](https://github.com/edwardkim/rhwp/pull/3740)은 별도 review 경로로
  처리하고, #3755·#3774·#3786을 최신 `upstream/devel` `6ab503fe9` 위
  `review/planet6897-20260803`에 오래된 번호 순서로 체리픽했다. 충돌은 없었고, 기본
  `~/rhwp` 작업트리에 이 branch를 checkout해 Source Control과 Git Graph에서 확인 가능하다.
- #3755와 #3774 focused regression은 각각 2 / 2 통과했다. 새 HWPX fixture의 IR field
  sweep 589행과 overflow-cell 22행 기준선도 모두 일치했다. release-test 전체는 마지막
  visual round-trip baseline까지 실패 없이 통과했고, Native Skia 라이브러리 58 / 58도 통과했다.
- #3786의 r29 10k 수치는 제외된 #3740까지 포함한 세 commit 조합의 결과다. 보고서에 이
  범위를 명시해 현재 두 code commit 누적 후보의 직접 기준선으로 오해되지 않게 보정했다.
- 로컬 CI는 focused test, fixture baseline, release-test 전체, Native Skia 3종, WASM 및
  정적 검사까지 모두 통과했다. #3774는 한컴 PDF와 SVG sweep 2쪽을 비교해 자동 구조 후보 0건과
  쪽 경계 일치를 확인했고, 대표 review PNG를 안정 경로에 보존했다. #3755는 한컴이 빈 PDF를
  반환해 기준 비교는 불가했으나, rhwp SVG/PNG 2쪽과 focused out-of-page 계약으로 분할 결과를 확인했다.
- 이제 통합 PR을 생성한다. 생성 뒤 최신 GitHub Actions 통과와 작업지시자 승인 뒤에만 merge한다.

상세 근거: [#3755 검토](../pr/pr_3755_review.md),
[#3774 검토](../pr/pr_3774_review.md), [#3786 검토](../pr/pr_3786_review.md),
[누적 구현 기록](../pr/pr_3755_3774_3786_review_impl.md).

## planet6897 PR #3740 — #3738 TAC sibling-line 및 후속 흐름 보정

- [PR #3740](https://github.com/edwardkim/rhwp/pull/3740)의 contributor 원 변경
  `5be9b6ec`은 같은 host 문단의 두 번째 자리차지 개체가 첫 `LINE_SEG`를 잘못 재사용해
  뒤 문단을 page-out시키던 조판·layout 반대 방향 결함을 함께 고친다. contributor 원 commit을
  rewrite하지 않고, 시각 검토에서 필요한 보정만 source head 뒤의 별도 commits로 추가했다.
- 최신 code candidate `fd069734`는 TopAndBottom raw anchor의 적용 범위를 실제 그림 구조로 좁히고
  빈 각주 virtual fragment range를 compose 결과로 clamp했다. 로컬 full
  `cargo test --profile release-test --tests`, Native Skia 58+2+4, fmt, diff check, clippy를
  통과했다. 사용자가 수동으로 확인한 WASM package build도 성공이다.
- [GitHub CI 30791602165](https://github.com/edwardkim/rhwp/actions/runs/30791602165)는
  Build & Test, 8개 default-feature shard, Lint, Native Skia, Canvas visual diff, CodeQL을
  통과했다. 원작자의 r29 10k oracle 값은 `8cd4901dd` 시점의 세 PR 조합에 한정하며 현재 대형
  head의 결과로 재인용하지 않는다.
- review·구현 기록·오늘할일만 담는 이 docs-only trailing commit을 source branch에 올린다. LFS 대상
  여부를 먼저 판독하고 비-LFS이면 `GIT_LFS_SKIP_PUSH=1` dry-run/실제 push를 사용한다. latest docs
  head의 preflight 및 `Build & Test` fast-pass aggregate가 success이고 `CLEAN`·`MERGEABLE`이면,
  이미 받은 작업지시자 승인 범위에서 merge와 후속처리를 진행한다.
- 이번 PR이 모든 PDF fidelity 결함을 해결했다고 주장하지 않는다. p94/p106/p107–108, p156 Square-wrap,
  PDF 215 ↔ native 219 page-map은 [#3820](https://github.com/edwardkim/rhwp/issues/3820)·
  [#3821](https://github.com/edwardkim/rhwp/issues/3821)에 분리했다. merge 뒤 두 원작자 코멘트를
  함께 답하는 통합 답글과 별도 merge announcement는 실제 merge SHA·CI 결과를 채운 초안을
  사용자 승인 뒤에만 게시한다.

상세 근거: [PR #3740 검토](../pr/archives/pr_3740_review.md),
[구현·보정 기록](../pr/archives/pr_3740_review_impl.md),
[#3820](https://github.com/edwardkim/rhwp/issues/3820),
[#3821](https://github.com/edwardkim/rhwp/issues/3821).
