# 오늘 할일 - 2026년 8월 2일

## 공통 — 운영 작업

| Issue | 타스크 | 상태 | 비고 |
| --- | --- | --- | --- |
| [#3790](https://github.com/edwardkim/rhwp/issues/3790) | CI 변경 영향축 classifier 도입 | 진행중 | Stage 1 shadow classifier·과거 PR fixture focused 검증 완료. 실제 worker skip 조건은 변경하지 않음 |

## #3784 macOS 영문 Option+G 찾아가기

- [#3784](https://github.com/edwardkim/rhwp/issues/3784) 등록 후
  `fix/3784-macos-option-g-goto`에서 `Option+G`의 `©` 문자값을 물리 `KeyG`로 보정했다.
- shortcut-map 6건, Studio 전체 721건, TypeScript 검사와 headless Chromium 활성 입력기 검증을
  통과했다.
- 코드·테스트·계획·결과·PR 본문 초안을 하나의 커밋으로 고정하고, 원격 push 및 PR 생성 승인 대기다.
## #3778 batch stdin `BrokenPipe` CI 레이스

- 첫 기여자 `@enigma-jerry72`의 [PR #3778](https://github.com/edwardkim/rhwp/pull/3778)을 최신
  `devel` `cc3829116` 기준으로 검토했다. contributor code head `639d6bd`는 테스트 헬퍼만 바꾸며,
  인자 검증 거부 뒤 stdin 조기 종료가 정상일 때의 `BrokenPipe`만 허용한다.
- 전용 release-test로 `batch_axes_contract` 17건을 통과했고, 실제로 실패했던
  `batch_convert_rejects_flag_as_out_dir_before_any_write`를 포함해 컴파일된 테스트 바이너리를
  100회 반복 실행해 모두 통과했다. `cargo fmt --check`, `git diff --check`도 통과했다.
- contributor code head의 [CI 30730240090](https://github.com/edwardkim/rhwp/actions/runs/30730240090)는
  8개 default-feature shard와 `Build & Test`를 포함해 통과했다. review·오늘할일 trailing commit
  `7f92ecc`도 fork source head에 push했고, [fast-pass CI 30742380062](https://github.com/edwardkim/rhwp/actions/runs/30742380062)의
  preflight와 `Build & Test` aggregate가 success임을 확인했다.
- merge 후 `devel` sync와 branch/worktree/검토 전용 target 정리를 수행하고, 실제 CI·로컬 검증 결과와
  함께 첫 기여에 대한 감사 문구를 PR에 남긴다.

## Kevin #3689–#3735 · planet6897 #3736 통합 검토

[#3742](https://github.com/edwardkim/rhwp/pull/3742)
`review/kevin9327-20260801`는 `upstream/devel`
`fe9749d542f46643e408c23878229c326e341363` 위에 Kevin의 열린 PR #3689–#3735와, 사용자 지정
cutoff인 `@planet6897`의 #3736을 한 번에 누적한 통합 PR이다. 각 원 PR의 저자와 source head는
archive review에 분리 보존하며 #3736을 Kevin 기여로 합산하지 않는다.

- code candidate `b1e961943`는 full release-test integration 최종 exit 0, fmt, clippy를
  통과했다. GitHub [CI 30711901379](https://github.com/edwardkim/rhwp/actions/runs/30711901379)의
  lint, Native Skia, 8 shards, `Build & Test`, CodeQL, Canvas visual diff도 success다.
- #3698/#3701/#3705/#3710은 #3716 canonical stack으로 대체하고, #3725는 #3724와 같은
  patch라 이중 적용하지 않는다. #3722 baseline, #3731 capability drift, #3734/#3735 diagnostics와
  batch 계약은 메인터너 보정으로 별도 기록한다.
- #3718은 renderer/layout 변경이므로 HWP 2020 기준 PDF 66쪽과 current-head p060을 대조했다.
  stable review PNG는 `mydocs/pr/assets/pr_3742_issue_3658_terminal_tail_review_p060.png`이고,
  대상 tail paragraph가 남았음을 확인했다. 이는 문단 소실 회귀의 증적이며 전체 PDF 픽셀 동등성
  주장이 아니다.
- code candidate 뒤에는 archive review 32개, 공유 반영 기록, 이 오늘할일, PDF/PNG만 single-parent
  docs-only tail로 올린다. push 직전에 LFS 대상 여부를 파일별로 판독하고, 비-LFS이면
  `GIT_LFS_SKIP_PUSH=1` dry-run 및 실제 push를 한다.
- docs head의 review-only fast-pass preflight와 required `Build & Test` aggregate가 success이고,
  latest `CLEAN`·`MERGEABLE`이면 #3742 하나만 merge한다. fast-pass가 거부되거나 base가 drift하면
  code candidate를 보존한 채 full CI 경로로 전환한다.
- merge 후 devel fast-forward, merge SHA·issue 상태 확인, 정확한 review branch/ref 및
  `target/review-kevin9327-20260801`만 정리한다. 원 PR의 감사와 supersede close comment는 현재
  지시대로 review 단계에서는 게시하지 않고 후속 시점에 실제 줄바꿈 body로 처리한다.

상세 근거는 [공유 반영 기록](../pr/archives/pr_3689_review_impl.md),
[#3718 review](../pr/archives/pr_3718_review.md), 그리고 원 PR별 archive review에 있다.

## M100 — v1.0.0 편집 기반

| Issue | 타스크 | 상태 | 비고 |
|------|--------|------|------|
| #1528 | export-structure 후속 개선 상위 추적 | 진행중 | #3695 통합 후 #3744와 최종 검증 진행 |
| #3695 | auto·outline confidence와 HeadType::Number 선택 정책 | 진행중 | PR #3749 High 2건 보정·코퍼스 영향 0·전체 release-test 완료, 보정 head CI 대기 |
| #3744 | clause 문맥 게이트 만료·날짜·목 confidence 후속 | 예정 | #3695 merge 뒤 착수 |
## Kevin #3747–#3779 통합 검토

`review/kevin9327-20260802`는 `upstream/devel`
`a8d7bdfbf54dfa31f2cfe3b05d5f1e0c0cb18c99` 위에 @kevin9327의 열린 PR 18건을 누적 검토한
integration branch다. contributor 원 branch는 수정하지 않았고, 기능 commit 27개만 `-x`로 적층했다.

- #3766 BrokenPipe test와 #3758/#3767/#3769 source에 중복된 같은 commit은 이미 #3778로 merge돼
  이중 적용하지 않았다. #3779의 devel merge commit도 제외했다.
- local 검증은 focused Rust 108건, release-test 전체, fmt, diff check, clippy, Native Skia
  58+2+4건, WASM package build, Node unit 389·native integration 425건·pack, Python 254건·mypy·ruff·package를
  모두 통과했다. renderer/layout/sample/golden 변경이 없으므로 기준 PDF 시각 증적은 적용하지 않았다.
- 누적에서 발견한 dry-run test 소실, Node schema envelope drift와 frozen install 부재, Python
  capabilities schema public API 누락은 `a6f453fb3`, `90a208916`, `3bba6f628`으로 contributor 원 commit과
  분리해 보정했다.
- [통합 반영 기록](../pr/archives/pr_3747_3779_kevin_review_impl.md)과 원 PR별 archive review 18개는
  code candidate 뒤 docs tail로 고정한다. push 전 LFS 판독을 수행하고, 새 통합 PR의 최신 head CI 및
  mergeability를 확인한 뒤 작업지시자 승인 범위에서 merge와 원 PR supersede close를 처리한다.
