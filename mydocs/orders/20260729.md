# 오늘 할일 - 2026년 7월 29일

> 복원 기록: 이 파일의 앞 4개 섹션(#3529·#3399 보완·#3486·kevin9327 통합)은 새벽 세션이
> 작성했으나, 아침 세션(별도 Claude)이 파일 생성 시 존재 재확인 없이 덮어써 63줄이
> 소실됐다가 `388e43e4e^` 에서 복원됐다. 원인·재발 방지는 아래 "오늘할일 소실 사고" 절.

## PR #3529 — HWP3 암호 문서 렌더링 정합 (CI 대기)

[PR #3529](https://github.com/edwardkim/rhwp/pull/3529)는 #3486의 현재 확인 가능한 범위를
`task_m100_3486_hwp3_render_fidelity_v2`에서 통합한다.

- Stage 9(옛한글 shaping 판정)와 Stage 10(실제 Studio Canvas/PDF 24쪽 대조), p1 시각 증적을
  함께 올렸다. p1 PDF·Canvas 대조는 PR review archive와 asset에 실제 이미지로 남겼다.
- HWP3 Square-wrap 도형·배경 투영·Canvas fractional A4 경계, HWP3/HWPX 암호 fixture와 IR
  field-sweep baseline을 검증했다.
- CI에서 발견한 Canvas2D·CanvasKit fractional bitmap 크기 불일치(794×1123 vs 793×1122)는 두 Studio
  경로와 비교 창의 ceil 규칙으로 보정했고, 별도 포트 readiness corpus 7/7을 재확인했다.
- 최신 head CI에서 일반 HWP3 drawing-group round-trip과 SO-SUEOP p22 위치 회귀를 발견했다. 가시
  개체 control은 일반 HWP3 marker 1칸·실제 암호 HWP3 8-unit으로 분리했고, HWPX `HwpUnitChar`
  앞·뒤 간격의 잘못된 전역 반감은 공통 2배 IR 스케일로 복원했다. 기준 `upstream/devel` 별도
  worktree 대조와 `issue_1892` 4건, #1692 p22, HWP3 암호 fixture 8건, HWPX 암호 fixture 3건을
  재통과했다. HWPX spacing 정규화의 field-sweep 순개선 109건과 `raw_header_extra` 20개 집계 변경은
  683 divergence path(684 TSV 행) baseline으로 재생성해 2건 모두 통과했다. 보정 head의 CI와
  `MERGEABLE`을 확인한 뒤, 메인터너 검토 및 작업지시자 승인에 따라 merge한다.
  한컴 전용 옛한글 glyph/metric과 p3 이후 전수 fidelity는 #3486을 닫지 않고 후속 Stage로 유지한다.

## PR #3399 — bug-hunter 방법론과 fidelity 비교 하네스 보완 (CI 대기)

[PR #3399](https://github.com/edwardkim/rhwp/pull/3399)의 메인터너 후속 요청 5건을
collaborator 보정으로 반영하고 원 contributor branch에 함께 보존한다.

- PDF 텍스트층과 SVG의 페이지별 NFC·공백 제외 문자 멀티셋 대조, 크로스 플랫폼 실행 파일 탐색,
  `--out-dir`, 기준 PDF 등급·provenance, Chrome 캡처 1회 재시도와 stderr 표면화를 구현했다.
- macOS 1쪽·10쪽 실제 비교와 Linux·Windows 격리 worktree의 1쪽 end-to-end 비교를 통과했다.
  Linux 1쪽은 pixel diff 2.3%, Windows 1쪽은 2.16%였고 양쪽 text multiset 차이는 0/0이었다.
- 보정 code·test와 archive review 문서를 contributor 원 commit과 분리했다. 최신 PR head의 full CI,
  CodeQL, Render Diff와 `CLEAN` 상태를 확인한 뒤 merge 및 관련 이슈·contributor 후속 처리를 진행한다.

## Issue #3486 — HWP3 암호 문서 한컴 기준 PDF 렌더링 정합 (재설계 중)

[#3486](https://github.com/edwardkim/rhwp/issues/3486)의 #3529 범위는 병합됐고, Stage 10의 실제
Studio Canvas/PDF 24쪽 대조도 A4 bitmap 경계 `24/24` 정합까지 완료했다. 남은 작업은 이전 Stage 3의
쪽 배경 색조가 아니라 p3 이후의 표·목록·본문 fidelity를 하나의 증명된 원인씩 처리하는 것이다.

- 새 [v2 수행계획서](../plans/task_m100_3486_v2.md)는 #3529 완료 축, Stage 11의 옛한글 glyph
  배제 조건, p3 table bullet/폭의 source→IR→layout→paint→Studio Canvas 판별 게이트를 분리한다.
  `ᄒᆞᆫ → 한` 전역 치환이나 HWP3 전용 parser 보정은 금지한다.
- `visual_sweep.py`는 `--pages` 선택은 가능하지만 실행마다 산출물을 비우고 마지막에만
  summary를 쓴다. page raster/overlay가 단일 실행 한도를 넘은 경우를 위해, 다음 작은 도구 작업은
  provenance 검증형 page checkpoint·`--resume`·incomplete summary를 도입하는 것이다. SVG sweep은
  후보 탐지이며 Studio Canvas 증적을 대체하지 않는다.
- 도구 보강 뒤 p3을 주 대상, p10·p24를 공통성 표본으로 조사한다. raw/IR 손실, render-tree PaintOp
  누락, Canvas 전용 누락, font capability 중 한 경로가 재현될 때만 코드 변경으로 승격한다.
- 전체 release-test, PR 생성·원격 push·24쪽 최종 수용 선언은 focused 근거와 실제 Studio
  before/after/overlay를 먼저 공유하고 별도 승인을 받은 뒤에만 수행한다.

## kevin9327 PR #3457–#3541 — 체리픽 통합 검토 (통합 PR CI 대기)

열린 contributor PR 10건을 최신 `upstream/devel` 기준의
`review/kevin9327-20260729`에 고유 contributor commit 15개로 누적했다. 원 contributor branch에는
push·rebase·amend를 하지 않았다.

- #3478·#3482·#3541의 CLI conflict를 `ambiguous`·`overflow`·`outputFormat` 계약이 함께 남도록
  해소했다. MCP `hwp_set_cell` outputFields의 `overflow` 누락과 #3533 test의 clippy no-op closure는
  각각 독립 메인터너 보정 commit으로 고정했다. 최초 CI가 #3478 호환 래퍼의 DelegatesTo ledger
  누락을 차단해 실제 raw-stream 무효화 대상과 근거를 `d0b42ae18`로 등록하고 재실행한다.
- release library 3019/0/7, 전체 release-test integration, Native Skia library 3076/0/7, wasm-pack
  web build, fmt·diff·clippy·doc test를 전용 target에서 통과했다.
- AIFT HWP와 74쪽 기준 PDF의 56·60·62쪽 시각 sweep은 0/3 flagged였다. HML에는 공식 기준 PDF가
  없어 parser/serializer 계약으로만 검증하고, 최종 외부 시각 수용은 주장하지 않는다.
- archive review 10개와 대표 시각 panel을 보존했다. 통합 PR의 최신 CI와 mergeable을 재확인한 뒤,
  작업지시자 승인 범위에서 merge하고 그 후에만 원 PR·관련 issue 후속 처리를 한다.

## Discussion #3498 — yuyu04 양식 후처리 연산 9종 제안 응답 (완료)

연구노트 docx/PDF → HWP 양식 일괄 변환 파이프라인을 만드는 downstream 사용자(@yuyu04)가
fork 의 `document_core` 연산 9종 upstream 수용 의향을 물었다(v0.7.13 기준, 4,600+커밋 낙후를
스스로 고지). 답글 0건 상태였다.

- **범위 판정: 적합.** 작업지시자가 짚은 대로 rhwp 초기 설계의 한컴 웹기안기 대체 목표
  (`tech/archive/webgian_replacement_strategy.md`, historical)의 직계이고, 기안문 서식
  자산(#3372)·CLI edit 연작·54쪽 정부 서식 실증과 같은 줄기다. 공개 답변에는 계보 사실만
  언급하고 해당 문서의 가격·경쟁 분석은 인용하지 않았다(외부 공개 자기검열).
- **발견 사항 3건 검증**: ①(treatAsChar 저장 유실)은 **현행 devel 실버그로 확인** —
  `set_table_properties_native`(table_ops.rs:2076~)가 IR 만 갱신, serializer(control.rs:487)는
  raw_ctrl_data 재사용, raw 무효화는 수식에만 존재. ②(셀 reflow 줄 겹침)·③(delete_text_at
  ref 중복)은 개연성 있음 — 보고자가 회귀 테스트와 함께 별도 정리 예정.
- **응답 게시**(작업지시자 승인): 환영 + 질문 3건 답(범위 적합·개별 native 함수 형태·
  (a)(b)(c) 조각 분할 동의) + 발견 3건 선행 요청 + ①의 upstream 수정 방향(3곳 동기화가
  아니라 raw 무효화 + #1916 재합성 폴백) 안내.
  [답변](https://github.com/edwardkim/rhwp/discussions/3498#discussioncomment-17825902)
- **이슈 [#3552](https://github.com/edwardkim/rhwp/issues/3552) 등록**(작업지시자 승인):
  treatAsChar 저장 유실 — 무효화 계약의 표 CommonObjAttr 구멍(#3469 동형 패턴). 재합성
  경로의 손실 축 확인 주의와 red-check 완료 조건 포함.

## PR #3548 — HWPX 선 좌표 core 네임스페이스 복원 (review-only fast-pass 대기)

[@JamesPsh](https://github.com/JamesPsh)의 rhwp 첫 제출인 [PR #3548](https://github.com/edwardkim/rhwp/pull/3548)은
비-connector `hp:line`의 좌표 자식을 `hc:startPt`/`hc:endPt`로 바로잡아 한컴오피스 호환을 복원한다.

- 최신 `upstream/devel` 병합 시뮬레이션은 충돌 없이 통과했다. focused 회귀 1건, 전체 release-test
  integration, fmt, clippy 및 실제 HWPX 재저장 `--verify --verify-pages`(IR 차이 없음, 2쪽)를
  전용 target에서 완료했다.
- connectLine의 정당한 `hp:` 좌표·subject 참조는 유지하고, connectLine 없는 실물 fixture의 출력에는
  `hc:` 좌표만 남는 것을 확인했다. layout·paint 변경이 아니므로 visual sweep은 merge 판단의 필수
  근거로 사용하지 않는다.
- 원 code head `c928a73689b60880f6dac9ad80d35bf96e03761c`의 CI preflight·Lint·test archive·Native
  Skia·기본 기능 test 8개 shard·`Build & Test` aggregate와 CodeQL은 모두 성공했고 병합 상태도
  `MERGEABLE`·`CLEAN`이었다. review-only 기록 commit의 최신 fast-pass aggregate를 확인한 뒤,
  첫 기여자의 조사·최소 수정·회귀 고정의 가치를 구체적으로 전하는 merge comment와 함께 처리한다.

## Issue #3559 — review-only 기록이 Canvas visual diff를 재실행하는 CI fast-pass 결함 (진행 중)

[#3559](https://github.com/edwardkim/rhwp/issues/3559)는 renderer/paint 영향이 없는 code PR 뒤에
review·오늘할일만 추가했을 때, `mydocs/**` trigger와 Render Diff candidate 조건의 조합으로 무거운
Canvas visual diff가 다시 실행되는 결함이다.

- `mydocs/**` 단독 기록은 Render Diff trigger에서 제외하고, renderer 영향 경로의 변경은 기존처럼
  실행한다. trigger 정책을 정적 Python 회귀 검사와 CI Lint 단계로 고정한다.
- 공통 PR review 절차에는 GitHub Markdown 다단락 본문을 `--body-file`로 게시하고 literal `\\n`을
  API로 재확인하는 규칙을 추가한다. merge 후 issue comment 예시도 같은 절차로 바꾼다.

## 이월 추적

- #3508(reaper fork 커버): 다음 릴리즈로 main 반영 후 실증 → close. same-repo 는 복원 실증
  완료(#3517, run 30360295018).
- #3399 는 jangster77 이 28일 17:17Z merge 완료 — 위임 경로 정상 작동.
- bug-hunter 시행 격차 후보 4건 이슈 등록 여부: 작업지시자 판단 대기. 후보 3(PUA 책괄호)
  계열은 planet6897 #3520 이 텍스트 추출 축을 이미 다루는 중 — 등록 시 중복 확인 필요.
- 열린 PR 20건: #3550(jangster77 의 kevin9327 10건 통합) 진행 중, JamesPsh 신규 2건 중
  #3548은 검토·review-only fast-pass 대기, #3549는 별도 접수 대기, planet6897 6건, seo-rii draft 1건.
- planet6897 6건(#3503·#3520·#3525·#3527·#3530·#3535): reviewer 배정과 최신 devel 누적
  검토·local gate 완료. 통합 PR 생성 뒤 full CI 성공을 기다려 merge 및 원 PR/이슈 후속 처리를
  진행한다. 세부 판정은 `mydocs/pr/archives/pr_3503_review_impl.md`와 PR별 review 문서에 기록.
- main 대비 169커밋 — 다음 릴리즈는 0.9.0(MINOR) 성격(암호 문서 지원 + HWP3 렌더 정합).

## PR #3554 — playbook 판정 함정 4종 + 예시 7 (코멘트만, 처리 별도)

kevin9327 이 하루 실전에서 판정 오류 4건을 함정 절로 명문화하고 인터넷 실물 왕복 여정
(예시 7, ZIP 엔트리 축)을 추가한 문서 전용 PR. 인용 근거 6건(#3551 #3553 #3368 #3518
#3407 #3383) 전수 대조 일치 확인.

- "오라클 축 사각" 이 이번 주 메인테이너 교훈(print.html 산출물, #3399 텍스트층 대조)과
  동일 구조 — 사례 3개가 "오라클마다 보는 축 명시" 원칙으로 수렴.
- "인터넷 실물 수집" 축의 samples/ 생존 편향 진단 정확(실물 4건→즉시 1건 검출).
- 작업지시자 지시로 [검토 코멘트만 게시](https://github.com/edwardkim/rhwp/pull/3554#issuecomment-5116825577),
  merge 등 처리는 별도.

## 오늘할일 소실 사고 — 원인과 재발 방지

collaborator(@jangster77)가 "머지 뒤 오늘할일이 다 지워진다"고 지적했다. 사실 확인 결과
**아침 세션(Claude)의 실수**였다.

- 경위: 아침 조사 시점(약 새벽 세션 push 전)에 `20260729.md` 부재를 확인 → 이후 Discussion
  #3498 처리 중 ff-merge 로 새벽 세션의 파일이 로컬에 들어왔는데, **존재 재확인 없이
  `cat >`(덮어쓰기)로 생성**해 새벽 세션의 4개 섹션 63줄이 소실됐다(`388e43e4e`).
- Write 도구는 미독 파일 덮어쓰기를 차단하지만 bash `cat >` 는 그 가드를 우회한다 —
  가드가 존재하는 이유를 우회 경로로 재확인한 사고다.
- **복원**: `388e43e4e^` 버전과 현행을 병합해 7개 섹션 전부 보존(이 커밋).
- **재발 방지 규칙**: 오늘할일 등 공유 append 파일은 ① 쓰기 직전 fetch+존재 재확인,
  ② 신규 생성이 아니면 반드시 append(`>>`) 또는 Read 후 Edit/Write 도구 사용,
  ③ bash `cat >` 로 mydocs 파일을 만들지 않는다. 세션 메모리에도 기록.
