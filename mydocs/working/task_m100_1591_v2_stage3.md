# Task #1591 v2 3단계 완료 보고 — 전수 통제 비교 (채택 게이트)

## 방법

- 말뭉치: `C:/Users/planet/hwpdocs/samples` 전수 — **hwpx 28,844건** (부서 폴더 1,168개).
  역대 fidelity12→13 비교(공통 10,150)보다 넓은 범위.
- BEFORE: stage-1 커밋(수정 직전) worktree 빌드 바이너리 / AFTER: 본 수정 빌드.
- 실행: 부서 폴더 단위 `rhwp hwpx-roundtrip --batch` 재개형 스윕(done-list 기반,
  10분 타임아웃 대응). 집계: `tools/roundtrip_control_compare.py` (신규 커밋 —
  검증 스크립트 PR 포함 규칙).

## 결과 — 채택 게이트 통과

```
공통 28,855건 — 개선 4 / 악화 0 / 순효과 +4   (exit 0)
```

| 전이 | 파일 | 클래스 |
|------|------|--------|
| IR_DIFF 1 → PASS 0 | 36384689 화재발생종합보고서(제2026-298호) | C1 (+8) 타깃 |
| IR_DIFF 1 → PASS 0 | 36385445 화재발생종합보고서(제2026-189호) | C1 동형 타깃 |
| IR_DIFF 1 → PASS 0 | 36388711 사회보장제도 신설 협의요청서 | C2 (fieldEnd) 타깃 |
| IR_DIFF 1 → PASS 0 | **36401777 화재발생종합보고서(제2026-292호)** | C1 동형 — **보너스 개선** (말뭉치 신규 유입분) |

### 비-PASS 전수 교차검증 (동시-append 파싱 아티팩트 방어)

스윕 이중 실행으로 출력 일부에 줄 간섭이 있어(결과는 멱등·동일), 파일명 dict 대조와
별도로 **원시 로그의 비-PASS 레코드 전수를 직접 대조**:

- BEFORE 비-PASS 8건 → AFTER 4건. 소거된 4건 = 위 개선 4건과 정확히 일치.
- 잔존 4건(36372352·36401553·36380743·36399822)은 **양측 동일**(diff 수까지 일치) —
  본 수정과 무관한 별개 클래스 잔여. **악화 0 재확정.**

## 산출물

- `tools/roundtrip_control_compare.py` — before/after 배치 stdout 통제 비교 집계
  (악화 ≥1 시 exit 1 게이트 모드).
- 원시 로그·done-list: `output/poc/task1591_v2/` (gitignore 영역, 로컬 보관).

## 수용 기준 대조

| 기준 | 결과 |
|------|------|
| 1. 36384689·36385445 char_shape +8 해소 (IR diff=0) | ✅ PASS/0 |
| 2. 36388711 fieldEnd 드롭 해소 (IR diff=0) | ✅ PASS/0 |
| 3. baseline + lib 회귀 0 | ✅ (2단계) + 전체 스위트 별도 확인 |
| 4. 통제 비교 순효과>0, 악화 0 | ✅ **+4 / 0** |
