# Task M100 #2278 Stage 1R v2 완료보고서 — 3D 방 선 처리 한컴 정합 (시각판정 보정)

- 이슈: #2278 "C2b: 3D 입체·ofPie 보조플롯 렌더"
- 브랜치: `local/task2278`
- 계획: `task_m100_2278_v2.md` (수행 v2) / `task_m100_2278_impl_v2.md` (구현 v2)
- 선행: Stage 1R (view3D 파싱 + 시어 투영)
- 작성일: 2026-07-19

## 1. 시각판정 피드백 (2026-07-19)

작업지시자 판정: **누적 가로/세로 막대형에서 카테고리(항목 1~4) 주변과 우측의
선 처리가 한컴과 다름 — 정합성 보완 필요.**

## 2. 정답지 실측 (원인 진단)

한컴 2022 PDF의 차트는 **고해상도 비트맵(2702×1577) 임베드** → `pdfimages`로
원본 추출 후 픽셀 실측 (누적세로·누적가로 2종 교차):

| 항목 | 한컴 실측 | rhwp(Stage 1R) | 판정 |
|---|---|---|---|
| 축·격자·조그 선 | **전부 #808080 균일**(gray 126~148), 6px/8.34px/pt≈**0.72pt** | #cccccc(축/조그)+#e8e8e8(격자), 0.5pt | 색·굵기 불일치 |
| 값축 틱 | 눈금마다 존재 — 좌측(세로형) 44px≈5.3pt / 하단(가로형) 37px≈4.4pt | 없음 | 누락 |
| 카테고리 경계 틱 | 경계+양끝 = cat+1개 (세로형 하단 31px / 가로형 좌측 38px) | 없음 | 누락 |
| 뒷벽 | 모서리선 없음 (우측 세로·상단 스캔 0건) | rect stroke → 가짜 우측/상단 모서리 | 제거 필요 |
| 바닥 | 채움 없음(흰색) + 외곽선 #808080 | #f2f2f2 채움 + 연회색 테두리 | 채움 제거 |

- 격자 위치·간격·시어 방향(81/108=0.75=sin15/sin20)·바닥 우측 대각·조그 기하는
  **기존 투영 모델과 정확히 일치** — 투영 파라미터 무변경(v2 가드레일 준수),
  **선 스타일·틱만 보정**.
- 실측 좌표 (누적세로): 축선 x=336~342 세로 467→1320(0 아래 31px 연장),
  격자 y=389/595/802/1002/1208(+바닥 앞모서리 1289), 카테고리 틱 x=745/1157/1564/1976.
- 실측 좌표 (누적가로): 하단 값 틱 8개 x=464+225k, 좌측 카테고리 틱 5개
  y=432/645/851/1058/1270, 뒷벽 세로 격자 x=520+225k (y 396→1230).

## 3. 구현 (renderer.rs — 3D 전용, 2D 무접촉)

- `ROOM_LINE_STYLE`(#808080/0.75) + `ROOM_TICK_LEFT`(5.0)/`ROOM_TICK_DOWN`(4.0)
  상수 신설 — room 내 모든 선(축선·조그·격자·틱·바닥 외곽) 단일 스타일.
- `render_value_grid_3d`: 뒷벽 rect stroke 제거(흰 면만), 바닥 polygon
  `#f2f2f2`→`#ffffff`+외곽선 #808080, 값 눈금마다 틱(세로형 좌측/가로형 하단),
  **카테고리 경계 틱 루프 신설**(`cat_count` 파라미터 추가, k=0..=cat — k=0이
  축선 연장 역할). 세로형 값 라벨 x: 틱 밖(−4→−6)으로 이동.
- `render_category_labels` → `render_category_labels_at(…, left_gap)` 분리:
  2D는 4.0(바이트 불변), 3D 가로형 라벨은 좌측 틱 밖 6.0.

## 4. TDD 증적

신규 3종 선작성 → **RED 확인** → 구현 → GREEN:

| 테스트 | 검증 |
|---|---|
| `test_bar3d_room_hancom_line_style` | Column/Bar: room 내 연회색 어휘(#cccccc/#e8e8e8/#f2f2f2) 0건, 전 선 #808080/0.75, 뒷벽 무테두리, 바닥 흰 면+#808080 외곽 |
| `test_bar3d_axis_ticks_vertical` | 좌측 값 틱 수 == 뒷벽 수평 격자 수, 하단 카테고리 틱 == cat+1 |
| `test_bar3d_axis_ticks_horizontal` | 하단 값 틱 수 == 뒷벽 세로 격자 수, 좌측 카테고리 틱 == cat+1 |

기존 갱신: `floor_points` 헬퍼 앵커 `#f2f2f2`→room 첫 polygon,
`test_bar3d_room_only_when_3d`의 바닥 어휘 동일 교체. 그 외 room/투영 테스트
전부 무수정 통과(기하 불변의 방증).

## 5. 검증 결과

- ooxml_chart 유닛: **113 passed / 0 failed** (110+신규 3)
- 차트 통합·앵커 8스위트: 2278(1)/1453(2)/1882(4)/2277 stock(3)/legend(3)/
  mini(1)/2129(6)/1431_scatter(1) — **전부 통과**
- `cargo clippy --all-targets -- -D warnings`: 무경고
- `cargo test` 전체: **exit 0, 269 스위트 전부 ok, 3,236 passed / 0 failed**
  (로그: `$TMPDIR/c2b_stage1r_v2_full_test.log`). fmt는 수정 파일 한정
  (`rustfmt src/ooxml_chart/renderer.rs`, 무관 diff 없음)
- 시각판정 산출물 재산출: `output/poc/chart_c2b/{hwp,hwpx}/` 8종 —
  전 샘플 top 12·room 1·NaN 0, room 내 구 연회색 어휘 0건(잔존 1건은 차트
  외곽 프레임 rect로 room 밖), #808080 선 수가 산식과 일치
  (세로형 3×눈금+cat+1+축선+바닥 / 가로형 동형).

## 6. 위험·잔여

- 묶은 2종은 정답지 교차 실측 생략(동일 room 코드 경로) — 시각판정에서 확인.
- 하단 가로 범례·음수각 카메라 등 기존 잔여 항목 변동 없음.
