# 단계 완료 보고 — Task M100 #2001 3단계: 추출 2 (소형 컨트롤 코드 arm 헬퍼화)

- 작성일: 2026-07-06 / 브랜치: `local/task2001`

## 수행 내용

컨트롤 코드 `match ch`의 소형 arm 10개(합 327줄)를 **헬퍼 2개**로 추출 (동작 불변):

| 헬퍼 | arm | 크기 |
|---|---|---|
| `parse_field_control_char` | 18..=21 (필드/감추기 계열) | 118줄 |
| `parse_simple_control_char` | 1/7\|8/9/22/23/24\|25/26/28/30\|31 (고정 크기 데이터 계열) | 241줄 |

- 구현계획서의 "3~4개 묶음(정독 후 확정)" 조항에 따라 **2개로 확정** — arm 정독 결과
  나머지 9개가 전부 "고정 크기 버퍼 read + 소규모 부수효과"의 동일 기계 구조라 의미
  그룹 세분화의 이득이 없음. 18..=21만 필드 계열로 독립 의미가 있어 분리.
- `Hwp3CharScan` 재사용 (추출 1과 동일 시그니처 계약: 값 전달 i/utf16_len + 반환,
  break → `(_, _, true)` 반환 치환 — 소형 arm의 break 11곳 전수 nest0 확인).
- caller의 match는 병합 패턴 2개 arm + GSO(`_`) 호출로 축소. 헬퍼 내부 match의 `_`는
  `unreachable!` (caller 보장 범위).

## 게이트 결과 (전수 통과)

| 게이트 | 결과 |
|---|---|
| cargo fmt --check / clippy | 통과 / **경고 0** |
| cargo test --profile release-test --tests | **2,912 통과 / 실패 0** |
| OVR baseline 5샘플 | **추가 변동 0** (기지 #1936발 3건 시그니처 동일) |

## 계측 (라운드 3 누적)

| 함수 | 시작 | 현재 |
|---|---|---|
| `parse_paragraph_list` | 2,270줄 · 분기 459 | **896줄 · 170** (−61% / −63%) |
| 신규 3함수 (GSO/simple/field) | — | 1,039 + 241 + 118줄 |

## 다음 단계

4단계 — 재평가: 수정된 metrics.sh(`--snapshot r3 --no-coverage`)로 스냅샷 + 공식 CC
대비표 + typeset.rs 재성장 정량 확인 + 최종 보고서. 승인 후 착수.
