# Task #1716 Stage 3 완료보고서 — 렌더러 적용 + 대표 파일 페이지수 회복

## 작업
`src/renderer/layout/table_partial.rs` 두 곳의 반복 제목행 수집을 `leading_header_rows` 로 교체:
- ~166: `rows_to_set` 구성 시 `start_row` 이전 제목행 삽입 로직.
- ~308: `header_rows` 수집 로직.
둘 다 `table.leading_header_rows()` ∩ `{r < start_row}` 로 통일 → 페이지네이터(typeset.rs)와 동일 기준.

## 검증 (대표 파일 `국토교통부/3031803 건설공사 품질시험기준.hwpx`)
`dump-pages` 결과:
- **총 페이지수 173 → 53** (한글 52, +1 근접). A 유형 폭주 완전 해소.
- pi=12 표(183행) 페이지 배치: 40..86(46행) / 86..132(46행) / 132..176(44행) / 176..183(7행).
  기존 페이지당 1행 → 정상 다행 배치 회복.

잔여 +1쪽은 행/줄 높이 미세 오차(B/C 유형)로 A 유형과 별개.

## 상태
완료. Stage 4에서 전체 회귀·통합 검증 진행.
