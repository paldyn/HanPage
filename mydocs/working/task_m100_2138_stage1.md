# Task M100 #2138 — Stage 1 완료보고서 (판별 신호 규명 조사)

**날짜**: 2026-07-10 / **브랜치**: `local/task2138`

## 1. 신호 후보 검증 — 전부 기각

| 후보 | 결과 |
|---|---|
| 슬랙 스칼라 | 중첩 (분할 3.4~61.3 vs 흡수 37.1~72.9) — 완전 분리 불가 재확인 |
| 꼬리 lineseg 실저장/합성 | 저슬랙 흡수 2건(SYNTH)은 잡지만 SYNTH 분할 4건(43.9 포함)과 중첩 |
| writer/appVersion/layoutCompatibility | 24건 전부 동일 (v11.0.0.8227, HWP201X, lc_empty) — 무정보 |

## 2. 핵심 발견 — 한글 레이아웃의 세션 상태 의존성 (실증)

동일 문서 36378481 이 **fresh-open(인스턴스 첫 문서)이면 1쪽, warm-open(타 문서 선-open
후)이면 2쪽** — PageCount 뿐 아니라 **저장되는 PDF 쪽수까지** 상태를 따라감 (각 3회 재현).
즉 경계 코호트에서 "한글 정답" 자체가 측정 상태의 함수다.

- flip 실험: 흡수 문서(36358528)는 빈 줄 +1 로 즉시 2쪽 — 실제 여유 < 1줄(~26px),
  우리 슬랙 측정(39.6px)과 한글 내부 잔여가 다름을 시사.
- **권위 규약**: 오라클 하니스(fresh_hwp 가 probe 문서를 먼저 열음)와 동일한
  **warm 상태의 PDF** 를 경계 코호트의 권위로 통일 (r12 오라클 판정과 정합).

## 3. warm PDF 권위 재확정 → 마진 재보정 53→62px

슬랙셋 22건 PDF 전수(`pdf_count_probe.py`): 분할 정답 20건(3.4~**61.3**) / 흡수 정답
{37.1, 39.6, **67.7**, 72.9}. 53px 마진은 36394733(61.3, 분할 정답)을 흡수해 **r11 대비
신규 회귀 1건** — 최적 구간 [61.3, 67.7)의 62px 로 재보정.

- 검증: 36394733 2쪽 복귀, 고슬랙 흡수 2건 1쪽 유지, #1611 2쪽·fixture 1쪽,
  광역 359 재검 REGRESSED 0·SAME_OK 251 유지.
- 잔여 오류: 저슬랙 흡수 2건(36358528/36477251) — r11 과 동일한 기지 한계.
  진짜 판별 신호는 IR 밖(한글 내부 잔여 계산)으로 추정 — 스칼라로 종결.

## 4. 산출물

- `output/poc/survey10k_r12_0709/`: anchor_prev_synth_probe / compat_probe /
  flip_experiment / flap_check2 / pdf_count_probe (+ 각 결과)
- 수정: typeset.rs 마진 62px (PR #2104 반영 예정)
