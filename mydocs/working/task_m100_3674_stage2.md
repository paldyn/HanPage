---
kind: working
status: active
canonical: mydocs/plans/task_m100_3674.md
last_verified: 2026-08-04
---

# Task #3674 Stage 2 중간 보고 — LINE_SEG 오라클 전환 (A안)

## 새 기준 계측 — 한컴 재저장본에서 갭이 가장 순수하게 드러난다

`pyeonram_3565_hancom_resave.hwp`(한컴 2020 이 직접 재저장, #3565 산출물)를 rhwp 로
조판하면 **393쪽**이다. 한컴 자신은 이 파일을 383쪽으로 찍는다.

| 입력 | rhwp | 한컴 | 갭 |
|---|---|---|---|
| rhwp 저장 HWP | 386 | 383 | +3 (상쇄 후) |
| 원본 HWPX | 387 | — | |
| **한컴 재저장 HWP** | **393** | 383 | **+10** |

한컴이 쓴 파일은 한컴 조판 좌표(LINE_SEG)가 원본 그대로라, rhwp 의 자체 밀도 팽창이
상쇄 없이 드러난다. **이후 계측·수정의 기준 입력은 이 파일이다.**

## 결정적 발견 — 축 B 는 줄간격이 아니라 "표 셀 높이" 축이다

`dump` 로 저장 구조를 열어 보니 **질의·답변 블록 전체가 표다**:

- 질문 머리띠 = `h=4015`(53.5px) 선언 셀, `valign=Center`, rs=2 병합
- 질의 목차도 `h=159642` 선언 셀 하나에 73개 문단
- 답변 불릿들도 셀 내부 문단

축 B(+9)의 본질은 **표 셀/행 높이 계산의 한컴 대비 팽창**이다. Q1 쪽(재저장본 p285)
실측: rhwp 는 Q1 블록 표를 **289.6px**, Q2 를 **231.3px** 로 계산 — 한컴은 같은 쪽에
질문 3개를 넣는 밀도다.

## 도구 확인 — 하니스 재료가 이미 있다

| 쪽 | 명령 | 제공 |
|---|---|---|
| 저장 진실 | `dump` | 문단별 LINE_SEG(vpos·lh·ls), PS(line=180/Percent 등), 셀 지오메트리(h·pad·valign) |
| rhwp 계산 | `export-render-tree` | 쪽별 Table/TextLine bbox + pi |

하니스 = 이 둘의 조인. 문단은 pi, 표는 순서·위치로 대응시킨다.

## 다음 (Stage 2 계속)

1. Q&A 구간 표 블록별 rhwp 계산 높이 vs 한컴 실효 높이(다음 블록 vpos 차) 전수 대조
   → 팽창이 **셀 패딩/최소높이/문단 여백 중 어느 공식**에서 오는지 특정.
2. 본문 문단 축은 LINE_SEG 줄 전진(advance) 대조로 병행.
3. 공식별 오차 기여 순위표 산출 → 수정 대상 결정 요청.

## 계측 이력 (재현용)

- 재저장본 export-text: 393파일 (`scratchpad/hancom_pages`)
- dump 전문: `scratchpad/dump_hancom.txt` (48,574줄, 14구역 2,618문단)
- Q1 본문 쪽: 재저장본 p285, render-tree Table (y,h) = (147.4,41.6) (270.3,289.6) (567.9,231.3)


## 좁힘 실측 — 표 블록 높이는 결백하다 (2026-08-04 속보)

재저장본 p285~299 의 최상위 표 26개를 render-tree 로 재고 dump 선언 크기와 대조했다.

- **25/26 이 선언 높이와 1.5px 이내 일치** (예: Q1 표 21721HU=289.6px ↔ rhwp 289.6px).
- 유일 불일치: p290 의 534.6px 표 1건 (후속 확인 대상).

**함의**: 축 B(+9)는 표 높이 공식이 아니다. 남은 용의자는 둘 —

1. **쪽 채움(fit) 판정** — 한컴이 넣는 마지막 블록을 rhwp 가 다음 쪽으로 미는 경계
   결정 (#359 의 `LAYOUT_DRIFT_SAFETY_PX=10` 안전마진 계열 포함).
2. **블록 사이 간격** — 표 사이 빈 문단들의 줄높이(ls=480→6.4px, lh=100 등 미세값
   다수)와 문단 여백 누적, 그리고 가용 쪽 높이(머리말·꼬리말 예약) 차이.

다음 계측: 쪽별 채움율(Σ표높이+간격 vs 가용높이)과 "한컴은 넣고 rhwp 는 민" 경계
블록의 잔여 공간 수치화 → 안전마진/간격 중 지배 요인 확정.

주의: 발산 쪽 목록(p283/288/294…)은 rhwp 저장본(386) 기준이다. 재저장본(393) 축은
쪽 대응이 다르므로 혼용하지 않는다.

## 근인 후보 확정 — TAC 표의 쪽 경계 행 분할 미지원 (2026-08-04 2보)

쪽별 바닥 잔여 vs 다음 쪽 첫 표 높이 대조(p285~299):

- 잔여가 **89~512px(평균 ~230px, 가용 740.8px 의 31%)** 씩 남는데도 다음 표가 통째로
  다음 쪽으로 간다.
- Q&A 표는 전부 `treat_as_char=true` + `쪽나눔=RowBreak(행 단위 나눔 허용, attr=0x06)`.
  **rhwp 는 26개 전부를 분할 없이 통짜 배치** — 한컴은 행 분할로 잔여를 채운다.
- 극단 3건(p293·296·299)은 잔여가 다음 표 전체 높이보다 큰데도 밀렸다(210<258.8 등)
  — TAC 줄 상자의 문단 오버헤드(여백·줄간격) 가산 의심, 별도 확인.

**가설(강한 증거)**: 축 B(+9)의 지배 근인은 "TAC 인라인 표를 한 줄 상자로 취급해
쪽 경계에서 행 분할하지 않는" 경로다. 수식 TAC 인라인 배치와 같은 핵심 경로
(paragraph_layout)라 수정 파급 검토 필수.

**부수 확인 대상**: 가용 높이 740.8px(=196mm) 산정 — 용지 266mm·여백 25/23·머리말
24·꼬리말 15 조합과 정합하는지(머리말 예약 과다면 전 문서 공통 밀도 손실).

## Stage 3 제안

1. TAC 표 행 분할 — 한컴 동작(재저장본 LINE_SEG·PDF)을 오라클로 분할 규칙 구현 검토.
   #3236(표 분할 특례)·#2279 계열과 경로 공유 — 트러블슈팅 체크리스트 준수.
2. 극단 3건의 문단 오버헤드 계산 검증.
3. 가용 높이(머리말 예약) 대조.
효과 예상: 축 B 해소 시 재저장본 기준 −9±, 이후 축 C(대비표)로.

## 작업지시자 확인 요청 — 표 객체의 문단 배치 속성 소비 (2026-08-04 3보)

Q&A 표 호스트 문단의 [PS]: **`line=160/Percent`, spacing before=0 after=0**,
표 common: `treat_as_char=true, wrap=자리차지, vert/horz=문단(0mm)`.

가설 검증 상태 — "TAC 표 줄에 160% 줄간격이 곱해져 유령 공간이 생긴다":

| 사례 | 수치 | 가설 부합 |
|---|---|---|
| p293 밀림 | 210×1.6=336 > 잔여 258.8 | ✓ |
| p296 밀림 | 304.7×1.6=487 > 잔여 405.8 | ✓ |
| p299 밀림 | 280.2×1.6=448 < 잔여 512 | ✗ (선행 TextLine 동반 이동 의심) |
| p285 표간 전진 | 289.6 표 다음 표까지 +8px | ✗ (쪽 내 배치에는 미적용) |

**해석(잠정)**: 줄간격 160% 가 쪽 "채움 판정(fit)"에는 개입하고 실제 "배치 전진"에는
빠지는 이중 계산 가능성 — #359 의 fit/누적 분리 결함과 같은 형태. 다음 세션에서
경계별로 rhwp 의 fit 요구량을 직접 재현해 확정한다(각 쪽나눔 지점의 needed vs slack).

행 분할 축(RowBreak 무시)과 이 축은 독립 — 분할이 되면 fit 과대평가와 무관하게
잔여가 채워진다. 두 축 모두 수정 후보.
## 코드 진입점 확보 (2026-08-04 4보)

- `src/renderer/typeset.rs:16403` — "TAC 표: trailing line_spacing 복원 (Paginator
  place_table_fits:777-783 동일)" → **fit 이 호스트 줄간격을 표 높이에 가산**하는 지점.
- `typeset.rs:873` — provenance 별 "빈 앵커 TAC 표 host_line_spacing 미가산" 분기 존재.
- `typeset.rs:14440` — 비-TAC 표도 trailing line_spacing 포함.
- 다음: place_table_fits(777-783)와 위 두 곳의 가산량을 160%/표높이 기준으로 산출해
  p293·296·299 의 needed vs slack 을 수치 재현 → 확정 후 수정 설계.

## 5보 — 가산량 정량화, 이중 계산 가설 약화 (2026-08-04)

진단에 delta 출력 추가(`RHWP_DIAG_TAC`, 동작 불변) 후 재저장본 전체 실측:

- **발동 244건 / 합계 2,055px / 평균 8.4px / 최대 20.5px** — 갭(10쪽=7,408px)의 28%.
- 평균 8.4px 는 배치 전진 관측치(표높이+8px)와 **일치** → fit 과 배치가 정합.
  **"fit 만 크게 잡는 이중 계산" 가설은 약화**. trailing 가산 자체는 자기 일관적이다
  (한컴 대비 과잉인지는 별도 — 한컴 LINE_SEG 의 호스트 줄 lh 와 대조 필요).
- p293 재검: 210+8.4=218 < 잔여 258.8 — 그래도 밀렸다. **미설명 요인 존재**
  (호스트 문단 spacing_before? keep-with-next 로 선행 TextLine 동반 이동? 후속 확정).

**남은 지배 후보 = RowBreak 행 분할 부재** (쪽당 잔여 89~512px 가 통짜 이동으로
못 채워짐). 다음: ① p293 밀림의 정확한 fit 요구량 분해(RHWP_DIAG_TAC 주변 로그 확장
또는 코드 추적) ② 행 분할 축 설계 착수.

## 6보 — 첫 발산 문단 정밀 대조: 분할 문턱 차이로 확정 (2026-08-04)

구역 10(질의·답변)의 쪽 시작 문단 서열 비교(저장 vpos 오라클 vs 렌더트리):

- 한컴: 6,10,13,15,18,21,24,26,29,32,36,40 (11쪽) / rhwp: 6,10,12,14,15,18,22,23,24,27,28,30,33,35,37,40 (**15쪽, +4**)
- ★밀림 3건은 오탐 — 예: 10.26 은 **[쪽나누기] 컨트롤 문단**이고 저장 vpos=0. 강제
  쪽나눔은 양측 동일하다.

para 10~14 정밀 대조 (px):

| 항목 | 한컴(저장 vpos) | rhwp(렌더트리) |
|---|---|---|
| 표10 전진 | 269.3 | **269.3 동일** |
| 표11 전진 | 318.4 | **318.4 동일** |
| 표12 | **행 분할** (head ~153 + tail ~297) | **행 분할** (149.9 + 321.4) — 유사 |
| 문단13 전진 | 248.6 | **248.6 동일** |
| **표14** | **행 분할** (잔여 ~179px 에 head 채움) | **통짜 이월** (+1쪽 발생) |

**확정**: 블록 높이·간격·전진은 정합한다. 편차의 실체는 **"어떤 표를 쪽 경계에서
분할하고 어떤 표를 통짜로 넘기느냐"의 문턱 차이**다. rhwp 는 표 12(잔여 ~153px)는
분할했고 표 14(잔여 ~179px)는 통짜로 넘겼다 — 잔여가 더 큰데도 분할하지 않은 것이므로
잔여량 문턱이 아니라 **분할 진입 조건(최소 행 수·head 구성·TAC 경로 분기 등)**이
용의자다. 다음: 표 14 통짜 결정의 코드 경로 추적(place_table_fits:777 주변,
is_effective_tac_table 분기) → 조건 규명 → 수정 설계.

## 7보 — Stage 2 종결: 결함 좌표 최종 (2026-08-04)

표 12/14 의 셀 병합 구조는 **완전 동일**(rs=2 가 r0-1·r1-2 를 사슬로 묶음 → 첫 깨끗한
행 경계는 row 2 뒤). 잔여 153px 인 표 12 는 분할됐고 잔여 179px 인 표 14 는 통짜
이월됐다 — **잔여량이 아니라 분할 진입 판정이 콘텐츠 높이에 따라 갈린다.**

코드 좌표: `typeset.rs:18577` `first_row_splittable`(first_block_protected·
is_row_splittable), `:18644` `multirow_clean_defer`(#1046 — 첫 블록이 잔여에 안 들어가고
fresh 쪽에 통째 들어가면 이월). 유력 시나리오: 표 14 의 첫 블록(rows 0~2 실제 높이)이
179px 를 초과 → clean_defer. 한컴은 같은 상황에서 head ~179px 를 잘라 넣었다 —
**병합 사슬 내부 행 경계 분할을 한컴은 허용, rhwp 는 원자 취급** 가설.

### Stage 3 제안 (승인 대상)

1. 분할 판정 계측 DIAG 추가(first_block_end·블록 실높이·min_content·remaining 출력,
   동작 불변) → 표 12/14 판정 차이 수치 확정.
2. 한컴 분할점 오라클화 — 재저장본 stored vpos 로 표 14 의 한컴 head 높이(=다음 문단
   vpos)를 정확히 재고, 분할점이 병합 사슬 내부인지 판별.
3. 결과에 따라 수정 설계: (a) 병합 사슬 내 분할 허용 or (b) clean_defer 조건 완화.
   paragraph_layout 핵심 경로(수식 TAC 공유)라 #359 체크리스트 + 10k 서베이 회귀
   게이트 필수.

기대 효과: 구역 10 에서만 +4쪽(한컴 11 vs rhwp 15). 같은 패턴이 제5장 전체(+9)와
재저장본 갭(+10)의 지배 성분일 가능성이 높다.

## 8보 — Stage 3-1 계측: 결함 서명 확보 (2026-08-04)

`RHWP_TABLE_DRIFT` + 신규 `RHWP_DIAG_SPLITSCAN`(scan 결과 출력, 동작 불변) 실측:

| | pi=12 (sec=10) | pi=14 (sec=10) |
|---|---|---|
| table_total | 448.3 | 554.6 |
| cur_h → 잔여 | 579.7 → **161.1** | 566.7 → **174.1** |
| 분할 시도 | **TABLE_SPLIT_AVAIL/RESULT 발생** | **없음 — 스캔 미진입** |
| 결과 | rows 0~3 + **row4 내부 컷**(consumed 155.8, limit 24.5) → 이어짐 322.1 | 통짜 이월 (+1쪽) |

- rhwp 는 행 내부 컷 능력까지 있고 pi=12 에서 실제로 썼다. **pi=14 는 잔여가 더 큰데도
  분할 게이트 앞에서 거부**됐다 — 게이트는 TABLE_DRIFT 출력 지점과 TABLE_SPLIT_AVAIL
  사이의 상류 분기.
- 한컴 오라클: 14 의 head=총 554.6−tail 376(다음 문단 15 저장 vpos)≈**178.6px** —
  잔여 174.1+host_sp 와 사실상 일치. **한컴은 잔여를 그대로 head 로 채웠다.**
- 로그: `scratchpad/diag_drift.log`·`diag_split.log`. 다음 세션: TABLE_DRIFT →
  TABLE_SPLIT_AVAIL 사이 분기 조건 식별(eff_h 553.3 vs 440.3, host_sp 1.3 vs 8.0,
  declared 175.2 vs 159.8 중 어느 입력이 갈랐는지) → 수정 설계.
