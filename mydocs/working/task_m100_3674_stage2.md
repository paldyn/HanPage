---
kind: working
status: active
canonical: mydocs/plans/task_m100_3674.md
last_verified: 2026-08-04
---

# Task #3674 Stage 2 중간 보고 — LINE_SEG 오라클 전환 (A안)

## 새 기준 계측 — 한컴 재저장본에서 갭이 가장 순수하게 드러난다

`pyeonram_3565_hancom_resave.hwp`(한컴 2020 이 직접 재저장, #3565 산출물)를 rhwp 로
조판하면 **393쪽**이다. 한컴 자신은 이 파일을 383쪽으로 찍는다.

| 입력 | rhwp | 한컴 | 갭 |
|---|---|---|---|
| rhwp 저장 HWP | 386 | 383 | +3 (상쇄 후) |
| 원본 HWPX | 387 | — | |
| **한컴 재저장 HWP** | **393** | 383 | **+10** |

한컴이 쓴 파일은 한컴 조판 좌표(LINE_SEG)가 원본 그대로라, rhwp 의 자체 밀도 팽창이
상쇄 없이 드러난다. **이후 계측·수정의 기준 입력은 이 파일이다.**

## 결정적 발견 — 축 B 는 줄간격이 아니라 "표 셀 높이" 축이다

`dump` 로 저장 구조를 열어 보니 **질의·답변 블록 전체가 표다**:

- 질문 머리띠 = `h=4015`(53.5px) 선언 셀, `valign=Center`, rs=2 병합
- 질의 목차도 `h=159642` 선언 셀 하나에 73개 문단
- 답변 불릿들도 셀 내부 문단

축 B(+9)의 본질은 **표 셀/행 높이 계산의 한컴 대비 팽창**이다. Q1 쪽(재저장본 p285)
실측: rhwp 는 Q1 블록 표를 **289.6px**, Q2 를 **231.3px** 로 계산 — 한컴은 같은 쪽에
질문 3개를 넣는 밀도다.

## 도구 확인 — 하니스 재료가 이미 있다

| 쪽 | 명령 | 제공 |
|---|---|---|
| 저장 진실 | `dump` | 문단별 LINE_SEG(vpos·lh·ls), PS(line=180/Percent 등), 셀 지오메트리(h·pad·valign) |
| rhwp 계산 | `export-render-tree` | 쪽별 Table/TextLine bbox + pi |

하니스 = 이 둘의 조인. 문단은 pi, 표는 순서·위치로 대응시킨다.

## 다음 (Stage 2 계속)

1. Q&A 구간 표 블록별 rhwp 계산 높이 vs 한컴 실효 높이(다음 블록 vpos 차) 전수 대조
   → 팽창이 **셀 패딩/최소높이/문단 여백 중 어느 공식**에서 오는지 특정.
2. 본문 문단 축은 LINE_SEG 줄 전진(advance) 대조로 병행.
3. 공식별 오차 기여 순위표 산출 → 수정 대상 결정 요청.

## 계측 이력 (재현용)

- 재저장본 export-text: 393파일 (`scratchpad/hancom_pages`)
- dump 전문: `scratchpad/dump_hancom.txt` (48,574줄, 14구역 2,618문단)
- Q1 본문 쪽: 재저장본 p285, render-tree Table (y,h) = (147.4,41.6) (270.3,289.6) (567.9,231.3)
