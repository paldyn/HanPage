# Task #1759 Stage 2 완료보고서 — 1차 서베이 (razor 8 + 대조군 30)

## 실행
`metric_drift_survey.py --list`(38파일) → OK 37 / SKIP_PAGES_DIFFER 1.
산출: `output/poc/drift_survey1/{pages,tables,summary}.tsv` (+ 한글 PDF 캐시).

## 신뢰 필터
- 표: |dh| ≤ 30px (전면 서식 표의 find_tables **부분 검출**로 dh +100~800px 오탐 배제 —
  하니스 알려진 한계로 확정) → 신뢰 매칭 33/44.
- pitch: 행 클러스터 수 유사(±20%) 페이지만 → 64/70.

## 발견 1 — 표 높이 계통 과대(+) [지배 원인 후보]
신뢰 매칭 33건 중 **+방향 25건 vs -방향 1건** (razor 6:0, 대조군 19:1).
- 전형: **+0.6~0.8px/표** (다수), 대형: +5.2(12×4)·+5.3(50×3)·+7.2(12×16)·+10.9(20×11).
- razor 사례의 경계 표(36388181 pi18 +1.6px)가 정확히 이 계열 — 만재 페이지에서 이
  계통 편향이 경계 문단 fit 을 가르는 구조.
- 후속: 행 단위 분해(rhwp row_heights vs 한글 행 경계)로 원인 특정(테두리/half-px 반올림
  vs 행높이 환산) — 개별 이슈 등록.

## 발견 2 — 줄 pitch 는 대부분 정확 일치, 이산 케이스 소수
- 신뢰 64페이지 median **+0.00** (mean +0.02) — 줄간격 환산은 전반적으로 정합.
- |d|>0.5px 19건: 동일 서식 반복 케이스 존재(8.05→9.85 **+1.80** ×2, 초과근무내역서 계열),
  결재문서 계열 p2 에서 -0.55~-0.90(rhwp 조밀 — S1 방향과 부호 일치). 개별 조사 후보.

## 발견 3 — dy(표 시작 y)는 종속 지표
razor 군 dy median -10.7px(상류 누적 결과), 대조군 +0.1px — 원인 1·2 의 누적 결과로 해석.

## 하니스 한계 (문서화)
전면 서식 표 find_tables 부분 검출(오탐), pitch 는 행 인구 차 오염 가능 — per-line 정렬
정밀화는 후속.

## 상태
완료. Stage 3 (원인 분해 보고 + 후속 이슈) 진행.
