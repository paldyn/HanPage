# Task M100: planet6897 stacked PR integration Stage 6

## 목적

체리픽 누적 검증 결과를 collaborator 통합 PR로 올릴 최종 준비를 한다.

## 기준

- 통합 브랜치는 `upstream/devel`의 `9e5f29b3` 위에 있으며 2026-07-13 fetch 시점에 추가 rebase 대상이 없다.
- 원격 stacked head와 통합 코드의 source/sample patch 동일성은 `git range-diff`와 source diff로 확인했다.
- 전체 `cargo test --profile release-test --tests`, Clippy, WASM build, focused tests, HWP 2020 MCP PDF, visual sweep을 완료했다.
- 이번 PR에 HWP 2020 MCP PDF 7개를 포함한다.

## 이슈 처리 계획

- close 후보: #2195, #2240, #2238, #2239, #2243, #2236.
- open 유지: #2097은 압축 수용 축만 처리했고 sliver pagination 백로그가 남는다.
- open 유지: #2070은 초대형 CellBreak 표 계열의 별도 잔여 축이다.

## PR 준비 순서

1. 최초 remote push 전 최신 `mydocs/orders/20260713.md`에 통합 PR 준비 항목을 추가한다.
2. collaborator 작업 브랜치를 `upstream`에 push하고 `devel` 대상으로 Open PR을 생성한다.
3. PR 번호가 정해지면 통합 PR review/implementation 문서와 대표 visual asset을 별도 후속 커밋으로 추가한다.
4. 코드 head CI와 후속 기록 fast-pass를 확인한 뒤 merge한다.
5. merge SHA, issue close 상태, contributor 감사 코멘트, 원격/로컬 작업 브랜치 정리를 순서대로 수행한다.
