# Task M100 #2277 단계별 완료보고서 — 5단계: 0.5축 게이트 + line3D 라우팅 + 종합 시각판정 산출

- 이슈: #2277 (C2a stock HLC 렌더 + 2D fidelity 정합)
- 브랜치: `local/task2277`
- 단계: 5/5
- 작성일: 2026-07-15

## 구현 내용

### 특이케이스 0.5축 게이트 (`renderer.rs` `render_bars`)

C1c v2에서 "섣부른 반영은 앵커 파손 위험"으로 기록만 했던 항목. 비누적·비3D 축
분기에서 **`가로 && 1카테고리`일 때만 step 절반** — 정답지 실측(값 4.3 → 0~5
step 0.5, 라벨 11개)과 정확 일치하며 범위는 불변. 기존 가로축 앵커(12.3→2 /
5.0→1 / 2.6→0.5)와 단일 규칙 불성립·단일 샘플 근거임을 주석에 명기, 코퍼스
나머지 27종은 전부 4카테고리라 회귀 반경 0. 세로 1카테고리는 미실측이라 불변.
소수 라벨은 `render_value_grid`의 비정수 step 가드(scatter 0.5와 동일 경로)가 처리.

### line3DChart 방어 라우팅 (`parser.rs`)

코퍼스 27종에 없는 요소(C1d에서 갭 기록만). C1a bar3D/pie3D 선례대로 **Line +
`is_3d` 라우팅** — placeholder 방지 목적, 입체 표현은 C2b(#2278)·2D 근사.
lineChart와 동일하게 콤보 주 타입은 덮지 않음. handle_end plot 종료 리스트에도
추가(axId 복사·`line_grouping` 채택 동작 유닛 검증).

## 테스트 (RED→GREEN)

| 테스트 | 단언 | 종별 |
|--------|------|------|
| `test_hbar_single_category_half_step` | 1카테고리 4.3 → `>0.5<`·`>4.5<`·`>5<` + 다카테고리 무회귀 핀 | 신규(유닛) |
| `test_parse_line3d_routing` | Line+is_3d+line_grouping 채택+axId 복사 | 신규(유닛) |
| `tests/issue_2277_mini_chart_axis.rs` | 특이케이스 hwp/hwpx: 0.5 step 라벨 + 단일 시리즈 제목 유지 | 신규(통합) |

RED (구현 전): 3건 정확한 사유(step 1·line3D 미인식)로 실패 → 93 passed; 2 failed
(유닛) + 통합 1 failed. GREEN: `cargo test --lib ooxml_chart` **95/95** + 차트 통합
7스위트(issue_2277_stock 3·legend_order 3·mini_chart_axis 1·2129 6·1431_scatter 1·
1882 4·1453 2) 전부 통과.

## 시각판정 산출물

- **`output/poc/chart_c2a/{hwpx,hwp}/{종류}/{stem}.svg` — 코퍼스 28종 × 2포맷 = 56파일**
  (`target/debug/rhwp export-svg`, 전 파일 성공·placeholder 0건).
- CLI 산출물 스팟 검증 (통합 테스트와 동일 신호 재확인):
  - 시가고가저가종가: 고저선 4·캔들 4·종가 마커 4·축 `>80<`
  - 표식이있는꺽은선형: 범례 글리프 3 / 표식만있는분산형: 플롯 마커 6(circle 0)·범례 글리프 2
  - 누적세로막대형: 범례 텍스트 등장순 계열 3→2→1 (역순)
  - 특이케이스: `>0.5<`·`>4.5<` 라벨

### 정답지 대조표 (C2a 변경 영역 중점 — `pdf/chart/{종류}/{stem}-2022.pdf` 대조)

| 영역 | 파일 | 정답지 | rhwp (본 브랜치) | 판정 근거 |
|------|------|--------|------------------|-----------|
| stock HLC | 고가저가종가 | 검정 고저선 + 종가 회색 ▲, 축 0~80 step 20, 범례 고/저 빈 스와치·종가 ▲, 정순 | 동일 구조 렌더 (고저선 4·▲ 4·`>80<`·글리프 1) | 통합 3테스트 + 스팟 |
| stock OHLC | 시가고가저가종가 | 캔들(1월 하락 진회색/상승 흰+테두리) + 종가 노란 ×, 범례 시/고/저 빈 스와치·종가 × | 동일 구조 (캔들 4·하락 1 `#404040` 근사·× 4) | 〃 (하락 채움색은 근사 — 시각판정에서 확정 요청) |
| scatter 마커 | 분산형 5종 | 계열1 ◆ 파랑/계열2 ■ 주황 (선/곡선 스타일별 조합) | 사이클 글리프 (circle 폐지) | 유닛+통합 |
| 범례 순서 | 역순 8종/정순 19종 | stage3 전수 실측표 | `legend_order_reversed` 규칙 일치 (예외 0) | 통합 16파일 y좌표 비교 |
| 범례 스와치 | 표식 라인·분산형·stock | 선+글리프 / 글리프 / 빈 스와치 | SwatchKind 정합 | 유닛 6 + 통합 2 |
| 묶은가로 슬롯 | 묶은가로·3D묶은가로 | 슬롯 위→아래 = 계열3→1 | 반전 적용 | 유닛 rect y 비교 |
| 특이케이스 축 | 가로막대 1카테고리 | 0~5 step 0.5 (11라벨) | step/2 게이트 | 통합 hwp/hwpx |
| 무회귀 스윕 | 나머지 (막대 6·라인 5·원형 5·3D 5) | C1a~C1d 기존 정합 | 기존 테스트 17개 전부 통과 + placeholder 0건 | 전수 스위트 |

**알려진 근사(시각판정 확인 요청 항목)**: ① OHLC 하락 캔들 채움색 #404040(픽셀 실측
미확정) ② scatter 마커 반경 4.5·글리프 반경 3.0/3.5(근사) ③ stock 캔들 폭
gapWidth 150 규칙(슬롯 40%). 픽셀 parity는 비목표(#1251) — 구조·순서·형상 정합까지.

## 검증 결과

- [x] 유닛 95/95 + 차트 통합 7스위트 전부 통과
- [x] **전체 `cargo test` 전수 (클린 로그 실행): 266스위트 전부 ok — 3,189 passed /
  0 failed, cargo exit 0** + `cargo clippy --all-targets -- -D warnings` 무경고 (exit 0)
- [x] fmt: 수정 파일 rustfmt, `cargo fmt --check` 지적 0건
- [x] 코퍼스 56파일 export 성공, "차트 (미지원)" placeholder **0건**

## C2a 범위 완료 선언 (승인 대기)

구현계획서 5단계 전부 구현·검증 완료. 이슈 #2277 범위(마커 인프라·stock 2종·범례
순서·스와치 글리프·0.5축·line3D 라우팅) 소진. **작업지시자 시각판정**
(`output/poc/chart_c2a/` ↔ `pdf/chart/`) 요청 — 통과 시 최종 결과보고서
(`task_m100_2277_report.md`) 작성 단계로 진행.
