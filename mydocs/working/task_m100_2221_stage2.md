# Task M100 #2221 — 2·3단계 완료 보고: 측정·렌더 pad 회계 일관화 (재정의 범위)

- 이슈: #2221 (B안 재정의: 내부 일관성) / 브랜치: `local/task2221` / 작성일: 2026-07-12

## 2단계 — 드리프트 귀속

재현: 36389312 결재문서 p1, pi=6 하단앵커 표(= 1×1 자리차지 + 중첩 12×41 결재란).

- 하단앵커 배치는 **측정 높이**(HeightMeasurer)로 top을 잡고, 그리기는 **렌더
  행합**(resolve_row_heights) — 실측: 측정 320.42 vs 렌더 316.70 = **3.73px**
  (=pad 상하합 280HU) → 표 하단이 body 하단에서 3.73px 들뜸.
- 원인: #2211이 layout에만 `relaxed_pad`(중첩/tac의 저장 LINE_SEG 셀 pad
  미가산)를 적용하고 측정은 (#1748 충돌로) 되돌린 **의도적 비대칭**의 잔여.
  중첩 표는 measured_table 없이 layout이 직접 행높이를 계산하므로 비대칭이
  실드리프트로 발현 — 결재란 '06/25' 행(h=1100=ext, pads 280) 등이 측정에서만
  +pad 성장.

## 3단계 — 정정 ([height_measurer.rs](../../src/renderer/height_measurer.rs))

`relaxed_pad_mirror = depth > 0 && 저장 LINE_SEG 텍스트 셀` — 측정의 단일행·
병합행 성장 판정에 layout과 동일 규칙(줄 흐름 pad 미가산 / 개체 pad 유지)을
**중첩(depth>0) 한정**으로 미러.

### 가드 설계 실측 (2회 수렴)

- 1차: `depth>0 || treat_as_char` 미러 → **6건 실패** (KTX/exam_kor/복학원서
  golden 3 + #1753/#1755/reflow 일관성). 원인: 상위 TAC 표는 렌더가 measured
  행높이를 그대로 쓰므로(mt 우선) 측정·렌더가 이미 일관 — tac 포함 미러는
  실제 지오메트리를 이동시킴.
- 2차: `depth > 0` 단독 → 전 게이트 수렴. 비일관은 measured 부재 경로(중첩)
  뿐이라는 구조 사실과 정합.

## 정량 효과

| 항목 | 수정 전 | 수정 후 |
|------|--------|--------|
| pi=6 하단앵커 표 bottom − body 하단 | −3.73px (들뜸) | **0.00 (밀착)** |
| pi=5 | 0.00 | 0.00 (불변) |
| `issue_1858` 허용오차 | 6px (드리프트 수용) | **2px 강화** |

## 게이트

- fmt 통과 / clippy 0 / `--tests --no-fail-fast` **3,048 / 실패 0**
  (golden 8/8 무변동, #1748/#1858/#1486/#2211/#2220/주보 아크 전부 포함)
- OVR 5샘플 분리 폴더 회귀 0건 / 추적 파일 삭제 재발 없음

## 시각 판정 자산

`output/poc/issue2221/compare_bottom_block.png` — 하단 결재 블록 3-way
(2024 오라클/수정 전/수정 후). 변화는 pi=6 블록의 3.7px 하강(밀착)뿐.
