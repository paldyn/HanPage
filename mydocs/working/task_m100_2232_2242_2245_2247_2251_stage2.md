# Task M100 PR #2232/#2242/#2245/#2247/#2251 Stage 2 - stacked 체리픽 통합

## 적용 순서

`upstream/pr/2251`의 고유 커밋을 다음 순서로 한 번씩 적용한다.

1. `c7fba900` - #2232 NO_LS 재계산 축
2. `1194de03` ~ `4a1886fd` - #2242 픽스처, used 표시, 괄호 narrow 조건, 사다리 검증
3. `8704a6dd` ~ `fa603712` - #2245 표 경로 vpos 앵커와 lazy 가드
4. `a4e80619`, `14c84b13` - #2247 rowspan 중간 행 밴드 컷과 후속 safety 마진
5. `a78fa302` ~ `706012d3` - #2251 하단 압축 수용과 검증 샘플

## 체리픽 기준

- 적용 대상은 `upstream/devel` `22778a662`에서 시작한 통합 브랜치다.
- 원 PR head는 모두 stacked 상태이므로 #2251 전체를 한 번 적용하는 것과 같은 고유 커밋
  체인을 유지한다.
- 충돌은 현재 `devel`의 변경을 우선 확인하고, 문서 구조나 특정 샘플을 기준으로 한 예외가 아닌
  일반 layout 회계 규칙으로 보정한다.

## 검토 항목

- 모든 원 PR에서 `README.md`는 발견되지 않았다.
- 체리픽 후 #2232, #2242, #2245, #2247, #2251별 주장과 fixture/page-count pin을 분리 검증한다.
- 원격 push, 원 PR review/merge, 통합 PR 생성은 로컬 적용과 검증 결과를 정리한 뒤 승인받아 수행한다.
