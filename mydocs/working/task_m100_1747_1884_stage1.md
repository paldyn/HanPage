# Task M100 #1747/#1884 Stage 1

## 범위

- #1747: `export-pdf`에서 generic fallback font family를 CLI 옵션으로 지정할 수 있게 한다.
- #1884: `export-pdf`에서 수식 SVG font-family의 우선 폰트를 CLI 옵션으로 지정할 수 있게 한다.

두 이슈는 모두 PDF export의 fontdb/수식 font-family 설정 문제이므로 같은 브랜치에서 처리한다.

## 구현 방향

- `src/renderer/pdf.rs`에 PDF 전용 폰트 옵션 구조체를 추가한다.
- 옵션 미지정 시 OS별 기본 fallback family를 사용한다.
- 사용자가 지정한 family가 PDF 변환 fontdb에 없으면 warning을 출력한다.
- 기존 `svgs_to_pdf` API는 유지하고, 옵션을 받는 변형 API를 추가한다.
- `export-pdf` CLI에 fallback serif/sans/mono, 수식 폰트, 폰트 탐색 경로 옵션을 연결한다.

## 검증 계획

- `cargo fmt --check`
- `cargo test --lib renderer::pdf`
- `cargo run --bin rhwp -- export-pdf --help` 성격의 도움말/파싱 경로 확인

## Stage 1 결과

- `PdfExportOptions`를 추가하고 기존 `svgs_to_pdf` API는 default 옵션 경로로 유지했다.
- `export-pdf`에 `--fallback-serif`, `--fallback-sans`, `--fallback-mono`, `--equation-font`, `--font-path`를 추가했다.
- 옵션 미지정 시 OS별 기본 fallback family를 사용하도록 했다.
- 최종 선택된 fallback family와 수식 우선 폰트가 fontdb에 없으면 warning을 출력하도록 했다.
- PDF 변환 직전 SVG 후처리에서 한글 fallback과 수식 font-family를 반영하도록 했다.
- 공백 포함 경로/폰트명 예시는 큰따옴표(`"..."`) 기준으로 도움말과 매뉴얼을 정리했다.

## 검증 결과

- `cargo fmt --all`: 통과
- `env CARGO_INCREMENTAL=0 cargo check --bin rhwp`: 통과
- `env CARGO_INCREMENTAL=0 cargo test --lib renderer::pdf -- --nocapture`: 통과
- `git diff --check`: 통과
- `cargo run --bin rhwp -- export-pdf --help`: 새 PDF 폰트 옵션 표시 확인
- `cargo run --bin rhwp -- export-pdf samples/exam-kor-4p.hwp -p 0 -o target/tmp-pdf-font-options/exam-kor-4p-p1.pdf --fallback-serif "AppleMyungjo" --fallback-sans "Apple SD Gothic Neo" --fallback-mono "Menlo" --equation-font "STIX Two Math"`: PDF 생성 통과
- `pdfinfo target/tmp-pdf-font-options/exam-kor-4p-p1.pdf`: 1페이지 PDF 확인
- `cargo run --bin rhwp -- export-pdf samples/exam-kor-4p.hwp -p 0 -o mydocs/report/assets/task_m100_1747_1884/macos_export_pdf_p1.pdf --fallback-serif "AppleMyungjo" --fallback-sans "Apple SD Gothic Neo" --fallback-mono "Menlo" --equation-font "STIX Two Math"`: macOS 검증 PDF 생성 통과
- `pdfinfo mydocs/report/assets/task_m100_1747_1884/macos_export_pdf_p1.pdf`: `Producer: rhwp`, `Pages: 1`, `Page size: 841.88 x 1190.52 pts (A3)`, `PDF version: 1.7` 확인
- `pdftoppm -png -singlefile mydocs/report/assets/task_m100_1747_1884/macos_export_pdf_p1.pdf tmp/pdfs/task_m100_1747_1884/macos_export_pdf_p1`: macOS 검증 PDF 렌더 PNG 육안 확인

## PR 전 Full CI 검증

`mydocs/manual/pr_review_workflow.md` 옵션 1 PR 처리 전 full CI 성격으로 다음 명령을 순차 실행했다.

- `env CARGO_INCREMENTAL=0 cargo build --release`: 통과
- `env CARGO_INCREMENTAL=0 cargo test --release --lib`: 통과 (`2084 passed`, `6 ignored`)
- `env CARGO_INCREMENTAL=0 cargo test --profile release-test --tests`: 통과 (`svg_snapshot` 포함)
- `cargo fmt --check`: 통과
- `git diff --check`: 통과
- `env CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings`: 통과
- `env CARGO_INCREMENTAL=0 cargo test --doc`: 통과 (`0 passed`, `1 ignored`)
- `(cd rhwp-studio && npx tsc --noEmit)`: 통과
- `(cd rhwp-studio && npm test)`: 통과 (`153 passed`)
- `wasm-pack build --target web --out-dir pkg`: 통과

## Linux Ubuntu 검증

`ssh ubuntu-ted`로 Ubuntu 6.8.0 환경에 접속해 현재 커밋 소스를 `/tmp/rhwp-pdf-font-options`에 전개한 뒤 검증했다.

- Rust toolchain: `cargo 1.93.1`, `rustc 1.93.1`
- `env CARGO_INCREMENTAL=0 cargo test --lib renderer::pdf -- --nocapture`: 통과
- `env CARGO_INCREMENTAL=0 cargo check --bin rhwp`: 통과
- `cargo run --bin rhwp -- export-pdf --help`: 큰따옴표 권장/작은따옴표 구분 도움말 출력 확인
- `cargo run --bin rhwp -- export-pdf samples/exam-kor-4p.hwp -p 0 -o target/tmp-pdf-font-options-linux/exam-kor-4p-p1.pdf --fallback-serif "Noto Serif CJK KR" --fallback-sans "Noto Sans CJK KR" --fallback-mono "Noto Sans Mono CJK KR" --equation-font "STIX Two Math"`: PDF 생성 통과
- `pdfinfo target/tmp-pdf-font-options-linux/exam-kor-4p-p1.pdf`: 1페이지 PDF 확인
- Ubuntu fontconfig에서 `Noto Sans CJK KR`은 설치 확인, `STIX Two Math`는 미설치라 `--equation-font` warning이 정상 출력됨을 확인했다.

## Windows 검증

`ssh win10-ted`로 Windows 10 개발환경에 접속했다. 기존 `C:\Users\admin\Desktop\rhwp\rhwp` checkout에는 미커밋 변경이 있어 직접 건드리지 않고, `GIT_LFS_SKIP_SMUDGE=1`로 별도 worktree `C:\Users\admin\Desktop\rhwp\rhwp-pdf-font-options-win`를 만든 뒤 현재 커밋 패치를 적용해 검증했다.

- Rust toolchain: `cargo 1.93.1`, `rustc 1.93.1`
- `cargo test --lib renderer::pdf -- --nocapture`: 통과
- `cargo check --bin rhwp`: 통과
- `ssh win10-ted` 기본 shell: 도움말 출력과 `export-pdf` PDF 생성 통과
- `cmd /C`: `.cmd` 스크립트에서 큰따옴표 포함 font family 인자 처리와 PDF 생성 통과
- `powershell -NoProfile`: `.ps1` 스크립트에서 큰따옴표 포함 font family 인자 처리와 PDF 생성 통과
- Windows 환경에서 `D2Coding`이 fontdb에 없어 `--fallback-mono` warning이 정상 출력됨을 확인했다.

## 검증 PDF 첨부

검증 산출물은 PR 근거로 사용할 수 있도록 저장소에 포함했다.

- `mydocs/report/assets/task_m100_1747_1884/macos_export_pdf_p1.pdf`
- `mydocs/report/assets/task_m100_1747_1884/linux_ubuntu_export_pdf_p1.pdf`
- `mydocs/report/assets/task_m100_1747_1884/windows_ssh_default_export_pdf_p1.pdf`
- `mydocs/report/assets/task_m100_1747_1884/windows_cmd_export_pdf_p1.pdf`
- `mydocs/report/assets/task_m100_1747_1884/windows_powershell_export_pdf_p1.pdf`

로컬 `pdfinfo` 재확인 결과 다섯 파일 모두 `Producer: rhwp`, `Pages: 1`, `Page size: 841.88 x 1190.52 pts (A3)`, `PDF version: 1.7`이다.
