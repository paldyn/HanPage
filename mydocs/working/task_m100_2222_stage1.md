# Task M100 #2222 — 1·2단계 완료 보고: 회귀 정량 + 레이어 JSON 캐시

- 이슈: #2222 / 브랜치: `local/task2222` / 작성일: 2026-07-12

## 1단계 — 회귀 정량 (v0.7.18 태그 대조 벤치, 네이티브 3회 중앙값)

| 문서 | 0.7.18 | devel | 판정 |
|------|--------|-------|------|
| 주보(4p) | 65ms | 64ms | 동등 |
| 결재(1p) | 9.5ms | ~18ms | 절대값 미미 |
| rowbreak(18p) | 90ms | 94ms | 잡음 수준 |
| 시장구조(307p) | 42.0s | **16.9s** | **2.5배 개선** (#2223 /2 효과) |

→ **네이티브 레이아웃 무회귀** — 이번 주 렌더 정정들(수렴 루프 포함)의 비용
영향 없음.

## 회귀 귀속 — studio 측 (실측 확정)

#2227의 DOM 이미지 경로(`getFlowImagePaintOps`)가 renderPage마다
`getPageLayerTree`(주보 p2 = **1.05MB JSON**, base64 인라인)를 호출. #2227의
트리 캐시가 있어도 LayerBuilder 재실행 + 재직렬화가 **회당 15.2ms**(워밍업 후
실측) = 렌더 자체(14.1ms)와 동급의 추가 비용이 이미지 보유 페이지의 매 렌더에
반복 — 스크롤/입력/유휴 프리렌더 체감 저하와 정합. 작업지시자 콘솔 실측
(투명선/잘림보기 클릭 330~345ms)과도 부합 (4페이지 동기 재렌더 × 반복 비용).

## 2단계 — 정정: 레이어 트리 JSON 변형 캐시 (Rust 측 — TS 무접촉)

`DocumentCore.layer_tree_json_cache`: 페이지당 (출력옵션 5종 비트 지문, JSON)
변형 최대 4개. 편집 무효화는 기존 `invalidate_page_tree_cache{,_from}` 동일
지점. jangster77의 #2228 작업(page-renderer.ts)과 파일 무접촉으로 충돌 없음.

| 검증 | 결과 |
|------|------|
| 캐시 히트 비용 | 15.2ms → **65µs (233배)** |
| 옵션 지문 정확성 | 토글 시 상이 JSON / 원복 시 원본 동일 |
| 표적 테스트 신설 | 일관성+지문+성능 가드(<3ms) — `tests/issue_2222_layer_json_cache.rs` |
| fmt/clippy/전수 | 통과/0/**3,051/0** (렌더 불변 정정 — golden 포함) |

## 잔여 (범위 정리)

1. **토글 첫 렌더**: 투명선/잘림보기는 조판 영향 옵션(투명 괘선 노드 조건
   방출)이라 트리 재빌드가 본질 비용 — 캐시 대상 아님. 0.7.18 대비 잔여
   초과분은 JS측 1MB `JSON.parse`(TS 소관) — #2228 수정과 같은 함수이므로
   jangster77에게 공유 예정.
2. **3단계(원 이슈: dirty-페이지 재조판 캐시)**: 별도 구현계획서 승인 게이트.
