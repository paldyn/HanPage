# 단계 완료 보고 — Task M100 #2029 3단계: 추출 2 재실측 → 축소 (v2 §0 규칙 3)

- 작성일: 2026-07-08 / 브랜치: `local/task2029`

## 재실측 결과

추출 1 이후 `layout_partial_table` = 549줄 (준비부 357 + cells 호출 33 + 마무리 159).

- **마무리(159줄)**: 준비부 산출 **19개** 소비 (grid_row_y/h·v_edges/caption 4종/table 기하
  5종 등) — 파라미터 19개 : 159줄의 추출비가 나쁘고, 묶으면 준비부 산출 전체를 담는
  광폭 컨텍스트 struct 가 필요.
- **준비부(357줄)**: cells 호출(14종)과 마무리(19종)가 소비하는 산출의 생산자 — 추출 시
  ~20필드 반환 struct 신설 필요. 동일 문제.

## 판단 — 축소

추출 1로 목표(1,773→549, 분기 300→96)를 이미 달성했고, 잔여 두 구간은 "표 프레임 기하"
라는 단일 응집 책임의 생산-소비 쌍이라 **현 상태가 응집적**이다. 무리한 절단은
광폭 struct 만 낳으므로 3단계를 축소 종료하고 4단계(재평가)로 직행한다.
후속 여지: `PartialTableFrame`(기하 묶음) 설계가 유용해지는 시점(표 계열 2차 라운드,
`layout_table_cells`(124)·`typeset_block_table`(127) 함께)에 일괄 검토.

## 동기화 기록

원격 전진분(#2039/#2040 — lpaiu-cs 재기여 2건 등) 병합(충돌 0) + 결합 재검증:
테스트 **2,938/0** · OVR 5/5 회귀 0.
