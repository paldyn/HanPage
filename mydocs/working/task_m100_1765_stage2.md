# Task #1765 Stage 2 완료보고서 — 가설 기각 판별 (수정 없음)

## 판별 (Stage 1 가설 정정)
행높이 대조 프로브(렌더 행 vs 선언 셀높이):
- 17931383 p2 표의 +5.2px 는 **row9 단일 행**에 집중 — 렌더 810.7px vs 선언 최대
  764.1px(57308HU). 셀 전부 **rs=1** (병합 셀 경로 아님).
- row9 는 밀집 셀(paras=23, ~50줄)이 선언높이를 **진짜 초과**하는 행 — 양 엔진 모두
  확장하며(한글도 ~805px 렌더), rhwp 가 **+0.1px/줄 수준의 per-line 누적**으로 한글보다
  5px 더 클 뿐. trailing ls(152HU=2px)도, 병합 경로도 원인 아님.

## 처리
- **병합 셀 경로 가드 확장 기각** — 재현 증거 없는 예방적 수정은 제외(2-b 경로의 코드
  비대칭은 #1763 보고서 한계 항목에 이미 기록, 실증 재현 발견 시 재개).
- 17931383 은 **per-line 콘텐츠 측정 누적**(razor 가족)으로 재분류 — #1759 프로그램의
  다음 표적(줄높이 corrected_line_height 정밀 대조)에 정확한 재현 사례 제공.
- 시도했던 2-b 가드 코드는 되돌림(소스 무변경 종결).

## 상태
완료. 최종보고서(판별) → 이슈 재분류 코멘트 → PR (문서+샘플).
