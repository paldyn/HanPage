# Task M100 #1904 3단계 완료보고서 — typeset 해체 라운드 1 (추출 1: 미주 배치 분리)

- 이슈: #1904 / 브랜치: `local/task1904` / 작성일: 2026-07-04 / 단계: 3/4

## 정찰 발견 (해체 전략을 결정한 구조)

`typeset_section_with_variant`(7,059줄·CC 282)는 3덩어리였다:
[초기화 ~110줄(소스 플래그)] + [본문 문단 배치 루프 ~1,425줄] + **[미주 가상 삽입·배치
~5,510줄 = 함수의 78%]** (+마무리). 미주부는 #836(미주를 본문처럼 2단 플로우) 구현이
집적된 것으로, **소스-포맷 분기 0회** — 1차 원칙(분기 비접촉 + 위험 최저 + 복잡도 최대)에
정확히 부합하는 첫 추출 대상.

## 수행 — 추출 1: `typeset_section_endnotes`

- 의존 분석: 캐리오버 지역 변수 **`st` 하나**(나머지 10개 후보는 shadowing/필드 접근 오탐
  — 단어 경계 재검증으로 확정). 파라미터 9개로 추출.
- 블록 5,526줄 이동 + 원위치 호출 11줄 + 조정 2곳(`match &x`→`match x`, `&mut st`→`&mut *st`
  reborrow) — **로직 무변경**.

| 함수 | 추출 전 | 추출 후 |
|---|---|---|
| `typeset_section_with_variant` | 7,059줄·CC 282 | **1,555줄 (−78%)** |
| `typeset_section_endnotes` (신규) | — | 5,554줄 (책임 분리 완료 — 내부 분해는 후속 라운드) |

## 게이트 (manifest 전수)

| 게이트 | 결과 |
|---|---|
| fmt / clippy | clean / typeset 무경고 |
| 전체 테스트 | **FAILED 0 (2,858 passed)** |
| **OVR 5샘플** | baseline(`00014ecf`) 대비 **회귀 0건 전수** |
| golden SVG / roundtrip 3종 | 4스위트 전부 ok |

## 다음

계획상 라운드 1 은 추출 2~3개 — 남은 후보: 본문 루프 내 책임(분할/앵커/flow) 또는
`typeset_section_endnotes` 내부 1차 분해. **관문 원칙에 따라 여기서 중간 판단 요청** —
(a) 추출 1~2개 추가 후 재평가, (b) 현 시점 재평가(4단계)로 진행.
