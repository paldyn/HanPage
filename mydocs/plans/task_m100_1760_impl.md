# Task #1760 구현계획서 — 표 높이 계통 과대 (4단계)

수행계획서: `mydocs/plans/task_m100_1760.md`

## Stage 1 — 행 단위 분해 조사
- 프로브(스크래치): 대표 표 4건의 행 경계 대조 — rhwp render-tree 셀 bbox(있으면) 또는
  `dump` 행높이 vs 한글 find_tables 셀 행 경계(pt→px).
- 판별: 상수(테두리 아티팩트) / 행비례(환산 결함) / 특정 행(콘텐츠 재측정 차) 분리.
- 산출: Stage 1 보고서 (판별 결과 + 원인 코드 후보 지점).

## Stage 2 — 수정 + 단위테스트
- 판별 결과에 따라: (b) 행비례면 행높이 환산/최소행높이/cell_spacing 누적 지점 수정,
  (a) 아티팩트면 하니스 bbox 기준 보정(tools) + 실 결함 잔여만 수정.
- 재현 샘플 동결(samples/task1760/) + 단위/통합테스트.

## Stage 3 — 하니스 재측정
- #1759 서베이 38파일 재실행 → dh 분포 중앙값 0 수렴 확인, 악화 0.

## Stage 4 — 회귀 검증 + 최종보고
- lib/통합/페이지 게이트 9종, 한글 OLE 대조(mismatch 잔여 + MATCH 150) → 보고서 →
  squash → PR.
