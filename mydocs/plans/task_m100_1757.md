# Task #1757 수행계획서 — verify_pi_page_vs_hangul 알려진 한계(캐럿-개체 분리) 문서화

## 배경
hwpdocs 페이지·PI 대조 잔여 분류(#1745 후속)에서 한글 PDF 시각 판정으로, 시각 정합인데도
PI_MISMATCH 로 나오는 도구 한계 2유형을 확정(이슈 #1757). 미문서화 상태라 향후 조사에서
오탐 재조사 낭비 위험.

## 작업 (문서 전용 — 소스 로직 무변경)
1. `tools/verify_pi_page_vs_hangul.py` 모듈 docstring 에 "알려진 한계(캐럿-개체 분리)" 절:
   - 자리차지 다쪽 표 anchor (한글 캐럿 = 표 끝 쪽; 예 17991519 pi1 1↔4쪽).
   - 쪽 경계 TAC 표 문단 (표는 양쪽 모두 다음 쪽 통째 렌더, 한글 캐럿은 이전 쪽;
     예 2789777 pi4, 36389863 pi9).
2. `mydocs/manual/verify_pi_page_vs_hangul.md` 신설 — 사용법 + 판정 의미 + 알려진 한계
   (오탐 판별 절차: 한글 PDF 시각 확인).

## 게이트
- `python -m py_compile tools/verify_pi_page_vs_hangul.py` (docstring 변경 무결성).
- 로직 무변경(diff 는 docstring/문서만).

## 비고
문서 전용이라 구현계획서·단계 보고서는 생략, 본 계획서 + 커밋 + 최종 보고(요약)로 진행.
