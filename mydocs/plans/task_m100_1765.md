# Task #1765 수행계획서 — 병합 셀 경로 trailing ls 가드 확장 (#1763 후속)

## 배경
#1763 은 rs=1 셀 경로의 선언높이 권위 가드만 적용. 병합 셀 경로(height_measurer 2-b)도
동일한 trailing ls 포함 측정으로 required 가 부풀어 마지막 스팬 행을 deficit 확장한다.
재현: 17931383 (12×4, rowspan 구조) — #1759 하니스 dh **+5.2px** (#1763 후 잔존).

## 수정 방향
2-b 경로에 #1763 과 동일 가드: `cell_last_trailing_ls` 산출(포함 조건 동일) 후,
`required > combined(스팬 행 합)` 이고 `content − trailing + pad ≤ combined` 이면
deficit 확장 억제.

## 회귀 게이트
1. 재현: 17931383 표 dh +5.2 → ~0 (#1759 하니스), 페이지 게이트 10종 무회귀.
2. lib + 표 계열 통합 무회귀, MATCH 표본 150 유지.

## 한계
- 대조군 잔여(36382196 pi24 +6.3 등 별개 메커니즘)는 범위 밖 — #1759 잔여.
