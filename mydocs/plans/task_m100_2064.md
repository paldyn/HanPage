# 수행계획서 — Task M100 #2064: 라운드 7 (typeset_endnote_paragraphs 내부 분해)

- 이슈: #2064 / 브랜치: `local/task2064` (base: devel `641a685f`) / 작성일: 2026-07-08
- 거버넌스: v2 전 조항 + 스캐너 체크리스트 5종(r4~r6 확정분)

## 1. 목표 (보수적 설정)

`typeset_endnote_paragraphs` (**4,397줄 · CC 153 = 전체 1위**, 라운드 5 통추출 산물)의
내부 분해 **1차 회전**. 정찰 결과 본문(4,353줄)은 측정·방출이 교직된 **평탄 파이프라인**
(지배 블록 2개뿐: compute_en_metrics 클로저 127줄, split 후보 처리 133줄 / 12-indent
let ~230개 / advance·emit 지점 17곳 산재)이라, 한 라운드 완결이 아닌 **여러 라운드
분할**을 전제한다. 이번 목표: 국면 지도 확보 + 저위험 추출 2~3건 (CC 153 → 110~120대).

## 2. 수행 단계 (관문형)

### 1단계 — 국면 지도 + 의존 실측 + 구현계획서 (분석 비중 大)
- 랜드마크(#1082 다단 누적 / #1363 SSOT 점유높이 / #1363 v2 A2 split 시뮬 / split 후보
  8275~8407) 기준으로 미주-당 파이프라인의 국면 경계·캐리를 정밀 매핑 (체크리스트 5종).
- 추출 후보 확정: ① `compute_en_metrics` 클로저 → fn 승격(127줄, 저위험) ② split 후보
  처리(133줄) ③ 실측이 허락하는 국면 1건. 축소 가능성 명시(v2 §0 규칙 3).

### 2단계 — 추출 1 (①, 저위험 선행) / 3단계 — 추출 2(~3)
### 4단계 — 재평가(`--snapshot r7 --no-coverage`) + 잔여 국면 지도 인계 + 보고

## 3. 게이트

fmt/clippy 0 · `--tests` FAILED 0 · OVR 5샘플 회귀 0 · **미주 표적 issue_1116 13/13** ·
페이지 오라클/roundtrip. 미주 배치는 쪽수 전이가 큰 영역 — 추출마다 즉시 게이트.

## 4. 위험/범위 제외

- 평탄 파이프라인 절단은 경계 오판 위험이 큼 — 국면 지도 없이는 추출하지 않는다.
- 범위 제외: layout_composed_paragraph(146) 잔여 / 표 계열 2차 / cells(115) 2차.
