# 수행·구현계획서 — Task M100 #2120: 라운드 15 (build_single_column, goal 루프 2차 2/4)

- 이슈: #2120 / 브랜치: `local/task2120` / 2026-07-09 / goal 루프 2차.

## 재지도·설계

`build_single_column` (layout.rs, 1,482줄·CC 116). 지배 블록 = **문단 테두리/배경 연속
그룹 병합 렌더링 (285줄·분기 40, Task #321 v6)** — 자기완결 국면(self.para_border_ranges
소비→border 노드 방출), 탈출은 전부 클로저/지역 루프 소속(정밀 확인).

→ `render_para_border_groups(&self, tree, composed, col_node, styles, col_area)` 통이동.
컴파일러 수렴 2건(composed 파라미터, col_node &mut). **표적 CC 116→90 — 1건으로 목표
달성, §0 에 따라 종료** (잔여 후보 B 116줄 블록은 hcursor 상태 캐리 얽힘 — 후속).

## 게이트

기존 체인 전수 + `--snapshot r15`.
