# Task #1753 수행계획서 — 지연 자리차지 표의 후속 텍스트 선행 채움 (부분: 후속 문단)

## 배경
hwpdocs 페이지·PI 대조 S3 심층조사에서 한글 PDF 시각 대조로 확정한 실결함(이슈 #1753).
대표 `2814765_[별표 2] 건설사업관리기술인 배치기준...hwpx` — pi51(제목 텍스트 + 57×9
자리차지 RowBreak 표) 뒤의 pi52("※ 시공단계...")·pi53("2) 보정계수")을 한글은 **9쪽 하단에
렌더**(저장 LINE_SEG vpos=72581/74121 일치)하는데, rhwp 는 표가 끝난 11쪽으로 밀어냄.

## 원인
`typeset_block_table` 의 다행 RowBreak 표 이월(multirow_clean_defer,
`advance_column_or_new_page`)이 표만 다음 쪽으로 보내고, **후속 문단은 순차 모델이라 표
fragment 뒤에서만 배치** — 한글의 지연 float 선행 채움(fill-before-deferred-float) 시맨틱과
불일치.

## 수정 범위 (부분 — Option A)
표 이월 직전, **후속 control-free 문단들을 현재 쪽 잔여 공간에 선행 배치**:
1. `typeset_block_table` 이월 분기에서 가드 성립 시 후속 문단 prefill:
   - 가드: 단일 단 + current_items 비어있지 않음 + 텍스트 anchor(para_has_visible_text) +
     비-TAC + vert_rel_to=Para(v_off≥0) + RowBreak + 자리차지(비 TopAndBottom-visible).
   - 후보 문단: controls 없음 + 첫 실줄 저장 vpos 가 (host vpos, 본문높이HU] 구간(같은 쪽
     연속 인코딩; 누적좌표 문서는 자연 배제) + 누적높이 fit. 최대 8개, 첫 실패에서 중단.
   - 배치: FullParagraph + flow 높이 소비, `st.prefilled_paras` 기록.
2. 메인 루프: `prefilled_paras` 에 든 문단 스킵.
3. `typeset_block_table` 에 `paragraphs`/`composed` 인자 플럼빙 (호출부 2곳).

**범위 밖 (후속 분리)**: pi51 host 제목 줄의 9쪽 잔류(한글 정합)는 layout 의 "분할 표
첫 부분 호스트 텍스트" 렌더 억제 신호 설계(ColumnItemCtx 확장)가 필요해 별도 이슈로.
pi51 자체의 PI 판정은 캐럿-개체 분리(도구 한계)로 잔존.

## 회귀 게이트
1. 재현 파일 pi52/53: 11쪽 → **9쪽** (`verify_pi_page_vs_hangul` n_mismatch 3→1, 잔여는 pi51 캐럿).
2. `cargo test --lib` + 통합(1417/546/1486/diag1042/1750/1749) 무회귀.
3. 페이지 게이트: byeolpyo1/4 · 승강기 · task1700/1745/1749/1750 · 국제고속선기준(251) 무회귀.
4. hwpdocs mismatch 잔여 + MATCH 표본 150건 — 악화 0.

## 한계
- 선행 채움은 control-free 문단만 (표/그림 포함 후속은 순차 유지 — 배치 순서 복잡성 회피).
- host 제목 줄은 여전히 표 첫 fragment 쪽(현행) — 후속 이슈로 분리.
