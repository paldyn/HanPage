# 수행 계획서 — Task M100 #1891

## 개요

- **이슈**: [#1891 규제영향분석서 hwpx 계열 라운드트립 쪽수 팽창 (표본 173중 14 비-PASS, 최대 149→173쪽) — 비표준 ZIP(EOCD) 단서](https://github.com/edwardkim/rhwp/issues/1891)
- **발견 경위**: 2026-07-04 20k 라운드트립 렌더링 위치 검사
- **브랜치**: `pr/devel-1891` (origin/devel 기반, #1892/#1893 merge 후)

## 조사 계획

1. 대표 80250 의 ZIP 구조 덤프 (EOCD/트레일링) → 컨테이너 비표준 여부 확정
2. 14건 전수 시그니처 판별 + 현 devel 재측정
3. 축별 최소 재현체 특정 → 수정/후속 분리

## 판별 결과 (조사로 이슈 전제 재구성)

- **"비표준 ZIP" 아님 — 확장자 위장**: 비-PASS 14건 중 10건은 시그니처가
  `D0 CF 11 E0` 인 **HWP5(OLE) 파일이 .hwpx 확장자로 업로드**된 것(부처 시스템
  산출물). `hwpx-roundtrip` 의 EOCD PARSE_FAIL 은 비-ZIP 파일에 대한 정상 거절이고,
  `render-diff --via hwpx` 는 `parse_document` 자동 감지로 HWP5 파스 후
  **HWP5→HWPX 변환 왕복**을 측정한 것이다. 쪽수 팽창(149→173) 전건이 이 부류.
- 나머지 4건이 진짜 hwpx: 3건(370px STRUCT)은 #1893 merge 로 이미 PASS,
  잔여 1건(73504, STRUCT 167px)이 본 타스크의 수정 대상.

## 축 분해

- **축 A (HWP5-in-.hwpx 10건)**: LINE_SEG 부재 기계생성 HWP5 의 빈 문단 h=0
  (HWP5 로드 `include_empty` reflow 미적용) vs 변환 HWPX 의 합성 lineseg 높이
  — 같은 IR 의 로드 시멘틱 비대칭. 한글 2022 pyhwpx 오라클(80250: 한컴 17쪽
  vs A 16 vs B 19, 84522: <붙임> 표 위치는 B 정답 / 표 영역 쪽경계는 A 정답)
  로 **양쪽 다 한컴 비정합** 판정 — 높이 계산기 3자(라이브 측정·reflow 합성·한컴)
  가 모두 불일치. 보정된 reflow + 소스 무관 게이트가 이상적 해법이나 설계
  결정 사안 → **후속 분리**.
- **축 B (진짜 hwpx 1건, 73504)**: 외부 참조(BinData Link) 그림 왕복 소실
  — 본 타스크에서 수정.

## 산출물

- 수정: `src/serializer/hwpx/context.rs`, `src/serializer/hwpx/mod.rs`,
  `src/serializer/hwpx/content.rs`, `src/serializer/hwpx/package_check.rs`,
  `src/parser/hwpx/content.rs`
- 픽스처: `samples/issue1891_external_bindata_link.hwpx` (73504)
- 테스트: `tests/issue_1891.rs`
- 보고서: `mydocs/report/task_m100_1891_report.md`
