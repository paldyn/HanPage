# M100 #2513 수행계획서 - embed loadFile 기본 대화상자 교착

## 1. 작업 정보

- 이슈: [#2513](https://github.com/edwardkim/rhwp/issues/2513)
- 관련: #2187, #2219, #2389
- 기준 브랜치: 최신 `upstream/devel`
- 작업 브랜치: `fix/issue-2513`
- PR 대상: `edwardkim/rhwp:devel`

## 2. 문제와 실제 활동

`@rhwp/editor` 사용자는 cross-origin iframe 내부 UI를 직접 제어하지 않고
`editor.loadFile(bytes, name)`의 Promise 완료를 문서 준비 신호로 사용한다. 현재 기본 호출은
로드 후 글꼴/검증 안내창을 기다리므로, iframe이 로딩 overlay 아래 있거나 사용자가 버튼을 누르지
않으면 문서는 렌더돼도 RPC가 60초 timeout된다.

#2389는 `{ suppressDialogs: true }` opt-in을 추가했지만 public SDK의 기본값과 첫 사용 예제는
여전히 대화형이다. embed 전용 SDK의 zero-option 경로가 결정적으로 완료되지 않는 것이 남은 결함이다.

## 3. 목표와 비목표

### 목표

- `@rhwp/editor.loadFile()`에서 `suppressDialogs` 생략 시 비대화형 로드를 기본 적용한다.
- `{ suppressDialogs: false }`를 명시하면 기존 안내창 동작을 유지한다.
- top-level Studio와 raw embed protocol의 기존 기본값은 유지한다.
- modal click 없는 fresh iframe 회귀 테스트로 Promise settle을 증명한다.

### 비목표

- Studio 전체 dialog 정책 변경
- font detection/validation UI 제거
- MessageChannel protocol version 변경
- legacy transport 제거 및 배포 인프라 변경

## 4. 선택지와 결정

1. **SDK 생략 기본값만 `true`로 변경 (권장).** embed API의 일반 호출을 안전하게 만들고
   explicit `false`로 호환 경로를 남긴다. 변경 범위가 `npm/editor`와 embed E2E에 한정된다.
2. `createEditor()` 전역 옵션 추가. 인스턴스별 정책은 명확하지만 API surface와 상태 전달이 늘어난다.
3. 현 opt-in 유지 + 문서만 강조. 이미 README가 권장하고도 실제 consumer가 timeout했으므로
   root cause를 제거하지 못한다.

결정: 1번. direct protocol은 기존 `false`를 유지해 SDK 외 consumer의 계약을 바꾸지 않는다.

## 5. 영향 범위와 보호 계약

- 변경: `npm/editor/index.js`, `npm/editor/index.d.ts`, `npm/editor/README.md`
- 회귀 테스트: SDK params contract, `rhwp-studio/e2e/embed-transport.test.mjs`
- 보존: caller buffer 비변형, MessageChannel v1, explicit false, top-level modal, HML/HWP/HWPX load/export
- 보안: origin/session/capability 협상 변경 없음

## 6. 수용 기준

- [ ] 옵션 생략 `loadFile()` 요청이 `suppressDialogs:true`를 전송한다.
- [ ] 명시적 `{ suppressDialogs:false }`는 false를 전송한다.
- [ ] embed E2E가 fallback 버튼 polling 없이 load를 완료한다.
- [ ] top-level/direct protocol 기본 dialog 계약은 유지된다.
- [ ] npm editor unit/contract와 embed E2E가 통과한다.
- [ ] `cargo fmt --all -- --check`, `cargo test --profile release-test --tests`,
      `cargo clippy -- -D warnings`가 통과한다.
- [ ] fork branch push 후 upstream `devel` 대상 PR이 생성된다.

## 7. 승인

- 상태: 승인 완료
- 기록: 2026-07-20 작업지시자 `승인`
- Stage 1 소스 수정을 승인받았다.
- 추가 기록: 2026-07-20 작업지시자 `해결책도 pull request로 구현해서 review 요청까지 . stage2 그후 진행`
- Stage 2와 후속 전체 검증, upstream PR 생성 및 리뷰 요청까지 승인받았다.
