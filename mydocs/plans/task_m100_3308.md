---
kind: plan
status: draft
canonical: mydocs/manual/codex/docs_and_git_workflow.md
last_verified: 2026-08-01
---

# Task #3308 수행계획서 — 직인 박스 위치 이탈 (+ 용지규격 문구 해소 판정)

- Issue: [#3308](https://github.com/edwardkim/rhwp/issues/3308) (M100) — 외부 리포터
  `Neurlect`, #3307 과 동일 문서·동일 p7 의 별개 증상 2건
- 브랜치: `local/task3308` / 착수: 2026-08-01 (작업지시자 "별도 착수" 결정)
- fixture·정답지: **#3307 자산 재사용** — `samples/task3307/issue3307_outline_number.hwpx`,
  `pdf/task3307/beopryeong_3307-2020.pdf`

## 1. 확정된 사실 (착수 실측 — 좌표 정량)

한컴 정답지 p7 좌표(pt→px ×4/3 환산) vs rhwp render tree:

| 대상 | 한컴 | rhwp | 판정 |
|---|---|---|---|
| "…요구권자" 끝 | x≈534.2 | x≈577.6 (x=194.6+w=383) | 참고 |
| **직인** | **x=598.7, y=1004.5** | **x=696.2, y=1003.4** | **x +97.5px 이탈** (y 정합 1px) |
| 용지규격 "210mm×297mm[" | x=590.7, y=1035.3 | x=590.9, y=1037.7 | **정합 (0.2/2.4px)** |

- **작업지시자 판정 (2026-08-01)**: 리포터가 본 형태의 증상 1("오른쪽 아래" 밀림)·
  증상 2(문구 소실)는 **시각적으로 해결됨에 동의**. **남은 본질은 중첩 표 너비
  오계산이다** — 문제 표는 1×2 구조:

  ```
  | 징계의결 또는 징계부가금 부과 의결 요구권자(직위) | 직인 |
  ```

  직인 셀의 x +97.5px 는 이 1×2 표(표 안에 표)의 폭/열 폭이 잘못 계산된 결과다.
- 증상 2 는 #3127(svg2pdf body-clip 소실 수정, v0.8.0, `350b00ac8`) 이 유력한 해소
  커밋 — 귀속 확정 후 리포터 회신에 버전을 명시한다.
- 구조 판독 (작업지시자, 2026-08-01 — 한컴 편집기 실물 확인): 리포터 표현(텍스트
  박스)과 달리 **표 안에 표** 조판이며, 직인 표시는 사각형 개체가 아니라 중첩 표의
  **오른쪽 셀에 테두리 "바깥쪽" 4변을 0.7mm 주황(빨간 계열)으로 지정**한 경우다
  (셀 테두리/배경 대화상자 스크린샷 확보). render tree 경로
  (`/Table/Cell/Table/Cell/TextLine`)와 정합. 따라서 문제는 그 오른쪽 셀의 x 배치 —
  **중첩 표 열 폭/열 원점 해석**으로 좁혀진다. Stage 1 에서 IR 의 셀 border_fill
  (바깥 4변 0.7mm 주황)과 열 폭 선언을 실측해 확증한다.

## 2. 조사 방향 (Stage 1)

1. 직인이 속한 중첩 표의 IR 구조 덤프 — 셀 폭·col_addr·표 폭·offset. 선언 값과
   렌더 좌표의 어긋남 지점 특정 (셀 x 계산 vs 중첩 표 원점 vs 열 폭 해석).
2. 한컴 좌표(598.7)를 만들 수 있는 해석이 IR 에 존재하는지 — 선언 열 폭 합,
   outer margin, TAC/inline 여부.
3. #2097·#3396(TAC 인라인 표 x-원점 outMargin) 등 기존 표 x 배치 계열과의 관계.
4. 증상 2 의 해소 커밋 귀속(#3127 전후 실측 — `git worktree` 나 커밋 checkout
  빌드로 1회 확인. **stash 금지**).

## 3. 단계 계획

| 단계 | 내용 | 산출물 |
|---|---|---|
| Stage 1 | 근인 규명(위 조사 4축) + 증상 2 귀속 확정 | stage1 보고 |
| Stage 2 | 증상 1 수정 + red-check + 회귀 테스트(직인 x 좌표 계약: 한컴 대비 허용 오차 내) | 구현 + stage2 보고 |
| Stage 3 | 게이트 — 정답지 스왑(p7 직인 위치), samples 쪽수 A/B, release-test 전체·clippy·Skia·wasm, 이중 baseline(fixture 신규 없음 — 확인만) | stage3 보고 |
| Stage 4 | 시각 판정 → 최종 보고 → PR(별도 승인) → merge 후 리포터 회신(증상 2 해소 버전 포함) | 보고서 + PR |

## 4. 검증 기준

1. 직인 x 가 한컴 598.7px 대비 **허용 오차 내**(Stage 1 에서 폰트 폭 차이를 감안해
   확정, 목표 ≤5px).
2. 같은 중첩 표의 다른 셀("…요구권자" 셀) 불변.
3. 표 x 배치 계열(#3396 등) 무회귀 — 관련 테스트 + 666건 쪽수 A/B 차이 0.
4. 정답지 이미지 스왑 + 작업지시자 시각 판정.
5. 증상 2: 해소 커밋 귀속 근거를 리포터 회신에 명시.

## 5. 위험과 제약

- 표 x 배치는 #3396(TAC outMargin)·#2097 계열과 얽힌 핵심 경로 — 케이스별 명시
  가드 원칙(메모리: 한컴 호환은 구조 가드가 안전) 준수.
- 이탈량 97.5px 가 "선언 열 폭" 해석 차이라면 수정 범위가 셀 폭 계산 공통부일 수
  있다 — Stage 1 후 범위 재확인.
- 직인 계열은 인쇄/시각 판정 오인 방지 메모리(placeholder 인쇄 억제)와 무관하나,
  회신 시 용어를 "중첩 표 셀"로 정정해 리포터 멘탈 모델을 맞춘다.
