# 수행 계획서 — Task M100 #2137

**이슈**: [#2137](https://github.com/edwardkim/rhwp/issues/2137) 부동 그림/도형 문서 과분할 (10k r12 OVER+SHAPE 44건, 보도자료 계열)

**브랜치**: `local/task2137` (base: local/task2136 스택) / **작성일**: 2026-07-10

## 1. 조사 (대표 156618554 분해 완료)

- p2로 밀린 항목 = **빈 앵커(pi13, 문서 마지막) + 작은 부동 글상자**(사각형, 자리차지
  TopAndBottom, tac=false, 19148×3736HU ≈ 49.8px高, vert=문단 Top).
- 앵커 저장 vpos=68600 → 줄 경계 68600+1400=70000 ≤ 본문 70018HU — **저장 흐름이 p1
  하단을 인코딩** (문서 끝이라 page-last 증거 성립). 한글 1쪽 = 개체가 하단 여백
  (15mm=56.7px)으로 스필.
- rhwp: 앵커+개체 통째 p2 — 두 배제 요인:
  ① `saved_single_line_bottom_fits`(#2093)의 `controls.is_empty()` 조건,
  ② #1027-E2 가 TopAndBottom 글상자를 atomic top-fit 스필(#409)에서 제외
  (AI 184p 대형 박스는 한컴도 다음 쪽 — 반대 진실 병존).

## 2. 가설·수정 방침

**저장 증거 게이트로 두 진실을 판별**: 앵커 줄의 저장 경계가 현재 쪽에 fit 하고
page-last 증거가 있으면(=#2093 신뢰 조건, controls 조건만 완화) 한글이 이 쪽에 뒀다는
저장 증거이므로 — 비-TAC TopAndBottom float 만 가진 단일 줄 앵커를 신뢰 경로에 편입,
개체는 하단 여백 스필로 렌더. #1027 케이스는 앵커 저장 vpos 가 다음 쪽을 인코딩하면
경계 fit 실패로 자연 보호(확인 필요 — AI 184p 게이트 테스트 존재 시 필수 통과).

## 3. 단계

1. 조사 보강: 44건 중 동형(단일줄 앵커+소형 float+저장 fit) 비율 스캔, #1027 보호
   케이스(tests/issue_1027*?) 확인
2. 구현: `saved_single_line_bottom_fits` controls 조건 완화(비-TAC TopAndBottom float
   전용) — float 스필 렌더 경로 점검
3. 검증: 대표 3건 오라클, 44건 재오라클(FIXED 계수), 광역 359 재검 REGRESSED 0,
   #1027/#409 계열 테스트, byeolpyo4/1, **시각 스윕**(스필 렌더 확인 필수)
4. 보고 + PR

---

**승인 요청**: 위 수행 계획으로 진행 여부 판단을 요청합니다.
