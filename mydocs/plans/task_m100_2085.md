# 수행·구현계획서 — Task M100 #2085: 라운드 10 (typeset_block_table, goal 루프 1/4)

- 이슈: #2085 / 브랜치: `local/task2085` / 작성일: 2026-07-09
- goal 루프 (작업지시자 승인): 게이트 자체 검증, close 만 루프 말미 일괄 승인.

## 대상·재지도

`typeset_block_table` (typeset.rs:12686, **1,550줄·공식 CC 129**, 전체 1위). 지배 블록 =
**행-스캔 `while r < row_count` 루프 (13765~14123, 359줄·분기 86)** — 표 분할점 산출 국면.

실측: 경계 넘는 mut 캐리 5개(consumed/end_row/split_block_start/split_end_cut/
split_end_limit — 전부 블록 직전 정의·블록 후 소비), 탈출 16곳 전부 루프-지역
(break/continue = while 제어, return 1곳은 클로저 내부). **통이동 최적 조건.**

## 설계

- `struct BlockTableRowScan`(캐리 5: f64/usize/Option<usize>/Vec<usize>/f64) — **값 왕복**
  (RunEmitState 전례, Vec 은 move).
- `struct BlockRowScanVars`(Copy 스칼라 ~12: cursor_row/row_count/cs/can_intra_split/
  is_continuation/avail_for_rows/header_overhead/landscape 4종/rowbreak 허용치) — §6 의무.
- 참조 직접: st(&, base_available_height 1회)/layout_engine/mt/table/styles +
  슬라이스 cut_row_h/rowspan_touched/start_cut.
- **§1 소스분기**: `st.is_hwpx_source` 기반 landscape 허용치 4종은 블록 밖 계산 —
  통이동 시 자연히 caller 잔류.
- 불확실 파라미터는 컴파일러 E0425 수렴 (라운드 9 정착 방식).

## 단계·게이트

① 본 계획 ② 통이동+게이트 ③ 재평가(표적 CC + 스냅샷 r10)·보고·devel push.
게이트: fmt/clippy 0/`--tests` 0실패/issue_1116 13/13/OVR 5샘플 회귀 0.
