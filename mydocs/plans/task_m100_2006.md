# task_m100_2006 — 그림 배치 과소페이지 수행계획서

- 이슈: #2006 (그림+본문 혼재 과소페이지, #1995 minor 분리)
- 브랜치: `local/task2006`
- 관련: #2004(이미지 스택, PR #2011 해소), #2007(표 행높이), #1995

## 1. 범위 재정의 (검증 기반)

이슈는 두 문서를 들었으나, PR #2011(#2004 양변종) 빌드 실측으로 **1번 문서는 해소**됨을 확인:

| 문서 | devel | PR #2011 | 오라클 | 판정 |
|------|------:|---------:|-------:|------|
| 1430000 최종보고서 | 384 | **403** | 404 | ✅ #2011로 −20→−1 해소 |
| **1790387 HIV 최종결과보고서** | 130 | **130** | 146 | ❌ **−16 잔여 (본 이슈 실질 대상)** |

→ #2006의 실질 잔여는 **HIV 문서 −16** 하나로 축소.

## 2. HIV 문서 진단 (본 조사)

- 구성: 그림 컨트롤 **567개**, 표 104개(RowBreak 78), 과적재(used>body_h) **14쪽**(+2~+31px).
- 과적재 최대 page30(+30)·page96(+31). page30: `used=956.2px vs hwp_used≈365.3px, diff=+590.9px`.
- **핵심 신호**: 과적재/과소 구간의 문단이 **인라인(tac=true) 그림을 포함**한다. 예 page30 pi=306~309:
  ```
  FullParagraph pi=306 h=105.6  + Shape 그림 tac=true (vpos 25694..31634, 동일 라인 프레임)
  FullParagraph pi=307 h=85.9   + Shape 그림 tac=true
  ```
  → 인라인 그림이 붙은 문단의 라인높이 재계산이 저장 LINE_SEG(vpos 프레임)와 어긋나 페이지 충전량이 한글과 달라짐.

## 3. 가설

**인라인 tac=true 그림 라인의 높이 재계산 드리프트** (≠ #2004 부동/인라인 "스택 개체", ≠ #2007 표 행높이).
- #1842(셀 내부 인라인 개체 라인높이)·#1898(fix/1898-tac-image-line-advance)와 동일 축 가능성 — 셀 밖 본문 문단으로 확장된 형태.
- 그림+본문 혼재 문단에서 rhwp 라인높이가 한글과 달라 누적 압축 → −16.

## 4. 검증 자료

- 페이지수 오라클: pyhwpx PageCount 146 (기 서베이).
- 시각 오라클: ✅ **확보 완료** — `output/poc/task2006/hiv_hangul_2022.pdf` (한글 2022, **146쪽**, visible=True COM). 오라클 페이지수 146 확인.
- baseline 대조: `tools/hangul_pdf_baseline.py --compare` 로 인라인 그림 문단 라인 pitch 정량화.

## 5. 구현 계획 (초안, 승인 후 확정)

1. ~~오라클 확보~~ ✅ 완료 (`output/poc/task2006/hiv_hangul_2022.pdf` 146쪽). 다음: baseline 대조로 인라인 그림 문단 라인높이 과소/과대 pt 정량화 (단, `hangul_pdf_baseline.py` rhwp SVG 파서가 대형 혼합문서엔 부정확 → 도구 보강 또는 국소 표/문단 수동 대조 필요).
2. **RCA**: 인라인 tac=true 그림 라인의 `line_advance`/`height_measurer` 산출 vs 저장 LINE_SEG lh 대조 (#1898/#1842 경로 재점검).
3. **국소 수정**: 인라인 그림 라인높이 전용 게이트. 광역 라인높이 변경 금지.
4. **회귀검증**: `cargo test`(renderer lib), roundtrip baseline, #1898/#1842/#1658 게이트, 오라클 pi-page 표본.

## 5.5 RCA 추가 (pi-page 국소화 — 본 세션)

`verify_pi_page_vs_hangul.py`(신뢰 도구)로 HIV 발산 패턴 확인:
- 첫 발산 **pi152 (rhwp_p14 | hwp_p15)** — 앞 13쪽은 정합.
- 이후 페이지 경계마다 **+1씩 분산 누적**(pi166 p15|16, pi188 p16|17, pi190 p17|18 …). **국소 점프(단일 지점 +2↑) 0건**(detail 앞 40개 기준, TSV detail은 앞부분만 캡처 — 전체 확인엔 도구 detail 상한 확장 필요).
- 산출물: `output/poc/task2006/pipage_localization.tsv`, `hiv_hangul_2022.pdf`(146쪽).

**시사점**: HIV −16은 특정 이미지 페이지의 국소 붕괴가 **아니라 분산 누적** — **#2007과 동일 클래스(페이지 분할/keep 타이밍)** 가능성이 높다. §2의 인라인 그림 라인높이 신호(page30 diff+590)는 이미지 페이지의 `hwp_used` 계산 불안정에 의한 정황일 수 있어 재검 필요.

**재분류 후보**: #2006 HIV + #2007 을 **단일 "heading/keep 페이지 분할 타이밍" 축**으로 병합 조사(+ #1995 초기 keep 관측 d27a98b1, #1921 ±1 롱테일). 광역 페이지분할 경로 = 회귀 위험 최상 → 코퍼스 pi-page 게이트 갖춘 독립 정식 타스크.

## 6. 위험/판단

- 인라인 그림 라인높이는 본문·셀 공통 경로 → 회귀 위험. #1842/#1898 게이트 동시 확인 필수.
- −16 중간 심각도. **시각 오라클 확보가 선행 조건**(현재 페이지수만). 그 전 hot-path 라인높이 수정 보류.
- #2004(PR #2011 병합 대기)와 독립 — 병합 후 잔여만 재확인.
