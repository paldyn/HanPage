# 수행계획서 — Task M100 #2152: per-pi 오라클 미주 문단 정렬 — [미주] 라벨 전 kind 확장 + 도구 제외

- 이슈: #2152 / 브랜치: `fix/2152-oracle-endnote-pi` / 작성일: 2026-07-10
- 배경: samples 전수 스윕(#2154)에서 PARA_COUNT 44건 — 각주·미주 다량 문서 ~30건이
  per-pi 검증 사각지대. rhwp pi 열거가 미주 문단을 본문 인덱스 뒤에 이어 붙임
  (typeset.rs `en_para_idx = body_len + 미주 로컬`)이 원인.

## 원인

- `dump_page_items`(rendering.rs)가 미주 항목에 `[미주]` 라벨을 달지만
  **FullParagraph에만** 부착 — PartialParagraph/Table/PartialTable/Shape 의 미주
  항목은 무라벨로 출력 (실전_통합_2022: 라벨 제외 후에도 max pi 996 > 본문 486).
- `tools/verify_pi_page_vs_hangul.py`는 `pi=` 정규식 전건 카운트 → rhwp 1013 vs
  한글 451 → PARA_COUNT 정렬 불가.

## 수정

1. rendering.rs `dump_page_items`: 4개 kind(PartialParagraph/Table/PartialTable/
   Shape)에도 `para_index >= body_len` 시 `[미주]` 라벨 부착 (FullParagraph와 동일 기준)
2. verify 도구: `[미주]` 라벨 행을 본문 pi 카운트/매핑에서 제외

## 검증

- 예측: 실전_통합_2022 본문 max pi 450 → rcount 451 = 한글 451 정확 일치 (확인 완료)
- 오라클 재검 5건: 실전_통합(hwp/hwpx)·endnote-01·SO-SUEOP **PARA_COUNT→MATCH**,
  편람은 off-by-one 잔존 (별축, 이슈에 기록)
- cargo test 전체 / clippy / fmt
