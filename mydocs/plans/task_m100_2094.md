# 수행·구현계획서 — Task M100 #2094: 라운드 13 (typeset_section_with_variant, goal 루프 4/4)

- 이슈: #2094 / 브랜치: `local/task2094` / 2026-07-09 / goal 루프.

## 재지도 (실측)

`typeset_section_with_variant` (1,745줄·CC 120, r12 전체 1위) — 라운드 1(#1904)에서
282→104 해체 후 재성장분(104→117→120) 트림.

| 후보 | 규모 | 판정 |
|---|---|---|
| A hwp3 vpos 리셋 판정 | 143줄·분기 61·탈출 0 | 추출 — **guard 의 is_hwp3_variant 는 caller 유지(§1)**, 반환 bool |
| C 표 없는 문단 마무리 | 104줄·분기 15·탈출 0 | 추출 — st 변이만, &mut st 메서드 |
| B 문단간 판정 (122줄·분기 45) | 내부 소스분기 산재 | **제외** — Provenance/Profile 이후 (§1) |
| E wrap_around (206줄·분기 42) | 외부 루프 continue 1곳 | **추출 (계획 수정)** — R12 early_return 전례를 continue 신호(bool 반환)로 확장 |
| D controls 루프 (176줄) | 컬렉션 변이 다수 | 제외 — 후속 |

## 컴파일러 수렴 이력

positive_vpos_end_before_negative_wrap = free fn 판명(파라미터 아님) /
page_def 누락 1건 / &mut st 코어전 1건 — 전부 빌드로 해소.

## 게이트

기존 체인. 재평가 = 표적 CC + `--snapshot r13`.
