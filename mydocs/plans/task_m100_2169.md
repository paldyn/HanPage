# 수행 계획서 — Task M100 #2169

## 개요

- **이슈**: #2169 [metrics/linebreak] 래핑 줄바꿈 지점 축 (#2148 랜딩 선결)
- **브랜치**: `local/task2169` (base: local/task2148 — width+중첩가산 실험 상태)
- **성격**: 통제 프로브 분석 → 줄바꿈 규칙 수정 → 3축(em·중첩·폭) 동시 정식화

## 단계

1. **줄바꿈 임계 사다리**: 표본 문장(80168 r4 c2 para1)의 프리픽스 사다리를
   고정 폭 셀(ls=100%)에 배치 → 한글 행높이 역산 줄수로 한글의 1→2줄 임계
   프리픽스 특정 vs rhwp `measure-width` 누적 폭 임계 → 차이를 문자 단위 특정.
2. **원인 판별**: 어절 단위 줄바꿈 / 행두·행말 금칙 / HWPUNIT 양자화 누적 /
   공백 처리 중 축 확정.
3. **수정 + 3축 동시 게이트**: em 공식 + 중첩 가산 재적용 후
   issue_1891 4문서(82/157/17/65), 21761835 세그 오라클, byeolpyo4,
   359 recount, 전체 cargo test.

## 게이트 기준

구성 매트릭스(task_m100_2148_report)의 "부분 참값 전부 실패"를 3축 동시
적용으로 뒤집는 것이 성공 조건 — 80168 = 157쪽 + per-row 정밀 동시 성립.
