# 수행·구현계획서 — Task M100 #2122: 라운드 16 (main.rs CLI 축, goal 루프 2차 3/4)

- 이슈: #2122 / 브랜치: `local/task2122` / 2026-07-09 / goal 루프 2차.

## 수행 (ir_diff 116 → 37)

1. **문단 비교부 함수 승격**: `ir_diff_paragraph_fields(pa,pb,doc_a,doc_b) -> Vec<String>`
   (188줄, 순수 생산 — diffs Vec 수집 구조라 무변경 이동). 116→89.
2. **macro 확장 인라인 제거 (핵심 발견)**: fn-지역 `emit_header!/emit_diff!` 가 호출부
   ~30곳마다 본문을 인라인 전개해 CC 를 증폭 → `IrDiffEmitter` struct(상태 5필드) +
   `header/diff` 메서드로 이관, 호출부는 `em.diff(format!(…))` 치환. 89→**37**.
   **CLI 진단 도구 계열의 표준 처방으로 기록** (dump 계열 재발 시 동일 적용).

## 동작 증거 (CLI output = 불변 원칙)

`ir-diff` 3모드(전체/--summary/--max-lines 30) 출력 **바이트 동일**
(행정업무운영 편람 hwpx↔hwp 쌍, before/after diff -q).

## §0 축소

`dump_controls`(69) 는 macro 아닌 856줄 루프 구조 + 전역 순위 하위 — 제외, 후속 인계.

## 게이트

기존 체인 전수 + 출력 동등성(위) + `--snapshot r16`.
