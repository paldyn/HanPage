# 수행계획서 — Task M100 #2067: 리팩토링 라운드 8 (layout_composed_paragraph 잔여 분해)

- 이슈: #2067 / 브랜치: `local/task2067` / 작성일: 2026-07-08
- 거버넌스: `plans/refactoring_plan_2026.md` v2 (§0 stage-gate, §1 금지 목록, §5 산식, §6 PR 규격)

## 1. 대상 현황 (r7 스냅샷 = 전체 1위)

`layout_composed_paragraph` (`src/renderer/layout/paragraph_layout.rs:1594`):
**2,093줄 · 공식 CC 146**. 라운드 2(#1925 — 마커/est/빈줄 3건)와 라운드 4(#2003 —
RunEmitState+emit_line_runs)에 이은 **3차 회전**.

사전 계측: 분기 지표 365(if 152/&& 132/=> 33), let 323. 지배 블록 =
**`for line_idx` 루프 1,641줄** (본문의 78%) — CC 밀집처. 진입부/마무리부는 소규모.

## 2. 추출 후보 (루프 내부, 위험 낮은 순)

| # | 후보 | 위치 | 규모 | 변이 표면 | 위험 |
|---|---|---|---|---|---|
| 1 | **justify 여분 간격 계산** | 2515~2606 | 92줄·분기 22 | `let (a,b,c) = if …` 표현식 블록, 변이 0·탈출 0 | 최저 |
| 2 | **조판부호 shape_markers 구성** | 2815~2877 | 63줄·분기 24 | `let v: Vec<…> = if show_ctrl` 표현식 블록, 변이 0·탈출 0 | 최저 |
| 3 | **TAC 개체 배치 블록군** | 2991~3385 (연속 4블록) | ~380줄·분기 ~69 | `line_node.children.push` + &mut 2 + **`continue` 2곳** | 중 |

- 후보 1·2는 순수 계산(입력→튜플/Vec 반환)으로 값 반환 함수 승격이 그대로 성립.
- 후보 3은 라운드 3 전례(break 28곳 전수 분류)에 따라 `continue` 2곳을 반환값
  (`enum` 또는 bool)으로 치환 후 caller에서 `continue` 유지 — **루프 제어는 caller에 남긴다**.
  4블록이 `tac_offsets_px`를 공유하므로 단계 내 소분할(2~4함수) 여부는 구현계획서에서 확정.
- 소스-포맷 분기 이산 없음(§1): 대상 구간에 hwp3/hwpx 분기 없음을 1단계에서 재확인.

## 3. 목표 (보수)

- CC **146 → 100 내외** (후보 1~3 완수 시. 미달분은 다음 회전 인계 — §0 축소 허용)
- 신규 함수는 가급적 CC≤25. TAC 군은 크기상 25 초과 가능 → 초과 시 §5 예외 심사
  기재 + 소분할 검토
- 행동 회귀 0, 예외 목록(현 87) 순증 ≤ +1~2 (과도기 허용치)

## 4. 단계 구성 (4단계)

1. **정밀 분석**: 후보 3군 의존/변이 전수 스캔 — **사각지대 체크리스트 6종** 적용
   (①포맷 문자열 ②인덱스 대입 ③클로저 리터럴 오탐 ④중첩 필드 push ⑤&mut 인자
   ⑥클로저 캡처 변이). 읽기 12↑/mut 10↑ 시 vars struct 설계 선행(§6). 구현계획서 확정.
2. **추출 1·2** (justify + shape_markers): 저위험 표현식 블록 2건 → 게이트 → 보고.
3. **추출 3** (TAC 배치 블록군): continue 치환 + (필요 시) 소분할 → 게이트 → 보고.
4. **재평가**: `--snapshot r8 --no-coverage` + 공식 CC 대비 + 최종 보고서 + devel 반영.

## 5. 게이트 (매 단계)

`cargo fmt --check` / `clippy --profile release-test --all-targets` 0 /
`--tests` FAILED 0 / OVR 5샘플 회귀 0 (00014ecf ×4 + a05e6f1b) /
표적 핀: issue_1116 13/13 · rowbreak 20/20.

## 6. 경합 주의

열린 PR #2065(planet6897)가 `layout.rs`·`table_layout.rs` 접촉 — 본 대상
`paragraph_layout.rs`와 파일 교집합 없음. devel 반영 시점에 재확인.
