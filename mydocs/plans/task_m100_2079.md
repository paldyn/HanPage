# 수행계획서 — Task M100 #2079: 리팩토링 라운드 9 (typeset_endnote_paragraphs 2차 회전 — P6 중심)

- 이슈: #2079 / 브랜치: `local/task2079` / 작성일: 2026-07-09
- 거버넌스: `plans/refactoring_plan_2026.md` v2
- **진행 방식**: 작업지시자 지시(2026-07-09)로 **goal 방식** — 단계별 승인 대기 없이
  게이트 자체 검증으로 연속 진행, 최종 일괄 보고 후 devel 반영·이슈 close 만 승인 게이트.
  단계별 완료보고서(`_stage{N}.md`)는 종전대로 각 단계 커밋과 함께 남긴다.

## 1. 대상 (r8 스냅샷 = 전체 1위)

`typeset_endnote_paragraphs` (`src/renderer/typeset.rs:4392`): **4,227줄 · 공식 CC 138**.
라운드 7(#2064 — P3 compute_endnote_metrics + P8 emit_endnote_split)에 이은 2차 회전.
라운드 7 인계 국면 지도 기준, 본론 = **P6 fit 판정 연쇄(~1,600줄)** — advance 조건
거대 불리언 연쇄로 CC 최대 지분처.

## 2. 접근 (라운드 7 인계 설계)

- **판정 함수군 분리**: P6 의 거대 불리언 연쇄를 의미 단위의 순수 판정 함수
  (불리언/enum 산출)로 절단. 각 판정은 읽기 전용 — mut 캐리는 caller 에 유지.
- P7 배치/전진(~970줄)은 실측이 허락하는 저위험 단위만 (v2 §0 규칙 3 — 축소 가능).
- 평탄 파이프라인 원칙 유지: **국면 재지도 없이는 추출하지 않는다** (라운드 7 지도는
  P3/P8 제거·r8 전진분으로 좌표가 이동 — 1단계에서 재실측).

## 3. 목표 (보수)

- CC **138 → 100 내외** (판정 함수군 분리 소득에 따라 조정, 미달분 3차 회전 인계)
- 신규 함수 가급적 CC≤25 (초과 시 §5 예외 심사 기재)
- 행동 회귀 0, CC>25 예외 목록(현 87) 순증 ≤ +1~2

## 4. 단계 구성 (4~5단계, 구현계획서에서 확정)

1. **재지도 + 정밀 분석**: P6/P7 경계 재실측, 판정 연쇄 목록화(사각지대 체크리스트
   6종), 추출 후보 확정 → 구현계획서
2~4. **판정 함수군 추출** (저위험 순, 단계당 1~3건) → 게이트 → stage 보고
5. **재평가**: `--snapshot r9 --no-coverage` + 최종 보고서 → **승인 요청** (devel 반영·close)

## 5. 게이트 (매 단계, 자체 검증)

fmt --check / clippy(release-test, all-targets) 0 / `--tests` FAILED 0 /
**issue_1116 13/13** (미주 표적) / OVR 5샘플 회귀 0 (00014ecf ×4 + a05e6f1b).
미주 배치는 쪽수 전이가 큰 영역 — 추출마다 즉시 게이트 (라운드 7 원칙).

## 6. 경합 주의

열린 PR 4건(#2073 planet6897 CellBreak, #2074/#2076/#2078 lpaiu-cs) — typeset.rs 미주
경로와 무관 추정이나 devel 반영 시 재확인. 원격 전진 시 fetch→병합→결합 재검증 후 push.
