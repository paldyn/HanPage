---
kind: plan
status: draft
canonical: mydocs/manual/codex/docs_and_git_workflow.md
last_verified: 2026-07-31
---

# Task #3236 수행계획서 — 페이지 높이를 넘는 표의 분할 미처리로 내용 소실

- Issue: [#3236](https://github.com/edwardkim/rhwp/issues/3236) (M100) — 외부 리포터 `hsKim25`
- 브랜치: `local/task3236` / 착수: 2026-07-31
- fixture: `samples/task3236/issue3236_split_table.hwpx` (리포터 첨부, 102KB)
- 정답지: `pdf/task3236/issue3236_split_table-2020.pdf` (한컴 2020 MCP 생성, `run_status 0`)

## 1. 확정된 사실 (착수 전 실측)

**첫 페이지에서 시작된 표가 페이지 높이에 도달하면 다음 페이지로 분할되어야 하는데,
분할 조각이 생성되지 않고 잘린다.** 전체 문서의 63%가 소실된다.

| 층 | 측정 | 판정 |
|---|---|---|
| 한컴 정답지 (권위) | 총 **6,651자** (p1 3,018 / p2 **3,633**) | 기준 |
| rhwp SVG | 총 **2,450자** (p1 2,067 / p2 383) | **−4,201자 (−63%)** |
| IR(파서) | `export-tables`: table0 = 1행 1열 단일 셀에 **1,471자** | 정상 — 읽기는 됨 |
| render tree p1 | `Table` 1 + `Cell` 1(자식 44) | 분할 조각 없음 |
| render tree p2 | `Cell` 6 — 이는 **다른 표**(table1, 3×2) | 이어짐 아님 |
| studio Canvas p2 | 하단 33% 잉크 **0.02%** (p1 하단 17.72% 대비) | 백지 |

즉 **파서는 읽었고 레이아웃이 버렸다.** SVG·Canvas·확장 등 백엔드 차이가 아니라
공통 상류(레이아웃)의 결함이다.

### 조사 오판 기록 (재발 방지)

초기에 "재현 안 됨"(SVG·wasm 정상) → "Canvas 백엔드 결함"(p2 384자를 정상으로 오인)으로
두 번 오판했다. 원인은 **정답지 없이 자기 산출물끼리만 비교**한 것이다. p2 에 384자가
있다는 사실만 보고, 있어야 할 3,633자와 대조하지 않았다. 정답지 PDF 를 먼저 만들었다면
첫 측정에서 잡혔다.

## 2. 유력 가설

`layout_partial_table_item`(`renderer/layout.rs:7668`)은 **행 범위**(`start_row`/`end_row`)
기반으로 분할 조각을 만든다. 이 fixture 의 문제 표는 **1행 1열 단일 셀**이라 나눌 행이
없어 분할 대상이 되지 못하는 것으로 보인다. 즉 "행 단위 분할"만 지원하고 **"셀 내부
문단 단위 분할"이 없다.**

Stage 1 에서 이 가설을 확정하거나 기각한다. 기각되면 다른 축(높이 측정, 페이지 잔여
높이 판정, `is_block_split` 경로)을 순차 확인한다.

## 3. 단계 계획

| 단계 | 내용 | 산출물 |
|---|---|---|
| Stage 1 | 근인 정밀화 — 분할 경로가 어디서 끊기는지 계측(단일 셀 vs 다행 표 대조, 높이 측정값, 분할 판정 분기). 오늘 devel 반영된 #3593·#3595(셀 앵커·중첩 표 분할)와의 관계 확인 | `mydocs/working/task_m100_3236_stage1.md` — 근인 코드 위치·기각한 가설 |
| Stage 2 | 수정 구현 + red-check(수정 전 63% 소실 재현) + 회귀 테스트 | 구현 + `tests/issue_3236_*.rs`, stage2 보고 |
| Stage 3 | 검증 — **정답지 PDF 이미지 스왑 대조**(작업지시자 지시 방법), studio CDP 잉크 재측정, release-test 전체·fmt·clippy, wasm 빌드 | stage3 보고(before/after 수치표 + 이미지 증적) |
| Stage 4 | 작업지시자 시각 판정 → 최종 보고 → PR 준비(생성은 별도 승인) | 최종 보고 + PR |

## 4. 검증 기준

1. **정답지 대비 텍스트 총량 일치** — 한컴 6,651자에 근접(허용 오차는 Stage 3 에서
   글리프·공백 처리 차이를 감안해 확정). 특히 p2 가 3,633자 수준이어야 한다.
2. 분할 조각이 render tree p2 에 실제로 생성될 것(`Cell` 노드가 table0 의 이어짐).
3. studio Canvas p2 하단 잉크가 백지(0.02%)가 아닐 것.
4. **정답지 PDF 이미지 스왑 대조** 통과.
5. 회귀: 기존 표 분할(다행 표) 무회귀 — 92셋·release-test 전체.
6. 최종: 작업지시자 시각 판정.

## 5. 위험과 제약

- 표 분할은 조판의 핵심 경로라 회귀 위험이 크다. 특히 오늘 devel 에 들어온 #3593·#3595
  수정과 상호작용할 수 있으므로 Stage 1 에서 관계를 먼저 확인한다.
- 셀 내부 문단 분할을 새로 만드는 경우 범위가 커진다 — Stage 1 결과에 따라 작업지시자께
  범위를 재확인받는다(최소 수정 vs 구조 추가).
- 리포터는 **7일째 무응답** 상태다. Stage 1 완료 시점에 재현 확인 사실을 먼저 회신한다
  (`docs_and_git_workflow.md` 의 외부 회신 의무).
- 정답지 PDF 는 `pdf/task3236/` 에 보존(50MB 미만, PR 리뷰 규칙).
