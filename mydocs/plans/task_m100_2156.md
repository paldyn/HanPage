# 수행 계획서 — Task M100 #2156

## 개요

- **이슈**: #2156 [metrics/font] 폰트 폭(래핑 줄수) 축 정합 — 한글 유효 문자폭
  통제 프로브 + 편차 정량화
- **브랜치**: `local/task2156` (base: local/task2150)
- **성격**: 측정·분석 우선 — #2150 확정 줄높이 공식의 적용 선행 조건

## 원리

#2150 공식(행높이 = n×em + (n−1)×(ls−100%)×fs + pad)을 역산하면 한글 행높이
에서 **정확한 줄수 n** 이 나온다. 고정 폭 셀에 동일 문자 반복 문자열을 넣고
반복수를 사다리로 올리면, 줄수가 바뀌는 임계 반복수에서 **한글 유효 문자폭**
이 서브픽셀 정밀도로 직독된다.

## 단계

1. **문자폭 프로브 도구**: `make_ls_ladder.py` 계열 확장 — 문자 클래스
   (한글 음절/ASCII 숫자/영문/괄호/구두점/공백 혼합) × 반복수 사다리 표 생성
   + COM 행높이 직독 → 유효 폭 역산.
2. **rhwp 대조**: 동일 문자열 `estimate_text_width` → 클래스별 편차 테이블.
3. **원인 귀속**: 폰트 폴백 메트릭/자간/장평/공백 폭 중 편차 축 식별,
   수정 후보 도출.
4. **(수정 진입 시) 게이트**: #2150 공식 적용 재실험 동시 수행(상쇄 해소 검증),
   issue_1891/1842/1623/2146, byeolpyo4, 359 recount, 전체 cargo test.

## 검증 코호트

- 21761835 (공식 적용 시 +10.3px 잔여의 래핑 축: r74 c4 3→4줄, r75 c2 9→8줄)
- 규제영향분석서 계열 (76076/80168/63607/73238/60979 — #1921 |Δ|≥3)
