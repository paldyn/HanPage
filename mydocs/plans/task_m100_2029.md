# 수행계획서 — Task M100 #2029: 1차 리팩토링 라운드 6 (layout_partial_table 해체)

- 이슈: #2029 (계획 #1883 v2, umbrella #1582, 선행 5라운드) / 브랜치: `local/task2029`
- 작성일: 2026-07-07 / 거버넌스: v2 전 조항 + 스캔 보정 절차(r4·r5 확립분)

## 1. 목표

`layout_partial_table` (`table_partial.rs:85`, **1,773줄 · CC 169 = 전체 1위**, 표 계열
첫 진입) 해체. r4 163 → r5 169 재성장분(#2016 pre_emitted 접촉 등 기능 유입)의 흡수 겸.

## 2. 정찰 결과

- **소스-포맷 분기 0** — v2 §1 안전.
- 구조: 준비부(85~441, 356줄 — 행 범위/높이 산출) → **셀 루프(442~1698, 1,257줄**,
  12-indent 상위문 55개 — 셀 geometry/배경/반복헤더 처리 + 내부 `!is_repeated_header_cell`
  블록 569줄 = 셀 문단 배치 본문) → 마무리(1699~1857, 158줄).
- 시사점: 라운드 2·4 계열의 "루프 통추출"(셀-당 처리 함수화)과 라운드 5 계열의
  "책임 절단"(준비부/마무리) 모두 적용 가능 — 의존 실측으로 확정.

## 3. 수행 단계 (관문형)

### 1단계 — 의존 정밀 실측 + 구현계획서
- 셀 루프의 셀-간 캐리(누적 y/스팬 추적 등)와 준비부 산출물 실측 (보정 스캐너:
  문자열 제거·인덱스 대입 포함·클로저 파라미터/필드 리터럴 오탐 배제).
- struct 필요성 판단(v2 §6 임계) → `task_m100_2029_impl.md` → 승인.

### 2단계 — 추출 1 (예상: 셀-당 처리 `layout_partial_table_cell` 또는 셀 문단 배치 569줄)
### 3단계 — 추출 2 (준비부/마무리 중 결합 약한 쪽)
- 각 추출 = 1 커밋, 게이트 전수 + 완료보고 → 승인.

### 4단계 — 재평가 + 최종 보고
- `--snapshot r6 --no-coverage` + CC 대비 + CC>25 추세(현 86) + 다음 라운드 제안.

## 4. 게이트 (모든 추출 공통)

fmt/clippy 0 · `--tests` FAILED 0 · **OVR 5샘플 회귀 0건** (rowbreak baseline 은
2026-07-07 현행화본 — 판정 완료로 "추가 변동" 단서 해제) · 페이지 오라클/roundtrip.
표 분할 직결 — 표적: rowbreak 계열 핀 20/20, issue_1853(분할 표)·issue_1835(TAC 표).

## 5. 위험/범위 제외

- 표 분할은 페이지 경계 계산의 핵심 — 게이트 변동 시 즉시 중단·원인 규명.
- 범위 제외: `typeset_endnote_paragraphs`(153) 내부 분해 · `layout_composed_paragraph`(146)
  잔여 · `typeset_block_table`(127) — 후속 라운드.
