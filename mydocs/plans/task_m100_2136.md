# 수행 계획서 — Task M100 #2136

**이슈**: [#2136](https://github.com/edwardkim/rhwp/issues/2136) PI TAIL_PUSH/PULL — 쪽수 동일·특정 pi부터 쪽 경계 ±1 밀림 (10k r12, 136건 최대 PI 계열)

**브랜치**: `local/task2136` (base: `local/survey10k-r12` = 전 열린 PR + #2104 재보정)

**작성일**: 2026-07-10

## 1. 문제

10k r12 PI_MISMATCH 209건 중 136건(65%)이 "쪽수는 한글과 동일한데 특정 pi부터 쪽 경계가
한 쪽 밀림/당김". PUSH(rhwp 이르게 배치) 74 vs PULL(늦게 배치) 62 — 거의 대칭이라 계통
편향이 아닌 케이스별 fit 캘리브레이션 결함 혼재로 추정.

사전 프로브 (PUSH 대표 1570100-201800041, **저장 lineseg 부재 문서**):
- p1 = TAC 표(16×7, 808.7px) + 제목 pi2(56px), used 897.8. 한글은 pi2를 다음 쪽
  표(pi3, 845.6px) 앞에 붙여 p2 상단 배치 — "표 직전 제목 문단" 결속 내지 한글 실측
  차이. 측정 기반 fit 만 있는 문서 클래스.

## 2. 단계

1. **조사 — 서브시그니처 분해**: 대표 8건(PUSH 4 + PULL 4)의 경계 pi를 dump 수치로
   분해 → {제목-표 결속, 빈 문단 경계, sa/줄간격 소비, 각주 예약, lineseg 유무} 서브
   버킷. 136건 목록에 lineseg 유무(ls=0) 플래그를 일괄 부착해 클래스 규모 정량화.
2. **구현계획서**: 최다 서브버킷 1~2개에 대한 수정 방침 확정 (승인 후 구현).
3. **구현** + 회귀 테스트/fixture.
4. **검증**: 대상 버킷 오라클 재검(FIXED 계수), 광역 359문서 재검 REGRESSED 0,
   byeolpyo4/1, 표 계열 테스트, 시각 검증.
5. **최종 보고** + PR.

## 3. 리스크

- 136건은 이질 혼합 — 한 번에 다 풀리지 않음. 서브버킷 우선순위(규모×수정 명확성)로
  절단하고 잔여는 재분류 인계 (#2093 계열 방법론).
- PULL 방향 수정은 OVER(과분할) 유발 위험 — 양방향 게이트 필수.

---

**승인 요청**: 위 수행 계획으로 진행 여부 판단을 요청합니다.
