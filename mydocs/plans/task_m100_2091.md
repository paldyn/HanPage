# 수행·구현계획서 — Task M100 #2091: 라운드 12 (layout_table_item, goal 루프 3/4)

- 이슈: #2091 / 브랜치: `local/task2091` / 2026-07-09 / goal 루프.

## 재지도 (실측)

`layout_table_item` (layout.rs:5920, 1,067줄·CC 121, r11 전체 1위). 지배 블록 =
**표 컨트롤 배치 `if let Some(Control::Table(t))` (663줄·분기 101)**.
외부 변이 4(y_offset/tac_seg_applied/para_float_lane_info + &mut 파라미터 경유) —
캐리 3은 반환 struct, **함수 조기 return 1곳**은 `early_return: Option<(f64,bool)>`
프로토콜로 caller 복원.

## 설계

`layout_table_control_block` &self 통이동: `TableControlVars`(Copy 9, §6) +
`TableControlOut`(캐리 3 + early_return). ctx(&ColumnItemCtx)는 메서드 내부 재-destructure
로 파라미터 절약. `comp` 는 지역 스코프 산물 판명 — 파라미터 아님 (컴파일러 검출).
tac_seg_applied/para_float_lane_info 초기화도 메서드로 이동 (블록 내부만 변이).

## 게이트

기존 체인 (fmt/clippy/`--tests`/issue_1116/OVR 5). 재평가 = 표적 CC + `--snapshot r12`.
