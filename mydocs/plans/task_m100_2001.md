# 수행계획서 — Task M100 #2001: 1차 리팩토링 라운드 3 (parse_paragraph_list 해체 + metrics.sh 소품)

- 이슈: #2001 (계획 #1883 v2, umbrella #1582, 선행 #1904·#1925) / 브랜치: `local/task2001` (base: `local/devel` = devel `6e819dc9`)
- 작성일: 2026-07-06
- 거버넌스: 계획 v2 (§0 stage-gate / §1 금지 목록 / §5 산식 / §6 PR 설명 3요소)

## 1. 목표

`parse_paragraph_list` (`src/parser/hwp3/mod.rs:446`, **2,270줄 · CC 234 = 현 전체 1위**)를
라운드 1·2 검증 루틴(의존 실측 → 동작 불변 추출, 1 추출 = 1 커밋)으로 해체한다.
부수로 metrics.sh 소품 3건(라운드 2 재평가 지적분)을 정비해 라운드 재평가 도구를 바로잡는다.

**위험 프로파일이 선행 라운드보다 낮음**: HWP3 파서는 `src/parser/hwp3/` 격리 영역
(CLAUDE.md — 공통 모듈 무접촉 규칙)이고, 소스-포맷 분기(v2 §1 금지 목록)와의 접촉이
구조적으로 제한적이다. 출력은 `Document` IR 하나로 수렴하므로 게이트는 파스 결과
등가성(전체 테스트 + roundtrip + 페이지 오라클)이 핵심.

## 2. 정찰 결과 (계획 근거)

구조: 문단 loop(481~2706, 함수의 98%)
- **문자 루프** 544~2013 (1,470줄) — 컨트롤 코드 `match ch` 11 arms (합 1,443줄)
  - **`_` arm 884~1999 = 1,116줄 (최대)**: GSO/개체 컨트롤 — `ch==10` 표/글상자/수식/버튼
    861줄(셀 루프 135줄 포함) + 후속 `ch==10` 블록 188줄 + 잔여
  - 소형 arm 10개 (9/18..=21/22/23/24|25/26/28/30|31/7|8/1, 합 ~330줄)
- **후처리** 2013~2706 (~690줄): line_infos 조립(빈 경우 처리 131줄 포함)·스타일 연결

## 3. 수행 단계 (관문형 — 각 단계 승인 후 다음)

### 1단계 — 도구 소품 + 의존 정밀 분석 + 구현계획서
1. **metrics.sh 소품 3건**: 요약 줄 버그(CC>25 자리에 전체 함수 수 출력) 수정, 같은 날짜
   `--snapshot` 라벨 옵션, tarpaulin skip 옵션(`--no-coverage`) — 라운드 재평가 도구를
   먼저 바로잡아 3단계 스냅샷의 신뢰를 확보. (소스 무접촉, 도구 전용 커밋)
2. `_` arm·ch==10 블록·소형 arm 군·후처리의 외부 의존(캐리오버 mut 포함)을 라운드 1·2
   방식으로 실측 → 추출 단위·시그니처 확정, `task_m100_2001_impl.md` 작성 → 승인.
   파서 상태 묶음(struct) 필요성 판단 포함 (v2 §6: 읽기 12↑ struct / mut 10↑ 설계 선행).

### 2단계 — 추출 1 (예상: ch==10 표/개체 블록 861줄 또는 `_` arm 통추출)
- 의존 실측 결과로 확정. 게이트 전수 + 커밋 + 완료보고 → 승인.

### 3단계 — 추출 2~3 (소형 arm 군 헬퍼화 / 후처리 분리)
- 게이트 전수 + 커밋 + 완료보고 → 승인.

### 4단계 — 재평가 + 최종 보고
- 대시보드 스냅샷(수정된 도구로) + CC 대비표 + **typeset.rs 재성장 정량 확인**
  (라운드 2 이후 1,555→1,677줄 관측 — 재축적 패턴 검증) + 다음 라운드 제안 + 최종 보고서.

## 4. 게이트 (모든 추출 공통)

```bash
cargo fmt --check && cargo clippy --profile release-test --all-targets   # 경고 0
cargo test --profile release-test --tests                                # FAILED 0
OVR baseline 5샘플 무변동 (00014ecf)                                      # 개체 회귀 0
```
- HWP3 특화 확인: hwp3 통합 테스트·hwp5_roundtrip_baseline(HWP3 변환 경유)·페이지 오라클.
- **주의**: rowbreak-problem-pages OVR은 #1936발 기지 이탈 3건(시각 판정 대기)이 있는
  상태 — 라운드 3 게이트는 "**추가 변동 0**"으로 판정한다 (기지 3건과 동일해야 통과).

## 5. 위험 관리 / 범위 제외

- v2 §1 금지 목록 준수: 파서 내부라 해당 분기 접촉 없음 예상 — 발견 시 caller 유지.
- 추출과 동작 수정 혼합 금지(금지 C). 파싱 등가성이 흔들리면 즉시 중단.
- 범위 제외: B 블록 `RunEmitState`(#1925 이연), `typeset_section_endnotes`, #1936 OVR
  이탈 대응(별도 시각 판정 사안).
