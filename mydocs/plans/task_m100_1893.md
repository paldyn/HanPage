# 수행 계획서 — Task #1893

**이슈**: #1893 해양경찰청 별지 서식 hwpx — IR 보존인데 라운드트립 렌더 위치 452~752px 분기
**브랜치**: `local/task1893` (origin/devel 12f7c03a 기반; 열린 PR 없음 → devel 단독 베이스)
**마일스톤**: M100
**작성일**: 2026-07-04

---

## 1. 배경

20k 라운드트립 위치 검사(2026-07-04)에서 해양경찰청 범죄수사규칙 별지 서식 hwpx 계열이
STRUCT 8 + OVER 3 (표본 257중 11). 대표 `3066571_[별지 92] 통신사실확인자료 제공요청
집행결과 보고`: 1쪽 서식, `hwpx-roundtrip` PASS(IR diff 0)인데 `render-diff --via hwpx`
STRUCT 752px.

## 2. 진단 (완료 — 이슈 코멘트 게재)

- 렌더 분기 지점: Table1/Cell5 내부, TextRun 84→86(+2), TextLine0 이동 752px.
- `dump` IR 대조 0줄 / XML 태그 카운트에서 **`<t>` 만 40→65(+25)**.
- 원본(한컴 저장)은 **자식 없는 빈 `<run/>` 33개** — rhwp 직렬화기가 이를 전부
  `<run><t></t></run>` 으로 변형 방출. (부수: ctrl/tbl 앵커 run 경계 재편, fieldBegin
  fieldid/dirty 속성 소실 — 아래 실험으로 무해 판별.)
- **판별 실험**: rt 에서 빈 `<t></t>` 34개만 제거한 패치본 vs 원본 = **0.00px 전 노드
  정합**(Δ TextRun −1만 잔존). ⇒ 근본원인 = **빈 `<t/>` 방출**, run 재편은 무해.

## 3. 수정 방향

**1순위 — 직렬화기 충실(한컴 계약 정합)**: IR 상 텍스트 세그먼트가 없는 run 은 `<t>`
원소 없이 `<run/>` 으로 방출한다. 전제 확인 필요: 원본에 빈 `<t></t>` 가 실존하는 run
9개(정당 케이스)와 "t 부재" run 33개를 IR 이 구별 표현하는지 — 구별 불가라면
record-only 마커(#1773 설계 계열) 또는 보수적 규칙(텍스트 세그먼트 0개 → t 미방출)의
영향면을 확인 후 결정.

**보조 — 파서 정규화(빈 `<t/>` ≡ 부재)**: rt 뿐 아니라 원본 렌더도 바꾸므로(TextRun −1)
영향면이 넓다. 1순위로 계열이 해소되면 착수하지 않고, 잔존 시에만 별도 검토.

## 4. 단계 개요 (구현계획서에서 상세화)

1. **IR 표현 확인**: 파서가 `<run/>` vs `<run><t/></run>` 을 IR 에서 어떻게 구별/합치는지
   + 직렬화기의 `<t>` 방출 조건 코드 특정. 빈 `<t/>` 가 재파스 조판을 바꾸는 정확한
   메커니즘(run 경계→compose 입력) 1건 트레이스.
2. **직렬화기 수정**: 빈 run 에 `<t>` 미방출(구별 가능 시 원형 보존). 최소 diff.
3. **fixture 검증**: 3066571 render-diff 752→~0px. 해경 계열 11건 전수 재검.
4. **회귀 게이트**: `cargo test`(hwpx_roundtrip_baseline 포함), samples/hwpx 전수
   roundtrip, big_hwpx 2,500 render-diff, 20k 표본 중 hwpx 축 재검(개선 확인).
5. **fixture 동봉 + 보고서**: 재현 hwpx 를 samples/ 에, 필요 시 정답지 확보 불가하므로
   roundtrip 자기정합 테스트로 핀 고정. 최종 보고서.

## 5. 리스크

- 직렬화기 `<t>` 방출 규칙은 전 hwpx 저장에 영향 — hwpx_roundtrip_baseline(구조 뼈대
  게이트)과 저장 계약 테스트로 전수 확인 필수.
- 빈 `<t></t>` 실존 케이스(본 문서 9개)의 왕복 보존이 깨지면 역방향 분기 발생 —
  Stage 1 의 IR 표현 확인이 관건.

---

**승인 요청**: 위 진단과 수정 방향(직렬화기 충실 1순위)으로 진행 여부 확인 부탁드립니다.
