# 수행·구현계획서 — Task M100 #2173: advance_row 계열 워크 통합 (2차 R2)

- 이슈: #2173 (#2131 서브) / 브랜치: `local/task2173` / 2026-07-10 / goal 방식.
- v2.1 선평가: 공유부 128줄·분기 35(밀도 0.27) — **기대 CC 총량 −15~25 명시 착수**.

## 분석

- `advance_row_cut`(194) ↔ `advance_row_block_cut`(152): 유사도 0.74. drift =
  C 전용(CUT_DBG 덤프 / rowspan-cover orphan 게이트 / #1658 tiny_fragment) +
  **orphan rewind 호출 방식**(C: 조건부 force=true / A: 무조건 force=false —
  rewind(false)가 no-op 이 아님을 확인, Option<bool> 정책으로 정확 재현).
- `_with_row_offsets`(80)는 정책 이산 진화 — 범위 외.
- 차순위 실사 병기: shape_props 쌍 유사도 0.05(함수 내 반복 유형 — 위임 불가),
  diff_page_def CC 13(스캐너 ② 과대평가 — 보정 소재).

## 구현

`advance_cells_cut(&self, table, cells, start_cut, avail_height, styles, policy)` +
`RowCutPolicy { hard_break_orphan, tiny_fragment_enabled, debug_row, orphan_force: Option<bool> }`
— C 본문 기반 1벌화, 두 원함수는 셀 수집 + 정책 구성 wrapper (각 ~25줄).

## 게이트

1차 체인 전수 + rowbreak 표적 5핀(1156/1488/1748/1937/2015 — 선행 통과 10/10) +
metrics `--snapshot c2r2 --diff c2r1` 총량 판정.
