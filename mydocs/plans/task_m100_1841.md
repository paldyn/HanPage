# Task #1841 수행 계획 — 자리차지 표 outer_margin bottom 재개 갭

## 배경 (이슈 #1841, visual sweep 저일치 검토에서 등록)

최근 2일 closed PR 의 visual sweep 저일치율 문서 재검(통합 스택 재측정에도 미개선)에서
지배 축으로 특정: 자리차지(TopAndBottom, 비-TAC) 표 직후 host 본문 재개가 표의
`outer_margin_bottom` 만큼 위에 붙는다. 결재문서 헤더 표(852HU=8.5pt) 계열이 코퍼스에
대량 존재.

실측 (동작소방서 36385142, 한글 2022 PDF 대조):
- 헤더 표 하단 경계 rhwp 224.6 = 한글 224.3pt (정합)
- 표 하단→'1. 관련근거' gap: rhwp 10.2pt vs 한글 18.7pt — Δ8.5pt = outer bottom 852HU

## 원인 경로

1. `typeset.rs format_table`: `outer_top/outer_bottom` 이 `is_tac` 게이트로 비-TAC 에서
   0 강제 — 바로 아래 `before` 식이 비-TAC wrap=1 에 `outer_top` 을 쓰는 의도와 모순
   (pagination 북키핑 축).
2. `layout.rs` 표-in-문단 렌더: visible-host float 재개 y 가
   `table_visual_end (+inter_float_gap)` 로 outer bottom 미포함 (실제 렌더 좌표 축 —
   RHWP_TABLE_DRIFT 트레이스로 table_y_end=299.5 직후 다음 줄 시작 확인).

## 수정

1. typeset `format_table`: is_tac 게이트 제거 — outer_margin top/bottom 을 소스·TAC
   무관 적용 (host_spacing.after 에 outer_bottom 포함)
2. layout 재개 y: `visible_outer_bottom_px` 를 visible-host float 의 비양수 offset
   두 분기(hwpx: `table_visual_end+inter_float_gap+ob`, 그 외:
   `table_y_before.max(table_visual_end+ob)`)에 가산. 양수 offset 분기(part-3 host
   title 경로)는 불변.

## 검증 계획

- 케이스: 동작 36385142 p1 baseline median −8.45→±0.1pt, proxy 13.8→27.8% /
  관악 36389312 median −8.95→−0.4pt
- cargo test --release 전수 (svg 골든 — 특히 #874 aift 계열)
- big_hwpx/big_hwp 2,500×2 render-diff 배치 회귀 0
- 잔여 축 분리 기록: 하단 결재부 −34pt 는 표 높이 누적(#1759/#1763 트랙), 셀 내부
  '****' 국소는 셀 콘텐츠 축 — 본 태스크 비범위
