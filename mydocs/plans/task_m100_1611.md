# 수행계획서 — Task #1611

**제목**: 렌더링 −1쪽 갭 요인 B — footer(발신명의) PAGE-앵커 블록 page-fit 누적 ~60px 과소
**마일스톤**: M100 · **이슈**: edwardkim/rhwp#1611 · **브랜치**: `local/task1611` (base: `local/task1608`, #1608 반영)
**전사**: Task #1600(−1쪽 갭) 다요인 중 요인 B. 요인 A(`is_hwp3_origin`)는 #1608에서 해소.
상세: `mydocs/tech/investigations/issue-1600/render_minus1_page_gap.md`

## 1. 배경·증상 (대표 36387725, #1608 반영 tolerance=0)

- 한글 정답지: **2쪽** (page1 본문 / page2 발신명의 블록 단독, 권위 PDF y=581~783pt).
- rhwp: **1쪽** (발신명의를 본문 직후 inline 흡수).

발신명의 블록(`dump -s0 -p11`): 마지막 `Table 1x1`, `field="발신명의"`,
**`vert_rel_to=PAGE`(쪽, offset 0.0mm), `valign=Bottom`, `wrap=자리차지`**, 선언 351.4px(26353 HU).
rhwp 배치: vpos=48053(≈640.7px), 본문 pi=10 직후 inline.

## 2. 핵심 모순 (조사 진입점)

`dump-pages` page1 (body h=990.2px):
- footer top vpos 640.7px + 351.4 = **≈992.1px > 990.2px** → 분할되어야 함.
- 그러나 rhwp `used=929.8px` (높이 단순합 979.0px, vpos하단 992.1px 와 **모두 불일치**).
- → rhwp page-fit 누적 높이가 vpos 기준보다 ~60px 과소 → 990.2 안으로 잘못 들어가 1쪽.

세 수치(used 929.8 / 합 979.0 / vpos하단 992.1)의 출처를 분해하는 것이 1차 과제.

## 3. 가설 (Stage 1 확정 대상)

| # | 가설 | 검증 |
|---|------|------|
| H1 | PAGE-앵커+valign=Bottom 블록을 rhwp가 inline 으로 강등 → 한컴은 별쪽 앵커 | dump vpos vs 권위 PDF 위치, 코드 경로 |
| H2 | page-fit 임계가 vpos하단(992.1)이 아닌 `used`(929.8) 기준 → ~60px 과소 | typeset page-fit 계산부 추적 |
| H3 | 본문 inter-element gap 누적이 한글보다 작음 | per-item vpos vs 누적합 비교 |

요인 A 와 달리 본문 per-line 은 저장 LINE_SEG(한글 동일)이므로 H1/H2(footer 앵커·임계)가 유력.

## 4. 방법론 (Task #1600 자산 재사용)

1. 고정 −1쪽 셋: 통제셋 잔여 21건(`output/poc/task1608_after.tsv` delta=-1).
2. 대표 36387725 에서 `dump-pages`/`dump` + 코드 추적으로 `used` vs vpos하단 ~60px 출처 분해.
3. 권위 PDF(page2 footer)와 비교해 한컴의 footer 앵커 규칙 역추적.
4. 단일 메커니즘 확정 후 정합 수정 → 통제셋 게이트(해소−회귀>0).

## 5. 구현 단계 (조사 우선, 4단계)

- **Stage 1 — 근본 메커니즘 확정** (코드 수정 없음): used/합/vpos 3수치 분해 + H1~H3 판별 +
  권위 PDF 대조. 보고 후 승인. (단일 surgical fix 가능성 판정 — 불가 시 범위 재조정 보고.)
- **Stage 2 — RED**: 36387725 등 대표 케이스 페이지수 가드(현재 1쪽 → 기대 2쪽) + 메커니즘
  단위 테스트.
- **Stage 3 — 정합 수정**: 확정 메커니즘(footer 앵커/page-fit 임계) 한컴 정합. Stage2 GREEN.
- **Stage 4 — 게이트 검증**: 통제셋 net>0 + hwpx baseline + visual_roundtrip + #1589 붕괴 0 +
  lib/clippy. before/after tsv.

## 6. 회귀 위험 (매우 높음)

footer 앵커/page-fit 임계는 **전 코퍼스 페이지네이션 영향**. +1쪽/붕괴(#1589) 양방향 회귀
위험. 통제셋 게이트(개선−회귀>0)가 필수. Stage 1 에서 single-fix 불가 판정 시 부분 정합 또는
조사 보존으로 범위 재조정.

## 7. 산출물
- 조사: `mydocs/tech/investigations/issue-1600/render_minus1_page_gap.md` 갱신(요인 B 메커니즘)
- 소스: `src/renderer/typeset.rs` 등 (Stage 1 확정 후 범위 결정 — HWP3 전용 분기 금지)
- 테스트: 대표 케이스 페이지수 가드 + 메커니즘 단위
- 측정: `output/poc/task1611_before.tsv`, `_after.tsv`
- 문서: `_impl`, `_stage1~4`, `_report`
