# Task #1591 v2 수행계획서 — first-para mismatch-path 슬롯 위치추정 통합 정합 (#1593 포함)

## 배경

fidelity 잔여 IR_DIFF Class C(3건: 36384689·36385445 **+8**, 36388711 **−16/−8**).

- **1라운드(#1591) 결과**: 북마크 hoist 를 원인으로 지목해 슬롯 편입 수정 →
  컨트롤 순서는 교정됐으나 char_shape +8 미해소, 통제 비교(fidelity12→13, 공통
  10150) 순효과 0 → **불채택·전량 롤백** (`mydocs/report/task_m100_1591_report.md`).
- **재규명된 진짜 근본**: para0(첫 문단)은 #1584 ColumnDef 템플릿 흡수로 첫
  ColumnDef 가 `slots` 에서 제외되는데 `inferred_control_slot_count`(cc 기반)와
  어긋나(4≠3) **mismatch 경로**(section.rs:536)로 빠진다. 이 경로는 슬롯의 실제
  char-offset 위치를 추정하지 못해 "텍스트 전체 → 슬롯 일괄 말미 방출"로 char_shape
  경계를 오배치한다.
- **#1593(36388711)**: same-para 균형 필드의 fieldEnd 드롭(cc −8) + 북마크 hoist 결합.
  fieldEnd 드롭은 북마크와 독립. #1593 은 "단일 파일용 고위험 수정 대신 **F3/mismatch-path
  슬롯 위치추정 통합 리팩터(#1591 재범위 포함)**로 묶어 처리"를 권고 → 본 타스크가 그것.

## 범위

`src/serializer/hwpx/section.rs` 슬롯 방출 계통 (파서는 정상 확인됨):

1. **mismatch 경로의 위치추정 결함** — slot_count(cc 추정) ≠ slots.len()(템플릿 흡수
   제외) 불일치 시에도 슬롯의 char-offset 위치를 보존하도록 정합. 후보 방향
   (Stage 1 에서 확정):
   - (a) 템플릿 흡수를 slot_count 추정에도 일관 반영해 mismatch 자체를 해소
     (첫 ColumnDef 를 카운트·슬롯 양쪽에서 동일하게 취급), 또는
   - (b) 슬롯 위치를 cc 추정이 아니라 `char_offsets`/컨트롤 증거 기반으로 직접 산출해
     mismatch 경로를 메인 경로로 흡수.
2. **북마크 hoist 제거 편입** — 1라운드 Stage 2 교정(순서 보존, 회귀 0 확인됨) 재활용,
   `#[ignore]` RED 테스트(`task1591_bookmark_not_hoisted_before_slot`) 해제.
3. **same-para fieldEnd 드롭(#1593)** — 균형 필드(1/1)의 닫는 fieldEnd 가 방출 경로에서
   소실되는 지점 특정·수정 (F3 #1561 의 cross-para orphan 과 다른 변종).

## 회귀 위험 (높음 — F3급)

- F3(#1561)는 이 영역에서 **2회 회귀로 실패**, 1라운드 #1591 도 순효과 0 불채택.
- 채택 기준: **통제 비교 순효과>0 + 악화 0** (부분 교정 불채택, 악화 시 전량 롤백).
  조사 결과 광역 회귀 불가피로 판명되면 불채택·문서화 종료 옵션 유지.

## 단계 개요 (4단계, 구현계획서에서 상세화)

1. **근본 정밀 규명 + RED**: 36384689 의 mismatch 경로 char-offset 추정 실패 지점 특정
   (cc=33, controls=5, char_offsets 매핑 추적), 36388711 fieldEnd 드롭 경로 특정,
   수정 방향 (a)/(b) 확정. 타깃 3건 최소 재현 RED 테스트.
2. **구현**: 확정 방향 구현 + 북마크 hoist 편입 + fieldEnd 변종 수정.
   게이트: RED→GREEN, `cargo test --test hwpx_roundtrip_baseline` 회귀 0, `--lib` 회귀 0.
3. **통제 비교 (채택 게이트)**: hwpdocs 전수 `rhwp hwpx-roundtrip --batch`
   (공통 ~10150) 전/후 비교 — 타깃 3건 IR diff=0, 개선−회귀>0, 악화 0.
   악화 ≥1 시 전량 롤백.
4. **가드·최종 보고**: 단위 테스트 편입(가능 시 opengov 동형 가드), `_report.md`.

## 수용 기준 (이슈 #1591·#1593 통합)

1. 36384689·36385445 char_shape +8 해소 (IR diff=0).
2. 36388711 fieldEnd 드롭(cc −8) 해소 — 북마크 hoist 편입과 합쳐 IR diff=0 목표.
3. `hwpx_roundtrip_baseline` + `cargo test --lib` 회귀 0.
4. fidelity 통제 비교 순효과>0, 악화 0.

## 작업 브랜치

- `local/task1591-v2` — `local/task1748`(upstream/devel + 제출 열린 PR 19건 + #1748)에서 분기.
- 1라운드 산출물(`local/task1591` 의 조사 문서·RED 테스트)은 참조·재활용.
- 완료 시 upstream/devel 기준 1커밋 재구성 → `pr/devel-1591` push → PR
  (검증 스크립트·재현 파일 포함 규칙 준수).
