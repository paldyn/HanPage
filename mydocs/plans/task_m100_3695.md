# 수행계획서 — task_m100_3695

- **이슈**: #3695 `[export-structure] auto·outline confidence와 HeadType::Number 선택 정책 (#1528-2)`
- **상위 이슈**: #1528
- **선행 작업**: #3693, PR #3715, merge commit `fe9749d542f46643e408c23878229c326e341363`
- **브랜치**: `codex/issue-3695-export-structure-auto`
- **분기 기준**: `codex/issue-3693-export-structure-clause` `652e2ee27`
- **마일스톤**: M100 (`v1.0.0`)
- **절차 상태**: Stage 3 최신 `devel` 통합·PR 전 전체 검증 완료
- **WIP 증적**: `8343c98c6` — 삭제·수정하지 않고 사전 승인 draft로 보존
- **계획 승인**: 2026-08-01 — WIP 독립 대조·focused 검증·새 단계 보고서 작성 승인
- **Stage 2 판정**: 2026-08-01 — `8343c98c6`을 승인된 #3695 구현으로 채택
- **Stage 3 기준**: 2026-08-02 — `upstream/devel` `fe9749d54`, #3715 merge 결과와 통합
- **다음 승인 경계**: 원격 push·PR 생성; #1528 최종 검증·종료는 #3695·#3744 통합 뒤 진행

## 0. 절차 복구와 선행 게이트

전체 작업 순서 승인을 #3695 수행·구현 계획의 승인으로 잘못 해석했다. #3693 완료 코멘트 직후
별도 계획 검토 없이 stacked 브랜치에서 구현·테스트·보고를 진행했고, 모든 산출물을 한 커밋에 묶었다.

- #3695는 #1528의 두 번째 GitHub native sub-issue로 연결했다.
- 기존 커밋 `8343c98c6`은 승인된 구현이 아니라 검토 대상 WIP draft다.
- #3693은 독립 대조·focused 검증 뒤 WIP 채택 승인을 받아 선행 게이트를 완료했다.
- 작업지시자가 2026-08-01 #3695 정정 계획을 승인하고 WIP 독립 대조·focused 검증·단계 보고를 지시했다.
- 이번 단계에서는 기존 draft를 계획과 독립 대조하고 채택·수정·폐기 권고를 작성한다.
- 수정 필요 사항을 발견해도 소스를 고치지 않고 단계 보고서에 기록한 뒤 다음 승인을 요청한다.
- 판정과 검증을 새 단계 보고서에 기록한 뒤 GitHub 코멘트, push, PR 없이 중지한다.
- 작업지시자가 Stage 2 결과를 검토하고 `8343c98c6`의 구현 채택을 승인했다.

아래 목표·설계·검증 항목은 승인된 #3695 WIP 검토 기준이다. 기존 코드가 있다는 사실만으로 구현
채택을 확정하지 않고, 독립 대조 결과를 다음 승인 지점에 제시한다.

## 1. 목표

`export-structure --mode auto`가 `HeadType::Outline`과 `HeadType::Number`를 같은 확신도로 취급해,
일반 자동번호 문단 하나가 섞인 조문 문서 전체를 outline으로 뒤집는 문제를 보정한다. 명시적 mode와
순수 개요 문서의 결과는 유지하면서 문서 전체 증거의 우선순위를 결정적으로 정의한다.

## 2. 최신 기준선

- 현재 `has_outline()`은 참조된 para shape 중 `Outline|Number`가 하나라도 있으면 outline을 선택한다.
- synthetic 조문 문서에 `HeadType::Number` 문단 하나를 넣으면 `제1조`가 사라지고 mode가 outline이 된다.
- 저장소 668개 sample을 읽기 전용으로 조사한 결과, `HeadType::Number` 한 개만으로도 유효한 제목인
  문서가 있어 단순 개수 임계값은 사용할 수 없다.
- `samples/추진일정.hwp`는 Number 1개, `samples/biz_plan.hwp`는 Number 20개를 실제 개요로 쓴다.
- `samples/hwpctl_API_v2.4.hwp`는 명시적 Outline 155개를 사용한다.

## 3. 설계 결정

문서 전체를 스캔해 다음 순서로 effective mode를 선택한다.

1. 참조된 `HeadType::Outline`이 하나라도 있으면 outline을 선택한다.
2. 명시적 Outline이 없고 편·장·절·관·조 marker가 하나라도 있으면 clause를 선택한다.
3. 위 증거가 없고 `HeadType::Number`가 하나라도 있으면 outline을 선택한다.
4. 어느 증거도 없으면 기존처럼 clause를 선택한다.

`항`·`호`·`목` 모양은 일반 번호 목록과 겹치므로 auto 선택의 강한 clause 증거에서 제외한다.
명시적 `--mode outline|clause`는 selector를 거치지 않아 기존 동작을 유지한다.

## 4. 작업 단계

### 선행 게이트 — #3693 승인 단계 완료 (완료)

- #3693 정정 계획 승인, 독립 대조, focused 검증, WIP 채택 판정을 완료했다.
- 승인된 #3693 결과를 이 브랜치에 merge해 기존 #3695 WIP 해시는 재작성하지 않았다.

### 복구 게이트 — #3695 정정 계획 검토 (완료)

- 2026-08-01 본 수행계획서와 구현계획서의 복구 방향을 별도로 승인받았다.
- 승인 범위는 WIP 독립 대조, focused 검증, 새 단계 보고서 작성까지다.

### Stage 1 — red 기준과 selector 구현 (WIP 채택 승인으로 완료)

- Number 하나가 섞인 조문 synthetic 문서에서 기존 auto가 outline을 고르는 실패를 고정한다.
- `select_auto_mode()`로 증거 수집과 우선순위를 한곳에 정의한다.
- explicit mode, 명시적 Outline, Number-only, pure clause 경계를 단위·통합 테스트로 고정한다.

### Stage 2 — 실문서·계약 검증과 문서화 (완료·승인)

- 명시적 Outline, 복수 Number, 단일 Number 실제 sample의 결과를 고정한다.
- #3693 clause 회귀와 기존 CLI JSON 계약을 함께 확인한다.
- CLI 매뉴얼에 실제 auto 우선순위를 기록한다.
- fmt, clippy, diff 검사 뒤 단계·최종 보고서를 작성하고 커밋한다.

### Stage 3 — 최신 devel 통합과 PR 전 전체 검증 (완료)

- WIP 감사 이력을 재작성하지 않고 `upstream/devel` `fe9749d54`를 merge한다.
- #3715의 clause marker·문맥 변경과 #3695 selector가 함께 보존되는지 net diff를 독립 대조한다.
- focused 4종, 전체 release-test, fmt, diff check, clippy를 순차 실행한다.
- 결과를 `mydocs/working/task_m100_3695_stage3.md`와 최종 보고서에 기록하고 로컬 merge commit으로 고정한다.
- 원격 push와 PR 생성은 별도 승인 전까지 수행하지 않는다.

## 5. 비범위

- `classify_outline()`의 explicit outline 동작과 JSON 필드·봉투는 변경하지 않는다.
- #3693의 marker·clause stack 정책은 변경하지 않는다.
- 파서·렌더러·레이아웃·직렬화 코드는 변경하지 않는다.
- 새 binary fixture나 golden/baseline은 추가하지 않는다.

## 6. 검증

- `CARGO_INCREMENTAL=0 cargo test --lib document_core::queries::structure -- --nocapture`
- `CARGO_INCREMENTAL=0 cargo test --test issue_3695_structure_auto_policy -- --nocapture`
- `CARGO_INCREMENTAL=0 cargo test --test issue_3693_structure_clause_context -- --nocapture`
- `CARGO_INCREMENTAL=0 cargo test --test cli_json_contract export_structure_ -- --nocapture`
- `CARGO_INCREMENTAL=0 cargo test --profile release-test --tests`
- `CARGO_INCREMENTAL=0 cargo fmt --check`
- `git diff --check`
- `CARGO_INCREMENTAL=0 cargo clippy --all-targets -- -D warnings`

Stage 3에서 PR 전체 검증 승인을 받아 release-test까지 완료했다. 원격 push와 PR 생성은 다음 별도 승인
게이트를 따른다.

## 7. 완료 기준

- pure outline과 pure clause 선택 유지
- Number 하나가 섞인 조문 문서의 clause 구조 보존
- 단일·복수 Number-only 실문서의 outline 결과 보존
- explicit outline/clause 동작과 공개 JSON shape 불변
- focused·전체 release-test 검증 통과와 결과 문서화
