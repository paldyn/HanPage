# Task #1760 수행계획서 — 표 렌더 높이 계통 과대 조사·수정

## 배경
#1759 1차 서베이에서 확정한 razor-thin 지배 원인 후보: rhwp 표 높이가 한글 대비
+25:-1 편향. 두 계열 관찰 — (a) **상수 계열** +0.6~0.8px/표(크기 무관),
(b) **행비례 계열** +5~11px (12×16 +7.2 ≈ +0.45px/행, 12×4 +5.2 ≈ +0.43px/행,
20×11 +10.9 ≈ +0.55px/행).

## 조사 가설 (Stage 1 에서 판별)
- (a) 상수 계열: **측정 아티팩트 의심** — find_tables bbox 는 테두리 선 중심 기반,
  rhwp render-tree bbox 는 레이아웃 박스(획 폭 ~0.5px 의 절반×상하) — 실 결함 아닐 수 있음.
- (b) 행비례 계열: **행 단위 +0.4~0.55px 실 드리프트 의심** — 행높이 HWPUNIT→px 환산
  반올림 또는 cell_spacing/min-height 계열. razor 경계 표(36388181 +1.6px = 9행 × ~0.2?)
  포함 여부 확인.

## 절차
1. **행 단위 분해**(조사): 대표 표(17931383 12×4 +5.2 / 2501937 12×16 +7.2 /
   36388181 9×6 +1.6 / 상수 계열 34×5 +0.8)의 rhwp 행 경계(render-tree 셀 bbox 또는
   MeasuredTable) vs 한글 find_tables 셀 행 경계 대조 → 상수/행비례/특정행 분리.
2. 원인 코드 특정(측정·환산 지점) → 수정 + 단위테스트.
3. 검증: #1759 하니스 재실행(dh 분포 0 수렴), 페이지·PI 게이트 전체, 한글 OLE 대조 배치.

## 게이트
1. 대표 표들의 dh 가 ±0.5px 이내로 수렴 (아티팩트 판정 시 하니스 보정 후 기준 재설정).
2. `cargo test --lib` + 통합 + 페이지 게이트 9종 무회귀.
3. hwpdocs mismatch 잔여 + MATCH 표본 150건 — 악화 0 (개선 기대: razor S1/S2 일부).

## 한계
- (a)가 아티팩트로 판정되면 수정 범위는 (b) 행비례 계열 + 하니스 보정으로 축소될 수 있음
  — Stage 1 판별 결과에 따라 구현계획 조정(단계 보고서에 기록).
