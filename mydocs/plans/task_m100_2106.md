# 수행·구현계획서 — Task M100 #2106: 라운드 14 (typeset_endnote_paragraphs 3차, goal 루프 2차 1/4)

- 이슈: #2106 / 브랜치: `local/task2106` / 2026-07-09 / goal 루프 2차.

## 재지도 결과 — §0 축소 판단

P7(배치/전진)·P2·P4 재실측: **전 구획 평탄** (최대 블록 28줄) — 라운드 7·9의 회전으로
대형 지렛대 소진. 계획했던 "P7 분해"는 소득 미달로 축소하고, **잔여 판정 체인 12건
일괄 상환**(R9 judge_* 패턴)으로 대체. 이 함수는 이번 회전으로 **분해 포화** 선언 —
잔여 CC 는 aggregator·shadow 파이프라인(split_endnote_to_fit ×3)·소스분기 인접 구간으로,
구조 재설계(국면 메가분할)는 Provenance/Profile 이후 영역.

## 추출 12건 (전부 읽기 전용, 단일 호출부)

compact_endnote_own_vpos_span_fits / internal_rewind_full_advance_needed /
no_separator_tail_table_starts_next_column / large_between_equation_tail_starts_next_column /
large_between_last_column_flow_tail_split(Option<usize>) / allow_compact_question_title_tail /
large_between_last_column_{render,rewind}_title_tail_fits / zero_question_title_tail_fits_by_line_height /
zero_between_large_separator_tail_group_fits / zero_visible_text_tail_before_rewind_fits /
pre_emit_tail_before_non_tac_object_advance

- 자동 reads→파라미터 생성 + 컴파일러 수렴(E0425 15건·E0308 2건·E0599 1건 — ret 타입
  Option<usize> 1건 정정 포함).

## 게이트

기존 체인 전수 + 표적 CC + `--snapshot r14`.
