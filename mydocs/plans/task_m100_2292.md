# 수행계획서 — Task M100 #2292: skia 차트(RawSvg) PNG 잘림 정정

- 이슈: #2292 / 브랜치: `local/task2292` / 작성일: 2026-07-15
- 근인 (등록 시 확정): `src/renderer/skia/image_conv.rs:353`
  `rasterize_svg_fragment_to_png` 이 조각을 로컬 좌표로 가정
  (`viewBox="0 0 w h"`)하나, 차트 RawSvg 조각은 **페이지 절대 좌표**로
  방출 — viewBox 창 밖 콘텐츠 전부 클리핑. 4-backend 중 skia 만 계약 이탈
  (SVG 직접 삽입·web_canvas 정상 실측).

## 절차 (버그 정정형 — 구현계획서 생략, 정정→게이트→판정)

### 1. 표적 테스트 신설 + 수정 전 FAILED 실증

- `tests/issue_2292_chart_png_clip.rs` (native-skia feature 게이트):
  차트 샘플(표식이있는꺽은선형 + 고가저가종가)을 skia 래스터로 렌더 →
  **차트 bbox 우하단 사분면의 잉크 존재** 단언 (현재는 좌상단 조각만
  그려져 우하단이 공백 — 수정 전 FAILED).
- SVG 렌더와의 잉크 분포 상관은 과설계 — 사분면 존재 검증으로 충분.

### 2. 최소 정정

- `draw_svg_fragment` 에 조각 원점(src_x, src_y) 전달, wrapper 를
  `viewBox="{src_x} {src_y} {w} {h}"` 로 — 조각의 페이지 좌표 중 bbox
  창만 래스터. 호출부(PaintOp::RawSvg)는 bbox.x/y 를 이미 보유.
- **이슈 확인 항목 처리**: ①paint/json 백엔드 RawSvg 계약 점검(기록)
  ②PDF(svg2pdf, SVG 경유) 무영향 확인 ③차트 외 RawSvg 사용처 동계약 확인.

### 3. 게이트 + 판정

- 전수 `--tests --no-fail-fast` + `--features native-skia` 스위트 +
  fmt/clippy.
- OVR 5샘플 (skia 렌더 접촉 — 무회귀 확인).
- 시각 판정 자료: `output/png/chart_check/` 5종 before/after 재생성 →
  **작업지시자 판정** (정답지: 한컴 2022 PDF `pdf/chart/**`).

## 위험/주의

- viewBox 원점 변경은 RawSvg 경로 전체에 적용 — 차트 외 사용처(OLE 등)가
  로컬 좌표 조각을 방출한다면 회귀. 사용처 전수 확인을 정정 전에 수행
  (2단계 ③을 선행 조건으로).
- MAX_SVG_RASTER_PIXELS 등 기존 방어 한계 불변 유지.
