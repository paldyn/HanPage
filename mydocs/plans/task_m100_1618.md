# 수행계획서 — Task #1618

**제목**: 경로 A 1단계 — LINE_SEG vpos-reset 신뢰도 전수 분석
**마일스톤**: M100 · **이슈**: edwardkim/rhwp#1618 · **브랜치**: `local/task1618` (base: `local/task1616`)
**전사**: 잔여 −1쪽 근본 해소 경로 A(vpos-추종 페이지네이션)의 타당성 전제 분석.

## 1. 목표
LINE_SEG `vertical_pos` 역행 점프(=한글 페이지/단 경계)가 한글 페이지수를 얼마나 신뢰성 있게
예측하는지 측정. vpos-예측 vs 한글(통제셋) vs rhwp(전 코퍼스) 비교 + 오탐/과소 유형 분류.

## 2. 방법
- 분석기 `examples/vpos_reset_analyze.rs`: flow 순서 line_seg vpos 역행 점프(>2000HU 감소) 카운트
  → vpos-예측 페이지 = 역행 점프 + 1. 빈 세그먼트(tag bit16) 제외. 표/대형표 카운트.
- 통제셋(92, 한글 정답) join → vpos-예측·rhwp 정합률.
- 전 코퍼스(16.6k, rhwp 기준) → 분기율 + big_table 상관.

## 3. 산출 기준
- vpos-예측이 한글에 정합(+표 보정 가능) → 경로 A 2단계 근거.
- 과소/오탐 지배 → 경로 A 재설계 또는 한계 재확정.

## 4. 단계
- Stage 1: 분석기 + 신뢰도 측정 + 판정(본 태스크). 조사 전용(파이프라인 무영향).

## 5. 산출물
- `examples/vpos_reset_analyze.rs`, `output/poc/task1618_*.tsv`, `_stage1`, `_report`.
