# Task #1725 수행계획서 — 흐름 텍스트 각주 tail 문단 over-pagination 수정

## 배경
hwpdocs 페이지·PI 비교에서 발견된 over-pagination |Δ|≥2 케이스. 대표:
`[별표 1] 국제고속선기준(고속선 기준).hwpx` — rhwp 258쪽 vs 한글 242쪽 (Δ=+16). #1718의 정반대 방향.

## 원인 (empirical 규명)
- 흐름 텍스트 법령(각주 90개). rhwp 가 26개 near-empty 페이지(문단 1개만) 양산.
- 디버그 추적(pi=52): `cur_h=921.6 > avail=917.8 → fits=false → advance`.
  avail 이 정상 1005 → 917.8 로 축소(≈87px) — **각주 공간 예약**(`current_footnote_height` +
  `footnote_safety_margin=40px` 버퍼) 때문. tail 문단(pi=52)이 3.8px 초과로 다음 페이지로 밀림.
- 다음 문단(pi=53)이 vpos-reset(새 페이지)이라 pi=52 가 단독 near-empty 페이지에 격리.
- 한글 LINESEG vertpos=69120 → 한글은 pi=52 를 본문(각주 위)에 배치. rhwp 각주 버퍼가 밀어냄.

## 수정 방향
Task #359 `next_will_vpos_reset` 가드의 일반텍스트 분기에서, tail 문단에 한해 **각주 안전마진(40px
보수 버퍼)을 1회 되돌려** 본문에 유지. 실제 각주 높이는 유지(겹침 위험 최소). `src/renderer/typeset.rs`.

## 회귀 게이트
1. 국제고속선기준 258 → 감소(목표 근접).
2. byeolpyo1(4)·byeolpyo4(26)·승강기(42, #1718) 무회귀.
3. `cargo test` 무회귀.

## 한계
40px 버퍼로 잡히는 각주-tail 케이스만 수정. 잔여 near-empty(PartialParagraph/PartialTable 격리,
비각주 tail 등 다중 원인)는 별도 후속. #1718 처럼 razor-thin 다중원인 부분 개선.
