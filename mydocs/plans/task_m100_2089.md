# 수행·구현계획서 — Task M100 #2089: 라운드 11 (layout_table_cells, goal 루프 2/4)

- 이슈: #2089 / 브랜치: `local/task2089` / 2026-07-09 / goal 루프.

## 재지도 (실측)

`layout_table_cells` (table_layout.rs:2379, 1,624줄·CC 124). 셀 루프 내부가
세로쓰기(22줄, `layout_vertical_cell_text` 위임) / **가로쓰기 else(2724~3951,
1,229줄·분기 126)** 로 갈리고, 후자가 CC 대부분. 외부 변이 `cell_node.children` 단일,
탈출 5곳 전부 블록-지역(2809/2835/2861/2962/3724 루프 소속) — 통이동 성립.

## 설계

`fn layout_horizontal_cell_paragraphs(&self, tree, cell_node: &mut RenderNode, …)` 통이동.
- 셀-스코프 Copy 스칼라(cell_idx/cell_y/cell_h/content_cell_y/pad_top/inner_*/text_y_start/
  use_top_vpos_anchor/has_nested_table 등) = `HorizontalCellVars` struct (§6).
- 참조 직접: cell(&Cell)/composed_paras/table/styles/bin_data_content/row_y/enclosing_cell_ctx 등.
- 불확실 파라미터 컴파일러 E0425 수렴 (루프 정착 방식). 사각지대 6종 점검.

## 게이트

fmt/clippy 0/`--tests` 0실패/issue_1116/OVR 5샘플. 재평가 = 표적 CC + `--snapshot r11`.
