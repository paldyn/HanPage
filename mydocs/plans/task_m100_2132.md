# 수행·구현계획서 — Task M100 #2132: compute_char_positions 중복 소거 (2차 R1)

- 이슈: #2132 (#2131 서브) / 브랜치: `local/task2132` / 2026-07-09 / goal 방식.
- 산식 v2.1 잠재량 1위 (중복 243). **성공 기준 = 총량 순감소** (metrics --diff 판정).

## 1단계 분석 — diff 17 hunk 분류 (완료)

Embedded(507, 315줄) vs Wasm(1210, 280줄), 유사도 0.73:

| 분류 | hunk | 내용 |
|---|---|---|
| **의미 A** — 폭 산출원 | 2곳 | embedded 메트릭 lookup+폴백 사다리 vs wasm measure_char_width_hwp (+B 전용 준비부 9줄: measure_font/hangul_hwp) |
| **의미 B** — 인라인 탭 divergent | 2곳 | RIGHT+leader: A 43줄(raw high_byte legacy 파싱, body_right_legacy 기준) vs B 8줄(inline_tab_type 헬퍼, tab_target 기준) |
| 명명/주석만 | 나머지 ~13곳 | base_w↔char_px, 주석 워딩, has_more_tabs contains↔iter().any |

## 설계 — 공용 워크 + 훅 2개

`fn compute_char_positions_walk(text, style, hooks: &dyn CharPositionHooks) -> Vec<f64>`

```rust
trait CharPositionHooks {
    /// 의미 A: i번째 글자의 raw 폭 (preamble 상태는 구현체가 보유)
    fn char_px_raw(&self, i: usize, c: char, chars: &[char], cluster_len: &[usize], font_size: f64) -> f64;
    /// 의미 B: 인라인 탭 글자 처리 — 반환 = 새 x (divergent RIGHT+leader 경로 포함)
    fn inline_tab_x(&self, ...) -> f64;
}
```

- 공용부(~150줄 1벌화): 루프 골격, cluster/leader/자간 클램프, 공백, 기본 등간격 탭,
  positions 축적. 명명은 B판(char_px) 채택 — 동작 무관.
- 두 impl 은 훅 제공자로 축소 (Embedded: lookup 사다리 + legacy 탭 / Wasm: 측정 + 헬퍼 탭).
  **의미 차이는 각자 보존** — 통합하지 않는다 (행동 불변).
- 자유함수 1794(위임 3줄)·trait 선언·테스트 mock(2046)은 무변경.

## 게이트

1차 체인 전수(테스트/OVR/**golden SVG** — 텍스트 측정 민감) + metrics --diff 총량 순감소 확인.
