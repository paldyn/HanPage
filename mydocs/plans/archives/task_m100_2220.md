# 수행계획서 — Task M100 #2220: BehindText+tac 표 호스트 줄에 빈 문단 텍스트 줄 가산

- 이슈: #2220 (#2211 진단에서 분리) / 브랜치: `local/task2220` / 작성일: 2026-07-12
- 재현: `samples/basic/issue1994_behindtext_table_20200830.hwp` p1 우측 단
  (문단 0.6 빈 문단 + 1×1 표 wrap=글뒤로·treat_as_char=true·RowBreak)
- 정답지: `pdf/issue1994/issue_1994.pdf` p1 / 열린 PR 3건(#2219/#2216/#2187)은
  프론트·편집 계열로 본건과 무관 확인.

## 1단계 — 재현 + 정량 (완료, #2211 2단계 승계)

문단 0.6→0.7 사이 단일 +28.7px 계단 → 우측 단 마지막 줄("최한나…") 하단 절단.
저장 지오메트리 실증: 호스트 ls **lh 24700 = 표 22996 + outer_margin 상하
852×2**, 다음 문단 vpos 24440(=lh−260). 우리는 여기에 빈 문단 텍스트 줄
(CS 1300×150%=1950HU=26.0px)을 추가 가산 — 관측 +26.6px과 일치.

## 2단계 — 가산 지점 특정

- 빈 문단 + tac 표 호스트의 y 전진 경로 이분: 호스트 줄 높이가 "표 박스"가
  아니라 "표 박스 + 빈 문단 텍스트 줄"로 계산되는 지점을 특정
  (layout.rs 문단/표 블록 전진 vs paragraph_layout 줄 높이 산정).
- 두-경로 점검: dump-pages 단1 used=686.1(정상)과 렌더 밀림의 불일치 —
  typeset 회계는 정상 정황이므로 layout 방출 측 결함으로 추정, 실측 확정.
- wrap=글뒤로 + tac=true 조합의 분기 지도 확인 (tac 우선? 글뒤로 우선?
  스펙·한컴 저장 lh가 판정 기준).

## 3단계 — 정정 + 게이트

- 호스트 줄 높이를 저장 LINE_SEG lh(=표 박스) 기준으로 정정 — 빈 문단 텍스트
  줄 이중 가산 제거. 케이스별 명시 가드(빈 문단 + tac 표 호스트), 하드코딩
  금지 — 근거는 저장 lh/표 크기/outer_margin 스펙 필드만.
- 표적 회귀 테스트 신설 (우측 단 마지막 줄 페이지 내 포함 + 문단 0.7 y 검증,
  수정 전 FAILED 실증).
- 게이트: fmt/clippy/`--tests --no-fail-fast` 전수 + golden 8 + OVR 5샘플
  (분리 폴더) + #2211 표적(좌측 단 비회귀) + #1994 영향권.

## 4단계 — 시각 판정 + 보고

우측 단 하단 전/후/한컴 3-way → 작업지시자 시각 판정 → 최종 보고 → devel
반영 → close 승인 요청.
