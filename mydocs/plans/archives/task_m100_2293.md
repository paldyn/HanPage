# 수행계획서 — Task M100 #2293: skia RawSvg 조각 래스터 텍스트 소실 정정

- 이슈: #2293 / 브랜치: `local/task2293` / 작성일: 2026-07-15
- 근인 (등록 시 실험 확정): `src/renderer/skia/image_conv.rs` `svg_fontdb()`
  가 ①프로젝트 `ttfs/` 미로딩 ②generic 폴백을 존재 확인 없이 특정
  배포판 폰트명("Noto Sans CJK KR" 등)에 하드 고정 — 매칭 실패 시 resvg
  가 텍스트 드롭. 실존 폰트 지정 실험에서 잉크 0→451 즉시 표시.

## 절차 (버그 정정형 — 구현계획서 생략, 정정→게이트→판정)

### 1. 표적 테스트 신설 + 수정 전 FAILED 실증

- `tests/issue_2293_chart_png_text.rs` (native-skia): 차트 PNG 의 제목/축
  라벨 영역 잉크 존재 단언 — 수정 전 FAILED. #2292 표적과 별도 파일
  (기하/텍스트 축 분리).

### 2. 최소 정정 — PDF 경로의 폰트 체계를 조각 래스터에 정합

- `svg_fontdb()` 에 `ttfs/` 로딩 추가 (PDF 경로 `renderer/pdf.rs` 의 탐색
  규약 재사용 — 로컬 폰트 경로 규약: 저장소 `ttfs/` + 시스템).
- generic 폴백을 **존재 확인 후 체인**으로: 후보(Noto Sans CJK KR →
  Noto Sans KR → 함초롬돋움 계열 → DejaVu Sans) 중 fontdb 에 실존하는
  첫 항목. serif/monospace 동일 패턴. `tech/font_fallback_strategy.md`
  체인과 정합 (재확인 후 순서 확정).
- 폰트 alias 동기화 규칙(feedback_font_alias_sync)은 신규 폰트 추가가
  아니므로 비적용 — 로딩·폴백 결정만 변경.

### 3. 게이트 + 판정

- 표적 + #2292 표적(기하 회귀 가드) + native-skia 전수 + default 전수 +
  fmt/clippy + OVR 5샘플.
- CI 환경(폰트 부재 가능) 고려: 폴백 체인 최종 폴백이 시스템 어떤
  sans 라도 잡도록 — 표적 테스트는 잉크 존재만 단언(특정 글리프 픽셀
  비교 금지, 환경별 폰트 차이 허용).
- 시각 판정 자료: `output/png/chart_check_after/` 재생성(텍스트 포함) →
  작업지시자 판정.

## 위험/주의

- fontdb 는 OnceLock 전역 — ttfs/ 로딩 실패(폴더 부재 배포 환경)에도
  패닉 없이 시스템 폰트로 동작해야 함 (조용한 폴백 + 로딩 실패 무시).
- svg_fontdb 는 조각 래스터 전용 — PDF 경로(fontdb 별도)와 이중 로딩이
  되지만 OnceLock 1회라 비용 미미. 통합은 과설계로 비채택.
