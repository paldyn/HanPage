# 수행계획서 — Task M100 #2189: 고정폭 테두리 글상자 우측 텍스트 클리핑

- 이슈: #2189 (movieisover 리포트, 0.7.18) / 브랜치: `local/task2189` / 작성일: 2026-07-11
- 재현 파일: `samples/basic/issue1994_behindtext_table_20200830.hwp` p2 좌측 성명서 글상자

## 1단계 재현 (완료)

네이티브 PNG(--font-path 지정)에서 **재현 확정** — 거의 전 줄의 마지막 글자가 우측
테두리에서 세로 절단 (`output/poc/issue2189/`). Chrome 확장/WebView/native 3환경 동일
= 엔진 레벨 확정.

## 2단계 — 원인 이분 진단 (리포터 가설 2축 + hiSandog 제안 반영)

- **가설 1 (available width)**: 글상자 내부 줄바꿈 폭이 border/padding inset 을 덜
  차감. 검증: `dump -s/-p` 로 해당 글상자 문단 LINE_SEG segment_width vs
  글상자 내부 폭(선언 폭 − border − padding) 직접 대조. `--debug-overlay` 병용.
- **가설 2 (대체 폰트 advance)**: 저장 줄바꿈(LINE_SEG 보존 문서)이라면 한컴이 나눈
  줄을 우리 폰트 폭으로 그릴 때 넘침. 검증: 해당 줄 텍스트의 estimate_text_width
  vs seg 폭 vs 클립 폭 3자 대조.
- 판별 기준: seg 폭 자체가 클립 폭을 넘으면 가설 1(레이아웃), seg 폭은 맞는데
  그린 폭이 넘으면 가설 2(폰트 advance → 셀 underflow 압축(#2156 인접)류 보정 검토).

## 3단계 — 정정 + 게이트

- 원인 축에 따라: inset 차감 정정(shape_layout/textbox 내부 폭) 또는 프레임-제약
  advance 압축(기존 셀 underflow 보정 패턴 준용). **하드코딩 보정 금지 규칙 준수** —
  근거는 border/padding/폰트 메트릭 속성만.
- 두-경로 점검(레이아웃/리플로우) + 4-backend 확인(클립은 backend 공통 크롭이므로
  layout 층 정정이면 자동 정합).
- 게이트: 표적 회귀 테스트 신설 + 전수 + OVR/golden + #1994 영향권(같은 파일).

## 4단계 — 시각 판정 + 보고

수정 전/후 PNG 병치 + (가능 시) 한컴 기준 대조 → 작업지시자 시각 판정 → 최종 보고.
