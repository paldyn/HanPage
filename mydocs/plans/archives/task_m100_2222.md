# 수행계획서 — Task M100 #2222: 렌더 성능 — 최근 병합발 회귀 정량 + 재조판 캐시

- 이슈: #2222 / 브랜치: `local/task2222` / 작성일: 2026-07-12
- 착수 시 추가 관찰(작업지시자): **최근 다수 PR 병합 이후 속도가 상당히
  느려진 것으로 보임** — 원 이슈(입력마다 전량 재조판)에 앞서 회귀 축을
  먼저 정량·귀속한다.

## 1단계 — 회귀 정량 (기준점 대조 벤치)

- 벤치 하니스: 대표 3문서(주보 issue1994 / 결재문서 36389312 / 대형 표
  rowbreak-problem-pages) × [로드+페이지네이션, 전 페이지 렌더, 동일 페이지
  재렌더] 시간 측정 (네이티브, 각 5회 중앙값).
- 기준점: `v0.7.18` 태그(릴리즈) vs 현재 devel — 회귀 존재·규모 확정.
- 회귀 확인 시 구간 이분: 이번 주 병합 경계 커밋(주보 아크 #2189/#2211/#2226,
  PR #2210+#2223 줄간격, #2227 페이지트리 캐시)별 측정으로 기여 커밋 귀속.
- 유력 후보(사전 가설, 이분으로 검증):
  (a) #2189 자간 스필오버의 수렴 재측정 루프 — Justify 음수 슬랙 셀 줄마다
      최대 3회 × runs 재측정. WASM에서는 estimate가 JS measureText 브릿지라
      증폭 가능(캐시 키에 extra spacing 반영 여부 확인).
  (b) #2226 wrap 사다리·probe 계산의 셀당 추가 순회.
  (c) #2211/#2221 stored extent 재계산.
  (#2227 페이지트리 캐시는 개선 방향 — 상쇄 확인)

## 2단계 — 회귀 정정 (확인된 경우)

- 귀속 커밋의 산식을 유지한 채 계산 비용만 제거: 결과 캐싱(스타일·텍스트
  키), 조기 탈출(슬랙 ≥ 0 셀은 스필오버 산정 생략 — 이미 조건부인지 검증),
  중복 순회 통합. 동작 불변 게이트: 전수 + golden + 주보 아크 표적 + OVR.

## 3단계 — 원 이슈: 재조판 캐시 검토

- #2212로 예외 반복 축은 제거됨. 잔여: 입력마다 전 페이지 재조판.
- #2227이 도입한 `with_page_tree_cached`(페이지 트리 캐시) 위에서 **편집
  무효화 전략** 설계: dirty 페이지만 재조판, 캐럿 이동은 재조판 생략.
- 본 단계는 설계·범위가 크므로 2단계 결과 보고 후 **구현계획서 승인 게이트**
  를 둔다 (편집 정확성 회귀 위험 — reflow 무효화 누락 시 stale 렌더).

## 4단계 — 검증 + 보고

- 벤치 전/후 표 + studio 체감(작업지시자 실사용) 판정 → 최종 보고 → devel
  반영 → close 승인. WASM 빌드 제공.
