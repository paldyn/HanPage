# 수행계획서 — Task M100 #2226: 셀 내 겹침 배치 다중 그림 flow 합산

- 이슈: #2226 / 브랜치: `local/task2226` / 작성일: 2026-07-12
- 재현: `samples/basic/issue1994_behindtext_table_20200830.hwp` p2 우측 하단
  로고 표(2×3, tac 어울림) — 셀[2] 그림 3개(TopAndBottom×2 + Square, 오프셋
  겹침 배치)
- 정답지: **한컴 편집기 조판부호 스크린샷(작업지시자 제공, 1차)** — [그림]×3
  한 줄 나란, 행 무성장, 2행(주소 블록) 온전 + `pdf/issue1994/issue_1994.pdf` p2

## 1단계 — 재현·정량 (완료)

행높이 r0 68.9→131.3px / r1 80.8→141.5px (~1.9×), 표 하단 865px(페이지 794
밖) → 2행 텍스트 미방출(SVG 부재). r0 팽창분 = 셀[2] 그림 flow 합산
(5648+3846+282≈9776HU)과 일치. IR 병합(rs=2) 파싱은 정상.

## 2단계 — 합산 지점 특정 + 한컴 겹침 의미론 확정

- 셀 내 비인라인 그림 flow 회계(`paragraph_top_and_bottom_non_inline_flow_height`
  = per-para **max**, `calc_non_inline_controls_flow_height` = para 합,
  `paragraph_cell_non_inline_control_flow_parts` = TopAndBottom max + other 합)
  의 어느 축이 이 셀에서 합산을 만드는지 실측 귀속 (한 문단 ctrls=3 구성).
- 한컴 의미론 가설: 같은 문단의 다수 비인라인 그림은 **각자 (v_offset + 높이)
  의 max** (겹침 허용) — 편집기 스크린샷·저장 lh(셀[2] h=5164 = r0 선언)로
  검증. r1 팽창(+60.7px)의 귀속도 동일 절차로 확정.
- 검증식: max 의미론 적용 시 r0 = 선언 5164 유지 여부 수계산.

## 3단계 — 정정 + 게이트

- 확정 축에 최소 정정 (예상: 동일 문단 내 다수 비인라인 개체의 flow 기여를
  합산 → `max(v_offset+높이)` 로) — 기존 캘리브(#521 exam_eng, #1841 결재
  헤더, #874/#1086 aift) 비회귀가 관건. 케이스별 가드·하드코딩 금지.
- 두-경로: layout(table_layout parts)과 측정(height_measurer
  measure_non_inline_controls_height) 동시 점검 (#2221 대칭 교훈).
- 표적 테스트 신설(p2 주소 텍스트 방출 + 표 하단 페이지 내, 수정 전 FAILED
  실증) + 전수 + golden + OVR + 주보 아크 표적 5종.

## 4단계 — 시각 판정 + 보고

p2 로고 표 전/후/한컴 3-way → 판정 → 최종 보고 → devel 반영 → close 승인.
