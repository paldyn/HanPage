# 수행계획서 — Task M100 #2212: 중첩 표 셀 경로 bbox 해석 실패 (편집)

- 이슈: #2212 / 브랜치: `local/task2212` / 작성일: 2026-07-12
- 재현: `samples/basic/issue1994_behindtext_table_20200830.hwp` p1 좌측 단
  표 pi=5(외곽 3×6) → 셀[10] 내부 18×9 표. rhwp-studio에서 해당 영역 키보드
  이동/드래그 시 `경로 기반 표 셀 bbox를 찾을 수 없습니다: sec=0, ppi=5,
  path=[{ci:0,cell:10,cp:0},{ci:0,cell:46,cp:0}]` 반복 (#2211 진단 중 확보한
  콘솔 로그).
- 성격: 편집 레이어 조회 결함 — 문서 파손 없음, 선택 UI 불능 + 입력마다 예외
  (#2222 성능 증상의 한 축).

## 1단계 — 재현 고정

- 조회 진입점 확인 완료: `get_table_cell_bboxes_by_path_native`
  ([cursor_rect.rs:3264](../../src/document_core/queries/cursor_rect.rs#L3264))
  — 렌더 트리에서 TextRun.cell_context(parent_para + 2단 path) 매칭으로 Table
  노드를 찾아 직속 TableCell bbox 수집. 실패 = 매칭 TextRun 부재.
- 네이티브 재현 테스트로 고정: 동일 경로 JSON으로 호출 → Err 재현.

## 2단계 — 원인 이분

- **가설 A**: 내부 표 TextRun에 2단 cell_context.path가 기록되지 않음
  (중첩 표 레이아웃 경로 — table_cell_content/nested split — 가 ctx를 1단으로
  자르거나 미전달).
- **가설 B**: path는 기록되나 매칭 규칙 불일치 (cell_para_index 어긋남,
  마지막 entry 규칙 등).
- 판별: 재현 트리에서 내부 표 TextRun들의 cell_context 실덤프 → 기록 형태 확정.

## 3단계 — 정정 + 게이트

- 확정 축 최소 정정 (예상: 중첩 레이아웃 경로의 CellContext 전파 보강 또는
  매칭 규칙 정정). bbox 조회 계열 3종(get_cell_info/get_table_dimensions/
  bboxes)의 일관성 확인.
- 표적 테스트 신설: 재현 경로로 bbox 조회 성공 + 셀 수/좌표 타당성 (수정 전
  Err 실증). studio 측은 기존 예외 로그 소멸이 판정 기준.
- 게이트: 전수 `--tests --no-fail-fast` + golden + OVR 3샘플 + 주보 아크
  표적(렌더 비회귀 — 이 정정은 조회 전용이어야 함).

## 4단계 — 검증 + 보고

- studio 실사용 확인(작업지시자: 해당 영역 드래그/키 이동 시 콘솔 청정)은
  WASM 빌드 후 판정 요청. 렌더 픽셀 불변이므로 시각 판정은 콘솔/선택 UI 동작
  기준. 최종 보고 → devel 반영 → close 승인.
