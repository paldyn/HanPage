# 수행계획서 — Task M100 #2211: 중첩 표 행높이 팽창 (주보 p1 하단 클리핑)

- 이슈: #2211 / 브랜치: `local/task2211` / 작성일: 2026-07-11
- 재현: `samples/basic/issue1994_behindtext_table_20200830.hwp` p1 좌측 단 표 pi=5,
  셀[10] 중첩 18×9 내부 표 / 정답지: `pdf/issue1994/issue_1994.pdf` p1
- 연계: #2212(같은 표의 편집 레이어 bbox 실패, 별개 처리)

## 1단계 — 재현 + 구간 특정 (완료)

한컴 PDF 줄 단위 y 대조: 상류 정합(0~1px), 표 pi=5 내부에서 계단식 누적
+30px(≈2줄) → 마지막 문단 하단 절단. 초과 구간 3곳: 목회기도~기도송 +5~8px /
악보 행 전후 +17px / 모세가 행 앞 +8px. 외곽 r0/r1·악보 행 클립은 선언 h와
픽셀 일치.

## 2단계 — 행별 초과 귀속 (이분 진단)

후보 4축을 행 단위로 판별한다:

- **(a) 음수 vert offset 클램프**: `non_inline_control_flow_height` 의
  `max(0)` — 악보 그림 off=−660HU 끌어올림 미반영.
- **(b) 앵커 줄 + 그림 직렬 스택 vs 한컴 겹침**: 악보 셀 저장 LINE_SEG(가사
  vpos=2553=그림 높이)와 우리 스택 의미론 차이.
- **(c) rs 스팬 행 분배**: rs=2/rs=4 스팬 셀(목회기도·모세가·기도송 행)의 행
  높이 솔버 분배가 선언 h와 다른지.
- **(d) 중첩 표 호스트 줄 높이**: 셀[10] 호스트 문단 ls lh=25605(선언 합)와
  우리 내부 표 실적산 높이의 순환.

방법: 내부 표 행 경계(셀 클립 rect)를 행별로 선언 h·한컴 LINE_SEG 기대값과
3자 대조 → 초과 행의 셀 구성으로 축 확정. 필요 시 진단용 임시 로그(커밋 제외).

**(승인 시 추가 지시) 성능 축 검사**: 복잡/중첩 표의 파싱·조판에서 반복 루프로
느려지는 정황(스튜디오 콘솔 rAF 72ms, keydown/mousedown 154ms 위반)을 함께
진단한다 — 페이지·표 단위 조판 시간 측정, 중첩 표 레이아웃 재계산(다중 패스)
횟수 계측, 병목 지점 특정. 정정 범위는 진단 결과에 따라 본 이슈 또는 분리
이슈로 결정.

## 3단계 — 정정 + 게이트

- 확정 축에 따라 최소 범위 정정 (케이스별 명시 가드, 하드코딩 금지 — 근거는
  스펙 필드/저장 LINE_SEG만). 후보별 예상 처방: (a) signed 오프셋 반영,
  (b) 앵커-줄 겹침 의미론(#577/#2207 앵커-시점 계보), (c) 스팬 분배 정정,
  (d) 호스트 lh 우선.
- 표적 회귀 테스트 신설 (p1 좌측 단 하단 문단의 페이지 내 완전 포함 + 특정 행
  y 검증, 수정 전 FAILED 실증).
- 게이트: fmt/clippy/`--tests` 전수 + golden 8 + OVR 5샘플(분리 폴더) —
  rowbreak/exam_science 등 표 중심 샘플 비회귀 필수. 우측 단 동반 개선 확인.

## 4단계 — 시각 판정 + 보고

p1 전/후/한컴 3-way (하단 구간 확대 포함) → 작업지시자 시각 판정 → 최종 보고
→ devel 반영 → close 승인 요청.
