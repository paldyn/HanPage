# 수행계획서 — Task M100 #1925: 1차 리팩토링 라운드 2 (layout_composed_paragraph 해체)

- 이슈: #1925 (라운드 1 #1904, 계획 #1883, umbrella #1582) / 브랜치: `local/task1925` (base: `local/devel`)
- 작성일: 2026-07-05
- 거버넌스: SOLID + 복잡도 / stage-gate (병행 금지, 단계별 승인) / 시각 검증 거버넌스 준수

## 1. 목표

`layout_composed_paragraph` (`src/renderer/layout/paragraph_layout.rs:1458`, **3,771줄 · CC 288
= 현 전체 1위**, r1 스냅샷) 를 라운드 1 검증 루틴(분기 비접촉 추출, 동작 불변, 1 추출 = 1 PR
단위 커밋)으로 해체한다. 라운드 1과 동일하게 **2~3개 추출 후 중간 재평가**로 닫는다.

## 2. 정찰 결과 (계획 근거)

구조: 프리앰블(1482~1801, ~320줄) + **줄 루프(1802~5135, ~3,334줄 = 함수의 88%)** +
포스트앰블(~94줄). 소스-포맷 분기는 전 범위에 **4곳뿐** (1499 프리앰블 helper 호출,
1921/2194/4444 루프 내부).

줄 루프 내부 대형 블록 (brace 매칭 실측):

| 블록 | 범위 | 크기 | 소스분기 | 성격 |
|---|---|---|---|---|
| A. est 사전 폭 추정 run 패스 | 2291~2556 | 266줄 | 0 | 정렬/탭 계산용 폭 추정 |
| B. **본 run 방출 루프** | 2994~4049 | **1,056줄** | 0 | TextRun/글리프/탭/밑줄 방출 |
| C. 빈 runs 처리 | 4204~4377 | 174줄 | 0 | 빈 TextRun 생성 (빈 셀 편집용) |
| D. 빈 runs + tac 개체 라인 | 4383~4623 | 241줄 | **1** (4444) | tac 수식/개체 전용 줄 |
| E. ClickHere 필드 마커 | 4627~4949 | 323줄 | 0 | 누름틀 안내문/조판부호 |

## 3. 수행 단계 (관문형 — 각 단계 승인 후 다음)

### 1단계 — 의존 정밀 분석 + 구현계획서
- 후보 A/B/C/E(분기 0) 각각의 외부 의존(읽기/쓰기 변수, 캐리오버)을 라운드 1 방식
  (단어 경계, shadowing 오탐 배제)으로 실측 → 추출 순서·시그니처 확정.
- 라운드 1 교훈 반영: 의존 32개급(mut 다수)이면 struct 설계 선행 또는 축소 판단을
  **착수 전에** 명시. `task_m100_1925_impl.md` 작성 → 승인.

### 2단계 — 추출 1 (저위험 독립 블록)
- 의존 분석 결과 가장 결합이 약한 블록부터 (예상: E ClickHere 또는 C 빈 runs).
- 게이트 전수 + 커밋. 완료보고 → 승인.

### 3단계 — 추출 2 (최대 블록 B 또는 A+C 클러스터)
- B(1,056줄)는 의존 형태에 따라 통추출 vs 내부 재분할을 1단계 결과로 결정.
- 게이트 전수 + 커밋. 완료보고 → 승인.

### 4단계 — 중간 재평가 + 최종 보고
- 대시보드 스냅샷(`-r2` 라벨) + CC/줄수 대비표 + 다음 라운드 범위 제안.
- 최종 보고서 + #1925/#1904/#1883 갱신.

## 4. 게이트 (모든 추출 공통 — baseline manifest `task_m100_1904_baseline_manifest.md`)

```bash
cargo fmt --check && cargo clippy --profile release-test --all-targets   # 경고 0
cargo test --profile release-test --tests                                # FAILED 0
OVR baseline 5샘플 무변동 (--no-hwp, 기준 00014ecf)                       # 회귀 0건
```

- baseline 유효성: 기준 `00014ecf` 이후 devel 유입(PR 8건)은 전부 OVR 재검증 통과
  (2026-07-05 3회 실측 무회귀) — baseline 갱신 불요.
- 시각 영향 추출 없음 예상(동작 불변)이나, 게이트 변동 시 즉시 중단·원인 규명.

## 5. 위험 관리

- 소스분기 4곳(1499/1921/2194/4444)은 **추출 경계 밖에 유지** — D 블록(분기 1 포함)은
  이번 라운드 범위에서 제외하거나 분기를 caller에 남기는 형태로만.
- 열린 PR inventory (2026-07-05): #1923, #1924 (둘 다 planet6897, serializer 계열) —
  `paragraph_layout.rs` 무접촉, 충돌 없음. 렌더링 PR 신규 유입 시 hold 라벨 운용.
- `layout_raw_paragraph`(5232~)와의 중복 로직은 이번 범위에서 손대지 않는다 (관찰만 기록).

## 6. 범위 제외

- D 블록(tac 개체 라인, 분기 접촉) 및 프리앰블/포스트앰블 정리 — 다음 라운드.
- `typeset_section_endnotes` 내부 분해(후보 ②) / `parse_paragraph_list`(③) /
  metrics.sh 라벨 개선(④) — 별도 결정.
- 오늘 머지된 #1912/#1919 tac 가드 재설계 — vpos_adjust 해체(Phase 2d) 시점으로 이연.
