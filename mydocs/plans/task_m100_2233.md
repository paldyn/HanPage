# 수행계획서 — #2233 wasm-pack Actions/Docker toolchain 정합화

- 이슈: [#2233](https://github.com/edwardkim/rhwp/issues/2233)
- 마일스톤: M100 / v1.0.0
- 브랜치: `codex/issue-2233-wasm-pack-pin`
- 별도 worktree: `/private/tmp/rhwp-issue2233-wasm-pack-pin`
- 기준 브랜치: `upstream/devel`
- 최초 기준 커밋: `537639445332e85b76eb29c76e1dae4d8930369f`
- 최신 upstream 통합: `upstream/devel@62bcae435370b58373248c284c126c9572098522`
- 통합 merge commit: `98d25a140c9cb20dfa861c1f7d78770226f5ca58`
- 작성일: 2026-07-19
- 진행 상태: 로컬 구현·targeted 검증 완료, 신규 PR 원격 게시 승인 대기

## 1. 목표

GitHub Actions의 `curl | sh` installer가 선택하는 wasm-pack 버전과 Docker의
`wasm-pack 0.15.0` pin을 하나의 명시적 정책으로 정합한다.

완료 조건은 다음과 같다.

1. 모든 GitHub Actions wasm-pack 설치 지점이 동일한 0.15.0 installer를 사용한다.
2. fresh dev WASM과 핵심 frontend consumer 계약이 통과한다.
3. release WASM과 `wasm-opt` 최적화가 통과한다.
4. Render Diff의 WASM consumer 경로가 최신 PR head CI에서 통과한다.
5. workflow trigger, required check 이름, PR cache restore-only, release artifact 정책을 바꾸지 않는다.
6. 버전 위치와 갱신 절차를 장기 기술 문서로 남긴다.

## 2. 출처와 신규 PR 경계

[PR #2274](https://github.com/edwardkim/rhwp/pull/2274)는 2026-07-16 contributor가 사유를 남기지 않고
닫았으며 merge되지 않았다. 이 작업은 닫힌 PR을 reopen하거나 review 대상으로 삼지 않는다.

다만 원 구현은 재사용할 가치가 있으므로 source commit
`d6c9494812a054b1497f29cd9865185e92675d8a`를 `git cherry-pick -x`로 최신 `upstream/devel` 위에
이식했다.

- integration commit: `0213e52a4b4ea3388759272f78382586ce5ac2be`
- 원 author: `Jaeook Ryu <jaeook.ryu@gmail.com>`
- `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 보존
- source commit SHA를 commit body에 보존
- contributor commit을 amend 또는 squash하지 않음

신규 PR은 Issue #2233을 작업 기준으로 삼고 `Closes #2233`, `Supersedes #2274`를 명시한다.

## 3. 변경 범위

| 파일 | 변경 |
|------|------|
| `.github/actions/install-wasm-pack/action.yml` | 0.15.0 linux-musl release binary를 설치하는 composite action 신설 |
| `.github/workflows/ci.yml` | dev frontend gate와 release WASM job의 설치 단계를 composite action으로 전환 |
| `.github/workflows/deploy-pages.yml` | wasm-pack 설치 단계를 composite action으로 전환 |
| `.github/workflows/full-renderer-sweep.yml` | wasm-pack 설치 단계를 composite action으로 전환 |
| `.github/workflows/npm-publish.yml` | wasm-pack 설치 단계를 composite action으로 전환 |
| `.github/workflows/render-diff.yml` | Render Diff WASM 설치 단계를 composite action으로 전환 |
| `Dockerfile` | 기존 0.15.0 pin과 composite action의 동기화 주석 추가 |
| `mydocs/tech/wasm_pack_version_policy.md` | 버전 정책과 갱신 절차 기록 |
| `mydocs/tech/README.md` | 기술 문서 지도에서 정책 문서 연결 |

workflow job 구조, trigger, cache, Node/Rust toolchain, build 명령과 artifact 경로는 바꾸지 않는다.

## 4. collaborator 보정

최신 저장소 문서 규칙에 맞추기 위해 contributor commit 뒤 별도 commit
`460b1d8f9acecad1f4ec922b86e4c323c11e76f9`를 추가했다.

- 정책 문서에 `kind/status/canonical/last_verified` front matter 추가
- `mydocs/tech/README.md`의 WASM toolchain 진입점 추가
- 변경으로 이동한 `Dockerfile` 줄 번호를 문서가 고정하지 않도록 안정 경로·명령 참조로 변경

제품 코드나 workflow 동작은 이 보정 commit에서 바꾸지 않는다.

## 5. 검증 계획

### 로컬 targeted gate

- 변경 workflow `actionlint 1.7.12`
- old `init.sh` 잔존 여부와 composite action 6개 참조 전수 확인
- wasm-pack 0.15.0 linux-musl asset 존재·tar 구조·실행 형식 확인
- local `wasm-pack 0.15.0` fresh dev build
- generated binding과 editor embed contract test
- release build와 `wasm-opt`
- 변경 Markdown 링크·metadata 검사
- `git diff --check`, Git LFS, clean worktree 확인

### 신규 PR 최신 head gate

- `CI / Build & Test`
- frontend package gates
- `CodeQL`
- `Render Diff`

workflow 변경은 최신 PR head CI를 authoritative 결과로 삼는다. doc-only fast-pass를 적용하지 않는다.

## 6. PR 전 조건

- [x] 최신 `upstream/devel` 기준 별도 worktree 생성
- [x] contributor commit 출처·author·co-author 보존
- [x] collaborator 보정을 별도 commit으로 분리
- [x] targeted 로컬 검증 통과
- [x] Issue #2233 기준 계획·Stage 기록·PR 초안 준비
- [ ] 작업지시자의 remote push·신규 PR 생성 승인
- [ ] 신규 PR 최신 head GitHub Actions 통과
