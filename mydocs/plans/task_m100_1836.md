# Task #1836 수행 계획 — seoul_0776 회귀: 빈-앵커 host_line_spacing 억제 소스 무관화

## 배경 (이등분 → 심층 분석)

seoul_0776 HWPX→HWP 라운드트립 render-diff 가 과거 devel PASS → 현 devel
STRUCT_MISMATCH(646px). `git bisect run` → 첫 BAD 커밋 `e0c471a7`(#1763 셀 선언높이
clamp). 그러나 소거 검증에서 #1763 은 **트리거**일 뿐, 진짜 근본은 별개로 규명:

- clamp 트레이스 A/B 완전 동일 (표 높이 무관)
- TABLE_DRIFT: pi=20(6×2 CellBreak 빈 앵커) **host_sp A=0 / B=12** (900HU)
- 원인: `typeset.rs format_table` 의 `is_topbottom_empty_anchor_hwpx`(#1147, is_hwpx_source
  게이트)가 HWPX 만 빈-앵커 host_line_spacing 을 억제 → HWP5 재파스는 미억제(+12px)
  → typeset pagination 이 A/B 로 갈림. #1763 clamp 가 누적을 razor-thin 경계로 옮겨
  이 latent 12px 를 노출.

## 핵심 통찰

`layout.rs` 렌더 경로의 빈-앵커 억제(`suppress_empty_anchor_spacing =
is_current_empty_para_float && !next_...`)는 **이미 소스 무관**이라 가시 위치는 A/B
동일(833px). 반면 `typeset.rs` pagination 억제만 `is_hwpx_source` 게이트라 HWP5 에서
phantom 12px 를 예약 → **렌더(소스 무관)와 pagination(HWPX 한정)이 어긋남**. #1809/#1841
과 동일한 is_hwpx_source 파스경로 divergence.

## 수정

`typeset.rs`: `is_topbottom_empty_anchor_hwpx` 의 `is_hwpx_source` 게이트 제거 →
`is_topbottom_empty_anchor = !is_tac && TopAndBottom && text.is_empty()` (렌더 경로와
대칭, 소스 무관). 이제 typeset(A)==typeset(B)==layout 렌더 → 라운드트립 자기정합.
사용처 사라진 `is_hwpx_source: bool` 파라미터 제거(시그니처 + 호출 2곳).

- #1147 원 케이스(HWPX)는 억제 유지되어 불변.
- HWP5 재파스/네이티브는 이제 억제 → 표-표 사이(#1133, next=table anchor)는 종전대로
  보존(억제 조건에 `!next_is_empty_topbottom_table_anchor` 유지).

## 검증 계획

- seoul_0776: STRUCT_MISMATCH 646 → **PASS 0.00** (완료)
- issue_1133_nested_table_valign, svg_snapshot 골든
- big_hwpx 2,500 render-diff: 회귀 0
- big_hwp 2,500 네이티브 render-diff: 회귀 0 (본 수정이 HWP5 억제로 바꾸므로 핵심 게이트)

## 비고

- #1763 clamp 는 정상(한글 정합 개선)이므로 불변 유지.
- 한글 대비 46px 상향/p4 fidelity 는 #1763 이전부터 존재하는 별개 축(본 수정 범위 밖) —
  render 가 833px(p3)로 자기정합. 라운드트립 회귀(#1836) 해소가 본 태스크 목표.
