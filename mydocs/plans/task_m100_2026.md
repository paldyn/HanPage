# 수행계획서 — Task M100 #2026: 1차 리팩토링 라운드 5 (typeset_section_endnotes 해체)

- 이슈: #2026 (계획 #1883 v2, umbrella #1582, 선행 #1904·#1925·#2001·#2003)
- 브랜치: `local/task2026` (base: local/devel = devel `95f4cb7e`) / 작성일: 2026-07-07
- 거버넌스: v2 전 조항 + r4 확인 스캔 사각지대 2종 보정 절차

## 1. 목표

`typeset_section_endnotes` (**5,539줄 · CC 179 = 전체 1위**, 라운드 1 추출 산물)를
`EndnoteFlowState` mut-묶음 struct 설계로 해체한다 — 라운드 1에서 "의존 32개(mut 9)"
실측으로 이연했던 본체. 상태 struct 패턴 5종 실증으로 설계 여건이 성숙했다.

## 2. 정찰 결과

- 소스-포맷 분기 **0** (라운드 1 확인 유지, 07-05 유입분 포함 재확인) — v2 §1 안전.
- 구조: `if !st.endnotes.is_empty()`(5,508줄) → **미주 참조 루프**(3910~9386, 5,474줄) →
  `if let Some(en_ctrl) = resolve_endnote_content(...)` 본문. 다음 레벨에 200줄 이상의
  단일 지배 블록이 **없음** — 중간 블록(구분자 방출/컬럼 소진/en_para 배치/캐리 전파)의
  긴 연쇄. 따라서 라운드 2·4식 "블록 통추출"이 아니라 **책임 단계별 절단 + 상태 struct**가
  정공법.

## 3. 수행 단계 (관문형)

### 1단계 — 의존 정밀 재실측 + `EndnoteFlowState` 설계 + 구현계획서
- 참조 루프 본문의 단계 경계(구분자/페이지·컬럼 진행/en_para 조판/잔여 캐리)를 식별하고
  각 경계의 캐리오버를 실측 (r4 사각지대 보정: 포맷 문자열 가짜 대입 제거, 인덱스 대입
  `v[i]=` 포함).
- 라운드 1 실측(32/9)의 현재값 재확정 → `EndnoteFlowState` 필드 설계(값-왕복 Copy 스칼라
  vs &mut 참조 구분, RunEmitState 전례).
- `task_m100_2026_impl.md` → 승인.

### 2단계 — 추출 1 (결합 최약 단계부터, 예상: 구분자 방출/초기 예산 준비)
### 3단계 — 추출 2~3 (en_para 배치 본문 — EndnoteFlowState 경유)
- 각 추출 = 1 커밋, 게이트 전수 + 완료보고 → 승인. 의존 폭발 시 v2 §0 규칙 3(축소·연기).

### 4단계 — 재평가 + 최종 보고
- `--snapshot r5 --no-coverage` + CC 대비 + CC>25 추세(현 86) + 다음 라운드 제안.

## 4. 게이트 (모든 추출 공통)

fmt/clippy 0 · `cargo test --profile release-test --tests` FAILED 0 · OVR 5샘플
**추가 변동 0**(기지 #1936발 3건 동일 유지) · 페이지 오라클/roundtrip 무변동.
미주 관련 표적: `issue_1116`(sample16) 등 미주/각주 핀 + golden SVG.

## 5. 위험 관리 / 범위 제외

- 미주 배치는 페이지 흐름의 마지막 단계라 캐리 손상 시 쪽수 전이가 큼 — 단계 절단마다
  즉시 게이트, 변동 시 중단.
- 범위 제외: `layout_partial_table`(163) · 신규 추출 함수 2차 분해 · typeset 재성장 흡수
  (Phase P 연동) — 후속 라운드.
