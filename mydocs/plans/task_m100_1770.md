# Task #1770 수행계획서 — 분할 표 라운드트립 붕괴 조사 (판별 종결)

## 판별 결과
1. **2978463 "높이 72.9px 붕괴" = 오독**: dump-pages 의 Table 크기 표시는 common.height
   (통짜 표 표시 관례)이며, render-tree 실측은 원본(40.8+809.1)=변환본(849.9) **동일 —
   붕괴 없음**. 순수 devel 도 동일. 표/셀 IR·셀 lineseg 도 완전 일치(ir-diff 0).
2. 실제 차이는 **분할 vs 통째 이월 결정**: TABLE_DRIFT 계측 — 원본(hwpx 파스) host_sp=0.0
   vs 변환본(hwp5 파스) host_sp=**11.2px** → cur_h 11.2px 차 → 분할 경계 이동.
3. 잔여 케이스(3246541 Cell −3/1002px 변위 STRUCT 등)도 같은 분할 경계 이동 증상 —
   **파스 경로별 host spacing/saved-vpos 차이(±11.2px 군집)로 수렴 = 이슈 #1772
   ("11.36px 이동 군집")와 동일 근원**. #1772 는 타 에이전트 진행 중(작업지시자 지시로
   본 세션 제외) — 중복 회피를 위해 판별 단서만 제공하고 종결.

## 산출
- 이슈 #1770 에 판별 코멘트(오독 정정 + #1772 근원 단서: typeset_block_table host_sp
  11.2px 계측 방법) — 수정 없음.
