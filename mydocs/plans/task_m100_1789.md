# Task M100 #1789 수행 계획서 — exclusion 겹침 프로브에서 line_spacing 제외

## 배경

Issue #1789 (#1772 잔여 OVER 28건 중 A 유형, 본문 줄 세로 이동 3건·최대 345px).
조사 상세: `mydocs/tech/investigations/issue-1772/task_m100_1772_residual_over28.md`.

## 원인 요약

task 1510 이 도입한 HWPX 경로 전용 exclusion 겹침 프로브(`overlaps_zone`,
src/renderer/layout.rs)가 첫 줄 높이를 `line_height + line_spacing` 으로 계산한다.
잉크(text_height)는 문단 기준 자리차지 표 위 공간에 들어가는 줄이 spacing 포함분
수 px 겹침만으로 문단 전체가 zone.bottom(표 아래)으로 밀린다. 한컴 저장 lineseg
(36385142 pi8 vpos=34925 → 529.9px = 표 위 유지)와 HWP5 재파스 렌더가 일치하므로
HWPX 경로 결함.

## 수정 내용

프로브 2개 사이트(FullParagraph/PartialParagraph)에서 `+ seg.line_spacing` 제거 —
기존 text_height 우선 규칙은 유지.

## 검증 계획 (PR 통합 상태 기준 — 작업지시)

기준선/수정 모두 **열린 PR 전체 merge 통합 브랜치(test/all-prs-v3)** 위에서 수행.

1. 재현 3건(seoul_0377/0030/0973) render-diff 0.00px 확인
2. **task 1510 원 과제 테스트**(issue_1510) 통과 — 프로브 도입 목적 보존 게이트
3. 신규 회귀 테스트: 36385142 샘플, 저장 vpos 위치(529.9px) 유지 검증
4. 전체 cargo test + clippy
5. hwpdocs 코퍼스 300 + 2,500건 — 통합 기준선 대비 개선/악화 정량화
