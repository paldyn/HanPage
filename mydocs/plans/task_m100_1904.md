# 수행계획서 — Task M100 #1904: 1차 리팩토링 (복잡도 해체)

- 이슈: #1904 (계획 #1883, umbrella #1582) / 브랜치: `local/task1904` (base: `local/devel`)
- 작성일: 2026-07-04
- 거버넌스: SOLID(solid_scoring_guide) + 복잡도(대시보드) / 시각 검증 거버넌스 준수

## 1. 목표

작업지시자 확정 재편에 따른 **1차 리팩토링 = 복잡도 해체** 실행. 대시보드 영점(2026-07-04)
기준 복잡도 높은 순 × 위험도 낮은 순. postmelee 리뷰(계획 v2)와 병행 진행.

## 2. 수행 단계 (관문형 — 각 단계 승인 후 다음)

### 1단계 — Phase 0: Baseline Freeze
1. **열린 렌더/layout PR inventory**: 현재 열린 PR 전수를 merge/hold/rebase 대기로 분류
   (jangster77 보완 1 — baseline 이후 섞이면 bit-identical 판정 흐려짐).
2. **baseline manifest 작성**: 샘플 목록·생성 명령·기준 commit·환경(폰트/OS/rasterizer)·
   허용 임계값·제외 목록 — `mydocs/tech/task_m100_1904_baseline_manifest.md` (보완 2).
3. **행동 고정 자산 생성**: golden SVG(기존)·페이지 오라클(기존)·roundtrip baseline(기존)
   확인 + **OVR baseline 신규 생성**(대표 샘플 셋) + 대시보드 스냅샷(영점 기존 활용).
- 산출: manifest 문서 + OVR baseline + inventory 표. 완료 기준: 작업지시자 승인.

### 2단계 — 준비운동: object_ops.rs 도메인별 모듈 분할
- `commands/object_ops/` 하위로 표/그림/도형/각주·미주/수식/커넥터/텍스트박스/공통 분할.
  **함수 이동만**(로직 무변경) — 최저위험으로 게이트·절차 검증을 겸한다.
- 인라인 테스트 2,367줄도 도메인별로 동반 이동.
- 게이트: 전체 테스트 + golden/roundtrip/OVR **무변동**. PR 1개(이동만이라 리뷰 가능 규모).

### 3단계 — typeset_section_with_variant 해체 (라운드 1)
- 7,059줄·CC 282. **분기 비접촉 추출 단위부터** — 내부 책임(분할/앵커/flow 등)별로
  순수 함수 추출, **한 추출 단위 = 1 PR** (jangster77 채택).
- 이 단계에서는 2~3개 추출 PR 까지만 진행하고 중간 재평가(대시보드 스냅샷) — 1차 전체를
  이 타스크 하나로 닫지 않는다(후속 라운드는 재평가 후 새 이슈).
- 게이트: PR 마다 전체 테스트 + OVR + 페이지 오라클, 시각 영향 시 시각 판정.

### 4단계 — 중간 재평가 + 보고
- 대시보드 `--snapshot`(두 번째 측정 — 추이 차트 첫 가동) + 지표 대비표.
- 최종 보고서 + #1904/#1883 갱신. 다음 라운드 범위 제안.

## 3. 위험 관리

- 매 PR 무변동 게이트 강제 — 하나라도 변동 시 해당 PR 중단·원인 규명.
- 소스-포맷 분기 접촉 금지(1차 범위) — 분기 밀집 추출은 Provenance/Profile 이후.
- 렌더러 계열 열린 PR(#1875 등)과의 충돌: 1단계 inventory 에서 hold/선머지 결정.

## 4. 범위 제외

- layout_composed_paragraph 이하 후속 대상 — 재평가 후 다음 라운드 이슈.
- Provenance/Profile 분리(구 Phase 1) — 후속 단계.
- 계획 v2 개정 — postmelee 리뷰 도착 시 별도.
