# Task M100 #2195 수행계획서 — 본문 NO_LS 실폭 래핑 (45자 휴리스틱 대체)

## 목표
뷰 파이프라인의 본문 NO_LS(저장 LINE_SEG 부재) 문단 줄바꿈을 CHARS_PER_LINE=45 휴리스틱
(composer compose_lines, #994/#998 임시)에서 실폭 래핑(reflow_line_segs: tokenize+fill_lines,
condense/kbu(#2185 정정)/자간/HWPUNIT 정수누적 완비)으로 대체한다.

## 완료 게이트
- 76076 = 82, 86712 = 65 (원본 .hwp, PDF 정답) 복귀 — #2070 잠정 게이트 해제
- 기존 유지: 80168=157(hwp/hwpx), 80250=17, byeolpyo4/1=26/4, sample16-hwp5 3종=64(#998)
- cargo test 전체 + fmt/clippy

## 근거 (#2070 stage19-20 실측)
- 76076 pi181/183/184: 한글 3줄(PDF 좌표 직독 + 캐럿 마크 [49,90,126] 등) vs 우리 4줄 — 휴리스틱 산물
- 진입점 존재: DocumentCore::from_bytes 의 reflow_zero_height_paragraphs(include_empty) — 현재 HWPX 한정
- 주의: hwpx 픽스처는 #2197(intent 절반)로 비등가 — 오라클은 원본 .hwp 기준

## 단계 (4)
1. **현행 경로 조사**: reflow_zero_height_paragraphs 의 폭 산출(단 폭/여백)과 적용 범위 확인;
   76076 hwpx(이미 reflow 경유)에서 pi181 fill_lines 산출 vs 한글 마크 대조 — fill_lines 자체의
   정합도 판정 (hwp/hwpx 동일 83이므로 fill_lines 도 4줄 산출 의심)
2. **fill_lines 규칙 정합**: 셀 splitter 에서 확정한 규칙(줄끝 초과 공백 1 hang 등) 부재분을
   fill_lines 에 이식, pi181/183/184 마크 정확 일치까지 (통제 사다리/실문서 마크 검증)
3. **HWP5 본문 확장**: include_empty 를 HWP5 로 확장(또는 본문 전용 패스) + hwp3 변종 보호
   (#998 게이트, #2070 hwp3_variant 선례) — 이원화 재발 방지(hwp/hwpx 동일 쪽수 확인)
4. **게이트 검증 + 정식화**: 76076/86712 복귀 확인 → 전체 회귀 → 보고서

## 리스크
- 영향 광역(모든 NO_LS 본문 문서): 단계별 게이트 스냅샷 필수
- fill_lines/셀 splitter 규칙 이원화 잔존 시 문서별 편차 — 규칙 단일화가 이상적이나 본 타스크는
  본문 경로 정합에 한정, 통합 리팩토링은 후속
