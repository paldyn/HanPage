# Task M100 #2392 — `picture-props-dialog` 적용 파이프라인 책임 분리 수행 계획

- 이슈: #2392
- 상위 umbrella: #2022
- 선행 계획: #2023 / PR #2080
- 선행 baseline: #2124 / PR #2174
- 선행 Phase A: #2125 / PR #2254
- 선행 legacy 정리: #2313 / PR #2316
- 작업 브랜치: `issue-2392-picture-props-apply-pipeline`
- 기준 브랜치: `upstream/devel`
- 기준 커밋: `af5902b659be9a4d86ad458d79c63353dba88167`
- 작성일: 2026-07-19
- 단계: 수행 계획서

## 0. 현재 진행상황과 확정 결정

| 항목 | 현재 상태 |
|------|-----------|
| #2022 umbrella | OPEN. Phase 0/A와 legacy `/web` 정리 완료, Phase B 실행 추적 중 |
| #2392 | OPEN, #2022 sub-issue, `postmelee` 할당 |
| 작업 브랜치 | `upstream/devel@af5902b6` 통합, 계획 `293c7640`, Stage 1 `8773d6b4` |
| 오늘 할일 | `mydocs/orders/20260719.md`에 진행중으로 등록 |
| 최신 metrics | Stage 3 중간 측정 완료, Total CC `12,369 → 12,093`, CC>25 합 `4,297 → 3,949` |
| baseline gate | Stage 1 headless undo, Stage 3 focused 38/38·전체 390/390·build PASS |
| 구현 계획서 | `mydocs/plans/task_m100_2392_impl.md` 작성·승인 완료 |
| 다음 관문 | Stage 3 결과 승인 후 Stage 4 회귀·metrics 결산·PR 준비 |

다음 결정은 #2022와 #2392에서 확정됐으므로 다시 선택지로 열지 않는다.

- Phase B 첫 실행 단위는 `picture-props-dialog.ts`의 `handleOk` apply pipeline이다.
- `populateFromProps` 해체는 이번 범위에서 제외하고 후속 후보로 유지한다.
- dialog UI, public contract, WASM schema와 편집 동작은 변경하지 않는다.
- 프론트 전체 SOLID 총점을 자동 산출하지 않고 이번 apply pipeline만 review 단위로 사용한다.
- 단순 함수 이동이나 새 거대 context 추가를 복잡도 개선으로 계상하지 않는다.
- runtime UI framework와 runtime dependency를 추가하지 않는다.

## 1. 목적

`PicturePropsDialog.handleOk`는 하나의 함수에서 다음 변경 이유를 함께 소유한다.

1. DOM control의 현재 값을 읽고 HWPUNIT/ColorRef 등 저장 형식으로 변환한다.
2. 기존 image/shape properties와 비교해 최소 patch를 만든다.
3. image/shape/line/group/OLE별 적용 규칙과 기본값을 선택한다.
4. header/footer, cell path, 일반 본문에 맞는 WASM setter를 선택한다.
5. `CommandServices` 유무에 따라 snapshot undo 또는 직접 적용 fallback을 수행한다.
6. 적용 여부와 무관하게 dialog를 닫는다.

이번 작업의 목적은 이 책임들을 검증 가능한 경계로 분리하면서 기존 patch와 side effect를 그대로 보존하는
것이다. 함수 수나 파일 수 증가 자체가 목적이 아니며, 전체 Total CC와 고복잡도 합의 순감소를 함께
증명해야 한다.

## 2. 최신 기준선과 후보 선정

### 2.1 frontend 전체 기준선

`upstream/devel@af5902b6`에서 `scripts/frontend-metrics.mjs` schema v2로 재측정했다. 최초 후보 선정
기준 `eb9c7f1f` 이후 #2395와 후속 #2393 문서만 변경되어 제품 metrics는 동일하다.

| 지표 | 값 |
|------|---:|
| included files | 215 |
| reported CC functions | 2,386 |
| Total CC | 12,369 |
| global Top 20 합 | 2,660 |
| CC>25 개수 / 합 | 70 / 4,297 |
| CC>100 개수 | 7 |
| Max CC | 453 |

공식 #2124 snapshot 대비 누적 delta는 Total CC +564, Top 20 +79, CC>25 합 +365다. 이 누적치는
#2392 성과가 아니며, #2392는 동일 base pre/post snapshot으로 직접 delta를 분리한다.

### 2.2 대상 파일 기준선

| 항목 | 값 |
|------|---:|
| 파일 LOC | 2,825 |
| 파일 Total CC | 647 |
| 파일 CC>25 개수 / 합 | 2 / 560 |
| 파일 Max CC | 348 |
| `handleOk` | CC 348 / 381 LOC |
| `populateFromProps` | CC 212 / 286 LOC |

`handleOk`와 `populateFromProps`는 #2124 공식 snapshot과 최신 snapshot에서 stable function id, CC와 LOC가
모두 동일하다. 파일의 마지막 변경은 `70f9fbea`(2026-07-09)이며 #2316 이후 변경이 없다.

### 2.3 다른 후보를 먼저 선택하지 않은 이유

| 후보 | Total CC / Max CC | 판단 |
|------|------------------:|------|
| `diff-engine.ts` | 897 / 88 | 총량은 크지만 직접 characterization이 없어 별도 선행 gate 필요 |
| `input-handler-mouse.ts` | 748 / 453 | 감소 잠재력은 가장 크지만 selection/resize/drag 등 행동 표면이 넓음 |
| `input-handler-keyboard.ts` | 733 / 448 | 최근 undo/HF/FN 변경과 중첩돼 충돌·행동 위험이 큼 |
| `input-handler.ts` | 720 / 27 | 파일 크기와 함수 hotspot을 분리해야 하며 최근 변경이 집중됨 |
| `wasm-bridge.ts` | 373 / 12 | 고복잡도 함수가 없고 WASM/public boundary 위험이 큼 |

`picture-props-dialog`는 내부 dialog이면서 고복잡도가 두 함수에 집중돼 있고, #2317이 보강한 실제 키보드
undo E2E가 있어 `위험 낮은 순 x 복잡도 높은 순` 기준의 첫 실행 단위로 적합하다.

## 3. 현재 apply 계약

### 3.1 patch 책임군

Stage 1에서 아래 항목을 field 단위 characterization table로 확정한다.

| 책임군 | 현재 동작 |
|--------|-----------|
| 공통 | size protection, width/height, treat-as-char, wrap/position, overlap/restrict, description |
| shape 공통 | text-box margin, vertical align, rotation/flip, line, corner, fill, shadow |
| OLE | outer margin, caption과 제한된 line 저장. transform/fill/shadow 범위 제한 |
| image | rotation/flip, outer margin, caption, border, scale, crop, padding, effect, transparency |
| 변환 | mm/HWPUNIT, ColorRef/hex, percentage/alpha, integer/float default 처리 |
| patch 정책 | 기존 값과 다른 field만 전송하는 항목과 의도적으로 항상 전송하는 항목이 혼재 |

`parseInt`/`parseFloat`와 `||` fallback, clamp, `TakePlace`/`TopAndBottom` 변환, `Through` 보존 같은 세부
규칙도 동작 계약이다. 보기 좋은 정리라는 이유로 기본값 의미를 바꾸지 않는다.

### 3.2 setter target 계약

| object/target | 현재 setter |
|---------------|-------------|
| shape/line/group/OLE + cell path | `setCellShapePropertiesByPath` |
| shape/line/group/OLE + 일반 본문 | `setShapeProperties` |
| image + header/footer | `setHeaderFooterPictureProperties` |
| image + cell path | `setCellPicturePropertiesByPath` |
| image + 일반 본문 | `setPictureProperties` |

image는 header/footer를 cell path보다 먼저 판정한다. shape 계열은 cell path 우선이다. 이 우선순위와 setter
인자 순서는 변경하지 않는다.

### 3.3 undo와 fallback 계약

- `services.getInputHandler()`가 있으면 `kind: 'snapshot'`, `operationType: 'objectProps'`로 라우팅한다.
- snapshot operation은 patch setter 실행 후 현재 cursor position을 반환한다.
- InputHandler가 없으면 setter를 직접 호출하고 `document-changed`를 emit한다.
- patch가 비어 있으면 setter와 event를 실행하지 않는다.
- 성공·무변경 여부와 관계없이 마지막에 dialog를 닫는다.

## 4. 수행 범위

### 4.1 characterization

- `handleOk`의 field, 변환, default, diff/always-send 정책을 object type별로 분류한다.
- setter target과 undo/fallback matrix를 고정한다.
- 기존 source-oriented test와 headless E2E가 실제로 보장하는 범위와 공백을 분리한다.
- 새로운 계산 경계의 data-driven unit test 입력을 먼저 설계한다.

### 4.2 patch 계산 책임 분리

- DOM control 값 수집과 기존 properties 비교를 구분한다.
- 공통, shape/OLE, image patch 계산이 독립적으로 검토되도록 경계를 만든다.
- 계산 경계는 가능한 한 plain data를 받고 DOM, `WasmBridge`, `EventBus`에 직접 의존하지 않게 한다.
- helper가 기존 클래스보다 넓은 거대 context를 요구하면 추출을 강행하지 않고 경계를 다시 설계한다.

### 4.3 side-effect orchestration 분리

- patch 생성과 setter target 선택을 분리한다.
- target 선택, setter 인자, snapshot undo와 fallback을 독립적으로 검증한다.
- `handleOk`는 입력 수집, patch 계산 호출, 적용 orchestration과 종료를 조정하는 얇은 진입점으로 축소한다.

### 4.4 metrics와 회귀 검증

- 동일 base pre/post snapshot으로 aggregate와 stable function diff를 만든다.
- 대상 파일과 frontend 전체의 Total CC, Top 20, threshold 합·개수, Max를 모두 기록한다.
- Studio unit/build와 그림 속성 undo/Through headless E2E를 재실행한다.
- 변경 allowlist와 public/WASM/runtime dependency 무변동을 확인한다.

## 5. 범위 제외와 금지 목록

### 5.1 범위 제외

- `populateFromProps` 해체 또는 dialog 초기화 pipeline 변경
- dialog UI, 탭, 문구, DOM 구조, CSS와 keyboard UX 변경
- 새로운 개체 속성, validation, 오류 메시지 또는 저장 정책 추가
- `PictureProperties`, `ShapeProperties`, Rust schema와 generated WASM binding 변경
- `WasmBridge`, `InputHandler`, command history의 공통 API 리팩터링
- `core/types.ts` domain surface 분리
- image/shape getter routing 변경
- extension, VS Code, npm/editor, font와 renderer 동작 변경

### 5.2 확정 금지

- React/Vue/Svelte 등 runtime UI framework 도입
- package runtime dependency 추가
- public npm/embed 계약 또는 WASM JSON/byte schema 변경
- source refactor와 기능·보안·UI 변경 혼합
- 기존 diff-only field를 always-send로 바꾸거나 그 반대 방향으로 변경
- object type 또는 target 분기를 합치면서 setter 우선순위·인자 의미 변경
- source-oriented test를 단순 삭제해 리팩터링을 통과시키기
- 조건을 새 helper로 이동한 뒤 Total CC·stable diff 없이 개선으로 주장
- frontend 전체 SOLID 예비 점수를 공식 기준선으로 사용
- 필수 gate 미실행을 PASS로 표시

## 6. 보존할 계약과 검증 수단

| 계약 | 검증 수단 |
|------|-----------|
| field 변환·default·diff 정책 | data-driven unit test와 characterization table |
| image/shape/line/group/OLE 분기 | object type별 patch fixture |
| header/footer/cell/body setter | target matrix와 fake setter/호출 기록 |
| snapshot undo/fallback | existing source contract + focused orchestration test |
| 그림 속성 실제 apply/undo | `undo-contracts.test.mjs --mode=headless` |
| `Through` 무변경 보존 | existing unit + undo contracts case 5 |
| TypeScript/Vite 소비 | Studio test/build |
| 복잡도 순감소 | schema v2 same-base aggregate와 stable function diff |
| public/WASM/package 무변동 | 변경 파일 allowlist와 contract surface diff |

## 7. 예상 단계와 승인 관문

### Stage 1. 동일-base 기준선과 characterization 고정

1. 최신 `upstream/devel` drift와 열린 PR의 대상 파일 충돌을 확인한다.
2. schema v2 pre snapshot을 새로 만들고 commit/clean/tool hash를 기록한다.
3. `handleOk`의 field·변환·default·patch 정책을 object type별 표로 고정한다.
4. setter target과 undo/fallback matrix를 고정한다.
5. unit/build/headless undo E2E 기준선을 재확인한다.

관문: unclassified field, always-send 여부가 불명확한 field 또는 검증할 수 없는 setter target이 남으면 소스
분리를 시작하지 않는다.

### Stage 2. patch 계산 경계와 data-driven test

1. 구현 계획서에서 승인된 경계로 공통/shape/OLE/image 계산 책임을 분리한다.
2. DOM과 WASM detail 없이 계산 가능한 부분에 data-driven unit test를 추가한다.
3. 숫자·색·alpha 변환과 diff/always-send 정책을 fixture로 검증한다.
4. `handleOk`의 CC/LOC와 대상 파일 aggregate를 중간 측정한다.

관문: 새 helper가 CC>25 또는 과도한 context 의존을 만들면 Stage 3으로 넘어가지 않고 구조를 재검토한다.
기존 patch fixture가 하나라도 달라지면 기능 변경으로 간주하고 중단한다.

### Stage 3. setter와 undo orchestration 분리

1. patch 계산에서 target 선택과 setter 호출을 분리한다.
2. shape/cell, shape/body, image/header-footer, image/cell, image/body 5개 target을 검증한다.
3. snapshot undo와 no-service fallback, empty patch와 dialog 종료 계약을 검증한다.
4. 기존 source-oriented test가 새 경계를 추적하도록 보강하되 검증 강도를 낮추지 않는다.

관문: setter 우선순위·인자·event 횟수 또는 undo stack 의미가 달라지면 회귀 검증으로 넘어가지 않는다.

### Stage 4. 회귀·metrics 결산과 PR 준비

1. Studio unit/build와 headless undo contracts를 실행한다.
2. same-base post metrics와 stable function diff를 생성한다.
3. #2124 official 대비 누적 변화와 #2392 직접 delta를 분리한다.
4. stage 보고서, 최종 보고서와 PR 본문·리뷰 요청 초안을 작성한다.
5. 사용자 승인 후에만 push, draft PR, GitHub 진행 댓글을 생성한다.

관문: 정량 완료 조건이나 필수 gate가 하나라도 충족되지 않으면 PR을 생성하지 않는다. 예외가 필요하면
function id, before/after CC, 사유, 해소 anchor와 재검토 Phase를 먼저 제시하고 reviewer 승인을 받는다.

## 8. 계획 산출물

| 산출물 | 예정 경로 | 내용 |
|--------|-----------|------|
| 구현 계획서 | `mydocs/plans/task_m100_2392_impl.md` | 경계·파일별 수정·test fixture·명령·commit 순서 |
| 조사 색인 | `mydocs/tech/investigations/issue-2392/README.md` | #2392 characterization 문서 지도 |
| apply 계약 | `mydocs/tech/investigations/issue-2392/task_m100_2392_picture_props_apply_contract.md` | field/target/undo matrix |
| Stage 1 보고 | `mydocs/working/task_m100_2392_stage1.md` | pre metrics와 characterization |
| Stage 2 보고 | `mydocs/working/task_m100_2392_stage2.md` | patch 계산 경계·unit test·중간 metrics |
| Stage 3 보고 | `mydocs/working/task_m100_2392_stage3.md` | setter/undo orchestration과 focused gate |
| Stage 4 보고 | `mydocs/working/task_m100_2392_stage4.md` | 최종 regression·metrics·PR 준비 |
| 최종 보고서 | `mydocs/report/task_m100_2392_report.md` | 계획 대비 결과, 완료 조건과 잔여 위험 |
| PR 초안 | `mydocs/report/task_m100_2392_pr_draft.md` | PR 본문과 review 요청 초안 |

임시 metrics JSON/summary와 E2E screenshot/report는 `output/` 또는 task 전용 임시 경로에 생성한다. 공식
`mydocs/metrics/frontend/2026-07-11/` snapshot은 변경하지 않는다.

## 9. 구현 계획서에서 확정할 사항

| 결정 | 수행 계획의 기본 방향 | 구현 계획 판단 기준 |
|------|-----------------------|---------------------|
| 계산 경계 | plain data 기반, DOM/WASM detail 배제 | 인자 수, type 응집도, fixture 작성 가능성 |
| 파일 배치 | 기존 UI ownership 안에서 최소 분리 | 새 public surface와 순환 import 방지 |
| patch 분할 | 공통 / shape·OLE / image | field ownership과 object type별 중복 |
| target orchestration | patch 계산과 분리 | 5개 setter target을 독립 검증 가능해야 함 |
| type 설계 | 필요한 최소 draft/patch type만 도입 | `any`·unknown cast 확대와 거대 context 금지 |
| test 방식 | data-driven calculation + orchestration record | private DOM 구조·함수 순서에 대한 과결합 방지 |
| metrics target | global/file aggregate 순감소 | 단순 함수 이동과 새 threshold 초과 차단 |
| commit 경계 | characterization / patch / orchestration / evidence | 각 commit 독립 review·revert 가능성 |

private class method 추출만으로 충분한지, 별도 internal module의 pure calculator가 필요한지는 구현 계획서에서
dependency count와 testability를 비교해 확정한다. 별도 module 자체를 목표로 삼지 않는다.

## 10. 검증 계획

### 10.1 정량·정적 검증

- pre/post measured source clean과 동일 base commit 확인
- `picture-props-dialog.ts` Total CC/Max CC/CC>25 합이 647/348/560 미만
- frontend 전체 Total CC/CC>25 합이 12,369/4,297에서 순감소
- `handleOk` CC 348과 381 LOC 모두 감소
- global Top 20, CC>25/100 개수·합, Max와 stable function diff 공개
- 추출 helper의 CC>25 신규 발생 0. 예외는 reviewer 승인 필요
- 대상 외 stable function의 설명되지 않은 complexity delta 0
- public/WASM/runtime dependency 관련 파일 변경 0
- TypeScript diagnostics 0, `git diff --check` PASS

### 10.2 자동 gate 후보

| 표면 | 명령/확인 |
|------|-----------|
| metrics 도구 | `node --check scripts/frontend-metrics.mjs` |
| pre/post metrics | `node scripts/frontend-metrics.mjs --out ... --summary ...` |
| same-base compare | `node scripts/frontend-metrics.mjs --compare <pre> --out ... --summary ...` |
| Studio unit | `npm --prefix rhwp-studio test` |
| Studio production | `npm --prefix rhwp-studio run build` |
| 그림 속성 E2E | `npm --prefix rhwp-studio run e2e:undo -- --mode=headless` |
| repository | 변경 allowlist, `git diff --check`, status clean 확인 |

로컬 Chrome executable과 Vite server 준비 방식, E2E artifact 경로와 정리 명령은 구현 계획서에서 환경별로
확정한다. 기본 package script는 WSL host CDP를 사용하므로 macOS 로컬 검증에서는 명시적 headless mode를
사용한다.

### 10.3 시각 검증 판정

이번 범위는 DOM 구조·CSS·렌더링을 변경하지 않으므로 별도 visual sweep을 기본 완료 조건으로 두지 않는다.
기존 headless E2E screenshot은 dialog가 실제 경로로 동작했다는 보조 evidence다. 구현 중 DOM 생성이나
표시 상태가 바뀌면 이는 범위 drift이며, 작업을 중단하거나 별도 승인 후 visual gate를 추가한다.

## 11. SOLID·복잡도 적용 방식

평가 단위는 frontend 전체가 아니라 `picture-props-dialog` apply pipeline과 #2392 diff다.

| 원칙 | 이번 작업의 review 질문 |
|------|-------------------------|
| SRP | DOM 입력, patch 계산, target routing, undo orchestration의 변경 이유가 경계로 구분되는가 |
| DIP | 계산 정책이 `HTMLElement`, `WasmBridge`, `EventBus` concrete detail 없이 검증 가능한가 |
| OCP | object type별 규칙 추가가 기존 전체 pipeline 재작성 대신 제한된 계산 경계에서 가능한가 |
| LSP | 명시된 substitute가 없으면 점수를 추론하지 않고 setter target 계약만 검토하는가 |
| ISP | 새 context/type이 사용하지 않는 모든 DOM/WASM detail을 강제하지 않는가 |

`solid_scoring_guide.md`의 공식 점수는 maintainer/collaborator가 같은 평가 단위에 근거를 남기고 조정한
경우에만 기록한다. CC·LOC 감소를 SOLID 점수로 자동 변환하지 않는다.

복잡도 판정은 #1904/#2130의 최신 교훈을 따른다. Max와 threshold 개수만 보지 않고 Total CC, global Top 20,
CC>25 합과 stable function diff를 함께 사용해 복잡도를 다른 함수로 옮긴 결과를 차단한다.

## 12. 주요 위험과 대응

| 위험 | 대응 |
|------|------|
| `||` default 정리 중 0/NaN 의미 변경 | 기존 표현을 characterization fixture로 고정하고 별도 기능 변경 금지 |
| diff-only/always-send 정책 혼동 | field별 정책 표와 unchanged fixture를 Stage 1에서 고정 |
| OLE와 shape/image 공통화 과잉 | object type별 허용 field를 유지하고 분기 삭제를 목표로 하지 않음 |
| header/footer와 cell target 우선순위 변경 | 5-target 호출 기록 test로 setter와 인자 순서 검증 |
| undo/fallback event 횟수 변경 | InputHandler 유무와 empty patch case를 별도 검증 |
| 거대 draft/context로 책임 이동 | helper dependency count와 사용 field를 review하고 ISP 위반 시 재설계 |
| 함수 분할로 Total CC가 증가 | 중간 metrics gate와 stable diff로 Stage 3 전 차단 |
| source-oriented test가 리팩터링에 과결합 | 검증 의도는 유지하고 계산/호출 결과 중심 test로 보강 |
| upstream undo/dialog 변경과 충돌 | 각 Stage 시작 전 latest devel과 열린 PR을 확인하고 같은 파일 변경 시 중단·재평가 |
| UI 변화가 리팩터링에 혼입 | DOM/CSS diff allowlist 위반 시 별도 이슈로 분리 |

## 13. 완료 조건

- [x] #2392 assignee와 추적 상태가 현재 작업자 기준으로 정리됨
- [x] implementation 전 field/target/undo characterization이 완료됨
- [ ] `handleOk`의 patch 계산과 side-effect orchestration 책임이 분리됨
- [ ] `populateFromProps`와 범위 외 계약에 기능 diff가 없음
- [ ] 대상 파일 Total CC/Max CC/CC>25 합이 각각 647/348/560 미만
- [ ] frontend 전체 Total CC와 CC>25 합이 12,369/4,297에서 순감소
- [ ] stable function diff가 단순 복잡도 이동이 아님을 증명함
- [ ] Studio unit/build와 headless undo contracts PASS
- [ ] public contract, WASM schema, runtime dependency와 UI behavior 무변동 확인
- [ ] 단계별 문서와 최종 보고서 작성
- [ ] 작업지시자 승인 후 draft PR 생성과 maintainer/collaborator review 요청
- [ ] reviewer 승인과 PR CI PASS 후에만 merge/close 판단

## 14. 다음 관문

1. 작업지시자의 수행 계획과 구현 계획 승인을 반영했다.
2. 계획 commit을 최신 upstream에 통합했다.
3. Stage 1 pre metrics, field/target/undo characterization과 baseline gate를 완료했다.
4. Stage 1 결과 승인 후 Stage 2로 넘어간다.
5. GitHub 댓글, push와 PR 생성은 각각 초안을 먼저 제시하고 작업지시자 승인 후 수행한다.

이 문서는 수행 범위와 gate를 고정하며 구체적인 함수·type·파일 배치는 구현 계획서에서 확정한다.
