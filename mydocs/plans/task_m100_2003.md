# 수행계획서 — Task M100 #2003: 1차 리팩토링 라운드 4 (B 블록 RunEmitState + GSO 내부 분해)

- 이슈: #2003 (계획 #1883 v2, umbrella #1582, 선행 #1904·#1925·#2001) / 브랜치: `local/task2003`
- 작성일: 2026-07-06 / 거버넌스: v2 전 조항 준수
- 작업지시자 확정 범위: 후보 ① + ② (2026-07-06)

## 1. 목표

1. **① `layout_composed_paragraph` B 블록**(run 방출 루프 1,056줄, 함수 현 CC 226 = 전체
   1위)을 `RunEmitState` mut-묶음 struct 설계 후 추출 — 라운드 2 이연분의 해소.
2. **② `parse_object_control_char` 내부 분해**(ch==10 표/글상자/수식/버튼 861줄, CC 104)
   — 라운드 3 통추출 산물의 2차 분해 (HWP3 격리, 저위험).

효과 목표: 1위 함수 4라운드 연속 해소 + CC>25 재추적(과도기 누적분 84 확인).

## 2. 수행 단계 (관문형)

### 1단계 — 의존 재실측 + `RunEmitState` 설계 + 구현계획서
- B 블록: 라운드 2 실측(읽기 45/mut 11)을 현 코드로 재검증(그 사이 #1926/#1945 유입).
  mut 11의 struct 필드 확정(y/char_x_map/baseline/raw_lh/pending_right_tab_render/
  pending_right_leader_digit_render/inline_tab_cursor_render/run_char_pos/
  current_line_reserved_tac_picture_height 등) + 읽기 스칼라의 vars 묶음 여부 판단.
- ② ch==10 블록: 내부 구조 정찰(셀 루프 135줄 포함) + 의존 실측.
- `task_m100_2003_impl.md` → 승인.

### 2단계 — 추출 1: ② GSO 내부 분해 (저위험 선행)
- ch==10 블록 → `parse_hwp3_drawing_object` 계열 함수. `Hwp3CharScan` 계약 재사용 여부는
  1단계 실측으로 확정. 게이트 전수 + 커밋 + 보고 → 승인.

### 3단계 — 추출 2: ① B 블록 (`RunEmitState` + 본 추출)
- struct 도입 커밋과 추출 커밋 분리(동작 불변 검증 단위 유지). 게이트 전수 + 보고 → 승인.

### 4단계 — 재평가 + 최종 보고
- `--snapshot r4 --no-coverage` + CC 대비표 + CC>25 추세(과도기 누적 84 재평가) +
  다음 라운드 제안.

## 3. 게이트 (매 추출 공통)

fmt/clippy 0 · `cargo test --profile release-test --tests` FAILED 0 · OVR 5샘플
**추가 변동 0**(기지 #1936발 3건 동일 유지) · hwp5_roundtrip_baseline · 페이지 오라클.

## 4. 위험 관리 / 범위 제외

- **v2 §1 금지 목록**: B 블록 인접 소스분기(is_hwpx_source 1921 계열·hwp3_variant 2194
  계열)는 추출 경계 밖(caller 유지)으로 긋는다. 접촉 발견 시 보류가 기본값.
- B 블록은 렌더 방출의 심장부 — 게이트 변동 시 즉시 중단·원인 규명. #1936발 기지 OVR
  3건과의 구분을 위해 추출 전 기지 시그니처를 재확인 후 착수.
- 범위 제외: 후처리 `Hwp3FlowState`(후보 ③), typeset 재성장 흡수(④ — Phase P 연동),
  #1936 OVR 이탈 대응(별도 시각 판정 사안).
