---
kind: report
status: active
canonical: mydocs/plans/task_m100_3308.md
last_verified: 2026-08-01
---

# Task #3308 최종 보고 — 중첩 표 폭 스트레치 하한 + 셀 내 가운데 배치

- Issue: [#3308](https://github.com/edwardkim/rhwp/issues/3308) (M100) — 외부 리포터
  `Neurlect`, #3307 과 동일 문서 p7 / 브랜치 `local/task3308` / 2026-08-01 당일 완결
- 단계 기록: `mydocs/working/task_m100_3308_stage{1,2,3}.md`
- fixture·정답지: #3307 자산 재사용

## 증상별 결론

- **증상 2 (용지규격 문구 소실)**: 현행 devel 에서 이미 해소 — 존재·위치 정답지
  정합(0.2/2.4px). #3127(v0.8.0) 귀속 유력(리포터 회신에 안내, 릴리스 회신 시 확정).
- **증상 1 (직인 위치)**: 텍스트박스가 아니라 **1×2 중첩 표의 오른쪽 셀**(바깥 4변
  0.7mm 주황 테두리 — 작업지시자 편집기 판독). 전면 스트레치 정규화(#2195 기원,
  #2308 오버레이 이관)가 선언 435.1px 표를 부모 셀 폭 641.3px 로 늘려 직인 셀이
  한컴 대비 **+97.5px** 밀렸다.

## 한컴 규칙 실측 (권위 확보)

- 편집기 크기 판독 **115.13mm = 선언 폭 정확 일치** / 한컴 HWP5 재저장본도 선언 유지.
- 편집기 위치 값(단 왼쪽 21.96mm=선언 h_off)은 **조판에 쓰이지 않음** — 선언 offset
  모델 15~21px 불일치, **셀 내 가운데 모델 0.6px 정합** (작업지시자 "표 속성 값으로
  조판되지 않음" 판정).
- 보호 대상 분리: #2195 규제영향분석서 계열 스트레치 비율 0.956~0.995 vs 직인 0.679.

## 수정

1. `render_normalization.rs` — `NESTED_STRETCH_MIN_RATIO = 0.9` 하한. 근소 미달만
   셀 폭 스트레치(보호 대상 유지).
2. `table_layout.rs` `compute_table_x_position` — 하한 미만 비-TAC 중첩 표는
   선언 폭 유지 + 셀 내 가운데. 발동 좁게 가드(비-TAC·셀 내부·비율<0.9).

효과: 직인 x 696.2 → **600.2** (한컴 598.7 대비 1.5px).

## 검증

| 게이트 | 결과 |
|---|---|
| 좌표 계약 테스트 (`issue_3308_nested_table_width.rs`, render tree JSON, ±5px) | 통과 |
| red-check 두 축 독립 | 가드 제거→696.2 원결함 재현 / 가운데 제거→501.8 좌편향 — 각각 FAILED 확인 |
| #2195 쪽수 게이트·표/중첩 계열 91건·정규화 단위(경계 계약 신설) | 전부 통과 |
| release-test 전체·clippy·Skia 3종·wasm·fmt | exit 0 |
| 666건 쪽수 A/B · 이중 baseline | 차이 0 · 무변 |
| **작업지시자 시각 판정** | **통과** — 직인 박스가 "…요구권자(직위)" 인접 위치로 |

## 교훈

1. **편집기 대화상자 값 ≠ 조판 값** — 저장 h_offset(21.96mm)을 편집기가 표시하지만
   조판은 다른 규칙(셀 내 가운데)을 쓴다. 선언 필드의 stale 가능성을 항상 정답지
   픽셀로 교차 검증할 것.
2. 상한/하한 없는 정규화의 극단 포섭 — 오늘 #3236(1.5×)·#3308(0.9) 두 건 모두 같은
   패턴. 정규화 도입 시 보호 대상의 실측 분포로 경계를 함께 정의해야 한다.
3. red-check 두 축 독립 증명 — 복합 수정은 축별로 제거해 각각의 필요성을 증명.

## 후속

- PR 준비 완료(생성 별도 승인). merge 후 리포터 완결 회신(적용 버전 + #3127 귀속).
