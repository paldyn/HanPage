# Task #1760 최종 보고서 — 표 렌더 높이 계통 과대: 행 단위 분해 판별 (수정 없음)

## 요약
#1759 서베이의 지배 원인 후보 "표 높이 계통 과대(+25:1)"를 행 단위로 분해한 결과,
**표 레이아웃 알고리즘 결함이 아님**을 판별. (a) 상수 계열(+0.6~0.8px/표)은 행별
±0.1~0.2px 측정 반올림 노이즈의 누적, (b) 대형 계열(+5~11px)은 **특정 행(대형 폰트
제목 셀)의 콘텐츠 높이 차** — 셀 내부 줄간격 메트릭 도메인으로 재분류. 코드 수정 없이
판별 보고로 종결(수행계획서 한계 조항).

## 근거 (Stage 1 프로브)
- 3187293 (34×5, +0.8): **전 행 ±0.2px 일치** — 노이즈 누적 확정.
- 2501937 (12×16, +7.1): **row 0 단일 행 +7.0** (나머지 11행 ±0.2px). row 0 = 빈 줄 +
  26.7px 대형 폰트 제목 + 텍스트 3줄 — 소형 폰트 pitch 는 21.3px 정확 일치, 차이는
  대형 폰트 제목 줄 전후 간격에 집중.
- 행비례(+0.4px/행) 가설 기각 — 특정 행 집중 구조.

## 처리
- 이슈 #1760: 판별 결과 코멘트 + "셀 내 대형 폰트 줄간격 차"로 재분류(클로즈는 머지 시).
- 추적 이슈 #1759: 후속 방향 갱신 — per-line 정렬 하니스 확장 + 대형 폰트 줄간격 표본
  확대가 다음 단계. 하니스 해석 가이드: **|dh| < 1px 는 측정 노이즈로 간주**.

## 검증
소스 무변경(조사·문서 전용) — 기존 게이트 영향 없음.

## 산출물
- 판별: `mydocs/working/task_m100_1760_stage1.md` (행 분해 표 포함)
- 프로브 데이터: 스크래치(세션 로컬), 서베이 TSV `output/poc/drift_survey1/`
