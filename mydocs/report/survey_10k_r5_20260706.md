# hwpdocs 5차 10,000 표본 — 회귀 검증(#1937·#1949·#1950) + 신규 결함 발굴

- 일자: 2026-07-06 / 브랜치: `local/task1950`(이번 세션 #1937·#1949·#1950 **모두 포함**)
- 표본: hwpdocs 전체 **442,468개**(4차 대비 +30k)에서 시드(**2026070605**) 무작위
  **10,000개** (hwp 5,906 / hwpx 4,094). 산출물: `output/poc/survey_10k_r5_20260706/`
- 목적: 이번 세션 세 수정의 신선 표본 회귀 검증 + 신규 결함 발굴

## 1. 불러오기/저장 — 실질 결함 0

| verdict | 건수 | 판정 |
|---|---:|---|
| OK | 9,913 | 정상 |
| FORMAT_SKIP | 60 | 미지원 포맷(정상 스킵) |
| LOAD_FAIL | 22 | 전부 정당(DRM 6·빈 파일 11·HTML 3·손상 ZIP 2) |
| ENCRYPTED_SKIP | 3 | #1946 정분류 작동 |
| SAVE_FAIL | 2→**실제 1** | KIC=분류기 오탐(PASS); 3123273=IR_DIFF diff=2 but ir-diff=0(패키지 무해) |

- **실질 저장 결함 0.** 세 수정이 신선 표본에서 저장 회귀 없음.
- 분류기 개선: 파일명 대괄호(`[KIC]`·`[0811]`) 오탐 제거 — 줄시작 앵커 + verdict 화이트리스트.

## 2. pi-page vs 한글 오라클 (10,000건)

| 판정 | r5 | r1~r4 |
|------|-----:|-----:|
| MATCH | **92.8%** | 92~94% |
| PAGE_DELTA | 4.3% | 3.5~4.4% |
| PI_MISMATCH | 2.2% | 2.1~3.0% |
| ERR | **0.3%** | 0.2~6.9% |

- **r1~r4와 완전 일관.** ERR 0.3%(COM 안정 — r4의 6.9% 크래시 없음)로 실효 지표 최고.
- 결재문서본문 PI_MISMATCH 73(#1920), PARA_COUNT 대부분 오라클 단일문단 artifact.

## 3. 세 수정 회귀 검증 — 회귀 없음

- **#1937(각주 continuation)**: PAGE_DELTA **양수 극단 최대 +14**(r4 +88·r2 +315 같은 표
  과대 폭주 **없음**). #1937 canonical(소상공인 231→52) 계열 과대 페이지 완화 확인.
- **#1950(HWP3 탭)**: HWP3 문서 render-diff 이탈 신규 급증 없음(저장/pi 축 정합).
- **#1949(성능)**: 서베이 중 타임아웃 없음.
- **과소 페이지(−97 등)는 #1937 무관**: 참조 바이너리(upstream/devel, 세 수정 미포함)를
  별도 target 으로 빌드해 대조 → −97/−20/−16 **참조=현재 페이지수 동일** → 기존 결함,
  내 수정과 무관 확정.

## 4. 신규 결함 → 이슈 등록

- **#1995 등록**: **RowBreak 표 다수 문서 과소 페이지**(rhwp 171 vs 한글 268, −97).
  Δ≤−5 6건 전부 RowBreak 표 수십~수백 개 연구/보고서. 표 콘텐츠 높이 **과소측정**으로
  문서가 적은 페이지에 압축(#1937 과대의 반대 축). fill: 평균 59%, over-full 19/171.

## 5. 결론

- **회귀**: 이번 세션 세 수정 전부 신선 표본에서 유지, 저장·pi 축 회귀 0. 과대 페이지 극단
  대폭 완화(+88→+14).
- **신규 결함**: 1건(#1995 과소 페이지, 기존 결함이나 서베이로 신규 포착·등록).
- 서베이 축(로드/저장/pi) 안정 상태.
