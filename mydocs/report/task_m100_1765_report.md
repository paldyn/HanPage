# Task #1765 최종 보고서 — 병합 셀 경로 가설 기각, per-line 누적으로 재분류 (수정 없음)

## 요약
17931383(+5.2px)을 병합 셀 경로의 trailing ls 결함으로 가정했으나(이슈 #1765), 행높이
대조로 기각. 실체는 **선언높이를 진짜 초과하는 밀집 셀(row9, 23문단 ~50줄)의 per-line
콘텐츠 측정 누적(+0.1px/줄)** — razor 가족(#1759 잔여)이며 양 엔진 모두 행을 확장하고
rhwp 가 5px 더 클 뿐. 소스 무변경으로 종결(시도한 2-b 가드는 증거 부재로 되돌림).

## 근거
- 셀 전부 rs=1 — 병합 경로 미경유.
- +5.2px 는 row9 단일 행: 렌더 810.7px vs 선언 764.1px — 콘텐츠 진짜 초과(한글도 ~805px
  확장). trailing ls 는 152HU(2px)뿐.

## 처리
- 이슈 #1765 재분류 코멘트(클로즈는 작업지시자 판단), #1759 에 per-line 대조의 정확한
  재현 사례(17931383 row9)로 등록.
- 2-b 경로 코드 비대칭은 #1763 보고서 한계 항목 유지 — 실증 재현 발견 시 재개.

## 산출물
- 재현 샘플: `samples/task1765/merged_cell_trailing_ls.hwp` (per-line 누적 대표 사례로 유지)
- 판별: `mydocs/working/task_m100_1765_stage{1,2}.md`
