# 10k 한글 오라클 서베이 r29 — 제출 PR 3건 검증·신규 기준선

- **표본**: hwpdocs 무작위 10,000건 — r15~r28 동일 표본(seed=20260716) → r28 문서 단위 직접 대비
- **바이너리**: `devel cc3829116` + 3커밋(통합 브랜치 `survey/r29-integration`, `2ea59d24b`).
  release + native-skia. 상세 구성은 `BINARY_FINGERPRINT.txt`
- **실행 환경(필수)**: `RHWP_FONT_PATH=ttfs/hwp;ttfs/windows` (#2898 이후)
- **오라클**: 한글 COM per-PI 쪽위치 + PDF 픽셀(tol=40, 2-up 복구 + SCALE 가드) — r24 하니스
- **실행**: 병렬 2갈래(25+25청크), 앞갈래 385.8분 · 뒷갈래 414.8분
- **하니스**: `output/poc/survey10k_r29_20260802/`

> **적용 범위 주의 (2026-08-03)**: 이 r29 바이너리는 #3740, #3755, #3774의 세 기능
> commit을 함께 포함한다. `review/planet6897-20260803` 누적 검토는 작업지시자 결정으로
> #3740을 제외하고 #3755·#3774만 적용했으므로, 아래 수치는 현재 두-commit 후보의 직접
> 기준선이 아니라 세-commit 조합의 측정 기록이다.


## 1. 구성

`devel cc3829116` + 3커밋. 모두 오늘 제출한 PR 이다.

| 커밋 | 내용 | 이슈 / PR |
|---|---|---|
| `369941908` | 한 문단의 두 번째 자리차지 개체가 자기 줄만큼 전진 | #3738 / PR #3740 |
| `e11276dc7` | 적합 판정 높이의 사다리 span 가드에 단조성 요구 | #3751 / PR #3755 |
| `2ea59d24b` | zone 전환 가드가 직전 zone 을 실소비로 재게 | #3765 / PR #3774 |

세 건 모두 **저장 사다리(`LINE_SEG`)를 rhwp 의 쪽 소비량으로 잘못 쓰던** 계열이다.
사다리는 한글의 쪽 경계를 담고 있어, rhwp 의 쪽 경계와 어긋나면 값이 무의미해진다.

## 2. 결과 — r28 대비 순증

| 지표 | r28 | **r29** | 차이 |
|---|---|---|---|
| **PI 정합** | 93.71% (9,286/9,909) | **93.76%** (9,292/9,910) | **+0.05pp** |
| **픽셀 평균** | 94.406% (n=9,548) | **94.406%** (n=9,549) | ±0.000pp |
| MATCH | 9,219 | **9,223** | +4 |
| PAGE_DELTA | 390 | 387 | −3 |
| PI_MISMATCH | 229 | 227 | −2 |
| PI_MISMATCH_CARET | 67 | 69 | +2 |
| STALL | 63 | 62 | −1 |

집계는 공통 10,000문서 · 문서 단위 dedup(비-STALL 우선).

## 3. 문서 단위 전이 — 개선 6 · 회귀 0

| 전이 | 문서 |
|---|---|
| `PAGE_DELTA → MATCH` | 1270000-200800047 제17차 범죄방지 및 형사사법위원회 |
| `PI_MISMATCH → MATCH` | 156532689 2가백신 접종 실시 보도참고자료 |
| `STALL → MATCH` | 18566767 지급명령부 |
| `PI_MISMATCH → CARET` | 1290000-201100004 지뢰 최종보고서 |
| `PI_MISMATCH → CARET` | 1611000-200900116 해양영재 최종보고서 |
| `PI_MISMATCH → CARET` | 36300012 결재문서본문 |

`1270000-200800047` 은 #3751 로컬 게이트에서 `89 → 90`(한글 90)으로 해소된 문서다 —
10k 오라클에서 확인됐다.

### 회귀 판정 — STALL 2건은 일시 무응답

본 실행에서 `MATCH → STALL` 2건(진주시 공영개발사업 회계규칙 별지서식)이 나왔으나,
그 2건만 따로 재측정하니 **15초에 둘 다 `MATCH`**(rhwp=1 hwp=1, vis=OK)로 돌아왔다.
한글 COM 의 일시적 무응답이지 렌더 회귀가 아니다.

## 4. 판정

**세 커밋은 안전하고, 쪽수 축에서 실제 순증을 냈다.**

- 렌더 회귀 0 — 픽셀이 소수점 셋째 자리까지 동일(94.406%). 조판·레이아웃 축이라 예상과 일치
- 쪽수 회귀 0 · 개선 6
- PI 는 r22 이후 93.68~93.78% 밴드에 정체돼 있었는데 **밴드 상단**으로 올라섰다

r28(skia 2커밋)이 "안전하지만 지렛대가 아니다"(개선 0 · 회귀 0)였던 것과 대비된다.

## 5. 계열 (r23 이후, 폰트-클린 구간)

| | PI | 픽셀 |
|---|---|---|
| r23 (07-24) | 93.71% | 94.274% |
| r24 (07-26) | 93.72% | 94.102% ← crop 폴백 회귀 |
| r25 (07-28) | 93.72% | 94.450% |
| r26 (07-29) | 93.71% | 94.405% |
| r27 (07-30) | 93.68% | — ← native-skia 누락 사고 |
| r28 (07-30) | 93.71% | 94.406% |
| **r29 (08-02)** | **93.76%** | **94.406%** |

r23 이전 픽셀은 폰트 경로 오염으로 비교 대상이 아니다.

## 6. STALL 코호트

r29 STALL 62건에 makeup 을 걸어 확인한 범위다.

| 목록 | 건수 | 결과 |
|---|---|---|
| `chunk_060` 일괄 격리 피해자 | 3 | 재측정에서도 ERR/STALL — 보호 문서·오염 파일 계열 |
| `chunk_061` wedge 용의 | 61 | 8건까지 돌려 회복 0 (문서당 워치독 600초라 중단) |
| 본 실행 `MATCH → STALL` | 2 | **재측정 즉시 MATCH** |

STALL 은 두 부류다 — **hang 성향 문서**(makeup 무익)와 **일시 무응답**(재측정으로 회복).
전이 판정에서 후자를 회귀로 읽지 않도록 반드시 재측정할 것.

## 7. 이번 실행에서 밟은 함정

r28 README 의 함정 1~5 는 모두 회피했다. 새로 밟은 것 셋을 기록한다.

| 함정 | 증상 | 교훈 |
|---|---|---|
| **6. 감시기 타임아웃이 서베이보다 짧다** | 1시간 상한 감시기가 만료되며 러너 python 을 죽였다. 한글만 고아로 남고 11청크에서 멈춤 | 서베이는 무기한 감시기로 띄운다. 재개는 안전하다 — `run_chunks.py` 가 완료 청크 `[skip]`, 미완은 `remaining.txt` 로 이어받는다 |
| **7. 행 기준 집계로 ERR 이 부풀어 보인다** | 진행 중 "ERR 133" 으로 읽혀 회귀를 의심했다. 실제는 `.part` 파일 + 재시도 중복 행 탓이고 신규 ERR 은 0 | 집계는 **문서 단위 dedup**(비-STALL 우선). r28 원본도 같은 성질이다(`rhwp:rc` 행 131 vs 고유 문서 53) |
| **8. makeup 전량 실행은 비싸다** | `chunk_061`(61건)은 문서당 워치독 600초라 최대 10시간 | 판정에 필요한 문서만 따로 목록으로 돌린다. 회귀 2건만 돌리니 15초 |

## 8. 기록 주의

결과 TSV 헤더의 `git_head=` 는 **저장소 루트의 HEAD** 를 찍은 값이라 서베이 바이너리
커밋과 다르다(r29: 헤더 `510b5dd75` vs 실제 `2ea59d24b`). 실제 값은
`BINARY_FINGERPRINT.txt` 를 볼 것.
