# hwpdocs 8차 10k 표본 호환성 검증 보고 — 불러오기/저장/렌더 PI↔페이지

- 일자: 2026-07-07
- 대상: `C:\Users\planet\hwpdocs` 전수 스냅샷 **517,565개** (r7 507,456 → 증가)
- 표본: **10,000개** (seed=20260708, 신규 population 재추출)
- 바이너리: `git base=36c3102f` — **upstream/devel(a23b8ae1) + 내 제출 열린 PR 5건 통합(survey8)**
  - 코드: #2011(전면이미지 페이지네이션), #2016/#2015(부동 RowBreak 표 vert_offset 이중계상)
  - 문서: #2013(r6), #2014(#2007/#2006 RCA), #2018(r7)
  - 5건 전부 클린 머지(충돌 0). `cargo build --release --features native-skia` 성공.
- 산출물(비추적, gitignored): `output/poc/survey10k_r8_0707/`
  — `pipage.tsv`, `gates.tsv`, `collapse.tsv`, `visual/`, 드라이버/분류 스크립트 일체.

## 1. 렌더 PI↔페이지 vs 한글 2022 (10,000)

방법: rhwp `dump-pages` PI별 시작 페이지 ↔ 한글(pyhwpx) `SetPos`+`current_page` 본문 문단 1:1 대조.
청크 드라이버(200/청크 새 프로세스, 청크 사이 Hwp kill)로 **전 10k err=0** 완주.

| 판정 | 건수 | 비율 | r7(seed=20260707) |
|------|------|------|-----------|
| MATCH | 9,275 | 92.75% | 93.33% |
| PAGE_DELTA | 436 | 4.36% | 4.10% |
| PI_MISMATCH | 225 | 2.25% | 2.10% |
| PI_MISMATCH_CARET | 17 | 0.17% | (r7 별도집계) |
| PARA_COUNT | 13 | 0.13% | 0.15% |
| ERR | 34 | 0.34% | 0.32% |

- **실이탈(PI_MISMATCH+PAGE_DELTA) = 661 / 9,966 = 6.63%** — r7과 **동일**(6.63%). **통합 PR 5건 무회귀.**
- 페이지 델타 버킷: −1 93, 0(PI만) 225, +1 230, ±2..3 79, **|Δ|>3 34**.
- 방향: rhwp-MORE 우세(과분할). |Δ|>3 중 MORE 44건(|Δ|≥3 기준), FEWER 소수.

### 대형 델타(|Δ|>3, 34건) 근본원인 분해

1. **[신규] 부동 글상자 다수 별지 서식 과분할 → #2019 등록**
   - `74312 벤처투자 시행규칙(안)`: **rhwp 81p vs 한글 18p (4.5배)**. 글상자 227개로 구성된
     별지 서식이 흐름에 파편화(35p near-empty) + 서식 격자 조각 렌더 깨짐. **시각 확증**.
2. **대형 보고서 그림 blank + 누적 과분할** (#1937/#1921):
   - `3690000 노인복지관 타당성(+133)`, `1270000 업무편람(+14)`, `1170000(+13)`,
     `6420000(+9)` 등 prism D0150xxx. 초반 페이지(목차)는 정상 정합, 그림이 빈 공간으로
     렌더되며 세로 공간 과점유 → 점증 과분할.
3. **표 행분할 near-empty 페이지** (#1995/#2007/#1842):
   - `156667091 보도자료(+20)`: 표가 행분할되며 마지막 행만 남고 나머지 백지인 페이지.
4. **과소(FEWER)**: 단일 대형 표 콘텐츠 미분할(#1995/#2007) + 줄높이 누적(#1921).
   - `4900000 화순군 사업계획서(−39)` 등.

### PI_MISMATCH 225 (페이지수 동일, PI 재배치)

- 다수가 단일 PI ±1 + 빈 문단 = **캐럿-개체 분리 오탐**(#1920/#1757). `[empty-caret?]` 태그 17건 별도.
- 나머지는 페이지 내 경계 재분포.

## 2. 불러오기 (Load)

- **파싱 성공 9,900건 전부 로드 성공**(dump-pages nopages 0).
- **ERR 34 = rhwp 버그 0건**: 암호화(ODF AES) / DRM(SoftCamp SCDSA·Fasoo DRMONE) / 빈파일(0B) /
  손상 OLE / HTML·XML 오파일. 전부 **정상 거부**(데이터·보안 문제). PARSE_FAIL 2 = 암호화 문서.
- 포맷 분포: hwp5 6,066 / hwpx 3,863 / hwp3 41 / unknown 30.

## 3. 저장 (Save)

### 3-1. 구조 손실 (IR 뼈대, 결정론적 전 10k)

`hwpx-roundtrip`/`hwp5-roundtrip` per-file (parse→serialize→re-parse IR diff + 2-round + BinData).

| rt_status | 건수 | 비고 |
|-----------|------|------|
| PASS | 9,924 | IR_DIFF=0, BinData 드롭 0 |
| IR_DIFF | **1** | `3068005 차량방호 실물충돌시험 편람`: diff=6, **r2=0** |
| ENCRYPTED_SKIP / PARSE_FAIL | 2 / 2 | 암호화 문서 |
| HWP3_SKIP / unknown | 41 / 30 | 게이트 범위 밖 / 위장·오파일 |

- **IR_DIFF 1건은 실손실 아님**: `r2=0`(2라운드 안정) + `ir-diff --summary` 결과 **차이 0** →
  1라운드 직렬화 정규화(속성 순서/기본값) 잔차. 의미적 저장 무손실.
- **파싱 성공 9,900건 사실상 전부 구조 저장 무손실.**

### 3-2. 시각 손실 (한글 PageCount, 200 subset)

원본↔rhwp 저장본을 한글로 재열람해 PageCount 대조 (`collapse.tsv`).

- 결과: **OK 69 / EXPAND 1 / COLLAPSE 0** (비오류 70건 기준). ERR(com_error) 130 =
  단일 프로세스 COM 열화(원본+rt 2opens/file → ~350opens 초과 붕괴, 분모 신뢰도 낮음).
- **EXPAND 1건 = `36449410 결재문서본문.hwpx` 11→12**: **IR_DIFF=0**(구조 무손실) + rhwp 자체
  11p 일관인데 한글이 저장본을 12p로 재계산 → **IR-invisible 저장 확장**. r7 관측 잔여와 동일
  **#2017 / #1589-잔여 클래스**, 신규 아님.
- **COLLAPSE 0 → 신규 저장 시각붕괴 신호 없음.** (향후 이 축은 청크 드라이버 필요 — 2opens/file라 청크 100건.)

## 4. 렌더 자기정합 (render-diff, 저장 전후 기하)

| rd_status | 건수 | 비고 |
|-----------|------|------|
| PASS | 9,954 | |
| OVER | 6 | 최대 `1613000 제안서` 838px(page수 3=3 보존, 단일 Group 노드 이동) |
| STRUCT_MISMATCH | 6 | `7025561 별지`(page수 3=3, 노드수 미세차) = #1993 계열 |
| LOAD_FAIL | 4 | 암호화 |

- OVER/STRUCT 12건 전부 **페이지수 보존** + 노드 매칭/저장위치 미세드리프트 = **#1993 render-diff
  자기정합 엣지**. 신규 사용자 결함 아님.

## 5. 결론

- **통합 PR 5건 무회귀**: 실이탈률 6.63%(r7 동일), 구조 저장 무손실, 로드 100%.
- **신규 결함 1건 → #2019 등록**: 부동 글상자 다수 별지 서식의 과분할·조각 렌더(74312, 시각 확증).
  #1994(글상자 겹침)와 동근이나 증상 반대 + 코퍼스 내 재현 파일 확보.
- 그 외 대형 편차 전량 기존 트랙 귀속: #1937/#1921(그림 blank+누적), #1995/#2007/#1842(표 행분할),
  #1920/#1757(캐럿 오탐), #2017(IR-invisible 저장 붕괴), #1993(render-diff 엣지).
- ERR/PARSE_FAIL 전량 데이터·보안(암호화/DRM/빈파일) — rhwp 정상 거부.

## 6. 이슈 등록

- **신규 #2019** [Rendering/Layout] 부동 글상자 다수 별지 서식 과분할·조각 렌더 (rhwp 81p vs 한글 18p).
- (재측정 기록) 본 r8 서베이 → 통합 PR 5건 무회귀 확인, #1937/#1921/#1995/#2007 롱테일 무회귀.
