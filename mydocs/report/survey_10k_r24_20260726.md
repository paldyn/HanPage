# 10k 한글 오라클 서베이 r24 — 폰트 PR 2건 검증 + 오라클 오염 가드 하니스

- **표본**: hwpdocs 무작위 10,000건 — r15~r23 동일 표본(seed=20260716) → r23/r19 문서 단위 직접 대비
- **바이너리**: `integration/r24-20260726` `53fd73789` = upstream/devel `efc4b6c4c`
  (#2813[#3270]·crop[#3256]·렌더4건[#3227]·외부그림[#3302] 기포함) + 열린 PR 2건
  **#3310**(#3300 skia 폰트 custom/번들 분리) + **#3318**(#3314 base-family 폴백).
  release + native-skia, SHA256 `6FA53C50…30EB` (`BINARY_FINGERPRINT.txt`)
- **하니스(신규)**: `harness_r24_template` — r23 대비 정답지 오염 가드 2종 추가:
  ① **2-up 복구**(시트별 분할, `pp<hpages` 오판 가드) ② **SCALE 가드**(한글 PDF 1/√2
  축소 인쇄 오염 — 검출 시 PDF 1회 재생성, 잔존 시 격리). `RHWP_FONT_PATH` 필수 적용.
- **실행**: sweepA/B 병렬 2갈래(각 25청크, ~12.8h) + makeup(65건). 하니스:
  `output/poc/survey10k_r24_20260726/`

## 1. PI 위치 판정 분포 (dedup 10,000)

| verdict | r24 | 비율 | r23 |
|---|---:|---:|---:|
| MATCH | 9,218 | 92.18% | 9,218 |
| PI_MISMATCH (+CARET) | 229 (+67) | 2.29% | 228 (+67) |
| PAGE_DELTA | 389 | 3.89% | 391 |
| ERR / STALL (갭) | 52 / 13 = **65** | 0.65% | 65 |

- **PI 일치율 93.76%** (graded 9,903) — r23 93.75%와 동등, 측정 갭 65 유지(최소)
- **쪽수 이동: r23 대비 MATCH→PAGE_DELTA 0건 / PAGE_DELTA→MATCH 1건** —
  폰트 PR 2건과 devel 신규분(#3302 등) 전량 **쪽수 무회귀** (r19 대비도 0/+8)

## 2. 렌더(픽셀) — 오염 제거 후 첫 정직한 기준선

| | r24 | r23 | 변화 |
|---|---:|---:|---|
| 채점 문서 (OK+OK2UP) | **9,546** | 9,158 | **+388 (커버리지 +3.9%p)** |
| 픽셀 평균 / 중앙 | **94.10 / 94.46** | 93.89 / 94.42 | **+0.21pp** |
| 잔존 TRUNC | **18** | 547 | **−529 (2-up 복구 529건)** |
| SCALE 오염 격리 | 142 | (오염이 점수에 혼입) | 신규 가드 |
| sub-80 꼬리 | **16** | 42 | 허수(오염) 제거 |

- 문서 단위 r23→r24: mean **+0.200pp**, +1pp 초과 개선 **468건** vs −1pp 초과 하락 9건.
  개선 상위는 종전 2-up/SCALE 오염 문서의 정상 채점 복귀(+26.2/+21.8/+18.4pp)와
  폰트 수정 코호트.
- **폰트 PR 2건 모집단 실증**: #3300 대표 156467716 픽셀 84.3 → **91.6 (r19 91.2
  상회)**, r23의 r19 대비 잔여 발산 코호트 소멸. crop 수정 대표(평가결과서)는
  95.97 유지.
- 잔여 하락 −2pp 초과 4건은 SCALE 가드 경계(예: 156736331)·사진 대형 문서 코호트 —
  관찰 축으로 유지(코드 결함 신호 없음).

## 3. 하니스 가드 실측 (r23 대비 신규)

- **2-up 복구 529건**: 종전 "측정 불가 TRUNC"였던 코퍼스 5.3%가 실측 편입
  (OK2UP 평균은 OK와 동질). 가로 1쪽 문서 오판 가드(`pp<hpages`) 유효.
- **SCALE 격리 142건**: 한글 PDF 1/√2 축소 인쇄 오염이 sub-80 너머까지 광범위했음을
  실증 — 종전 회차들의 픽셀 평균은 이 오염만큼 하향 편향돼 있었다. 재생성 재시도로
  일부는 정상 복귀, 잔존분은 픽셀 집계에서 제외.

## 결론

- **PR #3310·#3318은 10k 게이트 기준 merge 안전**: 쪽수 회귀 0, 픽셀 순개선
  (+0.20pp, 개선 468 vs 하락 9), 대표 결함 코호트 소멸.
- **r24를 신규 기준선으로 채택**: 오염 가드 적용 후 첫 정직한 픽셀 기준선
  94.10%/94.46%, 채점 커버리지 역대 최대(9,546), 측정 갭 최소(65) 유지.
- 이후 서베이는 `harness_r24_template` + `RHWP_FONT_PATH` 를 표준으로 한다.
