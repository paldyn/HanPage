# Task M100 #2222 최종 보고 — 렌더 성능: 회귀 귀속 + 레이어 JSON 캐시 (A안 부분 완결)

- 이슈: #2222 / 브랜치: `local/task2222` / 기간: 2026-07-12
- 처리 방식: A안(작업지시자 결정) — Rust 측 정정 반영 + 파이프라인 축 #2228 이관

## 결론

"최근 병합 후 체감 저하"를 3층으로 분해해 각각 처리:

1. **네이티브 무회귀 확정** — v0.7.18 태그 대조 벤치(4문서): 동등~개선
   (시장구조 42.0s→16.9s, #2223 /2 효과). 이번 주 렌더 정정들의 비용 영향 없음.
2. **반복 렌더 축 정정(`c1ba9c18`)** — #2227의 renderPage당 1MB 레이어 JSON
   재직렬화(15.2ms/회 = 렌더와 동급)를 (출력옵션 지문, JSON) 변형 캐시로
   65µs(233배)로 절감. 스크롤/입력 반복 경로 해소. TS 무접촉 — #2228과 충돌 없음.
3. **첫 렌더 파이프라인 축 이관** — 유휴 프리렌더 페이지당 169~319ms(종전
   ~72ms)의 구성(2회 페인트 패스 + 1MB fetch + JS parse)을 실측해 #2228
   (jangster77, 동일 코드 영역)에 개선 옵션 2종과 함께 공유.

토글(투명선/잘림보기) 클릭 334ms는 조판 영향 옵션의 전체 재조판 본질 비용 +
첫 렌더 — 캐시 대상 아님을 계측으로 확인 (setter의 트리 무효화는 정당).

## 검증

| 항목 | 결과 |
|------|------|
| 캐시 히트 | 15.2ms → **65µs** / 옵션 지문 정확성(토글 상이·원복 동일) |
| 표적 테스트 신설 | `tests/issue_2222_layer_json_cache.rs` (일관성+지문+성능 가드 <3ms) |
| fmt/clippy/전수 | 통과/0/**3,051/0** (렌더 불변 정정) |
| WASM 빌드 | 통과 (studio 확인용 제공) |

## 잔여/조건부 재개

- 첫 렌더·토글 파이프라인: #2228 처리 결과 확인 후 잔여 시 재개.
- 원 이슈의 dirty-페이지 재조판 캐시 설계(계획 3단계): 예외 반복(#2212)·JSON
  반복(본 건) 제거 후 실증 수요 재평가 — 필요 시 별도 이슈로 재개.

## 산출물

- 커밋: `52d2465d`(계획), `c1ba9c18`(캐시+테스트+1·2단계 보고)
- 코멘트: #2222(결산)·#2228(이관 공유)
- 문서: `working/task_m100_2222_stage1.md`, 본 보고서
