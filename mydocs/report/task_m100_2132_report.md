# 최종 결과보고서 — Task M100 #2132: compute_char_positions 중복 소거 (2차 R1)

- 이슈: #2132 (#2131 서브) / 2026-07-10 / v2.1 총량 판정 첫 적용 라운드.

## 결과 — 정직 판정

| 지표 | phase2 | c2r1 | 판정 |
|---|---|---|---|
| CC 총합 | 11,701 | **11,699 (−2)** | v2.1 기준 형식상 순감 — **실질 미미** |
| 코드 | — | **−132줄** (워크 2벌 → 1벌) | 유지보수 실익 (측정 로직 단일 소스) |
| 게이트 | — | 전수 통과 (2,973/0 · golden 8/8 · OVR 5/5 · issue_1116) | 행동 불변 |

## 원인 분석 → 산식 즉시 보정 (라운드의 실질 산출물)

중복이던 워크 골격은 **분기-희박**(특수문자/자간/북키핑) 구간이고, 분기-밀집부(폭 사다리·
탭 legacy 경로)는 의미 차이라 훅으로 각자 보존됨 → CC 소득이 구조적으로 작았다.
**교훈: 중복 소거의 CC 소득 ∝ 중복부의 분기 밀도.**

→ `tools/reduction_potential.py` 유형 ① 에 **분기 밀도 가중** 반영. 재스캔 순위 재편:

| 재편 후 상위 | 점수 | 비고 |
|---|---|---|
| `advance_row_block_cut` | 44 | 분기-밀집 중복 — 차기 1순위 |
| `measure_table_impl` | 36 | |
| `paginate_pass` (④) | 36 | 소스분기 — Provenance 보류 유지 |
| `apply_shape_props_inner` | 36 | 함수쌍 동형 |

종전 1위였던 compute_char_positions 류(분기-희박 대형 중복)는 순위권 밖으로 —
**같은 실수의 반복이 산식 차원에서 차단**된다.

## 게이트

전수 통과 + metrics `--snapshot c2r1 --diff phase2` (신규 게이트 첫 실전).
