---
kind: report
status: active
canonical: mydocs/plans/task_m100_3311.md
last_verified: 2026-08-01
---

# Task #3311 최종 보고 — malformed CFB 패닉 해소 확정 + 회귀 가드 신설

- Issue: [#3311](https://github.com/edwardkim/rhwp/issues/3311) (M100) — 외부 리포터
  `Neurlect` (cargo-fuzz 발견, 격리 CDR 파이프라인 하드닝 중 제보)
- 브랜치 `local/task3311` / 2026-08-01 당일 완결
- 단계 기록: `mydocs/working/task_m100_3311_stage{2,3}.md`

## 결론

**보고 시점에는 실재한 결함이었고, `6a761a793`(#3220, 2026-07-24)에서 이미
해소됐다.** 본 타스크의 산출물은 **그 계약을 고정하는 회귀 가드**다.

| 시점 | 하니스 결과 |
|---|---|
| 보고 커밋 `8d3bfa4b` (07-17, 0.7.19 계열) | 패닉 재현 — `cfb_reader.rs:407:43`, 리포터 보고와 동일 지점·형태 |
| `6a761a793` (07-24, #3220 악성 입력 방어 6건) | **해소** |
| `1b02247ff` (07-25, #3301) | 무패닉 유지 |
| 현행 devel | 177 케이스 무패닉 (Err 165 / Ok 12) |

## 판단 과정에서 지킨 것

`chrisryugj` 가 코드 판독으로 "이미 해소된 듯하다"고 정보 공유했고 실제로 옳았지만,
**판독만으로 닫지 않고 실측으로 확정**했다. 경계 검사가 "보이는 것"과 그 입력에서
"실제로 패닉하지 않는 것"은 다른 명제이고, 후자만이 리포터에게 회신할 수 있는
근거다. `git worktree` 로 각 시점 커밋에 동일 하니스를 적용해 귀속까지 특정했다.

계측 자체의 신뢰도도 먼저 검증했다 — 합성 하니스가 구 커밋에서 리포터 보고와 동일한
패닉을 재현하는 것을 확인한 뒤에야 "현행 무패닉"을 근거로 채택했다. 통과하는 테스트는
결함이 없다는 증거가 아니라 **결함을 잡을 수 있음이 증명된 뒤에야** 증거가 된다.

## 산출물

`tests/issue_3311_malformed_cfb_no_panic.rs` — 177 케이스:

- 리포터 보고 값 상수 명시(FAT 824 / DIFAT 128 / poison 1,851,072,928 / len 3,072)
- 합성 스윕 150건(길이 6 × FAT 카운트 5 × 손상 섹터 id 5)
- 실 샘플 헤더 7필드 뮤테이션 21건 + 절단 5건
- 자기 방어 단언 2개(케이스 수 급감 감지 / 전부 정상 개봉 시 실패)

red-check: worktree 로 보고 커밋에서 **이 파일 그대로** 실행해 동일 패닉 FAILED 확인.

## 검증

release-test 전체 exit 0 · clippy `-D warnings` exit 0 · fmt 통과 · 인접 파서 계열
38 passed · 가드 <0.01s. `src/` 무변경이라 Skia·wasm·시각 증적·쪽수 A/B 는 N/A
(근거 stage3 기록).

## 남긴 것

- **fuzz 아티팩트 수령 제안** — 리포터가 422KB 아티팩트 공유를 제안했다. 회신에서
  수령 의사를 밝혔고, 접수되면 회귀 코퍼스로 추가한다. 현재 가드는 "이 클래스의
  대표 조건" 고정 수준임을 명시했다.
- **cargo-fuzz 타깃 상설화**(CI 편입) — 범위 밖, 후속 이슈 후보. 오늘 #3668(진단
  집계 게이트)과 같은 "견고성을 계약으로 만드는" 축이다.

## 교훈

1. **"이미 고쳐진 것 같다"는 판독은 실측 전까지 가설이다** — 외부 리포터에게
   회신하려면 해소 커밋 귀속까지 실측으로 확정해야 한다.
2. **해소 ≠ 완료** — 고쳐졌지만 계약이 없으면 재유입이 조용히 통과한다. 오늘
   #3668 과 같은 구조의 결론(가드 신설)이 나온 두 번째 사례.
3. `git worktree` 는 과거 시점 검증의 안전한 도구다 — stash 함정(오늘 실증) 없이
   상주 stash 6개를 그대로 두고 작업했다.
