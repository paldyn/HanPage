# 10k 한글 오라클 서베이 r25 — 표 렌더 정합 2건(#3410+A/C/D) 모집단 검증

- **표본**: hwpdocs 무작위 10,000건 — r15~r24 동일 표본(seed=20260716) → r24 문서 단위 직접 대비
- **바이너리**: `integration/r25-20260728` `b77326a7d` = upstream/devel `e7352137c`
  + **PR #3410**(#3396 TAC 표 outMargin x-원점, merged) + **보류 패치 #3386 A/C/D**(r25 동승 —
  모순 셀 cellMargin·중첩 anchor om_top·행 재분배 cellSz). release + native-skia,
  SHA256 `F1F16C38…9704` (`BINARY_FINGERPRINT.txt`)
- **하니스**: r24 검증판 그대로(2-up 복구 + SCALE 가드), `RHWP_FONT_PATH` 필수
- **실행**: sweepA/B 병렬(각 25청크, ~12.7h) + makeup(65건)

## 1. PI/쪽수 — 완전 불변 (렌더 전용 설계의 모집단 확정)

| verdict | r25 | r24 |
|---|---:|---:|
| MATCH / PI_MISMATCH(+CARET) / PAGE_DELTA | 9,218 / 229(+67) / 389 | 동일 |
| ERR / STALL (갭) | 52 / 13 = 65 | 동일 |

- PI 일치율 **93.76%** (r24 동일), **r24 대비 쪽수 이동 양방향 0건** — 두 패치가
  조판(쪽수)에 비접촉임을 10k 전량으로 확정.

## 2. 렌더(픽셀) — 역대 최대 순개선

| | r25 | r24 | 변화 |
|---|---:|---:|---|
| 픽셀 평균 / 중앙 (OK+OK2UP 9,546) | **94.45 / 94.84** | 94.10 / 94.46 | **+0.35pp** |
| 문서 단위 +1pp 초과 개선 / −1pp 초과 하락 | **1,209 / 1** | — | 사실상 무하락 |
| flagged 코호트(#3386 대상 381건) | **+0.265pp, 하락 0** | — | 잔존 343건 |
| sub-80 꼬리 | 16 | 16 | 유지 |

- 개선의 원천: 한글의 "TAC 표 = outMargin 포함 폭의 문자" 배치 규칙 배선(#3410,
  대표 문서 괘선 [157.0, 435.0, 714.0] 한글 픽셀 일치) + 표 내부 행경계 cellSz
  정합(A/C/D). 표를 포함한 문서 전반이 고르게 상승했다.
- 커버리지/가드 상태는 r24와 동일(OK 9,017·OK2UP 529·SCALE 격리 142·TRUNC 18).

## 3. 판정

- **PR #3410 merge 안전** + **#3386 A/C/D 보류 해제·정식 제출(PR #3458)**
  (등재 원칙: 다음 10k에서 판정 → 통과). 두 수정의 합성이 하락 1건(−1pp 초과)
  대비 개선 1,209건.
- **r25 를 신규 기준선으로 채택**: 픽셀 94.45/94.84, 쪽수·PI·갭은 r24 동일.
- 잔여 축: flagged 343건의 잔차(셀 내 재래핑·글리프 잔차 등 별개 성분),
  sub-80 16건(사진 잔차), BIG 204(maxpages 정책).
