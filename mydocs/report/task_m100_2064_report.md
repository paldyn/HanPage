# 최종 결과보고서 — Task M100 #2064: 리팩토링 라운드 7 (typeset_endnote_paragraphs 내부 분해 1차 회전)

- 이슈: #2064 / 브랜치: `local/task2064` / 기간: 2026-07-08
- 수행계획서: `plans/task_m100_2064.md` / 구현계획서: `plans/task_m100_2064_impl.md`
- 거버넌스: #1582 → #1883 마스터 플랜 v2 (§0 stage-gate, §1 금지 목록, §5 산식)

## 1. 결과 요약

계획했던 보수 목표(저위험 국면 2건 추출, P6/P7 인계)를 그대로 달성했다.

| 지표 | 라운드 시작 (r6) | 완료 (r7) |
|---|---|---|
| `typeset_endnote_paragraphs` 공식 CC | **153** (전체 1위) | **138** (전체 2위) |
| 동 함수 줄수 · 분기 | 4,397 · 1,792 | 4,227 · 1,717 |
| 전체 최대 CC | 153 | **146** (`layout_composed_paragraph`) |
| CC>25 예외 함수 | 87개 | 87개 (신규 진입 0) |
| 행동 회귀 | — | **0건** (통산 유지) |

신규 함수 2건 — 모두 CC 25 미만으로 예외 목록에 진입하지 않음:

| 함수 | 국면 | 줄수 | 방식 |
|---|---|---|---|
| `compute_endnote_metrics` | P3 SSOT 메트릭 (127줄) | 161 | `EnMetricsVars`(Copy 26필드) + &mut bool 1, 호출부 2곳 신선 구성 |
| `emit_endnote_split` | P8 split 방출 (133줄) | 159 | 직접 파라미터 21개(단일 호출부), mut 1 → 반환 bool |

## 2. 단계별 이력

1. **1단계** — 국면 지도 작성: 8구획(P1~P8) 식별, 평탄 파이프라인 특성(지배 블록 2개뿐,
   let ~230, advance/emit 17곳 산재) 확인 → "국면 지도 없이는 추출하지 않는다" 원칙으로
   1차 회전 범위를 P3+P8로 확정.
2. **2단계** (d51b5613) — 추출 1 `compute_endnote_metrics`. **스캐너 사각지대 6호 발견**:
   클로저 캡처 변이(FnMut) — 컴파일러 E0384로 검출, &mut bool 파라미터 분리로 해소.
3. **3단계** (9e39f8ae) — 추출 2 `emit_endnote_split`. 컴파일러 타입 확정 3건
   (Option<f64>/bool/pa라미터 누락) 보정. 게이트 전수 통과.
4. **4단계** — 재평가 `--snapshot r7 --no-coverage` → `mydocs/metrics/2026-07-08-r7/`.

## 3. 게이트 (매 단계 전수 통과)

fmt ✓ / clippy 0 / `--tests` **2,943/0** / issue_1116 **13/13** / rowbreak 20/20 /
OVR 5샘플 회귀 **0건** (baseline 00014ecf ×4 + a05e6f1b).

## 4. 의존 스캐너 사각지대 체크리스트 (누적 6종 — 후속 라운드 필수 점검)

① eprintln 포맷 문자열 가짜 대입 ② 인덱스 대입 `v[i]=` ③ 클로저 파라미터/필드 리터럴
오탐 ④ 중첩 필드 push ⑤ `&mut var` 인자 전달형 변이 ⑥ **클로저 캡처 변이(FnMut)** —
fn 승격 시 컴파일러 검증으로 안전망 확보 (본 라운드 신규).

## 5. 잔여 국면 지도 인계 (2차 회전용)

| 국면 | 규모 | 비고 |
|---|---|---|
| **P6 fit 판정 연쇄** | ~1,600줄 (CC 밀집처) | 본 함수 CC의 최대 지분. 판정 함수군 분해가 2차 회전의 본론 |
| P7 배치·전진 | ~970줄 | emit/advance 17곳과 얽힘 — P6 선행 후 착수 권장 |
| P4 A2 fit 시뮬 | ~470줄 | P6과 판정 로직 공유 가능성 조사 필요 |
| P2 다단 누적 판정 | ~525줄 | 독립성 높음, 저위험 후보 |
| P1 문단 준비 | 86줄 | 소규모, 우선순위 낮음 |

## 6. 다음 라운드 후보 (r7 공식 CC 기준)

1. `layout_composed_paragraph` 잔여 — **146** (현 전체 1위)
2. `typeset_endnote_paragraphs` 2차 회전 (P6/P7) — 138
3. `typeset_block_table` 129 + `layout_table_cells` 124 (표 계열 2차)
4. `layout.rs:5913` — 121
