# 최종 결과보고서 — Task M100 #2148 (분해 완료, 수정 랜딩은 폭 축 선결)

**날짜**: 2026-07-10 / **브랜치**: `local/task2148` / **이슈**: #2148

## 1. 요약

80168 잔여 축(em 공식 상쇄의 반례)을 행 단위로 완전 분해했다. **세 참값 축**
— (a) NO_LS 줄높이 em 공식(#2150), (b) NO_LS 셀 중첩 표 가산(신규 발견),
(c) 래핑 폭(한글이 장문 1줄 더) — 이 확인됐고 (a)(b)는 per-row 오라클로
정밀 검증됐다. 페이지 게이트가 상쇄 체제에 pin 되어 있어 (c) 해소 전에는
어느 부분 조합도 랜딩 불가 — 소스 변경 없이 분해 성과만 남긴다.

## 2. 성과

1. **오라클 v2 확장** (`tools/hangul_row_heights2.py`): `--col N` 걷기(0열
   전병합 대응), `--pi` 표 선택, **v3 판별자**(GetAnchorPos List==0 + Para
   매칭 — 한글 HeadCtrl 중첩 표 열거 우회). 임의 표 per-row 오라클 확보.
2. **em 공식 per-row 재확증**: pi=271 8개 세그먼트 한글 실측 ≤0.2px 일치.
   폭 단독 157쪽 정합 = 행별 상쇄(leading 과대 +8.8×10행 ↔ r4/r6 −72.7px) 해부.
3. **신규 축 발견 — NO_LS 셀 중첩 표 소거**: `cell_nested_controls_bottom`
   의 para_top(lineseg vpos)=0 → 중첩 표 위 텍스트가 max() 에서 소거.
   가산 수정으로 r6 105.7→158.0 (한글 160.2). 359 recount 완전 중립.
4. **여백 차감 가설 기각**: 한글은 NO_LS 셀의 여백 문단도 전폭 래핑.
5. **구성 매트릭스 완성** (stage3): 폭단독 157✓ / +em 153 / +em+중첩 154
   (per-row 최정밀) / +중첩 156 — 부분 참값은 전부 1891 실패.

## 3. 후속 (수정 랜딩 경로)

1. **래핑 폭 축**: r4형 장문(한글 1줄 더 래핑, −18.2px/행) — 한글 줄바꿈
   지점 대조(문자폭은 Haansoft 대체로 정합됐으므로 잔여는 줄바꿈 규칙/
   어절 처리/quantize 의심). 해소 시 em+중첩 동시 정식화.
2. 중첩 가산 수정은 recount 중립·per-row 참 — 폭 축 완료 시 1순위 랜딩.

산출물: stage1~3 보고서, 도구 확장(커밋 0135f9a0), 실험 커밋 3건(merge 금지
라벨), `output/poc/task2148/` 분석 스크립트.
