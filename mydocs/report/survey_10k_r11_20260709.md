# hwpdocs 11차 10k 표본 검증 보고 — 렌더 PI↔페이지 + 시각(raster) 2차원

- 일자: 2026-07-09
- 코퍼스: `C:\Users\planet\hwpdocs` (.hwp/.hwpx population **676,725**)
- 표본: **10,000** (seed=20260711, r10과 중복 136건뿐 — 독립)
- 바이너리: `073c8ae7` 빌드 (= upstream/devel 3728abfb, 차이는 #2067 순수 리팩토링·동작 불변)
- 산출물(gitignored): `output/poc/survey10k_r11_0709/` — `sample_10k.txt`, `tsv/`, `vsweep.tsv`, 드라이버·하니스 일체

## 1. 위치 차원 — 렌더 PI↔페이지 vs 한글 2022 (10,000 전수)

| 판정 | r11 | r10 | r8 |
|------|-----|-----|-----|
| MATCH | 9,245 (92.45%) | 92.32% | 92.75% |
| PAGE_DELTA | 442 (4.42%) | 4.41% | 4.36% |
| PI_MISMATCH | 205 (2.05%) | 2.09% | 2.25% |
| **실이탈(PI_MISMATCH+PAGE_DELTA)** | **647/9,918 = 6.52%** | 6.56% | 6.63% |

- **무회귀**: 3회 연속 독립 표본에서 6.5~6.6% 동등.
- 최대 델타 **Δ-166**(`1490000-201500076 유아교육 지원체계 정책연구`, rhwp 82p vs 한글 248p) — **과소분할**(rhwp 적음), #1995/#2007 단일 대형 표/콘텐츠 미분할 계열.
- |Δ|>3 = 45건, 전량 기지 클래스(#1937/#1921/#2063/#2070/#1995/#2007/#1842). **신규 위치 결함 0.**
- STALL 53건 = 한글 COM 배치 wedge 아티팩트(r10에서 STALL_ALONE 0 확증).

## 2. 시각 차원 — visual sweep (rhwp export-png ↔ 한글 PDF 픽셀 비교)

**자립형 시각 오라클**: pyhwpx `save_as(PDF)`(한글 권위 렌더) ↔ rhwp `export-png`(native-skia) 페이지별 픽셀 일치율. PyMuPDF + PIL/numpy, poppler/rsvg 불필요. 층화 서브셋 **359문서**(MATCH 200 + PAGE_DELTA 99 + PI_MISMATCH 60, ≤80p), 균등 표본 페이지, 문서별 격리+타임아웃.

| 지표 | 값 |
|---|---|
| 채점 | 358건 (BIG skip 1) |
| **평균 픽셀 일치율** | **92.49%** (중앙 93.21%, 최대 99.93%) |
| 쪽수 일치(OK 251건) | 평균 **93.59%** |
| 쪽수 불일치(PAGEDIFF 107건) | 평균 89.91% |
| 분포 | ≥95%: 103 / 90-95%: 172 / 85-90%: 69 / 80-85%: 9 / <80%: 5 |

- **rhwp 라스터 렌더가 한글과 광범위하게 유사**(중앙 93%). 페이지 정합 문서(OK)가 더 높음(93.6%) — 위치·시각 정합 상관 확인.
- 저점 대부분은 (a) 폰트 안티앨리어싱 차이, (b) 과분할 문서의 페이지 밀림(정렬 어긋난 페이지 비교) — 기지 요인.

### 신규 발견 — native-skia export-png 검은-페이지 결함 → #2083

**쪽수 일치(OK)인데 특정 페이지 픽셀 ~0%** 케이스 = PI-page 오라클이 못 잡는 페이지 내 렌더 결함:

| 문서 | 페이지 | PNG 밝기 | SVG 텍스트 |
|---|---|---|---|
| `36397551 안전점검 결과보고.hwpx` | p4 | 0.1(검정) | **552** |
| `1760000-202000024 최종보고서.hwp` | 표지 | 10.9(검정) | **123** |

- **SVG/render-tree 는 정상**(텍스트 존재), **native-skia PNG 만 전체 검정**. 단일 페이지 격리 렌더에서도 검정 → 해당 페이지 고유 raster 결함.
- 범위: export-png 전용, SVG·pagination·studio 뷰어 정상 추정. visual sweep 저점 3건(<15% worst)을 교란.
- → **이슈 #2083** 등록. 기존 #907(투명배경)과 별개.

## 3. 결론

- **위치**: 무회귀(6.52%), 신규 위치 결함 0, 대형 델타 전량 기지 클래스.
- **시각**: rhwp 라스터 렌더 중앙 93% 유사(광범위 정합). 위치 오라클이 못 보던 **페이지 내 렌더 결함(native-skia 검은 페이지)** 을 visual sweep 로 신규 검출 → #2083.
- **방법론 확립**: PI-page(위치, 전수) + visual sweep(raster, 서브셋) 2차원 병행으로 pagination·시각 결함을 상호보완 검출.
