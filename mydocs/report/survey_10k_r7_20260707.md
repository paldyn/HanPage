# hwpdocs 7차 10k 표본 호환성 검증 보고 — PI↔페이지 + 불러오기/저장 손실

- 일자: 2026-07-07
- 대상: `C:\Users\planet\hwpdocs` 전수 스냅샷 **507,456개** (r6 477,286 → 증가)
- 표본: **10,000개** (seed=20260707, 신규 population 스냅샷 = 문서 재추출)
- 바이너리: `git_head=3a758e67` — **내 제출 PR 3건을 upstream/devel 위에 통합한 브랜치**
  - #2011 (`fix/2004-image-stack-pagination`, 코드): 전면 이미지 다수 앵커 문단 쪽당-1장 페이지네이션
  - #2013 (r6 서베이 보고, 문서) / #2014 (#2007/#2006 과소페이지 RCA, 문서)
- 산출물(비추적, gitignored): `output/poc/pi_page_hwpdocs_10k_r7/`
  — `final.tsv`(PI↔페이지), `rt_checkpoint.tsv`(구조손실), `collapse_final.tsv`(시각붕괴), `driver.py`

## 0. PR 통합 + 빌드/테스트

- 3건 전부 upstream/devel(`a23b8ae1`) 위에 **클린 머지**(충돌 0).
- `cargo build --release` 성공. `cargo test`: 실질 전건 통과.
  - svg_snapshot 5건 실패는 **알려진 로컬 CRLF 노이즈**(autocrlf=true, 내용 diff 0줄) — 회귀 아님.
- **#2011 회귀 없음 확정**: 아래 서베이 분포가 r6과 사실상 동일 + 저장 self-drift 0.

## 1. PI↔페이지 vs 한글 (10,000)

방법: rhwp `dump-pages` PI별 시작 페이지 ↔ 한글(pyhwpx) `SetPos`+`current_page` 본문 문단 0-기반 1:1 대조.

| 판정 | 건수 | 비율 | r6(seed42) |
|------|------|------|-----------|
| MATCH | 9,333 | 93.33% | 93.07% |
| PI_MISMATCH | 210 | 2.10% | 2.28% |
| PAGE_DELTA | 410 | 4.10% | 4.08% |
| PARA_COUNT | 15 | 0.15% | 0.20% |
| ERR | 32 | 0.32% | 0.37% |

**신규 표본인데 r6과 거의 동일한 분포 → #2011 실질 회귀 없음.**

### PAGE_DELTA 410 분해

- 방향: rhwp-**MORE 291**(71%, 과분할) / **FEWER 119**(과소).
- 크기: |Δ|=1 **309(75%)**, =2 46, =3-4 30, =5-9 13, =10-19 11, =20+ 1.
- **|Δ|≥3 = 55건**(유의미), **|Δ|≥5 = 25건**(대형).

### PI_MISMATCH 210 분해 (페이지수는 동일, PI 재배치)

- n_mismatch=1 **106건** 중 **98건이 단일 PI ±1** → 캐럿-개체 분리 오탐 후보(#1757/#1920).
- 나머지(n≥2) 104건 = 페이지 내 경계 재분포.

### 대형 델타(|Δ|≥5, 25건) 근본원인 = 두 기존 클래스

1. **단일 대형 표 콘텐츠 미분할** → 과소(FEWER):
   - `42065` 규제영향분석서(rhwp 6 vs 한글 17, Δ=-11): **1x1 단일셀 표 콘텐츠 8177px를 한 페이지에** 배치(1쪽≈950px). 행 분할 불가 표의 셀 내부 콘텐츠를 페이지 분할 못 함.
   - `1230000-…`(Δ=-16), `2681901`(별표1)의 일부. → **#1995 / #2007**(표 콘텐츠 높이 과소측정).
2. **줄 높이 미세 과소패킹 누적** → 과소:
   - `2681901`(해수부 별표1, rhwp 284 vs 312, Δ=-28): 단일 과대오버플로 없음(max_used 1101px), 56쪽이 1050~1101px로 조금씩 초과 → 284쪽에 누적 −28. → **#1921**(규제영향분석서·정책연구 롱테일).
- 과분할(MORE) 대형은 정부 연구보고서(D0150xxx prism) 표/도형 과밀 = #1937/#1842/#1921.

### ERR 32 = rhwp 실버그 0건

magic-byte·CLI 메시지 분류: 암호화(ODF AES-256) 6 + DRM(SoftCamp SCDSA/DRMONE) 11 + 빈파일(0B) 10 + 손상OLE 1 + HTML/XML 오파일 4. **전부 rhwp 정상 거부** — 데이터/보안 문제.

### PARA_COUNT 15 = 대개 한글 카운팅 아티팩트

`hwp_paras=1`(한글이 본문 1문단으로 보고)이 다수인데 **페이지수는 일치**(44=44, 95=95 등) → 정렬 지표만 붕괴, rhwp 페이지 정상. 예외 `1220000`(348 vs 433, 26 vs 13)은 표 과분할(#1937 계열).

## 2. 불러오기/저장 손실

### 2-1. 구조 손실 (IR 뼈대) — 결정론적, 전 10k

`hwpx-roundtrip`/`hwp5-roundtrip` per-file (parse→serialize→re-parse IR diff + 2-round + BinData).

| 포맷 | PASS(IR_DIFF=0) | 비고 |
|------|-----------------|------|
| hwpx | 3,764 / 3,804 (98.9%) | 나머지 100=확장자 위장(실체 HWP5, FORMAT_SKIP) |
| hwp5 | 6,104 / 6,196 (98.5%) | 나머지=암호화/DRM/빈파일 |
| 위장 재검사 | +32 PASS | .hwpx 위장 HWP5를 hwp5-roundtrip 재검사 |

- **파싱 성공 9,900건 전부 IR_DIFF=0, BinData 드롭 0, PARSE_FAIL 0 → 구조적 저장 무손실.**
- 검사 불가 ~100건은 전부 암호화/DRM/빈파일/오파일(데이터 문제).

### 2-2. 시각 손실 (한글 PageCount 오라클) — 250 subset

원본↔rhwp 저장본(rt)을 한글로 열어 PageCount 대조 (무작위 250, 249 rt 생성).

- **rhwp 자체 페이지 드리프트(orig vs rt dump-pages): 0/249** → 저장이 rhwp 페이지네이션 완전 보존(#1637 클래스 무재현).
- 한글 PageCount: **OK 246, COLLAPSE 2, EXPAND 1** (붕괴 0.8%).

| 판정 | orig→rt | 포맷 | 구조판정 | 파일 |
|------|---------|------|---------|------|
| COLLAPSE | 4→1 | hwpx | PASS(IR=0) | 21965845 별표제1호서식 임시요원(울산 정보통신과 인사규칙) |
| COLLAPSE | 8→5 | hwpx | PASS(IR=0) | 156710762 혼례준비종합체험 개최 관련 |
| EXPAND | 1→2 | hwpx | PASS(IR=0) | 156665850 특허청 채용 7,058명 확대 |

- **3건 전부 HWPX·구조 IR_DIFF=0인데 한글 시각 페이지수 변동 = IR-invisible 저장 손실**(#1589 현상).
- **#1589는 CLOSED**(#1594~#1598로 ~16%→~0.5% 해소 후 종료)인데 **신규 표본에서 잔여 0.8% 재현**
  (21965845 4→1 등 |Δ|=−3 급). #1589 종료 시 "표본 붕괴 미관측"이었으나 fresh 10k에서 재노출.

## 3. 결론

- **내 PR 3건 통합 무회귀**: PI↔페이지 분포 r6과 동일, 구조 IR_DIFF=0 전건, 저장 self-drift 0.
- **신규 rhwp 결함 클래스 없음** — 불일치 전부 기존 트랙 귀속:
  - |Δ|≥5 과소: **#1995/#2007**(표 미분할) + **#1921**(줄높이 누적)
  - ±1 롱테일 / 단일 PI: **#1921** / **#1757·#1920**(캐럿 오탐)
  - ERR: 0 버그(데이터/DRM/암호화)
- **저장 손실**: 구조 무손실(9,900/9,900 IR=0). **시각(한글) IR-invisible 잔여 붕괴 0.8%(HWPX)** — **#1589(CLOSED) 후속 등록 필요**.

## 4. 이슈 등록

- (신규) IR-invisible HWPX 저장 페이지붕괴 잔여 — #1589 후속(21965845 4→1, 156710762 8→5, 둘 다 구조 IR=0).
- (데이터 포인트) 42065 1x1 단일셀 표 8177px 미분할 → #1995/#2007.
- (재측정 기록) 본 r7 서베이 → #1921 롱테일 무회귀 확인.
