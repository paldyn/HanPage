# hwpdocs 5,200 표본 — rhwp 렌더링 레이아웃 vs OLE 한글 페이지·PI 매칭 조사

- 일자: 2026-07-03 / 브랜치: `local/survey-pipage`
  (최신 upstream/devel 7f9bc045 + 제출 열린 PR 5건: #1823~#1826, #1828)
- 표본: hwpdocs 전체 180,727개(.hwpx 95,911 / .hwp 84,816)에서 시드(20260703) 무작위
  **5,200개** (hwpx 2,772 / hwp 2,428)
- 방법: `tools/verify_pi_page_vs_hangul.py`(#1643/#1757) — 한글 OLE `SetPos` 문단별
  페이지 vs rhwp `dump-pages` PI 시작 페이지. 재개형 드라이버(50개 청크,
  `output/poc/survey_pipage/`). 위치(같은 페이지 내)는 MATCH 다쪽 표본 20건에
  한글 COM PDF 생성 + `tools/compare_line_baselines.py` 대조.

## 1. 페이지·PI 매칭 총괄

| 판정 | 건수 | 비율 |
|------|-----:|-----:|
| **MATCH** (전 PI 동일 페이지 + 쪽수 동일) | **4,912** | **94.5%** |
| PAGE_DELTA (쪽수 불일치) | 151 | 2.9% |
| PI_MISMATCH (쪽수 동일, 일부 PI 다른 페이지) | 134 | 2.6% |
| PARA_COUNT / ERR | 1 / 2 | 0.1% |

## 2. PAGE_DELTA 151건 분석

- 델타 분포: **±1쪽이 130건(86%)** — under(−1) 60 / over(+1) 70 로 균형.
  극단: −13(1), ±5(2), −4(3), −3(2), −2(8), +2(4), +3(1).
- 확장자 편중 없음 (hwp 61 / hwpx 90 — 표본 비율 감안 시 유사).
- 최악 −13: `3015131 해사노동적합증서 인증검사 점검표.hwp` (rhwp 48 vs 한글 61) —
  대형 점검표 under-pagination, #1774(대규모 페이지 불일치) 계열.
- **문서 클래스 군집**: "(법령안) 직제 시행규칙"·"(규제영향분석서)" 류가 −4~−5 로
  반복 등장 — 공통 서식(다쪽 표+개정문) 기인으로 추정, #1774 Δ-맵과 병합 조사 권장.

## 3. PI_MISMATCH 134건 분석

- 국소(≤2 PI) 87 / 다건(3+) 47. 다건은 대부분 **경계 1곳 시프트가 후속 PI 다수로
  계상**된 것 (예: 83352 규제영향분석서 pi66부터 한 쪽 밀림 → n=325).
- **확장자 강한 편중: hwpx 111 vs hwp 23** — 동일 렌더 코드에서 HWPX 파스 경로만
  어긋나는 계열(#1811 원인2 '합성 seg 유닛 구성', #1772 saved-vpos 신뢰 차)과 정합.
  HWPX 경로 정합 작업의 우선순위 근거.
- 국소 불일치 방향: rhwp 이른 쪽 76 vs 늦은 쪽 27 — rhwp 과적재(경계 직전 한 항목
  더 채움) 우세.
- **알려진 오탐 포함**: "물품검사(수)조서" 군집(pi9 rhwp_p2|hwp_p1 반복)은 #1757
  문서화된 쪽 경계 TAC 캐럿-개체 분리 패턴 — 시각 정합인데 캐럿 쪽수만 다른 오탐
  후보. 정밀 감사 시 이 군집을 선차감해야 실질 불일치율이 나온다.

## 4. 같은 페이지 내 위치 정합 (MATCH 표본 20건, 54페이지)

페이지·PI 가 MATCH 여도 위치까지 같은 것은 아니다:

| 유형 | 규모 | 예 |
|------|------|----|
| 정합 (|Δbaseline| ≤ 1pt) | 표본의 약 절반 | 18145031(0.12pt), 3194769(0.96pt) |
| 표 셀 국소 배치 차 (수~십수 pt 스텝) | 다수 | 3114781 p1 (셀 제목줄 −10~−11pt 스텝) |
| **다쪽 표 continuation 페이지 전체 상수 오프셋** | 지배 패턴 | 2448877 p2 **전체 −54.3pt**, 3114781 p2 **전체 −33.9pt** (p1 은 −1pt 정합) |
| 줄바꿈 상이로 페어링 불가(측정 한계) | 소수 outlier | 156757711 (663pt 는 페어링 아티팩트) |

- **신규 지배 패턴**: 다쪽 표/서식의 continuation 페이지에서 rhwp 콘텐츠 시작 y 가
  통째로 30~54pt 위 — 페이지·PI 매칭으로는 잡히지 않는(같은 페이지) 위치 결함.
  continuation 상단(반복 제목행/이월 잔여 높이) 회계 차이로 추정 → 신규 이슈 등록.
- 측정 방법론: bbox top 이 아닌 **baseline** 대조 필수(#1827 판별 — 폰트 ascender
  차이로 bbox 는 +2~3pt 오탐).

## 5. 권고 (우선순위)

1. **다쪽 표 continuation 페이지 상수 오프셋** (신규, 30~54pt — 시각 임팩트 최대)
2. HWPX 편중 PI_MISMATCH (합성 seg 계열 #1811 원인2·#1772 통합 트랙)
3. 법령안·규제영향분석서 클래스 PAGE_DELTA 군집 (#1774 Δ-맵에 클래스 축 추가)
4. #1757 캐럿-TAC 오탐의 자동 선별(도구 개선) — 조서류 군집 선차감
