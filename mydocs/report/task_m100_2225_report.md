# Task M100 #2225 최종 보고 — 그림 미지정 placeholder 컨텍스트 분기

- 이슈: #2225 / 브랜치: `local/task2225` / 기간: 2026-07-12
- 판정: **통과** (studio 편집 뷰 점선+아이콘 / 인쇄 등가 미출력 — 작업지시자
  2컨텍스트 확인)
- 재현: 36389312 결재문서 "심볼" 필드 (그림 bin_id=0 미지정)

## 결론

한컴 동작(작업지시자 확인 — 편집기: 점선 테두리+그림-없음 아이콘 정보 제공 /
인쇄·인쇄 등가: 미출력)에 정합하도록, layout이 미지정 그림을
`PlaceholderKind::MissingPicture` 의미 노드로 방출하고 백엔드가 분기:
web_canvas(편집 뷰) = 편집기식 아이콘 표시, svg·skia(export) = 무방출.
기존 OLE placeholder·정상 그림은 불변.

## 검증

| 항목 | 결과 |
|------|------|
| export-svg / export-png | f0f0f0 무방출 / 회색 픽셀 0 (한컴 2024 PDF 정합) |
| 정상 그림(로고 bin_id=1) | 비영향 (과억제 없음) |
| 표적 테스트 신설 | 트리/SVG/정상그림 3검증 (신설 API로 수정 전 컴파일 불가 = 구조 판별) |
| 픽스처 정합 | 데이터 없는 위치 프로브 2건 술어 확장 (bbox 동일) |
| fmt/clippy/전수 | 통과/0/**3,052/0** / OVR 3샘플 0건 |
| studio 실사용 | **통과** (WASM 빌드, 점선+아이콘 확인) |

## 후속

- **#2230 등록**(작업지시자 지시): placeholder 선택 + 그림 삽입 편집 기능 —
  hit-test 연결·BinData 왕복 포함 별도 처리.

## 산출물

- 커밋: `ac7aa01b`(계획), `5ac24b46`(정정+테스트+2·3단계 보고)
- 문서: `working/task_m100_2225_stage2.md`, 본 보고서
- 메모리: hancom_placeholder_print_suppress (편집기 표시 사양 보강)
