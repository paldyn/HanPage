# hwpdocs 10차 10k 표본 검증 보고 — 렌더 PI↔페이지 오라클

- 일자: 2026-07-09
- 대상 코퍼스: `C:\Users\planet\hwpdocs` (.hwp/.hwpx population **651,040**)
- 표본: **10,000개** (seed=20260710, 신규 재추출 — 이전 r8 seed=20260708, r9 seed=20260709와 무중복 목표)
- 바이너리: `devel HEAD=073c8ae7` (upstream 최신 merge 반영: #2065/#2052/#2034/#2016/#2011 등 전부 포함)
- 산출물(비추적, gitignored): `output/poc/survey10k_r10_0708/`
  — `sample_10k.txt`, `chunks/`, `tsv/chunk_*.tsv`, 드라이버(`run_chunks.py`)·집계(`aggregate2.py`)

## 1. 렌더 PI↔페이지 vs 한글 2022 (10,000)

방법: rhwp `dump-pages` PI별 시작 페이지 ↔ 한글(pyhwpx) `SetPos`+`current_page` 본문 문단 1:1 대조.
청크 드라이버(200/청크 별도 프로세스, stall-watchdog + ERR 재시도)로 완주.

| 판정 | 건수 | 비율 | r8(seed=20260708) |
|------|------|------|-----------|
| MATCH | 9,232 | 92.32% | 92.75% |
| PAGE_DELTA | 441 | 4.41% | 4.36% |
| PI_MISMATCH | 209 | 2.09% | 2.25% |
| PI_MISMATCH_CARET | 24 | 0.24% | 0.17% |
| PARA_COUNT | 37 | 0.37% | 0.13% |
| ERR(최종) | 0 | 0.00% | 0.34% |
| STALL(한글 COM 반복 wedge) | 57 | 0.57% | — |

- **실이탈(PI_MISMATCH+PAGE_DELTA) = 650 / 9,906(graded) = 6.56%** — r8(6.63%)과 **사실상 동일 → 무회귀.**
- graded 중 MATCH률 93.20%.
- PAGE_DELTA 버킷: −1 84, +1 242, −3..−2 18, +2..+3 53, **≤−4 11 / ≥+4 33 (|Δ|>3 = 44)**.
- 방향: rhwp-MORE(과분할) 지배 — r8과 동일.

### 대형 델타(|Δ|>3, 44건) 근본원인 분해 — **전량 기지 클래스**

1. **거대 다행 표 행높이 과대측정 → 과분할** (#1937/#2063/#2070):
   - **[최대 델타] `1130000-201900011 2017년도 세출구조조정`(D0150004): rhwp 748p vs 한글 315p (Δ+433)**.
     단일 표 **pi=1298 = 2195행×8열 RowBreak**가 페이지당 **평균 10.6행**(row≈41.5~79.3px)씩만 담겨
     **207쪽으로 분할**. 한글은 같은 표 영역에 ~2.4배 더 담음. #2063(화성시 별표2 CellBreak +51)와
     동일 메커니즘의 **RowBreak 변종·최대 인스턴스**. → **#2070 근거 보강 대상**.
   - `1543000-202400004`(Δ+11), `1130000-201600020`(Δ+9) 등 다수 D0150004 동근.
2. **대형 보고서 그림 blank + 누적 과분할** (#1921/#1937):
   - `1051000-202400143 면세유 특별관리`(D0150004): rhwp 272p vs 한글 148p (Δ+124). 그림 131개가
     빈 문단(`pi=57 h=286.9px "(빈)"`)으로 렌더되며 세로 공간 과점유 → 점증 과분할.
3. **과소(FEWER, rhwp 적음)**:
   - `1450000-201500124 TiSA`(Δ−100, 512 vs 612), `1320000-201800005`(Δ−11) 등 — 단일 대형 표
     콘텐츠 미분할/줄높이 누적(#1995/#2007/#1921).
4. **별표·보도자료 소수**: `2486817 [별표] 책임관 처리규정`(Δ−6), `156241895 보도자료`(Δ+12) 등 —
   기존 #1995/#2007/#1842 클래스.

**→ 신규 결함 시그니처 0건.** 모든 대형 델타가 #1937/#1921/#2063/#2070/#1995/#2007/#1842 로 귀속.

### PI_MISMATCH 209 (페이지수 동일, PI 재배치)
- 다수가 단일 PI ±1 + 빈 문단 = 캐럿-개체 분리 오탐(#1920/#1757). `PI_MISMATCH_CARET` 24건 별도 분리.

## 2. STALL 57건 재검증 완료 (개별-프로세스 격리 실행)

배치 문맥에서 한글 COM 간헐 wedge로 격리됐던 57건을 **각 파일 fresh 프로세스+타임아웃(150s)**으로 재실행.

| 결과 | 건수 | 해석 |
|------|------|------|
| MATCH | 19 | 배치 COM wedge 아티팩트 (단독 실행 시 정상) |
| PI_MISMATCH | 1 | `1140100-201700003 청탁금지법 시행 1년 성과분석`(235p) — 페이지수 동일, PI 재배치 |
| ERR(정상 거부) | 37 | `UNSUPPORTED_FILE_FORMAT`(미지원 형식) / `ODF encryption-data AES-256-CBC`(암호화 HWPX) — rhwp 명확한 에러코드로 올바르게 거부 |
| **STALL_ALONE** | **0** | **단독 실행 시 hang 하는 파일 없음** |

- **STALL_ALONE=0** → 원래 STALL은 pathological 파일이 아니라 **배치 COM 불안정 아티팩트**로 확정.
- 37 ERR 은 r8 "정상 거부" 클래스(암호화/DRM/미지원)와 동일. **rhwp 결함 0건.**
- 최종 통합 집계: MATCH 9,251 / PI_MISMATCH 210 / CARET 24 / PAGE_DELTA 441 / PARA_COUNT 37 / ERR 37 / STALL 0.
  실이탈 = **651/9,926(graded) = 6.56%** (STALL 해소 반영, r8 6.63%와 동등 — 무회귀 유지).

## 3. 결론

- **무회귀**: 실이탈 6.56%로 r8(6.63%)과 동등. upstream 최신 통합본에서 PI↔페이지 정합 유지.
- **신규 결함 없음**: 최대 델타 Δ+433 포함 전 대형 델타가 기지 클래스.
- **#2070 보강**: 최대 인스턴스 Δ+433이 **RowBreak** 거대 표에서 동일 행높이 과대측정 과분할을 재현 —
  #2070(현 CellBreak scope)의 메커니즘이 RowBreak로 일반화됨을 확증하는 고임팩트 근거.
