# HWPX 불러오기/저장/렌더링 무손실 전수 검증 보고서 (2026-06-27, 3차원)

**대상**: `C:\Users\planet\hwpdocs` (서울시 opengov 결재문서, **13,867건**으로 증가)
**바이너리**: devel `a92a1fed` (PR #1597 #1584~1598 + PR #1533 merge 반영)
**검증 3차원**: ① IR roundtrip(불러오기/저장 구조) ② 한글 오라클(저장 시각충실) ③ rhwp 렌더링 레이아웃(한글 대비)

---

## 0. 코퍼스

| 항목 | 값 |
|------|----|
| 전체 HWPX | 13,867 (직전 12,733 → 증가) |
| HWP5(.hwp) | 0 |

## 1. ① 불러오기/저장 — IR Roundtrip (전수, parse→serialize→reparse)

| status | 건수 | 비율 |
|--------|------|------|
| **PASS (diff=0)** | **13,843** | **99.83%** |
| IR_DIFF | 4 | 0.03% |
| PARSE_FAIL | 20 | 0.14% |
| SERIALIZE/REPARSE/ROUND2/PKG_FAIL | 0 | 0 |

- **유효 파일(손상 20 제외) PASS = 13,843 / 13,847 = 99.97%.**
- **PARSE_FAIL 20건 = 전부 소스측 손상 ZIP**(`Could not find EOCD`, unzip도 못 엶 — 불완전 다운로드). rhwp 무관.
- **IR_DIFF 4건 = 전부 `char_shapes` offset shift**(내용 손실 아님): 36384689·36385445(#1591 C1), 36388711(#1593 C2), 36399822(표 셀 변종). first-para mismatch-path 클래스, 통합 리팩터 권고 대상. (코퍼스 증가에도 4건 불변 = 안정.)

## 2. ② 저장 시각충실 — 한글 페이지 오라클 (orig.hwpx ↔ rt.hwpx, 둘 다 한글 PageCount)

IR diff=0 인데 한글에서만 페이지가 붕괴하는 #1589 군집을 검출하는 게이트.

| 표본 | 측정(OK) | COLLAPSE | EXPAND | ERR(COM) |
|------|------|------|------|------|
| 본 코퍼스(13,867) seed42 | 344 | **0** | 0 | 347* |
| 직전 코퍼스(12,733) seed42+7 | 1,199 | **0** | 1 | – |
| **누적 측정** | **1,543** | **0** | 1 | – |

\* ERR(COM 열기 실패)은 한컴 자동화 환경 한계(Hwp.exe 누수·모달)로 **붕괴 아님** — 측정분 기준 판정. 개별 파일은 클린 환경에서 정상 개방(rhwp 무관).

- **페이지 붕괴율 0% (0/1,543 누적 측정)** — 이전 기준선 15.8%±2.8%(#1594~1598 이전)에서 해소 유지. rule-of-3 95% 상한 ≈ 0.2%.
- #1589 군집 4종(#1594 holdAnchorAndSO·#1595 ClickHere·#1596 generic-shape·#1598 ellipse/arc) 누적 효과가 증가 코퍼스에서도 유지.
- 잔여 EXPAND 1건(직전 코퍼스 36383219, 5→6쪽) — 붕괴 반대 방향 단발.

## 3. ③ 렌더링 레이아웃 — rhwp 자체 페이지수 vs 한글 페이지수(원본)

rhwp 레이아웃 엔진의 페이지네이션이 한컴(정답지)과 일치하는지. (rhwp `info` '페이지 수' vs 한글 `PageCount`, 조인 표본.)

| | 측정 표본 | 일치(rhwp==한글) | 불일치 |
|---|------|------|------|
| 무작위 표본 | 344 | **312 (90.7%)** | 32 (9.3%) |

**차이 분포(rhwp − 한글)**: −1쪽 29 · +1쪽 2 · +2쪽 1. 불일치 32건 중 19건이 한글 2쪽
문서(rhwp 1쪽) — 경계 근처 조밀 배치가 다발 지점.

- **~91% 페이지 정합.** 불일치 대부분(−1쪽) = rhwp가 한글보다 **약간 조밀하게 배치** → 경계 근처 문서(특히 한글 2쪽)가 1쪽 적게 렌더.
- 저장충실(②, orig↔rt 둘 다 한글=0% 붕괴)과 **독립** — ③은 rhwp **렌더 엔진**과 한컴의 차이(줄높이/줄간격/페이지 채움 임계 미세차)를 측정.
- 이것이 렌더링 레이아웃의 실질 갭. 후속 정밀화 대상(레이아웃 엔진 ↔ 한컴 정합).

---

## 4. 결론 (3차원)

| 차원 | 지표 | 결과 |
|------|------|------|
| ① 불러오기/저장(구조) | IR diff=0 | **99.97%**(유효), 잔여 char_shape offset 4건 |
| ② 저장 시각충실(한글) | 페이지 붕괴율 | **0%** (이전 ~16%) |
| ③ 렌더링 레이아웃(한글 대비) | rhwp==한글 페이지 | **~91%** (불일치 대부분 −1쪽) |

- **구조·저장 무손실은 사실상 완성**(99.97% / 붕괴 0%).
- **렌더링 레이아웃은 ~91% 페이지 정합** — 새 차원의 실질 갭(rhwp가 약간 조밀). 후속 과제.
- 소스 손상 20건은 rhwp 무관.

### 후속 권고
1. **렌더링 레이아웃 ~9% −1쪽 갭**: 줄높이/줄간격/페이지 채움 임계를 한컴과 정밀 정합(샘플: 한글 2쪽→rhwp 1쪽 다발).
2. first-para mismatch-path 통합 리팩터(IR_DIFF 4건).
3. 한글 COM 오라클 환경 안정화(ERR 다발 — Hwp.exe 누수).

산출물: `output/poc/fidelity_full_a92a1fed/{inventory,rhwp_pages,oracle_800}.tsv`.
