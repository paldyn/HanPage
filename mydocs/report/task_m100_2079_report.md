# 최종 결과보고서 — Task M100 #2079: 리팩토링 라운드 9 (typeset_endnote_paragraphs 2차 회전 — P6)

- 이슈: #2079 / 브랜치: `local/task2079` / 기간: 2026-07-09
- 수행계획서: `plans/task_m100_2079.md` / 구현계획서: `plans/task_m100_2079_impl.md`
- **goal 방식** (작업지시자 지시): 단계별 승인 생략, 게이트 자체 검증 ×3, 최종 일괄 보고.

## 1. 결과 요약

| 지표 | 시작 (r8) | 완료 (r9) |
|---|---|---|
| `typeset_endnote_paragraphs` 공식 CC | **138** (전체 1위) | **118** (전체 5위) |
| 동 함수 줄수 | 4,227 | **3,461** (−766) |
| 전체 최대 CC | 138 | **129** (`typeset.rs:12686`) |
| CC>25 예외 함수 | 87개 | 87개 (신규 진입 0 — judge_* 17건 전부 CC≤25) |
| 행동 회귀 | — | **0건** (통산 유지)

## 2. 수행 내용

1. **1단계 재지도**: P6 실체 = `let <판정명> = <불리언 연쇄/시뮬>;` 40여 건.
   집계자(advance_for_fit 논리 76 등)는 지역 불리언 집계라 추출 부적합 판정.
   **핵심 발견 — 104줄 렌더-시뮬 예측부가 함수 내 4곳 중복** (3곳 문자 그대로 + `?` 문체 1곳).
2. **2단계** (6e65143d): `predict_endnote_render_y` 승격 + 4곳 dedup — **−272줄**, CC 138→129.
3. **3단계** (b82dc9f9): 판정 함수군 배치 A+B 9건 (`judge_*` &self, 읽기 전용, 직접 파라미터)
   — CC 129→122. 판정 간 의존은 Copy 값 전달, mut 캐리 0.
4. **4단계** (직전 커밋): 배치 C 8건 — CC 122→118. **배치당 소득 체감**(−7 → −4)으로
   §0 축소 조항 적용해 종료.

컴파일러-확정 방식: 불확실 파라미터를 E0425/E0308 로 수렴 (fmt 7건·paragraphs 5건·
composed 타입 1건) — 게이트 전 전수 해소, 사각지대 신규 유형 없음.

## 3. 게이트 (매 단계 자체 검증, 전수 통과 ×3)

fmt ✓ / clippy 0 / `--tests` **2,945/0** / issue_1116 **13/13** /
OVR 5샘플 회귀 **0건** (00014ecf ×4 + a05e6f1b).

## 4. 잔여 인계 (3차 회전)

- **P7 배치/전진(~970줄)**: 중첩 if 밀집 — 국면 재지도 선행 필요 (라운드 7 원칙).
- P2 다단 누적 판정(~525줄) / P4 A2 fit 시뮬(~470줄).
- 집계자 3건(advance_for_fit/advance_for_new_endnote/large_between_notes_vpos_head_outside)
  은 구조상 잔류가 옳다 — 판정명이 지역 문서 역할.

## 5. 다음 라운드 후보 (r9 공식 CC)

1. `typeset.rs:12686` — **129** (전체 1위)
2. `layout_table_cells`(table_layout.rs:2379) — 124 (표 계열 2차)
3. `layout.rs:5920` — 121 / `typeset.rs:2313` — 120 / 본 함수 3차 회전 — 118
