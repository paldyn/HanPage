---
kind: report
status: active
canonical: mydocs/plans/task_m100_3674.md
last_verified: 2026-08-04
---

# Task #3674 최종 보고 — 편람 쪽수 편차: 정밀 진단 + 축 B 1차 수정

- Issue: [#3674](https://github.com/edwardkim/rhwp/issues/3674) (M100)
- 범위(작업지시자 3안 확정): **정밀 진단 완결 + 진단 인프라 반영**. 되감김 형상 수정은
  선결 조건(행높이 측정 정합) 발견으로 [#3931](https://github.com/edwardkim/rhwp/issues/3931)
  이관, 잔여 축은 서브 이슈 #3927~#3930 분리.
- 단계 기록: `mydocs/working/task_m100_3674_stage{1,2,3}.md` (stage2 는 1~11보)

## 결과

이 PR 은 **동작 변경 없음** — 진단 프로브(env 가드)와 문서만 반영한다. 되감김 형상
수정은 실험에서 **393→391(−2)·red-check 왕복까지 검증**됐으나, `issue_3738` fixture
에서 "증거는 참인데 행높이 과대측정이 실행을 깨뜨리는" 선결 관계가 드러나 #3931 로
이관했다(설계·재현 좌표 전문은 이슈 코멘트와 stage3 5~7절).

## 핵심 발견 (진단의 가치가 수정보다 크다)

1. **편차 +3 은 양방향 상쇄의 착시** — 실제 발산은 rhwp 전용 11쪽 + 한컴 전용 8쪽
   = 19쪽 규모. 총량 지표는 문제를 1/6 로 과소평가하며, 이것이 픽셀·PI 집계 지표가
   4개월간 정체된 구조적 이유의 실증이다.
2. **블록 높이·간격·전진량은 이미 정합** — Q&A 표 26개 중 25개 선언 일치, 문단 전진
   3건 완전 일치(269.3/318.4/248.6px). 편차의 실체는 **"어떤 표를 쪽 경계에서
   분할하느냐"의 진입 판정**이다.
3. **게이트 특정**: `declared_overflows_current`(typeset.rs ~18029) — 빈 host RowBreak
   다행 표를 declared(저장 최소 셀합) 기준으로, **0.1px 차이로도** 분할 검토 없이
   통짜 이월(pi=14: 741.9 > 관용 741.8). 한컴은 같은 지점에서 행 분할로 잔여
   174px 를 채웠다(head 178.6px, 저장 vpos 로 실증).

## 수정 — 저장 되감김 증거 형상

`saved_anchor_splits_here` 에 두 번째 저장-증거 형상 추가: **다음 문단의 저장 vpos 가
표 앵커보다 되감기고 그 크기가 실측 표 높이보다 작으면**, 원본 한글이 이 지점에서 행
분할했다는 증거이므로 declared 선이월을 건너뛰고 분할 경로로 보낸다. row_count>1 로
1×1 그림 표(#874·#2439) 보호, 기존 #2097 형상 불변.

구현 중 자체 검출 결함 1건: 꼬리 크기를 declared 와 비교(항상 탈락) → 실측
table_total 로 정정.

## 검증

- release-test 전체 0 실패(형상 제거 후 issue_3738 18/18 포함), clippy --workspace·fmt 통과.
- 되감김 형상 실험의 red-check 왕복(393↔391)은 stage3 에 실측 기록.
- 10k 서베이는 작업지시자 승인으로 **PR CI render-diff·native-skia 게이트 갈음**.
- 시각: Q7 분할 발현 확인(수정 전 통짜 단독쪽 → 수정 후 head/tail 분할, 한컴 PDF 와
  같은 요령). 단 **전면 시각 판정은 부분 수정 상태라 유보** — 작업지시자 판단으로
  잔여 축 수정 후 정제된 배치표와 함께 재요청.

## 진단 인프라 (서브 이슈들의 출발점)

- 쪽별 텍스트 **전역 DP 정렬**(shingle 유사도, 밴드 ±12) — 반복 머리말·목차 점선에
  면역인 발산 좌표화 방법.
- **LINE_SEG 오라클**: `dump`(저장 vpos·셀 지오메트리) × `export-render-tree`(계산
  bbox) 조인 — 시각 판정 없이 줄/블록 단위 정합 계측.
- 진단 프로브: `RHWP_DIAG_TAC`(delta 추가)·`RHWP_DIAG_SPLITSCAN`(신설:
  PRESPLIT/FIT/2439 3점) — env 가드, 동작 불변.
- 시각 스왑 세트 `output/issue3674_swap/` (A~E 쌍 + README 비교 원칙).

## 사고 기록

red-check 백그라운드가 트리를 되돌려 둔 사이 `git add -u` 문서 커밋이 철회분을 흡수
(47f279fef) → 복구(2ac1c5c46). 교훈을 메모리에 등재: 트리 변형 백그라운드 중 광역
add 금지, "코드 변경 후 빌드 0.1초"는 이상 신호.

## 서브 이슈 (전부 #3674 연결, v1.0.0)

| 이슈 | 축 | 기대 효과 |
|---|---|---|
| #3927 | B' Q&A 잔여 게이트 | 재저장본 −3± |
| #3928 | C 대비표 행 짝맞춤 | +8 해소(B 계열과 순서 조율 필수) |
| #3929 | A 다쪽 표 누적 | −1 |
| #3930 | D 저장 경로 입력 차이 | HWPX↔HWP 정합 |
| #3931 | E declared 문턱 일반화 | 무증거 입력(저장본 386·HWPX 387) 이동 |

## 교훈

**총량 지표의 상쇄가 4개월 정체의 정체였다.** 문서 하나를 줄 단위 오라클로 완전 분해하자
"조판이 전반적으로 다르다"는 인상이 "분기 하나의 0.1px 판정"으로 좁혀졌다. 남은 축들도
같은 방법이 통한다 — 이 타스크의 산출은 −2쪽이 아니라 그 방법이다.
