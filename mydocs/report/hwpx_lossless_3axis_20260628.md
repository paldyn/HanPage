# HWPX 불러오기/저장/렌더링 무손실 전수 재검증 보고서 (2026-06-28, 3차원)

**대상**: `C:\Users\planet\hwpdocs` (서울시 opengov 결재문서, **≈16,600건**으로 증가)
**바이너리**: `local/task1608` `e371214c` (**#1608 is_hwp3_origin 오탐지 제거 반영**)
**검증 3차원**: ① IR roundtrip(불러오기/저장 구조) ② 한글 오라클(저장 시각충실) ③ rhwp 렌더링 레이아웃(한글 대비)
**직전 보고**: `hwpx_lossless_3axis_20260627.md` (13,867건, devel a92a1fed)

---

## 0. 코퍼스

| 항목 | 값 |
|------|----|
| 전체 HWPX | ≈16,600 (직전 13,867 → +2,700여) |
| HWP5(.hwp) | 0 |

> 측정 도구별 카운트 차: roundtrip 배치 16,599 / rhwp info 스캔 16,601 (rglob 범위 차, 무시 가능).

## 1. ① 불러오기/저장 — IR Roundtrip (전수, parse→serialize→reparse→2round)

| status | 건수 | 비율 |
|--------|------|------|
| **PASS (diff=0)** | **16,574** | **99.85%** |
| IR_DIFF | 4 | 0.02% |
| PARSE_FAIL | 21 | 0.13% |
| SERIALIZE/REPARSE/ROUND2/PKG_FAIL | 0 | 0 |

- **유효 파일(손상 21 제외) PASS = 16,574 / 16,578 = 99.98%.**
- **PARSE_FAIL 21건 = 전부 소스측 손상 ZIP**(`BadZipFile` — unzip 무결성 검사 실패, 불완전 다운로드). rhwp 무관.
- **IR_DIFF 4건 = 전부 `char_shapes` offset shift**(내용 손실 아님):
  - 36384689 · 36385445 (첫 문단 `(24,n)→(32,n)` shift, #1591 C1 동류)
  - 36388711 (#1593 C2 동류)
  - 36399822 (표 셀 p[0] char_shape offset 변종)
  - first-para mismatch-path 클래스. **코퍼스 13,867→16,599 증가에도 4건 불변 = 회귀 없이 안정.**

## 2. ② 저장 시각충실 — 한글 페이지 오라클 (orig.hwpx ↔ rt.hwpx, 둘 다 한글 PageCount)

IR diff=0 인데 한글에서만 페이지가 붕괴하는 #1589 군집을 검출하는 게이트. 표본 800 (seed42, COM PageCount).

| 표본 | 측정(OK) | COLLAPSE | EXPAND | ERR(COM) |
|------|------|------|------|------|
| 본 코퍼스(≈16,600) seed42×800 | 338 | **0** | 2 | 460 |
| 직전 누적 | 1,543 | **0** | 1 | – |

- **페이지 붕괴율 0% (0/340 측정)** — 이전 기준선 ~16%(#1594~1598 이전)에서 해소 유지. #1589 군집 4종 대응 누적 효과가 증가 코퍼스에서도 유지.
- EXPAND 2건(rt>orig, 붕괴 반대 방향 단발) — 시각 손실 아님.
- ERR 460/800 = 57.5% — 한컴 COM 자동화 환경 한계(Hwp.exe 누수·모달)로 **붕괴 아님**(측정분 기준 판정). rhwp 무관. (환경 안정화는 후속.)

## 3. ③ 렌더링 레이아웃 — rhwp 자체 페이지수 vs 한글 페이지수(원본)

rhwp 레이아웃 엔진 페이지네이션이 한컴(정답지)과 일치하는지. (rhwp `info` '페이지 수' vs 한글 `PageCount`, 오라클 orig_pg 조인.)

| | 조인 표본 | 일치(rhwp==한글) | 불일치 |
|---|------|------|------|
| seed42 표본 | 340 | **309 (90.9%)** | 31 (9.1%) |

**차이 분포(rhwp − 한글)**: −1쪽 22 · +1쪽 7 · +2쪽 2. −1쪽 22건 중 18건이 한글 2쪽 문서(rhwp 1쪽) — 경계 근처 조밀 배치가 다발 지점.

- **~91% 페이지 정합** (rhwp info 스캔 자체는 16,600/16,601=99.99% 무크래시 렌더).
- 직전 보고(pre-#1608, 표본 344) 90.7% / 분포 −1:29·+1:2·+2:1 대비, **#1608 반영 후 −1쪽 군집이 줄고**(부당 tolerance 제거 효과) +초과가 소폭 증가. 별도 통제셋(92건) 측정에서는 일치 60→66 (net +6)로 확정.
- 저장충실(②, 붕괴 0%)과 **독립** — ③은 rhwp **렌더 엔진**과 한컴의 미세차(줄높이/줄간격/페이지 채움 임계).

---

## 4. 결론 (3차원)

| 차원 | 지표 | 결과 | 직전 대비 |
|------|------|------|------|
| ① 불러오기/저장(구조) | IR diff=0 | **99.98%**(유효), char_shape offset 4건 | 안정(4건 불변) |
| ② 저장 시각충실(한글) | 페이지 붕괴율 | **0%** (0/340) | 유지 |
| ③ 렌더링 레이아웃(한글 대비) | rhwp==한글 페이지 | **~91%** | #1608 반영, 통제셋 net +6 |

- **구조·저장 무손실은 사실상 완성**(99.98% / 붕괴 0%) — 코퍼스 +2,700 건 증가에도 회귀 없음.
- **렌더링 레이아웃 ~91% 페이지 정합** — #1608(요인 A)로 −1쪽 일부 해소, 잔여 −1쪽 다수는 요인 B(footer 콘텐츠 누적) 미규명. 후속 layout-fidelity 과제.
- 소스 손상 21건은 rhwp 무관.

### 후속 권고
1. **렌더링 레이아웃 −1쪽 갭 요인 B**: footer 콘텐츠 누적 부족(한글 2쪽→rhwp 1쪽 다발) 정밀 조사. 통제셋·게이트(`tests/fixtures/render_page_controlset.tsv`, `tools/render_page_gate.py`) 자산 활용.
2. first-para mismatch-path 통합 리팩터(IR_DIFF 4건).
3. 한글 COM 오라클 환경 안정화(ERR 57.5% — Hwp.exe 누수).

산출물: `output/poc/fidelity_full_e371214c/{inventory,rhwp_pages,oracle}.tsv`.
